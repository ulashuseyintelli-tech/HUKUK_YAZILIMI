# CI Pipeline - Core Checks
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

# Aynı ref için devam eden eski koşuyu iptal et (maliyet: çift/yığılan run önleme).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    working-directory: project

jobs:
  architectural-guardrails:
    name: Architectural Guardrails
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Governance writer coordination V1 guard
        env:
          GOV_COORD_EVENT_NAME: ${{ github.event_name }}
          GOV_COORD_BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          GOV_COORD_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          GOV_COORD_HEAD_REF: ${{ github.head_ref }}
        run: |
          node scripts/governance-coordination.cjs self-test
          node --test scripts/governance-coordination.test.cjs
          node scripts/governance-coordination.cjs validate-template request docs/governance/coordination-requests/_template/request.md
          node scripts/governance-coordination.cjs validate-template result docs/governance/coordination-results/_template/result.md
          node scripts/governance-coordination.cjs verify-register
          if [ "$GOV_COORD_EVENT_NAME" = "pull_request" ]; then
            node scripts/governance-coordination.cjs validate-pr-scope \
              --base "$GOV_COORD_BASE_SHA" \
              --head "$GOV_COORD_HEAD_SHA" \
              --head-ref "$GOV_COORD_HEAD_REF"
          else
            node scripts/governance-coordination.cjs validate-repository
          fi
      - name: ADR-007 check
        run: |
          if grep -r "req\.idempotencyContext\s*=" apps/api/src/; then
            echo "ADR-007 VIOLATION"; exit 1
          fi
      - name: CI-1 Bare fetch gate
        run: bash apps/api/scripts/ci-1-bare-fetch-gate.sh
      - name: CI-2 PII log leak gate
        run: bash apps/api/scripts/ci-2-pii-log-leak-gate.sh
      - name: CI-3 Scheduler unbounded gate
        run: bash apps/api/scripts/ci-3-scheduler-unbounded-gate.sh
      - name: CI-4 Trust proxy gate
        run: bash apps/api/scripts/ci-4-trust-proxy-gate.sh
      - name: CI-5 Scheduler metrics gate
        run: bash apps/api/scripts/ci-5-scheduler-metrics-gate.sh
      - name: CI-6 Symbol token singleton gate
        run: bash apps/api/scripts/ci-6-symbol-token-singleton-gate.sh
      - name: CI-7 Web no submission-level promote gate
        run: bash apps/api/scripts/ci-7-web-no-submission-promote-gate.sh
      - name: CI-8 Jest invocation budget gate
        run: bash apps/api/scripts/ci-8-jest-invocation-budget-gate.sh

  test-suite:
    name: Test Suite
    runs-on: ubuntu-latest
    # GH-03: job seviyesinde timeout yoktu → GitHub default'u 360 dk. Konsolidasyon
    # sonrasi beklenen sure ~13 dk; 20 dk sinir kacak bir job'i sinirlar, normal
    # kosuya mudahale etmez (client-workspace-live-smoke ile ayni deger).
    timeout-minutes: 20
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: hukuk_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 8.15.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: project/pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
        working-directory: project
      - name: Prisma generate
        run: pnpm --filter @hukuk/api exec prisma generate
        working-directory: project
      - name: Prisma migrate deploy
        run: pnpm --filter @hukuk/api exec prisma migrate deploy
        working-directory: project
      - name: Type check
        run: pnpm --filter @hukuk/api exec tsc --noEmit -p tsconfig.prod.json
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: pure/uyap-icrabot-tebligat ────────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   Icrabot tenant isolation tests (SEC-TENANT-HARDEN-P01)
      #     SEC-TENANT-HARDEN-P01: icrabot-digital-twin-tenant-guard.spec.ts (mevcut DBND-B1/B1B/B1C
      #     emsali) de dahil hiçbir icrabot tenant-isolation spec'i CI'da koşmuyordu. getQueueStats
      #     (ICRABOT-TID-01) sertleştirmesiyle birlikte ikisi de wiring'e eklendi.
      #     Dosya-varlığı pre-check (rename/silme → CI RED) + passWithNoTests YOK.
      #
      #   UYAP-ICRABOT-GOVERNANCE-GUARD-P03 acceptance tests
      #
      #   Integration Tests (legal-kernel, real Postgres)
      #
      #   Icrabot v28-engine unit tests (CI wiring gap)
      #     DEBTOR-OF01-HISTORY-P04-C-I02 CI step ekleme sırasında fark edildi: icrabot v28-engine
      #     modülündeki 5 unit spec (tenant boundary hardening, outbox-tenancy Phase 1, v28 platform
      #     outbox cron+retry, CAN-P0-001 notification truth, TimelineService aggregateVersion) hiçbir
      #     dar allowlist pattern'ine girmiyordu (v28-engine/__tests__/ hiçbir step'te referans
      #     edilmiyordu) → CI'da hiç koşmuyordu. Hepsi DB-free (mock prisma). Dosya-varlığı pre-check
      #     (rename/silme veya 0-eşleşme → CI RED) + passWithNoTests YOK.
      #
      #   OF01-HISTORY-P04-C-I02 NonRetryableOutboxError shared-infrastructure tests
      #
      #   MPB-028(a) PR-2 legal-deadline unit/static tests
      #     MPB-028(a) PR-2: LegalDeadlineService (rejim hesaplama, snapshot immutable/supersede,
      #     objectionPeriodDays validation) + TebligatService TK 21/2 correction / TK m.20 explicit-
      #     override unit-static spec'leri yukarıdaki dar pattern'lerin dışında kalıyordu → CI'da hiç
      #     koşmuyordu. Dosya-varlığı pre-check + passWithNoTests YOK (bkz. GATE-1 CI tenant-isolation
      #     regression coverage emsali).
      #
      #   OF01-HISTORY-P03 Tebligat/ServiceOccurrence atomic integration unit tests
      #
      #   OF01-HISTORY-P04-C-I01 service-occurrence-recorded consumer unit tests
      #
      #   OF01-HISTORY-P02 ServiceOccurrenceService unit tests
      #
      #   DBP-P2-SEC-P02 UYAP case-mapper DebtorRole mapping regression tests
      #     DBP-P2-SEC-P02: UyapCaseMapperService.mapDebtorRole (ikinci UYAP mapper) DebtorRole->rol
      #     behavior-lock regresyonu (PR #1327). Yukarıdaki dar allowlist pattern'lerinin hiçbirine
      #     girmiyordu → CI'da hiç koşmuyordu (yalnız o merge'in GO-IMPLEMENT'inde local `npx jest` ile
      #     doğrulandı). Ayrı/açık step; dosya-varlığı pre-check (rename/silme/0-eşleşme → CI RED) + passWithNoTests YOK.
      #
      #   UYAP-EVIDENCE-CI-P02A-R1 additional blocking trust-plane specs
      #     UYAP-EVIDENCE-CI-P02A-R1: P-E2B-R0 reconciliation sonrasi owner promote karari — alti mevcut
      #     trust-plane spec'ini blocking CI kapsamina alir (F4-a/R1 P-E2B adaylari; P2B-06
      #     interest-projection.mapping.spec.ts INVALID INVENTORY / NEVER EXISTED, dahil EDILMEDI). Gercek
      #     6. aday address-discovery/uyap-query.service.spec.ts. Testler DEGISMEZ; yalniz CI'da gercekten+
      #     surekli calismasi saglanir. 6x test -f false-green guard + exact --runTestsByPath (broad
      #     wildcard YOK, passWithNoTests YOK, scheduled workflow YOK). Olcum: 6 suite / 103 test, 3/3 PASS,
      #     batch max 17s (<180s).
      #
      #   UYAP-EVIDENCE-CI-P02A blocking trust-plane specs
      #     UYAP-EVIDENCE-CI-P02A: mevcut fakat explicit blocking step'i olmayan beş trust-plane spec'ini
      #     (F4-a/R1 DP-8 / P-E2A) blocking CI kapsamına alır. Testler DEĞİŞMEZ; yalnız CI'da gerçekten ve
      #     sürekli çalışması sağlanır. 5× test -f false-green guard + exact --runTestsByPath (broad
      #     wildcard YOK, passWithNoTests YOK). Ölçüm: 5 suite / 120 test, 3/3 PASS, batch ~17s (<180s).
      #
      #   CLIENT-SEC-H2B tenant-scope enforcement regression
      #     CLIENT-SEC-H2B: code-level tenant-scope enforcement regression (template update,
      #     UYAP case-status query, document/document-template fail-closed tenant guard).
      #
      #   UYAP-EVIDENCE-PII-PERSISTENCE-P01 debtor-assets durable PII tests
      #     UYAP-EVIDENCE-PII-PERSISTENCE-P01: CAP-11 queryDebtorAssets durable PII containment —
      #     ham debtorIdentityNo artık UyapRequestLog.requestData/responseData'ya YAZILMAZ (UYAP-CONST-007
      #     / UYAP-BC-DEBTOR-001). Yukarıdaki dar allowlist pattern'lerinin hiçbirine girmiyor → ayrı/açık
      #     step; dosya-varlığı pre-check (false-green guard) + passWithNoTests YOK.
      #
      #   CLIENT-SEC-H2A fail-closed containment regression
      #     CLIENT-SEC-H2A: EsignLog/UyapRequestLog fail-closed emergency containment regression.
      #     Şema-seviyesi tenantId yokluğu (H2C ile kalıcı çözülecek) — bu, o zamana kadarki containment kanıtı.
      #
      #   DBP-P2-UYAP-CONTRACT-A-P01 legacy truthfulness containment tests
      #     DBP-P2-UYAP-CONTRACT-A-P01: legacy Contract A yüzeyi TRUTHFULNESS CONTAINMENT (F1-F5) regresyonu.
      #     Preview envelope (legacy/noncompliant), validation semantics (local precheck), STUB submit
      #     (transmitted=false / evkNo=null), yorum/dead-code temizliği ve FE label doğrulukları.
      #     Yukarıdaki dar allowlist pattern'lerinin hiçbirine girmiyor → ayrı/açık step; dosya-varlığı
      #     pre-check (rename/silme/0-eşleşme → CI RED) + passWithNoTests YOK.
      #
      #   CLIENT-SEC-H1 cross-tenant containment regression
      #     CLIENT-SEC-H1: cross-tenant/unauth Client PII containment (S1-S4) regression.
      #     Explicit test -f guard: bir spec silinir/yeniden adlandırılırsa step FAIL eder (allowlist stale-test gap koruması).
      #
      #   UYAP-RETRY-CONTAIN-01 retry-failed containment tests
      #
      #   SEC-XTEN-UYAP-STATS-01 tenant-scope tests
      #     SEC-XTEN-UYAP-STATS-01: GET /uyap/status ve GET /uyap/stats cross-tenant aggregate leak
      #     containment (TRANSPORT-RISK-02) — getStats(tenantId) artık fail-closed tenant-scoped.
      #     Yukarıdaki dar allowlist pattern'lerinin hiçbirine girmiyor → ayrı/açık step; dosya-varlığı
      #     pre-check + passWithNoTests YOK.
      #
      #   CLIENT-SEC-H2C-P02 tenant write population regression
      #     CLIENT-SEC-H2C-P02: EsignLog/UyapRequestLog new-write tenant population regression.
      #     P02-R1: 5 UYAP write yolunun log ownership'i trusted execution tenant'tan (DTO'dan DEĞİL) alması.
      #
      #   DBP-P2-UYAP-CONNECTOR-TRANSPORT-CONTAIN-01 truthfulness containment tests
      #     DBP-P2-UYAP-CONNECTOR-TRANSPORT-CONTAIN-01: stub gönderim yollarının (sendPaymentOrder/
      #     pushHacizRequest/submitDocument/submitCriminalComplaint/submitCivilLawsuit) truthfulness
      #     containment'ı — REAL-TRANSPORT=0 iken hiçbiri provider kabulü/gerçek gönderim/hukuki sonuç
      #     iddia edemez; sendPaymentOrder CPE teknik-hatası action-matrix UYAP_SEND=failMode:'CLOSED'
      #     ile uzlaştırıldı (fail-open→fail-closed). Yukarıdaki dar allowlist pattern'lerinin hiçbirine
      #     girmiyor → ayrı/açık step; dosya-varlığı pre-check + passWithNoTests YOK.
      #
      #   LRV-02 UYAP XML DebtorRole mapping regression tests
      #     LRV-02 / DBP-P2-SEC-P01: UyapXmlService.mapDebtorRoleToUyapKod DebtorRole->UYAP rol-kodu
      #     mapping regresyonu (PR #1322). Yukarıdaki dar allowlist pattern'lerinin hiçbirine girmiyordu
      #     → CI'da hiç koşmuyordu (yalnız o merge'in GO-IMPLEMENT'inde local `npx jest` ile doğrulandı).
      #     Ayrı/açık step; dosya-varlığı pre-check (rename/silme/0-eşleşme → CI RED) + passWithNoTests YOK.
      #
      #   DBP-P2-UYAP-CONTRACT-A-P02A official role boundary + provenance tests
      #     DBP-P2-UYAP-CONTRACT-A-P02A — official builder BOUNDARY (provenance + role-translation skeleton).
      #     Runtime-dışı, değer içermeyen, yalnız test-reachable iskelet; legacy builder ve wiring'e dokunmaz.
      #     Dar allowlist pattern'lerine girmez → ayrı/açık step; dosya-varlığı pre-check + passWithNoTests YOK.
      #
      #   DBP-P2-UYAP-CONTRACT-A-P02B official serializer skeleton tests
      #     DBP-P2-UYAP-CONTRACT-A-P02B — official Contract A serializer SKELETON (contract-derived shape).
      #     Deterministik, SERIALIZED_DRAFT/REJECTED, unresolved-role rejection; runtime-dışı, değer içermez,
      #     legacy builder ve wiring'e dokunmaz. Ayrı/açık step; dosya-varlığı pre-check + passWithNoTests YOK.
      #
      #   DBP-P2-UYAP-CONTRACT-A-P04A-ENC official ISO-8859-9 byte encoder tests
      #     DBP-P2-UYAP-CONTRACT-A-P04A-ENC — resmî ISO-8859-9 (Latin-5) GERÇEK BYTE ENCODING foundation.
      #     iconv-lite arkasında FAIL-CLOSED: yalnız SERIALIZED_DRAFT girdisi; encode→decode round-trip
      #     KAYNAKLA birebir + code-point representability; ikame/kayıp → ENCODING_REJECTED. Runtime-dışı,
      #     in-memory-only (persist/transmit/wire/raw-log YOK), legacy/serializer/schema'ya dokunmaz. Dar
      #     allowlist pattern'lerine girmez → ayrı/açık step; dosya-varlığı pre-check + passWithNoTests YOK.
      - name: pure/uyap-icrabot-tebligat
        run: |
          cd apps/api
          for f in \
            src/modules/icrabot/__tests__/icrabot-digital-twin-tenant-guard.spec.ts \
            src/modules/icrabot/__tests__/icrabot-governance-guard.spec.ts \
            src/modules/icrabot/__tests__/icrabot-queue-stats-tenant-scope.spec.ts \
            src/modules/icrabot/domain-event-ingest/__tests__/domain-event-ingest.integration.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/action-handler-notification-truth.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/non-retryable-outbox.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/outbox-platform-cron.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/outbox-tenancy.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/tenant-boundary-hardening.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/timeline-aggregate-version.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/uyap-event-ingest.boundary.integration.spec.ts \
            src/modules/tebligat/__tests__/tebligat-ptt-result-tk212-regression.spec.ts \
            src/modules/tebligat/__tests__/tebligat-service-occurrence-integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded.consumer.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded.registrar.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence.service.spec.ts \
            src/modules/uyap-export/__tests__/uyap-case-mapper.debtor-role-mapping.spec.ts \
            src/modules/uyap-export/__tests__/uyap-export-passive-policy.spec.ts \
            src/modules/uyap-export/projection/__tests__/batch-faizt-readiness.contract.spec.ts \
            src/modules/uyap-export/projection/__tests__/due-claimitem-relation.classifier.spec.ts \
            src/modules/uyap-export/projection/__tests__/faizt-interest-projection.adapter.spec.ts \
            src/modules/uyap/__tests__/haciz-decision-audit.spec.ts \
            src/modules/uyap/__tests__/numeric-interest-projection.adapter.spec.ts \
            src/modules/uyap/__tests__/uyap-case-status-tenant-scope.spec.ts \
            src/modules/uyap/__tests__/uyap-debtor-assets-pii-persistence.spec.ts \
            src/modules/uyap/__tests__/uyap-history-h2a-containment.spec.ts \
            src/modules/uyap/__tests__/uyap-legacy-truthfulness.spec.ts \
            src/modules/uyap/__tests__/uyap-poa-tenant-scope.spec.ts \
            src/modules/uyap/__tests__/uyap-retry-containment.spec.ts \
            src/modules/uyap/__tests__/uyap-stats-tenant-scope.spec.ts \
            src/modules/uyap/__tests__/uyap-tenant-write-population.spec.ts \
            src/modules/uyap/__tests__/uyap-transport-truthfulness-containment.spec.ts \
            src/modules/uyap/__tests__/uyap-write-ownership-completion.spec.ts \
            src/modules/uyap/__tests__/uyap-xml.debtor-role-mapping.spec.ts \
            src/modules/uyap/__tests__/uyap-xml.numeric-interest-projection.spec.ts \
            src/modules/uyap/official/__tests__/official-contract-provenance.spec.ts \
            src/modules/uyap/official/__tests__/official-exchange-builder.spec.ts \
            src/modules/uyap/official/__tests__/official-iso8859-9-encoder.spec.ts \
            src/modules/uyap/official/__tests__/official-role-translator.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/icrabot/__tests__/icrabot-digital-twin-tenant-guard.spec.ts \
            src/modules/icrabot/__tests__/icrabot-governance-guard.spec.ts \
            src/modules/icrabot/__tests__/icrabot-queue-stats-tenant-scope.spec.ts \
            src/modules/icrabot/domain-event-ingest/__tests__/domain-event-ingest.integration.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/action-handler-notification-truth.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/non-retryable-outbox.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/outbox-platform-cron.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/outbox-tenancy.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/tenant-boundary-hardening.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/timeline-aggregate-version.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/uyap-event-ingest.boundary.integration.spec.ts \
            src/modules/tebligat/__tests__/tebligat-ptt-result-tk212-regression.spec.ts \
            src/modules/tebligat/__tests__/tebligat-service-occurrence-integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded.consumer.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded.registrar.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence.service.spec.ts \
            src/modules/uyap-export/__tests__/uyap-case-mapper.debtor-role-mapping.spec.ts \
            src/modules/uyap-export/__tests__/uyap-export-passive-policy.spec.ts \
            src/modules/uyap-export/projection/__tests__/batch-faizt-readiness.contract.spec.ts \
            src/modules/uyap-export/projection/__tests__/due-claimitem-relation.classifier.spec.ts \
            src/modules/uyap-export/projection/__tests__/faizt-interest-projection.adapter.spec.ts \
            src/modules/uyap/__tests__/haciz-decision-audit.spec.ts \
            src/modules/uyap/__tests__/numeric-interest-projection.adapter.spec.ts \
            src/modules/uyap/__tests__/uyap-case-status-tenant-scope.spec.ts \
            src/modules/uyap/__tests__/uyap-debtor-assets-pii-persistence.spec.ts \
            src/modules/uyap/__tests__/uyap-history-h2a-containment.spec.ts \
            src/modules/uyap/__tests__/uyap-legacy-truthfulness.spec.ts \
            src/modules/uyap/__tests__/uyap-poa-tenant-scope.spec.ts \
            src/modules/uyap/__tests__/uyap-retry-containment.spec.ts \
            src/modules/uyap/__tests__/uyap-stats-tenant-scope.spec.ts \
            src/modules/uyap/__tests__/uyap-tenant-write-population.spec.ts \
            src/modules/uyap/__tests__/uyap-transport-truthfulness-containment.spec.ts \
            src/modules/uyap/__tests__/uyap-write-ownership-completion.spec.ts \
            src/modules/uyap/__tests__/uyap-xml.debtor-role-mapping.spec.ts \
            src/modules/uyap/__tests__/uyap-xml.numeric-interest-projection.spec.ts \
            src/modules/uyap/official/__tests__/official-contract-provenance.spec.ts \
            src/modules/uyap/official/__tests__/official-exchange-builder.spec.ts \
            src/modules/uyap/official/__tests__/official-iso8859-9-encoder.spec.ts \
            src/modules/uyap/official/__tests__/official-role-translator.spec.ts
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: pure/client-portal ────────────────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   Client module tests (client-lane false-green guard)
      #
      #   CLIENT-P2-U03-TRACK-A-I02 asset-query projection mapper tests
      #     CLIENT-P2-U03-TRACK-A-I02: toCuratedAssetQuery() pure mapper — raw CaseDebtor
      #     AssetQueryStatus (UNKNOWN/YES/NO/PENDING/ERROR) → curated client-facing 5-durum
      #     (NOT_QUERIED/FOUND/NOT_FOUND/RESULT_PENDING/RESULT_UNAVAILABLE) exhaustive dönüşüm
      #     + tanınmayan/gelecek değer için fail-safe RESULT_UNAVAILABLE regresyonu.
      #
      #   CLIENT-P2-U03-I01 case-detail projection tests
      #
      #   CLIENT-P2-U03-TRACK-B-U00B case-list projection tests
      #     CLIENT-P2-U03-TRACK-B-U00B: getClientCases() (case-liste endpoint) — §33.4 Financial
      #     Disclosure Gate ile çelişen ham `collections` (Collection.amount/date) select'ten ve
      #     response'tan tamamen kaldırıldı (U00'ın case-detail kapsamındaki aynı kaldırmasının
      #     case-list ikizi); principalAmount (Dosya Alacağı) bilerek korundu.
      #
      #   CLIENT-P2-CREDENTIAL-RECOVERY-P01 portal reset-password tests
      #
      #   CLIENT-P2-U02 portal tokenVersion unit tests
      #
      #   CLIENT-P2-U03-I02 portal-document projection tests
      #     CLIENT-P2-U03-I02: getDocuments()/uploadDocument() explicit select projeksiyonu (POL-D
      #     §21/BP-06 §23) — filePath (internal storage location), reviewedBy/reviewedAt/reviewNote
      #     (internal review workflow), clientId/tenantId (authorization context) ve updatedAt artık
      #     select'te hiç yok; exact select shape + bounded response shape + getDocument()/
      #     deleteDocument()/getPendingDocuments() (internal/admin, select'siz) regresyonu kanıtlanır.
      #
      #   CLIENT-P2-U03-I03 portal-message projection tests
      #     CLIENT-P2-U03-I03: getMessages()/sendMessageFromClient() explicit select projeksiyonu
      #     (POL-D §21/BP-06 §23) — senderId (internal technical actor identifier — OFFICE
      #     mesajlarında staff User.id), clientId/tenantId/caseId/readAt artık select'te hiç yok;
      #     exact select shape + bounded response shape + sendMessageFromOffice()/getClientMessages()/
      #     getUnreadMessageCount()/markMessagesAsRead() (internal/staff, select'siz) regresyonu
      #     kanıtlanır.
      #
      #   CLIENT-P2-U03-I05 portal-notification projection tests
      #     CLIENT-P2-U03-I05: getNotifications() explicit select projeksiyonu (POL-D §21/BP-06
      #     §23) — brief'in taslağı gerçek şema ve consumer'a (portal/layout.tsx'in kendi
      #     interface Notification'ı) birebir uyuyor, sapma YOK. clientId/caseId/readAt artık
      #     select'te hiç yok; exact select shape + bounded response shape + getUnreadCount()/
      #     markAsRead()/markAllAsRead()/createNotification() (select'siz) regresyonu kanıtlanır.
      #
      #   CLIENT-P2-U03-I04 portal-poa projection tests
      #     CLIENT-P2-U03-I04: getClientPoas() explicit select projeksiyonu (POL-D §21/BP-06 §23) —
      #     approved contract gerçek consumer'a (portal/poas/page.tsx) göre doğrulanmıştır; brief'in
      #     taslak varsayımından KASITLI sapar (isActive: unused + WHERE'de zaten sabit true; scope:
      #     deprecated). filePath/fileSize/mimeType/clientId/scopeType/scopeDescription/createdAt/
      #     updatedAt/poaDate + join-metadata artık select'te hiç yok; exact select shape + bounded
      #     response shape kanıtlanır. poa.service.ts (staff/admin) DEĞİŞMEDİ, dokunulmadı.
      - name: pure/client-portal
        run: |
          cd apps/api
          for f in \
            src/modules/client/__tests__/client-action-catalog.spec.ts \
            src/modules/client/__tests__/client-address-route-collision.spec.ts \
            src/modules/client/__tests__/client-address.service.spec.ts \
            src/modules/client/__tests__/client-audit.spec.ts \
            src/modules/client/__tests__/client-controller-validation.spec.ts \
            src/modules/client/__tests__/client-core-p0.spec.ts \
            src/modules/client/__tests__/client-create-checksum.spec.ts \
            src/modules/client/__tests__/client-document-request-command.spec.ts \
            src/modules/client/__tests__/client-operating-snapshot.spec.ts \
            src/modules/client/__tests__/client-poa-reminder-command.spec.ts \
            src/modules/client/__tests__/client-poa-upload-command.spec.ts \
            src/modules/client/__tests__/client-reactivate.spec.ts \
            src/modules/client/__tests__/client-template-notification-command.spec.ts \
            src/modules/client/__tests__/client-timeline.spec.ts \
            src/modules/client/__tests__/client-update-duplicate-guard.spec.ts \
            src/modules/client/__tests__/contact-followup.spec.ts \
            src/modules/portal/__tests__/asset-query-projection.spec.ts \
            src/modules/portal/__tests__/case-detail-projection.spec.ts \
            src/modules/portal/__tests__/case-list-projection.spec.ts \
            src/modules/portal/__tests__/credential-recovery.spec.ts \
            src/modules/portal/__tests__/portal-auth-guard-token-version.spec.ts \
            src/modules/portal/__tests__/portal-document-projection.spec.ts \
            src/modules/portal/__tests__/portal-message-projection.spec.ts \
            src/modules/portal/__tests__/portal-notification-projection.spec.ts \
            src/modules/portal/__tests__/portal-poa-projection.spec.ts \
            src/modules/portal/__tests__/portal-token-version-revocation.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/client/__tests__/client-action-catalog.spec.ts \
            src/modules/client/__tests__/client-address-route-collision.spec.ts \
            src/modules/client/__tests__/client-address.service.spec.ts \
            src/modules/client/__tests__/client-audit.spec.ts \
            src/modules/client/__tests__/client-controller-validation.spec.ts \
            src/modules/client/__tests__/client-core-p0.spec.ts \
            src/modules/client/__tests__/client-create-checksum.spec.ts \
            src/modules/client/__tests__/client-document-request-command.spec.ts \
            src/modules/client/__tests__/client-operating-snapshot.spec.ts \
            src/modules/client/__tests__/client-poa-reminder-command.spec.ts \
            src/modules/client/__tests__/client-poa-upload-command.spec.ts \
            src/modules/client/__tests__/client-reactivate.spec.ts \
            src/modules/client/__tests__/client-template-notification-command.spec.ts \
            src/modules/client/__tests__/client-timeline.spec.ts \
            src/modules/client/__tests__/client-update-duplicate-guard.spec.ts \
            src/modules/client/__tests__/contact-followup.spec.ts \
            src/modules/portal/__tests__/asset-query-projection.spec.ts \
            src/modules/portal/__tests__/case-detail-projection.spec.ts \
            src/modules/portal/__tests__/case-list-projection.spec.ts \
            src/modules/portal/__tests__/credential-recovery.spec.ts \
            src/modules/portal/__tests__/portal-auth-guard-token-version.spec.ts \
            src/modules/portal/__tests__/portal-document-projection.spec.ts \
            src/modules/portal/__tests__/portal-message-projection.spec.ts \
            src/modules/portal/__tests__/portal-notification-projection.spec.ts \
            src/modules/portal/__tests__/portal-poa-projection.spec.ts \
            src/modules/portal/__tests__/portal-token-version-revocation.spec.ts
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: pure/office-auth-user ─────────────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   AUTH-01 tenant-aware authentication tests
      #     AUTH-01: tenant-aware authentication (tenantSlug ZORUNLU login, global findFirst(email)
      #     kaldırıldı, tenant-scoped invite email-uniqueness, enumeration-safe account-recovery).
      #     Dar allowlist'in dışında kalır → explicit step + test -f guard (ci-narrow-allowlist
      #     stale-test gap koruması — mevcut auth spec'leri de bu korumaya AYNI PR'da eklendi).
      #
      #   OFFICE-AUTH-P01 auth/user unit tests
      #
      #   CLIENT-P2-CREDENTIAL-RECOVERY-P01 portal reset-password tests
      #
      #   OFFICE-AUTH-P02 password-reset unit tests
      #     OFFICE-AUTH-P02: office/staff credential-recovery (şifremi unuttum) — tenant-scoped
      #     PasswordResetToken (hash-only, single-use), forgot/reset-password uçları, atomik
      #     tek-kullanımlık tüketim + tokenVersion session-invalidation + sibling-token revoke,
      #     e-posta-hatasında-token-kalmaz politikası, request/consume için ayrı rate-limit
      #     bucket'ları. Dar allowlist dışında kalır → explicit step + test -f guard
      #     (ci-narrow-allowlist stale-test gap koruması).
      - name: pure/office-auth-user
        run: |
          cd apps/api
          for f in \
            src/modules/auth/__tests__/auth-login-passwordhash-guard.spec.ts \
            src/modules/auth/__tests__/auth-register-seed.spec.ts \
            src/modules/auth/__tests__/auth-tenant-aware-login.spec.ts \
            src/modules/auth/__tests__/auth-tokenversion-revocation.spec.ts \
            src/modules/auth/guards/__tests__/credential-recovery-rate-limit.guard.spec.ts \
            src/modules/auth/invite/__tests__/user-invite.service.spec.ts \
            src/modules/auth/password-reset/__tests__/password-reset.service.spec.ts \
            src/modules/user/__tests__/change-password.service.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/auth/__tests__/auth-login-passwordhash-guard.spec.ts \
            src/modules/auth/__tests__/auth-register-seed.spec.ts \
            src/modules/auth/__tests__/auth-tenant-aware-login.spec.ts \
            src/modules/auth/__tests__/auth-tokenversion-revocation.spec.ts \
            src/modules/auth/guards/__tests__/credential-recovery-rate-limit.guard.spec.ts \
            src/modules/auth/invite/__tests__/user-invite.service.spec.ts \
            src/modules/auth/password-reset/__tests__/password-reset.service.spec.ts \
            src/modules/user/__tests__/change-password.service.spec.ts
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: pure/claim-collection-finance ─────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   ADR-014 PR-10 compatibility adapter and contract tests
      #
      #   UYAP-EVIDENCE-CI-P02A blocking trust-plane specs
      #     UYAP-EVIDENCE-CI-P02A: mevcut fakat explicit blocking step'i olmayan beş trust-plane spec'ini
      #     (F4-a/R1 DP-8 / P-E2A) blocking CI kapsamına alır. Testler DEĞİŞMEZ; yalnız CI'da gerçekten ve
      #     sürekli çalışması sağlanır. 5× test -f false-green guard + exact --runTestsByPath (broad
      #     wildcard YOK, passWithNoTests YOK). Ölçüm: 5 suite / 120 test, 3/3 PASS, batch ~17s (<180s).
      #
      #   RCV Claim Formation I02B typed admission and dormancy tests
      #     RCV-CLAIM-FORM-P02-S08-I02B: typed human-formation admission stays
      #     dormant/default-disabled; only intent + OfficeApproval + request audit
      #     may be created atomically. Exact resolver ports have no production adapter.
      #     D01B-CONTRACT-PARITY-I01: the shared Legal Basis eligibility validator
      #     (used by both admission and the dormant finalizer) is unit-tested here
      #     too — no DB dependency, same non-DB-gated group.
      #
      #   RCV Claim Formation I03 finalizer boundary tests
      #
      #   RCV-P2-WS04-P01 allocation drift baseline unit and static tests
      #     RCV-P2-WS04-P01: ratified drift-baseline-only slice. The explicit unit/static
      #     gate locks authority roles, the four comparison classes, same-input parity,
      #     legacy activation diagnostics and the runtime allocator no-write inventory.
      #
      #   Integration Tests (legal-kernel, real Postgres)
      - name: pure/claim-collection-finance
        run: |
          cd apps/api
          for f in \
            src/modules/case/__tests__/case-calculation-summary-shadow.spec.ts \
            src/modules/case/__tests__/case-calculation-summary.compatibility.spec.ts \
            src/modules/case/inventory/__tests__/rich-interest-uyap-readiness.core.spec.ts \
            src/modules/claim-item/__tests__/claim-formation-persistence-foundation.static.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-finalizer.static.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-intent-admission.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-intent-dormancy.static.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-legal-basis-eligibility.spec.ts \
            src/modules/collection/__tests__/collection-ledger-forward.spec.ts \
            src/modules/collection/__tests__/collection-payment-received.integration.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/case/__tests__/case-calculation-summary-shadow.spec.ts \
            src/modules/case/__tests__/case-calculation-summary.compatibility.spec.ts \
            src/modules/case/inventory/__tests__/rich-interest-uyap-readiness.core.spec.ts \
            src/modules/claim-item/__tests__/claim-formation-persistence-foundation.static.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-finalizer.static.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-intent-admission.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-intent-dormancy.static.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-legal-basis-eligibility.spec.ts \
            src/modules/collection/__tests__/collection-ledger-forward.spec.ts \
            src/modules/collection/__tests__/collection-payment-received.integration.spec.ts
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: pure/platform-scripts-shared ──────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   UYAP-EVIDENCE-CI-P02A blocking trust-plane specs
      #     UYAP-EVIDENCE-CI-P02A: mevcut fakat explicit blocking step'i olmayan beş trust-plane spec'ini
      #     (F4-a/R1 DP-8 / P-E2A) blocking CI kapsamına alır. Testler DEĞİŞMEZ; yalnız CI'da gerçekten ve
      #     sürekli çalışması sağlanır. 5× test -f false-green guard + exact --runTestsByPath (broad
      #     wildcard YOK, passWithNoTests YOK). Ölçüm: 5 suite / 120 test, 3/3 PASS, batch ~17s (<180s).
      #
      #   Security boundary tests (Gate-1 tenant + Gate-2/3 passive-writer + Gate-4 identity)
      #
      #   UYAP-EVIDENCE-CI-P02A-R1 additional blocking trust-plane specs
      #     UYAP-EVIDENCE-CI-P02A-R1: P-E2B-R0 reconciliation sonrasi owner promote karari — alti mevcut
      #     trust-plane spec'ini blocking CI kapsamina alir (F4-a/R1 P-E2B adaylari; P2B-06
      #     interest-projection.mapping.spec.ts INVALID INVENTORY / NEVER EXISTED, dahil EDILMEDI). Gercek
      #     6. aday address-discovery/uyap-query.service.spec.ts. Testler DEGISMEZ; yalniz CI'da gercekten+
      #     surekli calismasi saglanir. 6x test -f false-green guard + exact --runTestsByPath (broad
      #     wildcard YOK, passWithNoTests YOK, scheduled workflow YOK). Olcum: 6 suite / 103 test, 3/3 PASS,
      #     batch max 17s (<180s).
      #
      #   Address-task tenant isolation tests (SEC-TENANT-HARDEN-P01)
      #     SEC-TENANT-HARDEN-P01: address-task.service.ts/controller.ts hiçbir CI step'inde
      #     koşmuyordu (ADDR-TID-03..08 fail-closed sertleştirmesi bu keşifle birlikte yapıldı).
      #     Dosya-varlığı pre-check (rename/silme → CI RED) + passWithNoTests YOK.
      #
      #   Wave 0 tenant-boundary unit/static tests (Risk/AI/Notification/WorkflowEngine/EnforcementAction)
      #     GATE-1 CI Tenant-Isolation Regression Coverage: Borçlu Wave 0 (MPB-028/OD-3/PR-EA) tenant-
      #     boundary unit+static spec'leri yukarıdaki dar pattern'lerin dışında kalıyordu → CI'da hiç
      #     koşmuyordu (yalnız local `pnpm test` ile çalışırdı — GATE-1'in "cross-tenant testler yeşil"
      #     kriteri sürekli doğrulanamıyordu). Ayrı/açık step ile koşturulur. passWithNoTests YOK +
      #     önce dosya-varlığı pre-check: rename/silme veya 0-eşleşme → CI RED. Açık liste dizin-bazlı
      #     regex yerine bilinçli tercih edildi: enforcement-action-backfill-* dosyaları farklı bir
      #     dizinde (src/scripts/__tests__/), ve automation/__tests__/ altında tenant-ilgisiz dosyalar
      #     (rfa007-enforcement-guard, automation.service, poa-expiry-delivery) da var — dizin-geneli
      #     bir glob bunları yanlışlıkla dahil ederdi. Yeni Wave 0 tenant testi eklenirse bu listeye
      #     açıkça eklenmelidir (bkz. maintenance-register.md notu).
      #
      #   OFFICE-CAP09A-FOUNDATION-I01 audit attribution unit tests
      #     OFFICE-PHASE2-CAP09A-FOUNDATION-I01: AuditLog attribution kolonlarini (actorType/
      #     correlationId/decisionResult/policyRef/policyVersion/reasonCode/requestId) dogrulayan
      #     2 test dosyasi repository'de mevcuttu ama hicbir CI adimi bunlari toplamiyordu
      #     (CROSS-WORKSTREAM-LIVE-MIGRATION-TRAIN-R02 sirasinda tespit edilen narrow-allowlist
      #     gap). Bu adim yalniz mevcut testleri required execution path'e alir; migration/
      #     schema/runtime davranisi degismez.
      #
      #   DBP-P2-BP-01 all-passive case automation stop tests (WorkflowEngine.processCase)
      #     DBP-P2-BP-01 (LRV-03B, owner OPTION A): tüm CaseDebtor PASSIVE ise case-seviyesi otomasyon
      #     fail-closed durur (WorkflowEngine.processCase). Yeni dosya — yukarıdaki tenant/lifecycle
      #     allowlist'lerine dahil DEĞİL (ayrı invariant ekseni: case-level automation policy) —
      #     narrow-allowlist stale-test gap'i tekrarlamamak için ayrı, açık isimli step. db-gated satırı
      #     yukarıdaki "Wave 0 tenant-boundary disposable-DB tests" step'inin allowlist'ine eklendi.
      #
      #   LRV-03A CaseDebtor passivation guard tests (WorkflowEngine.createEnforcementAction)
      #
      #   MPB-028(a) PR-5 calculateNextActionTime cutover unit tests
      #
      #   CLIENT-P0-T04-C1 CaseBalance tenant containment regression
      #     CLIENT-P0-T04-C1: CaseBalance tenant fail-closed containment regression.
      #     getBalance/getLedger/credit/debit yollarının service-level ownership gate ile cross-tenant
      #     read/write reddi + zero side-effect (F-STOP-1 kod-yolu exposure containment kanıtı).
      #
      #   CLIENT-SEC-H2B tenant-scope enforcement regression
      #     CLIENT-SEC-H2B: code-level tenant-scope enforcement regression (template update,
      #     UYAP case-status query, document/document-template fail-closed tenant guard).
      #
      #   MPB-028(a) PR-4 finalizationDate cutover unit tests
      #
      #   Error-log security & observability tests (PR-1/2a/2b/3)
      #     PR-1 (güvenlik): RBAC + metadata redaction + source/tenant/level spoof engeli.
      #     PR-2a (observability): global ExceptionFilter + requestId + fingerprint + flood-guard
      #     + server-log builder (≥500 logging, 4xx noise exclusion, logging isolation).
      #     Dosya-varlığı pre-check (rename/silme → CI RED) + passWithNoTests YOK.
      #
      #   CLIENT-SEC-H2A fail-closed containment regression
      #     CLIENT-SEC-H2A: EsignLog/UyapRequestLog fail-closed emergency containment regression.
      #     Şema-seviyesi tenantId yokluğu (H2C ile kalıcı çözülecek) — bu, o zamana kadarki containment kanıtı.
      #
      #   CLIENT-SEC-H2C-P02 tenant write population regression
      #     CLIENT-SEC-H2C-P02: EsignLog/UyapRequestLog new-write tenant population regression.
      #     P02-R1: 5 UYAP write yolunun log ownership'i trusted execution tenant'tan (DTO'dan DEĞİL) alması.
      #
      #   ADR-014 PR-9 golden fixture matrix unit tests
      #     ADR-014 PR-9: the Wave 0 ScenarioDefinition is the single fixture and
      #     expected contract for both the in-memory and disposable-Postgres twins.
      #
      #   W0.3 diagnostic static and evidence tests
      #
      #   W0.2 materializer static boundary tests
      #     ADR-014 W0.2: explicit selection prevents a green job from hiding an
      #     unexecuted static boundary or disposable-DB materializer suite.
      #
      #   ADR-014 PR-1B reversal-netting unit and orchestration tests
      #     ADR-014 PR-1B: retain the PR-1A CaseBalance selection and add the pure
      #     mapper regression explicitly; rename/removal/zero-match must fail closed.
      #
      #   MPB-028(a) PR-2 legal-deadline unit/static tests
      #     MPB-028(a) PR-2: LegalDeadlineService (rejim hesaplama, snapshot immutable/supersede,
      #     objectionPeriodDays validation) + TebligatService TK 21/2 correction / TK m.20 explicit-
      #     override unit-static spec'leri yukarıdaki dar pattern'lerin dışında kalıyordu → CI'da hiç
      #     koşmuyordu. Dosya-varlığı pre-check + passWithNoTests YOK (bkz. GATE-1 CI tenant-isolation
      #     regression coverage emsali).
      #
      #   MPB-028(a) PR-3C proceeding-type rule matrix unit/static tests
      #
      #   OF01-HISTORY-P04-B-R2-I01 LegalServiceDateRuleCore unit tests
      #
      #   OF01-HISTORY-P04-B occurrence deadline calculation unit tests
      #     OF01-HISTORY-P04-B: occurrence-driven idempotent legal deadline calculation
      #     (ServiceOccurrenceDeadlineCalculationService). Yukarıdaki dar allowlist pattern'lerinin
      #     hiçbirine girmiyor (dosya adı hiçbirini eşlemiyor) → narrow named step olmadan CI'da hiç koşmaz.
      #
      #   MPB-028(a) PR-3 Evidence Activation controller + DI-registration tests
      #     MPB-028(a) PR-3 Evidence Activation: LegalTimeShadowController (compute + evidence-report
      #     route/tenant-scoping) + app.module.ts DI-registration regresyon testi (PR-4 Readiness
      #     analizinde tespit edilen "modüller koda yazıldı ama DI container'a hiç kayıtlı değildi"
      #     bulgusunu kilitler). Aynı CI-coverage silent-skip riski (bkz. GATE-1/PR-2/PR-3 emsali)
      #     baştan önlenmek için bu dosyaların kendi ekleniş turunda ayrı/açık step ile koşturulur.
      #
      #   MPB-028(a) PR-3 legal-time-shadow unit/static tests
      #
      #   CLIENT-SEC-H1 cross-tenant containment regression
      #     CLIENT-SEC-H1: cross-tenant/unauth Client PII containment (S1-S4) regression.
      #     Explicit test -f guard: bir spec silinir/yeniden adlandırılırsa step FAIL eder (allowlist stale-test gap koruması).
      #
      #   ReportingLine Population Core tests (CAP-02 object-scope enablement)
      #     CAP-02 ReportingLine Population Core (OFFICE object-scope enablement): ADMIN-only
      #     hiyerarşi yönetimi + invariant'lar (self-manager/cross-tenant/aktiflik/direkt+dolaylı
      #     döngü) + transaction/concurrency (tekil-aktif-amir, Serializable) + tarihsel kapatma +
      #     reconciliation + audit. Dar allowlist'in dışında kalır → explicit step + test -f guard
      #     (ci-narrow-allowlist stale-test gap koruması).
      #
      #   RCV-P2-WS04-P01 allocation drift baseline unit and static tests
      #     RCV-P2-WS04-P01: ratified drift-baseline-only slice. The explicit unit/static
      #     gate locks authority roles, the four comparison classes, same-input parity,
      #     legacy activation diagnostics and the runtime allocator no-write inventory.
      - name: pure/platform-scripts-shared
        run: |
          cd apps/api
          for f in \
            src/config/uyap-interest-crosswalk.spec.ts \
            src/modules/address-discovery/confidence-score.tenant-boundary.spec.ts \
            src/modules/address-discovery/uyap-query.service.spec.ts \
            src/modules/address-task/address-task.controller.spec.ts \
            src/modules/address-task/address-task.service.spec.ts \
            src/modules/ai/ai.service.tenant-boundary.spec.ts \
            src/modules/audit/__tests__/audit-metadata-builder.spec.ts \
            src/modules/audit/__tests__/audit.service.attribution.spec.ts \
            src/modules/automation/__tests__/all-passive-case-automation-stop.spec.ts \
            src/modules/automation/__tests__/automation-tenant-guard-propagation.spec.ts \
            src/modules/automation/__tests__/enforcement-action-guarded-write-path.spec.ts \
            src/modules/automation/__tests__/enforcement-action-guarded-write-path.static-guard.spec.ts \
            src/modules/automation/__tests__/enforcement-action-passivation-guard.spec.ts \
            src/modules/automation/__tests__/workflow-engine-build-context-tenant-guard.spec.ts \
            src/modules/automation/__tests__/workflow-engine-build-context-tenant-guard.static-guard.spec.ts \
            src/modules/automation/__tests__/workflow-engine-legal-time-cutover.spec.ts \
            src/modules/automation/__tests__/workflow-engine-remaining-tenant-boundaries.spec.ts \
            src/modules/automation/__tests__/workflow-engine-remaining-tenant-boundaries.static-guard.spec.ts \
            src/modules/case-balance/__tests__/case-balance-tenant-isolation.spec.ts \
            src/modules/client-notification/__tests__/client-notification-template-tenant-scope.spec.ts \
            src/modules/debtor/__tests__/debtor-finalization-cutover.spec.ts \
            src/modules/debtor/debtor-identity-format.spec.ts \
            src/modules/debtor/ihbarname-summary.tenant-boundary.spec.ts \
            src/modules/debtor/quick-note-passive-writer.spec.ts \
            src/modules/debtor/third-party-passive-writer.spec.ts \
            src/modules/document/__tests__/document-fail-closed-tenant.spec.ts \
            src/modules/error-log/__tests__/all-exceptions.filter.spec.ts \
            src/modules/error-log/__tests__/error-dedupe-key.spec.ts \
            src/modules/error-log/__tests__/error-fingerprint.spec.ts \
            src/modules/error-log/__tests__/error-flood-guard.spec.ts \
            src/modules/error-log/__tests__/error-log-retention.config.spec.ts \
            src/modules/error-log/__tests__/error-log-retention.service.spec.ts \
            src/modules/error-log/__tests__/error-log-tenant-scope.service.spec.ts \
            src/modules/error-log/__tests__/error-log.controller.spec.ts \
            src/modules/error-log/__tests__/error-log.dedupe.service.spec.ts \
            src/modules/error-log/__tests__/error-log.sanitize.spec.ts \
            src/modules/error-log/__tests__/error-reporter.spec.ts \
            src/modules/error-log/__tests__/resolve-error-log.dto.spec.ts \
            src/modules/error-log/__tests__/server-log.builder.spec.ts \
            src/modules/esign/__tests__/esign-h2a-containment.spec.ts \
            src/modules/esign/__tests__/esign-tenant-write-population.spec.ts \
            src/modules/interest-engine/__tests__/golden-fixture-matrix.spec.ts \
            src/modules/interest-engine/__tests__/scenario-diagnostic.static.spec.ts \
            src/modules/interest-engine/__tests__/scenario-evidence.spec.ts \
            src/modules/interest-engine/__tests__/scenario-materializer.static.spec.ts \
            src/modules/interest-engine/calc-prep/__tests__/payment-mapper.spec.ts \
            src/modules/interest-engine/orchestration/__tests__/case-balance.service.spec.ts \
            src/modules/legal-deadline/__tests__/legal-deadline.service.spec.ts \
            src/modules/legal-deadline/__tests__/legal-period-calculation.service.spec.ts \
            src/modules/legal-deadline/__tests__/legal-period-rule-matrix.spec.ts \
            src/modules/legal-deadline/__tests__/legal-service-date-rule-core.spec.ts \
            src/modules/legal-deadline/__tests__/proceeding-classification.service.spec.ts \
            src/modules/legal-deadline/__tests__/proceeding-classification.static-guard.spec.ts \
            src/modules/legal-deadline/__tests__/service-occurrence-deadline-calculation.service.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.app-module-registration.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.controller.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.service.spec.ts \
            src/modules/notification/notification.service.tenant-boundary.spec.ts \
            src/modules/pdf/__tests__/pdf-takip-talebi-authz.spec.ts \
            src/modules/policy-engine/gate-checker/__tests__/uyap-temporary-outage.spec.ts \
            src/modules/reporting-line/__tests__/reporting-line.service.spec.ts \
            src/modules/risk/risk.service.tenant-boundary.spec.ts \
            src/modules/summary-engine/__tests__/allocate-payment-ledger-in-tx.spec.ts \
            src/modules/summary-engine/__tests__/allocation-authority.static-guard.spec.ts \
            src/modules/summary-engine/__tests__/allocation-drift-baseline.spec.ts \
            src/modules/template-engine/__tests__/template-artifact-tenant-scope.spec.ts \
            src/modules/validation-gate/__tests__/validation-gate-authz-tenant.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-classifier.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-profiler.report-hardening.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-profiler.static-purity.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/config/uyap-interest-crosswalk.spec.ts \
            src/modules/address-discovery/confidence-score.tenant-boundary.spec.ts \
            src/modules/address-discovery/uyap-query.service.spec.ts \
            src/modules/address-task/address-task.controller.spec.ts \
            src/modules/address-task/address-task.service.spec.ts \
            src/modules/ai/ai.service.tenant-boundary.spec.ts \
            src/modules/audit/__tests__/audit-metadata-builder.spec.ts \
            src/modules/audit/__tests__/audit.service.attribution.spec.ts \
            src/modules/automation/__tests__/all-passive-case-automation-stop.spec.ts \
            src/modules/automation/__tests__/automation-tenant-guard-propagation.spec.ts \
            src/modules/automation/__tests__/enforcement-action-guarded-write-path.spec.ts \
            src/modules/automation/__tests__/enforcement-action-guarded-write-path.static-guard.spec.ts \
            src/modules/automation/__tests__/enforcement-action-passivation-guard.spec.ts \
            src/modules/automation/__tests__/workflow-engine-build-context-tenant-guard.spec.ts \
            src/modules/automation/__tests__/workflow-engine-build-context-tenant-guard.static-guard.spec.ts \
            src/modules/automation/__tests__/workflow-engine-legal-time-cutover.spec.ts \
            src/modules/automation/__tests__/workflow-engine-remaining-tenant-boundaries.spec.ts \
            src/modules/automation/__tests__/workflow-engine-remaining-tenant-boundaries.static-guard.spec.ts \
            src/modules/case-balance/__tests__/case-balance-tenant-isolation.spec.ts \
            src/modules/client-notification/__tests__/client-notification-template-tenant-scope.spec.ts \
            src/modules/debtor/__tests__/debtor-finalization-cutover.spec.ts \
            src/modules/debtor/debtor-identity-format.spec.ts \
            src/modules/debtor/ihbarname-summary.tenant-boundary.spec.ts \
            src/modules/debtor/quick-note-passive-writer.spec.ts \
            src/modules/debtor/third-party-passive-writer.spec.ts \
            src/modules/document/__tests__/document-fail-closed-tenant.spec.ts \
            src/modules/error-log/__tests__/all-exceptions.filter.spec.ts \
            src/modules/error-log/__tests__/error-dedupe-key.spec.ts \
            src/modules/error-log/__tests__/error-fingerprint.spec.ts \
            src/modules/error-log/__tests__/error-flood-guard.spec.ts \
            src/modules/error-log/__tests__/error-log-retention.config.spec.ts \
            src/modules/error-log/__tests__/error-log-retention.service.spec.ts \
            src/modules/error-log/__tests__/error-log-tenant-scope.service.spec.ts \
            src/modules/error-log/__tests__/error-log.controller.spec.ts \
            src/modules/error-log/__tests__/error-log.dedupe.service.spec.ts \
            src/modules/error-log/__tests__/error-log.sanitize.spec.ts \
            src/modules/error-log/__tests__/error-reporter.spec.ts \
            src/modules/error-log/__tests__/resolve-error-log.dto.spec.ts \
            src/modules/error-log/__tests__/server-log.builder.spec.ts \
            src/modules/esign/__tests__/esign-h2a-containment.spec.ts \
            src/modules/esign/__tests__/esign-tenant-write-population.spec.ts \
            src/modules/interest-engine/__tests__/golden-fixture-matrix.spec.ts \
            src/modules/interest-engine/__tests__/scenario-diagnostic.static.spec.ts \
            src/modules/interest-engine/__tests__/scenario-evidence.spec.ts \
            src/modules/interest-engine/__tests__/scenario-materializer.static.spec.ts \
            src/modules/interest-engine/calc-prep/__tests__/payment-mapper.spec.ts \
            src/modules/interest-engine/orchestration/__tests__/case-balance.service.spec.ts \
            src/modules/legal-deadline/__tests__/legal-deadline.service.spec.ts \
            src/modules/legal-deadline/__tests__/legal-period-calculation.service.spec.ts \
            src/modules/legal-deadline/__tests__/legal-period-rule-matrix.spec.ts \
            src/modules/legal-deadline/__tests__/legal-service-date-rule-core.spec.ts \
            src/modules/legal-deadline/__tests__/proceeding-classification.service.spec.ts \
            src/modules/legal-deadline/__tests__/proceeding-classification.static-guard.spec.ts \
            src/modules/legal-deadline/__tests__/service-occurrence-deadline-calculation.service.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.app-module-registration.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.controller.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.service.spec.ts \
            src/modules/notification/notification.service.tenant-boundary.spec.ts \
            src/modules/pdf/__tests__/pdf-takip-talebi-authz.spec.ts \
            src/modules/policy-engine/gate-checker/__tests__/uyap-temporary-outage.spec.ts \
            src/modules/reporting-line/__tests__/reporting-line.service.spec.ts \
            src/modules/risk/risk.service.tenant-boundary.spec.ts \
            src/modules/summary-engine/__tests__/allocate-payment-ledger-in-tx.spec.ts \
            src/modules/summary-engine/__tests__/allocation-authority.static-guard.spec.ts \
            src/modules/summary-engine/__tests__/allocation-drift-baseline.spec.ts \
            src/modules/template-engine/__tests__/template-artifact-tenant-scope.spec.ts \
            src/modules/validation-gate/__tests__/validation-gate-authz-tenant.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-classifier.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-profiler.report-hardening.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-profiler.static-purity.spec.ts
        working-directory: project
      - name: db/core-lifecycle
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          cd apps/api
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          for f in \
            src/modules/auth/__tests__/auth-cross-tenant-email.integration.spec.ts \
            src/modules/auth/password-reset/__tests__/password-reset.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/all-passive-case-automation-stop.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/automation-stats-tenant-scope.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/automation-toggle-tenant-scope.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/enforcement-action-guarded-write-path.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/enforcement-action-tenant-case-debtor-fk.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/workflow-engine-build-context-tenant-guard.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/workflow-engine-legal-time-cutover.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/workflow-engine-remaining-tenant-boundaries.db-gated.integration.spec.ts \
            src/modules/case/__tests__/case-calculation-summary.compatibility.db-gated.integration.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-finalizer.db-gated.integration.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-intent.db-gated.integration.spec.ts \
            src/modules/debtor/__tests__/debtor-finalization-cutover.db-gated.integration.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/uyap-event-ingest.boundary.integration.spec.ts \
            src/modules/interest-engine/__tests__/golden-fixture-matrix.db-gated.integration.spec.ts \
            src/modules/interest-engine/__tests__/scenario-diagnostic.db-gated.integration.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/auth/__tests__/auth-cross-tenant-email.integration.spec.ts \
            src/modules/auth/password-reset/__tests__/password-reset.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/all-passive-case-automation-stop.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/automation-stats-tenant-scope.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/automation-toggle-tenant-scope.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/enforcement-action-guarded-write-path.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/enforcement-action-tenant-case-debtor-fk.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/workflow-engine-build-context-tenant-guard.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/workflow-engine-legal-time-cutover.db-gated.integration.spec.ts \
            src/modules/automation/__tests__/workflow-engine-remaining-tenant-boundaries.db-gated.integration.spec.ts \
            src/modules/case/__tests__/case-calculation-summary.compatibility.db-gated.integration.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-finalizer.db-gated.integration.spec.ts \
            src/modules/claim-item/__tests__/claim-item-formation-intent.db-gated.integration.spec.ts \
            src/modules/debtor/__tests__/debtor-finalization-cutover.db-gated.integration.spec.ts \
            src/modules/icrabot/v28-engine/__tests__/uyap-event-ingest.boundary.integration.spec.ts \
            src/modules/interest-engine/__tests__/golden-fixture-matrix.db-gated.integration.spec.ts \
            src/modules/interest-engine/__tests__/scenario-diagnostic.db-gated.integration.spec.ts
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: db/domain-integration ─────────────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   W0.2 materializer disposable DB tests
      #
      #   MPB-028(a) PR-2 legal-deadline disposable-DB tests
      #     MPB-028(a) PR-2 (disposable-DB kısmı): tenant-scoped Tebligat lookup + idempotent/supersede
      #     snapshot zincirinin gerçek Postgres üzerinde doğru davrandığını doğrular. Mevcut job'ın
      #     Postgres service'i (yukarıdaki `services: postgres:`) reuse edilir — yeni container/job YOK.
      #
      #   MPB-028(a) PR-3C proceeding-type rule matrix disposable-DB tests
      #     MPB-028(a) PR-3C (disposable-DB kısmı): gerçek Postgres'te Case.proceedingType/
      #     rentalType/bankruptcyType/judgmentExecutionType sütunlarından doğru sınıflandırma +
      #     süre hesabı, tenant-isolation ve read-only (satır yazmama) davranışını doğrular.
      #
      #   OF01-HISTORY-P04-B occurrence deadline calculation disposable-DB tests
      #
      #   MPB-028(a) PR-3 legal-time-shadow disposable-DB tests
      #     MPB-028(a) PR-3 (disposable-DB kısmı): immutable diff kaydı + tenant isolation +
      #     feature-flag-kapalı no-op davranışının gerçek Postgres üzerinde doğru çalıştığını
      #     doğrular. Mevcut job'ın Postgres service'i reuse edilir — yeni container/job YOK.
      #     Evidence Activation turunda aynı dosyaya LegalTimeShadowController uçtan uca (gerçek
      #     servis + gerçek DB) testleri de eklendi — ayrı bir step gerekmez, mevcut dar pattern
      #     zaten dosya adına göre eşleşir.
      #
      #   CLIENT-P2-U01-R1 portal reset tenant-scope integration tests
      #     CLIENT-P2-U01-R1: credential-recovery.spec.ts (PR #1477) yalnız mocked-prisma unit
      #     seviyesindedir; forgot/reset-password akışının gerçek Postgres üzerinde tenant/client
      #     cross-account izolasyon kanıtı yoktu. Canonical PortalService davranışı DEĞİŞTİRİLMEDEN
      #     test edilir (gerçek Postgres, disposable service-container).
      #
      #   CLIENT-P2-U02 portal tokenVersion integration tests
      #     CLIENT-P2-U02 integration: reset/change/disable/reactivate sonrası eski portal JWT'nin
      #     gerçek Postgres üzerinde GERÇEKTEN reddedildiğini, ilgisiz hesap/tenant'ların
      #     etkilenmediğini, tenant/client claim-substitution'ın reddedildiğini, legacy (claim'siz)
      #     token cutover'ının ve eşzamanlı atomic increment'in kayıpsız olduğunu kanıtlar.
      #
      #   RCV-P2-WS04-P01 allocation drift disposable DB tests
      #     RCV-P2-WS04-P01 disposable-DB evidence: cent-exact LedgerAllocation/cache
      #     reconciliation plus injected-drift rollback. Skipped/zero-test is RED.
      #
      #   OF01-HISTORY-P04-A1 deadline-facts and snapshot-binding disposable-DB tests
      #     OF01-HISTORY-P04-A1: ServiceOccurrence.addressTypeAtOccurrence/serviceDateRole (immutable
      #     deadline-fact kolonları) + LegalDeadlineSnapshot.sourceServiceOccurrenceId (tenant-safe
      #     composite FK binding) doğrular. Yukarıdaki dar allowlist pattern'lerinin hiçbirine
      #     girmiyor (dosya adı hiçbirini eşlemiyor) → narrow named step olmadan CI'da hiç koşmaz.
      #
      #   OF01-HISTORY-P01 ServiceOccurrence schema disposable-DB tests
      #     OF01-HISTORY-P01 (PR #1503, retroactif eklendi — bkz. GATE-1/PR-2/PR-3/LRV-02/DBP-P2-SEC-P02
      #     emsali "CI-coverage silent-skip riski"): ServiceOccurrence Prisma modeli (Tebligat/
      #     LegalDeadlineSnapshot arasında immutable/supersedable "gözlemlenen hukuki olgu" katmanı) —
      #     composite tenant-scoped FK, 7 CHECK constraint, 2 immutability trigger. Yukarıdaki dar
      #     allowlist pattern'lerinin hiçbirine girmiyordu → CI'da hiç koşmuyordu (yalnız P01'in kendi
      #     GO-IMPLEMENT'inde local `npx jest` ile doğrulandı). Dosya-varlığı pre-check + passWithNoTests YOK.
      #
      #   OF01-HISTORY-P03 Tebligat/ServiceOccurrence atomic integration disposable-DB tests
      #     OF01-HISTORY-P03 (disposable-DB kısmı): gerçek Postgres'te Tebligat+CaseDebtor+
      #     ServiceOccurrence+outbox 4 yazımın tek transaction'da atomik olduğunu (occurrence veya
      #     outbox append başarısızsa TÜM yazımların rollback edildiğini) doğrular.
      #
      #   OF01-HISTORY-P04-C-I01 service-occurrence-recorded consumer disposable-DB tests
      #
      #   OF01-HISTORY-P04-A2 canonical event payload access disposable-DB tests
      #     OF01-HISTORY-P04-A2: canonical SERVICE_OCCURRENCE_RECORDED event payload access
      #     (ServiceOccurrenceRecordedEventAccessor + DomainEventIngestService.timelineEntryId stable
      #     reference). Yukarıdaki dar allowlist pattern'lerinin hiçbirine girmiyor (dosya adı
      #     hiçbirini eşlemiyor) → narrow named step olmadan CI'da hiç koşmaz.
      #
      #   OF01-HISTORY-P02 ServiceOccurrenceService disposable-DB tests
      #     OF01-HISTORY-P02 (disposable-DB kısmı): gerçek Postgres'te atomik create/supersede +
      #     concurrency race testleri (advisory-lock kanıtı dahil) doğrular.
      #
      #   OFFICE-AUTH-P01 change-password integration tests
      #     OFFICE-AUTH-P01 integration: tenant-scoped self-service parola değişimi, eski JWT
      #     (tokenVersion mismatch) reddi, yeni login PASS, cross-tenant DENIED, migration
      #     default/nullable doğrulaması (gerçek Postgres, disposable service-container).
      #
      #   SEC-XTEN-UYAP-STATS-01 disposable-DB tenant-scope tests
      #     SEC-XTEN-UYAP-STATS-01 disposable-DB kısmı: composite tenant-scoped where-clause'ın gerçek
      #     Postgres üzerinde doğru satırları eşleştirdiğini/reddettiğini ve legacy tenantId=NULL
      #     kayıtların dışlandığını kanıtlar. Mevcut job'ın Postgres service'i (yukarıdaki
      #     `services: postgres:`) reuse edilir — yeni container/job YOK.
      #
      #   Wave 0 tenant-boundary disposable-DB tests (WorkflowEngine/EnforcementAction)
      #     GATE-1 CI Tenant-Isolation Regression Coverage (disposable-DB kısmı): aynı Wave 0 ailesinin
      #     composite where-clause'ının gerçek Postgres üzerinde doğru davrandığını doğrulayan
      #     entegrasyon testleri de aynı nedenle CI'da hiç koşmuyordu. Mevcut job'ın Postgres service'i
      #     (yukarıdaki `services: postgres:`) reuse edilir — yeni container/job YOK.
      - name: db/domain-integration
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          cd apps/api
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          for f in \
            src/modules/interest-engine/__tests__/scenario-materializer.db-gated.integration.spec.ts \
            src/modules/legal-deadline/__tests__/legal-deadline.db-gated.integration.spec.ts \
            src/modules/legal-deadline/__tests__/proceeding-classification.db-gated.integration.spec.ts \
            src/modules/legal-deadline/__tests__/service-occurrence-deadline-calculation.db-gated.integration.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.db-gated.integration.spec.ts \
            src/modules/portal/__tests__/portal-reset-tenant-scope.db-gated.integration.spec.ts \
            src/modules/portal/__tests__/portal-token-version-revocation.db-gated.integration.spec.ts \
            src/modules/summary-engine/__tests__/allocation-drift-baseline.db-gated.integration.spec.ts \
            src/modules/tebligat/__tests__/service-occurrence-deadline-facts.db-gated.integration.spec.ts \
            src/modules/tebligat/__tests__/service-occurrence.db-gated.integration.spec.ts \
            src/modules/tebligat/__tests__/tebligat-service-occurrence-write.db-gated.integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded-consumer.db-gated.integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded-event.accessor.db-gated.integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-write.db-gated.integration.spec.ts \
            src/modules/user/__tests__/change-password.integration.spec.ts \
            src/modules/uyap/__tests__/uyap-stats-tenant-scope.db-gated.integration.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-profiler.db-gated.integration.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/interest-engine/__tests__/scenario-materializer.db-gated.integration.spec.ts \
            src/modules/legal-deadline/__tests__/legal-deadline.db-gated.integration.spec.ts \
            src/modules/legal-deadline/__tests__/proceeding-classification.db-gated.integration.spec.ts \
            src/modules/legal-deadline/__tests__/service-occurrence-deadline-calculation.db-gated.integration.spec.ts \
            src/modules/legal-time-shadow/__tests__/legal-time-shadow.db-gated.integration.spec.ts \
            src/modules/portal/__tests__/portal-reset-tenant-scope.db-gated.integration.spec.ts \
            src/modules/portal/__tests__/portal-token-version-revocation.db-gated.integration.spec.ts \
            src/modules/summary-engine/__tests__/allocation-drift-baseline.db-gated.integration.spec.ts \
            src/modules/tebligat/__tests__/service-occurrence-deadline-facts.db-gated.integration.spec.ts \
            src/modules/tebligat/__tests__/service-occurrence.db-gated.integration.spec.ts \
            src/modules/tebligat/__tests__/tebligat-service-occurrence-write.db-gated.integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded-consumer.db-gated.integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-recorded-event.accessor.db-gated.integration.spec.ts \
            src/modules/tebligat/service-occurrence/__tests__/service-occurrence-write.db-gated.integration.spec.ts \
            src/modules/user/__tests__/change-password.integration.spec.ts \
            src/modules/uyap/__tests__/uyap-stats-tenant-scope.db-gated.integration.spec.ts \
            src/scripts/__tests__/enforcement-action-backfill-profiler.db-gated.integration.spec.ts
        working-directory: project
      # ── GH-03 CONSOLIDATED MANIFEST: pure/architecture-guards ──────────────────────
      # Bu manifest asagidaki step'leri TEK jest process'inde birlestirir.
      # Spec kumesi degismedi (BEFORE/AFTER --listTests esitligi ile kanitlandi);
      # yalnizca process sayisi dustu. Orijinal step gerekceleri korunmustur:
      #
      #   Tests
      - name: pure/architecture-guards
        run: |
          cd apps/api
          for f in \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/drift-guard-killswitch.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/drift-guard-wiring.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/drift-guard.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-control-contract.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-golden.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-precedence-chain.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-state-machine.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-transition-guards.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-25-adaptive-shadow-wiring.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-26-d1-prom-metrics.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-26-d2-tenant-gating.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-mapper-selection.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-provider-health-classifier.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-real-input-mapper.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-telemetry.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-alert-inventory.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-dashboard-validity.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-evidence-pack.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-metrics-completeness.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-pilot-config.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-rollback-ordering.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-route-determinism.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-s1-evidence.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-s2-evidence.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-alert-noise-ratio.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b1-ewma-golden.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b2-window-validity.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b3-sigma-zone.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b4-compliance-criterion.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b5-tenant-segmentation.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-drift-baseline.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-infra-prerequisites.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-provider-error-profile.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-risk-matrix.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-runtime-smoke-checklist.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-s1-live-sequence.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-s2-live-sequence.spec.ts; do
            test -f "$f" || { echo "MISSING spec: $f"; exit 1; }
          done
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/drift-guard-killswitch.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/drift-guard-wiring.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/drift-guard.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-control-contract.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-golden.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-precedence-chain.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-state-machine.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-2-transition-guards.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-25-adaptive-shadow-wiring.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-26-d1-prom-metrics.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-26-d2-tenant-gating.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-mapper-selection.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-provider-health-classifier.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-real-input-mapper.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/sd-3-telemetry.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-alert-inventory.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-dashboard-validity.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-evidence-pack.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-metrics-completeness.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-pilot-config.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-rollback-ordering.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-route-determinism.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-s1-evidence.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-0-s2-evidence.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-alert-noise-ratio.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b1-ewma-golden.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b2-window-validity.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b3-sigma-zone.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b4-compliance-criterion.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-b5-tenant-segmentation.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-drift-baseline.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-infra-prerequisites.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-provider-error-profile.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-risk-matrix.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-runtime-smoke-checklist.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-s1-live-sequence.spec.ts \
            src/modules/calc-preview/diagnostics/simulation-api/guards/__tests__/stage-1-s2-live-sequence.spec.ts
        working-directory: project
      - name: PR-A4-2 rich-interest diagnostic fixture and disposable DB evidence
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          cd apps/api
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          for f in src/modules/case/inventory/__tests__/rich-interest-uyap-readiness.fixture.spec.ts \
                   src/modules/case/inventory/__tests__/rich-interest-uyap-readiness.fixture.db-gated.integration.spec.ts; do
            test -f "$f" || { echo "MISSING PR-A4-2 spec: $f"; exit 1; }
          done
          set -o pipefail
          output_file="$(mktemp)"
          trap 'rm -f "$output_file"' EXIT
          npx jest --ci --forceExit --runInBand \
            --testPathPattern="rich-interest-uyap-readiness\.fixture(\.db-gated\.integration)?\.spec\.ts$" \
            2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+2 passed, 2 total" "$output_file" || \
            { echo "BLOCKED: PR-A4-2 suites did not execute exactly twice"; exit 1; }
          grep -Eq "Tests:[[:space:]]+49 passed, 49 total" "$output_file" || \
            { echo "BLOCKED: PR-A4-2 tests did not execute 49/49"; exit 1; }
        working-directory: project
      - name: ADR014-PE-06A disabled local evidence harness tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-disabled-local-evidence-harness.ts
          functional_spec=src/scripts/__tests__/adr014-disabled-local-evidence-harness.spec.ts
          security_spec=src/scripts/__tests__/adr014-disabled-local-evidence-harness.security.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-PE-06A file: $f"; exit 1; }
          done
          for pattern in '@nestjs/' '@prisma/' 'node:fs' 'node:path' 'node:http' 'node:https' \
                         'node:net' 'node:tls' 'AuditService' 'AuditLog' 'prom-client' \
                         'worker_threads' 'balance-display-shadow-diff' "from 'fs'" 'from "fs"' \
                         "from 'path'" 'from "path"' "from 'http'" 'from "http"' \
                         "from 'https'" 'from "https"' "from 'net'" 'from "net"' \
                         "from 'tls'" 'from "tls"' 'axios' 'fetch(' 'cron' 'bullmq' 'ioredis'; do
            if grep -F -n "$pattern" "$source_file" "$functional_spec" "$security_spec"; then
              echo "FORBIDDEN ADR014-PE-06A dependency: $pattern"; exit 1
            fi
          done
          if grep -E -n 'require\.main|process\.argv|process\.env|NestFactory|function[[:space:]]+main|Math\.random|randomUUID|Date\.' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06A execution or nondeterminism surface"; exit 1
          fi
          if grep -E -n 'tenantId|caseId|debtorId|creditorId|clientId|personId|amount|principal|interest|fee|rawError|stack|reason|metadata' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06A payload field"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec"
        working-directory: project
      # ADR014-PE-06C1 remains preparation-only: v1 compatibility and the v2 mapper must stay
      # default-disabled, pure and dependency-free while all direct specs execute explicitly.
      - name: ADR014-PE-06C1 default-disabled observation contract tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-session-observation-producer.ts
          functional_spec=src/scripts/__tests__/adr014-session-observation-producer.spec.ts
          security_spec=src/scripts/__tests__/adr014-session-observation-producer.security.spec.ts
          compatibility_spec=src/modules/balance-display-shadow-diff/__tests__/balance-display-shadow-diff.events.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec" "$compatibility_spec"; do
            test -f "$f" || { echo "MISSING ADR014-PE-06C1 file: $f"; exit 1; }
          done
          if grep -E -n '@nestjs/|@prisma/|node:(fs|http|https|net|tls|crypto)|process\.(env|argv)|NestFactory|Date\.|new Date|Math\.random|randomUUID|fetch\(|axios|console\.|AuditLog|prom-client|function[[:space:]]+main|require\.main' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06C1 runtime or nondeterminism surface"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec" \
            "$compatibility_spec"
        working-directory: project
      # ADR014-PE-06D composes PE-06A/B1/C1 only in a default-disabled, synthetic,
      # test-only dry-validation shell. Runtime activation and external I/O remain forbidden.
      - name: ADR014-PE-06D local session orchestrator dry-validation tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-local-session-orchestrator-dry-validation.ts
          functional_spec=src/scripts/__tests__/adr014-local-session-orchestrator-dry-validation.spec.ts
          security_spec=src/scripts/__tests__/adr014-local-session-orchestrator-dry-validation.security.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-PE-06D file: $f"; exit 1; }
          done
          if grep -E -n '@nestjs/|@prisma/|node:(fs|path|http|https|net|tls|crypto)|process\.(env|argv)|NestFactory|new Date|Date\.|Math\.random|randomUUID|fetch\(|axios|console\.|AuditLog|AuditService|prom-client|setTimeout|setInterval|function[[:space:]]+main|require\.main' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06D runtime, I/O or nondeterminism surface"; exit 1
          fi
          if grep -E -n 'tenantId|caseId|debtorId|creditorId|clientId|personId|principal|interest|fee|amount|currency|rawError|stack|metadata' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06D business or financial payload field"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec"
        working-directory: project
      # ADR014-PE-06E defines only pure, default-disabled local preparation contracts.
      # Audit persistence, evidence acceptance, runtime telemetry, and alert delivery stay absent.
      - name: ADR014-PE-06E local observability surface contract tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-local-observability-surfaces.ts
          functional_spec=src/scripts/__tests__/adr014-local-observability-surfaces.spec.ts
          security_spec=src/scripts/__tests__/adr014-local-observability-surfaces.security.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-PE-06E file: $f"; exit 1; }
          done
          if grep -E -n '@nestjs/|@prisma/|node:(fs|path|http|https|net|tls)|process\.(env|argv)|NestFactory|Date\.now|Math\.random|randomUUID|fetch\(|axios|console\.|AuditLog|AuditService|prom-client|setTimeout|setInterval|function[[:space:]]+main|require\.main' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06E runtime, I/O or nondeterminism surface"; exit 1
          fi
          if grep -E -n 'tenantId|caseId|debtorId|creditorId|clientId|personId|amount|rawError|stack|metadata' "$source_file"; then
            echo "FORBIDDEN ADR014-PE-06E business or sensitive payload field"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec"
        working-directory: project
      # ADR014-REP-01B is an exact local execution boundary, not an activated run.
      # It must remain default-disabled, local-only, read-only and free of network clients.
      - name: ADR014-REP-01B local read-only representative runner tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-local-read-only-representative-runner.ts
          functional_spec=src/scripts/__tests__/adr014-local-read-only-representative-runner.spec.ts
          security_spec=src/scripts/__tests__/adr014-local-read-only-representative-runner.security.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-REP-01B file: $f"; exit 1; }
          done
          if grep -E -n '@nestjs/|node:(http|https|net|tls|dgram)|process\.(env|argv)|NestFactory|fetch\(|axios|console\.|AuditLog|AuditService|prom-client|setTimeout|setInterval|function[[:space:]]+main|require\.main' "$source_file"; then
            echo "FORBIDDEN ADR014-REP-01B runtime, egress or activation surface"; exit 1
          fi
          if grep -E -n 'tenantId|caseId|debtorId|creditorId|clientId|personId|rawError|stack|metadata' "$source_file"; then
            echo "FORBIDDEN ADR014-REP-01B business or sensitive payload field"; exit 1
          fi
          grep -F -q 'SET TRANSACTION ISOLATION LEVEL REPEATABLE READ, READ ONLY' "$source_file" || \
            { echo "MISSING ADR014-REP-01B read-only transaction guard"; exit 1; }
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec" \
            src/scripts/__tests__/adr014-local-session-orchestrator-dry-validation.spec.ts \
            src/scripts/__tests__/adr014-local-session-orchestrator-dry-validation.security.spec.ts
        working-directory: project
      # ADR014-REP-01A validates run-specific owner references only. It cannot access data,
      # execute the runner, emit telemetry or promote evidence/PR-11/runtime authority.
      - name: ADR014-REP-01A run-specific authorization package tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-run-specific-authorization-package.ts
          functional_spec=src/scripts/__tests__/adr014-run-specific-authorization-package.spec.ts
          security_spec=src/scripts/__tests__/adr014-run-specific-authorization-package.security.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-REP-01A file: $f"; exit 1; }
          done
          if grep -E -n '@nestjs/|@prisma/|node:(fs|http|https|net|tls|dgram)|process\.(env|argv)|NestFactory|PrismaClient|fetch\(|axios|console\.|AuditLog|AuditService|prom-client|setTimeout|setInterval|function[[:space:]]+main|require\.main' "$source_file"; then
            echo "FORBIDDEN ADR014-REP-01A runtime, persistence, egress or activation surface"; exit 1
          fi
          if grep -E -n 'tenantId|caseId|debtorId|creditorId|clientId|personId|rawError|stack|arbitraryMetadata' "$source_file"; then
            echo "FORBIDDEN ADR014-REP-01A business or sensitive payload field"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec" \
            src/scripts/__tests__/adr014-disabled-local-evidence-harness.spec.ts \
            src/scripts/__tests__/adr014-local-read-only-representative-runner.spec.ts
        working-directory: project
      # ADR014-REP-01A-R2-I1 materializes owner decisions only. Runtime facts, execution,
      # data access and authority promotion remain forbidden.
      - name: ADR014-REP-01A-R2-I1 v2 pre-run package instance tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-v2-pre-run-package-instance.ts
          functional_spec=src/scripts/__tests__/adr014-v2-pre-run-package-instance.spec.ts
          security_spec=src/scripts/__tests__/adr014-v2-pre-run-package-instance.security.spec.ts
          for f in "$source_file" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-REP-01A-R2-I1 file: $f"; exit 1; }
          done
          if grep -E -n '@nestjs/|@prisma/|node:(fs|http|https|net|tls|dgram)|process\.(env|argv)|NestFactory|PrismaClient|fetch\(|axios|function[[:space:]]+main|require\.main' "$source_file"; then
            echo "FORBIDDEN ADR014-REP-01A-R2-I1 runtime, persistence, egress or activation surface"; exit 1
          fi
          if grep -E -n 'tenantId|caseId|debtorId|creditorId|clientId|personId|password|secret|connectionString|databaseUrl|\bamount\b' "$source_file"; then
            echo "FORBIDDEN ADR014-REP-01A-R2-I1 identity, credential or financial payload"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec"
        working-directory: project
      # ADR014-REP-02 is an owner-authorized local-only execution call-site. CI exercises
      # only mocks/static guards; it never reads local or representative data.
      - name: ADR014-REP-02 local representative execution contract tests
        run: |
          cd apps/api
          source_file=src/scripts/adr014-rep-02-local-execution.ts
          runner_source=src/scripts/adr014-local-read-only-representative-runner.ts
          functional_spec=src/scripts/__tests__/adr014-rep-02-local-execution.spec.ts
          security_spec=src/scripts/__tests__/adr014-rep-02-local-execution.security.spec.ts
          for f in "$source_file" "$runner_source" "$functional_spec" "$security_spec"; do
            test -f "$f" || { echo "MISSING ADR014-REP-02 file: $f"; exit 1; }
          done
          grep -F -q 'ADR014_REPRESENTATIVE_RUNNER_READ_ONLY_SQL' "$source_file" || \
            { echo "MISSING ADR014-REP-02 canonical read-only transaction binding"; exit 1; }
          grep -F -q 'SET TRANSACTION ISOLATION LEVEL REPEATABLE READ, READ ONLY' "$runner_source" || \
            { echo "MISSING ADR014-REP-02 read-only transaction guard"; exit 1; }
          if grep -E -n "node:(http|https|net|tls|dgram)|from ['\"](axios|openai|ioredis|prom-client)|@aws-sdk|NestFactory|createApplicationContext|fetch\(" "$source_file"; then
            echo "FORBIDDEN ADR014-REP-02 external egress, telemetry or runtime bootstrap surface"; exit 1
          fi
          npx jest --ci --forceExit --runInBand --runTestsByPath \
            "$functional_spec" \
            "$security_spec" \
            src/scripts/__tests__/adr014-v2-pre-run-package-instance.spec.ts \
            src/scripts/__tests__/adr014-run-specific-authorization-package.spec.ts \
            src/scripts/__tests__/adr014-local-read-only-representative-runner.spec.ts
        working-directory: project
      - name: ADR-014 PR-1B real cancellation disposable DB tests
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          cd apps/api
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          test -f src/modules/collection/__tests__/collection-cancel-reversal.db-gated.integration.spec.ts || \
            { echo "MISSING ADR-014 PR-1B DB-gated spec"; exit 1; }
          set -o pipefail
          output_file="$(mktemp)"
          npx jest --ci --forceExit --runInBand --verbose \
            --testPathPattern="src/modules/collection/__tests__/collection-cancel-reversal\.db-gated\.integration\.spec\.ts$" \
            2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+1 passed, 1 total" "$output_file" || \
            { echo "BLOCKED: ADR-014 PR-1B DB suite did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen = 1; passed = $2; total = $4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed == total && total > 0) }' "$output_file" || \
            { echo "BLOCKED: ADR-014 PR-1B DB tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project
      - name: CI-8 Icrabot governance guard
        run: |
          test -f scripts/ci-8-icrabot-governance-guard.cjs || { echo "MISSING CI-8 guard script: scripts/ci-8-icrabot-governance-guard.cjs"; exit 1; }
          node scripts/ci-8-icrabot-governance-guard.cjs
        working-directory: project/apps/api
      - name: UYAP-P05A-R1 operation/attempt schema static contract
        run: |
          spec=src/modules/uyap/__tests__/uyap-operation-attempt-schema.static.spec.ts
          migration=prisma/migrations/20260722170000_uyap_operation_attempt_schema_foundation_r1/migration.sql
          test -f "$spec" || { echo "MISSING P05A-R1 static spec: $spec"; exit 1; }
          test -f "$migration" || { echo "MISSING P05A-R1 migration: $migration"; exit 1; }
          if grep -inE '^[[:space:]]*(update|insert|delete|truncate|drop)[[:space:]]' "$migration"; then
            echo "FORBIDDEN P05A-R1 non-additive DML statement in migration"; exit 1
          fi
          if grep -inE '^[[:space:]]*alter[[:space:]].*(drop|rename)' "$migration"; then
            echo "FORBIDDEN P05A-R1 destructive ALTER (drop/rename) in migration"; exit 1
          fi
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec"
        working-directory: project/apps/api
      - name: UYAP-P05A-R1 operation/attempt schema disposable DB acceptance
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          spec=src/modules/uyap/__tests__/uyap-operation-attempt-schema.db-gated.integration.spec.ts
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          test -f "$spec" || { echo "MISSING P05A-R1 DB-gated spec: $spec"; exit 1; }
          set -o pipefail
          output_file="$(mktemp)"
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec" 2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+1 passed, 1 total" "$output_file" || \
            { echo "BLOCKED: P05A-R1 DB suite did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen=1; passed=$2; total=$4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed==total && total>0) }' "$output_file" || \
            { echo "BLOCKED: P05A-R1 DB tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project/apps/api
      # UYAP-OPERATION-ATTEMPT-WRITER-P05B (F4-b/P-E5B): dormant internal writer foundation.
      # STEP 1 unit + DORMANCY/BOUNDARY guard: UyapModule writer'i KAYDETMEZ, UyapService import ETMEZ,
      # writer UyapRequestLog'a dokunmaz, state mutation (internalState/provider/legal-effect) YAPMAZ,
      # controller/DTO/schema dosyasi yoktur; branded idempotency key client string'inden uretilemez.
      # Dosya-varligi guard'ina EK olarak grep tabanli dormancy kontrolu: kayit/injection eklenirse RED.
      # STEP 2 disposable PostgreSQL 16'da esszamanli advisory-lock serilestirmesi, gap-free attempt
      # lineage'i ve optimistic compare-and-bump'i dogrular; skipped/zero-test RED.
      - name: UYAP-P05B operation writer unit and dormancy guard
        run: |
          spec=src/modules/uyap/operation-writer/uyap-operation-writer.service.spec.ts
          writer=src/modules/uyap/operation-writer/uyap-operation-writer.service.ts
          for f in "$spec" "$writer" \
                   src/modules/uyap/operation-writer/uyap-operation-writer.types.ts \
                   src/modules/uyap/operation-writer/uyap-operation-writer.errors.ts \
                   src/modules/uyap/operation-writer/uyap-operation-writer-lock.ts; do
            test -f "$f" || { echo "MISSING P05B file: $f"; exit 1; }
          done
          # P05C-P04 UYARLAMASI: writer artık YETKİLİ (module kaydı + orchestrator). Guard DARALTILDI:
          # UyapService raw operation-writer'ı DOĞRUDAN kullanamaz (yalnız orchestrator yoluyla).
          if grep -nE 'UyapOperationWriterService' src/modules/uyap/uyap.service.ts; then
            echo "FORBIDDEN P05C-P04: UyapService raw operation-writer'i DOGRUDAN kullanamaz (yalniz orchestrator)"; exit 1
          fi
          if grep -nE '\.uyapRequestLog|Controller|CasePolicyEngine|cpeDecisionLog' "$writer"; then
            echo "FORBIDDEN P05B surface: dual-write / controller / CPE linkage"; exit 1
          fi
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec"
        working-directory: project/apps/api
      - name: UYAP-P05B operation writer disposable DB acceptance
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          spec=src/modules/uyap/operation-writer/uyap-operation-writer.db-gated.integration.spec.ts
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          test -f "$spec" || { echo "MISSING P05B DB-gated spec: $spec"; exit 1; }
          set -o pipefail
          output_file="$(mktemp)"
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec" 2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+1 passed, 1 total" "$output_file" || \
            { echo "BLOCKED: P05B DB suite did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen=1; passed=$2; total=$4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed==total && total>0) }' "$output_file" || \
            { echo "BLOCKED: P05B DB tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project/apps/api
      # UYAP-CPE-DECISION-LINK-WRITER-P05C-P03 (Karar C): DORMANT UyapAttemptCpeDecisionLink writer.
      # Actor/lawyer/signer input ALMAZ; M5 composite FK + @@unique([cpeDecisionLogId]) authoritative;
      # schema/migration EKLEMEZ. Duplicate semantics: exact replay=IDEMPOTENT, farkli iliski=CONFLICT,
      # FK uyusmazligi=INTEGRITY error (fail-closed). STEP 1 unit + dormancy/boundary guard: module
      # kaydi, actor input, controller/DTO gorulurse RED. STEP 2 disposable PostgreSQL 16'da idempotent
      # replay, conflict reddi, esszamanli race, cross-tenant/case/attempt reddi, rollback dogrulanir.
      - name: UYAP-P05C-P03 link writer unit and dormancy guard
        run: |
          spec=src/modules/uyap/operation-writer/uyap-cpe-decision-link-writer.service.spec.ts
          writer=src/modules/uyap/operation-writer/uyap-cpe-decision-link-writer.service.ts
          for f in "$spec" "$writer" \
                   src/modules/uyap/operation-writer/uyap-cpe-decision-link-writer.types.ts \
                   src/modules/uyap/operation-writer/uyap-cpe-decision-link-writer.errors.ts; do
            test -f "$f" || { echo "MISSING P05C-P03 file: $f"; exit 1; }
          done
          # P05C-P04 UYARLAMASI: link writer artık YETKİLİ (module kaydı + orchestrator). Guard DARALTILDI:
          # UyapService raw link-writer'ı DOĞRUDAN kullanamaz (yalnız orchestrator yoluyla).
          if grep -nE 'UyapCpeDecisionLinkWriterService' src/modules/uyap/uyap.service.ts; then
            echo "FORBIDDEN P05C-P04: UyapService raw link-writer'i DOGRUDAN kullanamaz (yalniz orchestrator)"; exit 1
          fi
          if grep -nE 'actorUserId|actingLawyerId|signatureOwnerId|approverId|canPerformAction|Controller' "$writer"; then
            echo "FORBIDDEN P05C-P03 surface: actor/lawyer/signer input, CPE decision or controller"; exit 1
          fi
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec"
        working-directory: project/apps/api
      - name: UYAP-P05C-P03 link writer disposable DB acceptance
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          spec=src/modules/uyap/operation-writer/uyap-cpe-decision-link-writer.db-gated.integration.spec.ts
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          test -f "$spec" || { echo "MISSING P05C-P03 DB-gated spec: $spec"; exit 1; }
          set -o pipefail
          output_file="$(mktemp)"
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec" 2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+1 passed, 1 total" "$output_file" || \
            { echo "BLOCKED: P05C-P03 DB suite did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen=1; passed=$2; total=$4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed==total && total>0) }' "$output_file" || \
            { echo "BLOCKED: P05C-P03 DB tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project/apps/api
      # POLICY-CPE-DECISION-COMPOSITE-KEY-P05C-P01: CpeDecisionLog'u gelecekteki tenant-safe UYAP
      # evidence linkage icin composite FK HEDEFI yapar. Kapsam BILINCLI olarak tek additive unique
      # index: tenantId kolonu YOK, backfill YOK, link tablosu YOK, write-path DEGISMEZ.
      # STEP 1 statik kontrat + migration guard: migration TEK statement olmali ve yalnizca
      # CREATE UNIQUE INDEX icermeli; ALTER/DROP/CREATE TABLE veya DML gorulurse RED.
      # STEP 2 disposable PostgreSQL 16'da index'in UNIQUE kuruldugunu, PK/FK'nin korundugunu ve
      # migration'in HIC veri yazmadigini dogrular; skipped/zero-test RED.
      - name: P05C-P01 CpeDecisionLog composite key static contract
        run: |
          spec=src/modules/policy-engine/decision-logger/__tests__/cpe-decision-composite-key.static.spec.ts
          migration=prisma/migrations/20260722230000_cpe_decision_composite_reference_key/migration.sql
          test -f "$spec" || { echo "MISSING P05C-P01 static spec: $spec"; exit 1; }
          test -f "$migration" || { echo "MISSING P05C-P01 migration: $migration"; exit 1; }
          statements="$(grep -c ';' "$migration")"
          test "$statements" -eq 1 || { echo "FORBIDDEN P05C-P01: migration must contain exactly 1 statement (found $statements)"; exit 1; }
          grep -qE '^CREATE UNIQUE INDEX "CpeDecisionLog_id_caseId_key"' "$migration" || \
            { echo "MISSING P05C-P01 composite unique index statement"; exit 1; }
          if grep -inE '^[[:space:]]*(update|insert|delete|truncate|drop|alter)[[:space:]]|CREATE TABLE|tenantId' "$migration"; then
            echo "FORBIDDEN P05C-P01: non-additive DDL, DML or tenantId in migration"; exit 1
          fi
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec"
        working-directory: project/apps/api
      - name: P05C-P01 CpeDecisionLog composite key disposable DB acceptance
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          spec=src/modules/policy-engine/decision-logger/__tests__/cpe-decision-composite-key.db-gated.integration.spec.ts
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          test -f "$spec" || { echo "MISSING P05C-P01 DB-gated spec: $spec"; exit 1; }
          set -o pipefail
          output_file="$(mktemp)"
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec" 2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+1 passed, 1 total" "$output_file" || \
            { echo "BLOCKED: P05C-P01 DB suite did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen=1; passed=$2; total=$4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed==total && total>0) }' "$output_file" || \
            { echo "BLOCKED: P05C-P01 DB tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project/apps/api
      # UYAP-ATTEMPT-CPE-DECISION-LINK-P05C-P02: dormant link schema foundation + Policy Engine
      # referential legal-hold. Kardinalite 1 attempt -> N karar; AYNI kararin baska attempt'e
      # baglanmasi UYAP-CONST-002 geregi YASAK (@@unique([cpeDecisionLogId])). role/disposition
      # kolonu YOK. Uc composite FK case-attribution zincirini kapatir; ucu de ON DELETE RESTRICT.
      # STEP 1 statik kontrat + migration/dormancy guard: link'e YAZAN uretim kodu, controller/DTO,
      # Cascade/SetNull, role/disposition veya CpeDecisionLog.tenantId gorulurse RED.
      # STEP 2 disposable PostgreSQL 16: FK zinciri, cardinality reddi, fail-closed delete ve
      # retention'in bagli kaydi SILMEMESI + bagli satir yuzunden HATA VERMEMESI dogrulanir.
      - name: P05C-P02 UyapAttemptCpeDecisionLink static contract
        run: |
          spec=src/modules/policy-engine/decision-logger/__tests__/uyap-attempt-cpe-decision-link.static.spec.ts
          migration=prisma/migrations/20260723010000_uyap_attempt_cpe_decision_link/migration.sql
          test -f "$spec" || { echo "MISSING P05C-P02 static spec: $spec"; exit 1; }
          test -f "$migration" || { echo "MISSING P05C-P02 migration: $migration"; exit 1; }
          restricts="$(grep -c 'ON DELETE RESTRICT' "$migration")"
          test "$restricts" -eq 3 || { echo "FORBIDDEN P05C-P02: expected exactly 3 ON DELETE RESTRICT (found $restricts)"; exit 1; }
          if grep -nE 'ON DELETE (CASCADE|SET NULL)|ADD COLUMN|^[[:space:]]*(UPDATE|INSERT|DELETE|TRUNCATE|DROP)[[:space:]]' "$migration"; then
            echo "FORBIDDEN P05C-P02: destructive delete action, column change or DML in migration"; exit 1
          fi
          # P05C-P03 UYARLAMASI: yetkili link writer (uyap-cpe-decision-link-writer.service.ts, Karar C)
          # link tablosuna yazar. Guard DARALTILIR: yetkili writer ve testler HARIC baska hicbir
          # uretim kodu link tablosuna YAZMAMALI (yetkisiz ikinci writer / sizinti hala RED).
          if grep -rnE 'uyapAttemptCpeDecisionLink[[:space:]]*\.[[:space:]]*(create|createMany|upsert)' src --include='*.ts' \
               | grep -vE '__tests__|\.spec\.ts' | grep -v 'uyap-cpe-decision-link-writer.service.ts'; then
            echo "FORBIDDEN P05C-P02: yetkisiz link writer (yalnizca P05C-P03 uyap-cpe-decision-link-writer yazabilir)"; exit 1
          fi
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec"
        working-directory: project/apps/api
      - name: P05C-P02 link + referential legal-hold disposable DB acceptance
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          spec=src/modules/policy-engine/decision-logger/__tests__/uyap-attempt-cpe-decision-link.db-gated.integration.spec.ts
          kvkk=src/modules/policy-engine/__tests__/kvkk-compliance.spec.ts
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          for f in "$spec" "$kvkk"; do test -f "$f" || { echo "MISSING P05C-P02 spec: $f"; exit 1; }; done
          set -o pipefail
          output_file="$(mktemp)"
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec" "$kvkk" 2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+2 passed, 2 total" "$output_file" || \
            { echo "BLOCKED: P05C-P02 suites did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen=1; passed=$2; total=$4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed==total && total>0) }' "$output_file" || \
            { echo "BLOCKED: P05C-P02 tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project/apps/api
      - name: UYAP-P05C-P04 evidence activation unit
        run: |
          for f in src/modules/uyap/operation-writer/uyap-operation-idempotency-key.factory.spec.ts \
                   src/modules/uyap/operation-writer/uyap-operation-evidence.orchestrator.spec.ts \
                   src/modules/uyap/__tests__/uyap-operation-evidence-activation.spec.ts \
                   src/modules/uyap/operation-writer/uyap-operation-evidence.orchestrator.ts; do
            test -f "$f" || { echo "MISSING P05C-P04 file: $f"; exit 1; }
          done
          # raw Idempotency-Key digest'lenir; hiçbir uretim kodu ham header'ı persist/log ETMEZ.
          if grep -rnE "logger\.(log|warn|error|debug).*idempotencyToken|console\..*idempotencyToken" src/modules/uyap; then
            echo "FORBIDDEN P05C-P04: raw Idempotency-Key log/persist edilemez"; exit 1
          fi
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath \
            src/modules/uyap/operation-writer/uyap-operation-idempotency-key.factory.spec.ts \
            src/modules/uyap/operation-writer/uyap-operation-evidence.orchestrator.spec.ts \
            src/modules/uyap/__tests__/uyap-operation-evidence-activation.spec.ts
        working-directory: project/apps/api
      - name: UYAP-P05C-P04 evidence activation disposable DB acceptance
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
        run: |
          spec=src/modules/uyap/operation-writer/uyap-operation-evidence.orchestrator.db-gated.integration.spec.ts
          test -n "$TEST_DATABASE_URL" || { echo "BLOCKED: TEST_DATABASE_URL is required"; exit 1; }
          test -f "$spec" || { echo "MISSING P05C-P04 DB-gated spec: $spec"; exit 1; }
          set -o pipefail
          output_file="$(mktemp)"
          pnpm exec jest --ci --forceExit --runInBand --runTestsByPath "$spec" 2>&1 | tee "$output_file"
          grep -Eq "Test Suites:[[:space:]]+1 passed, 1 total" "$output_file" || \
            { echo "BLOCKED: P05C-P04 DB suite did not execute exactly once"; exit 1; }
          awk '/^[[:space:]]*Tests:/ { seen=1; passed=$2; total=$4 } END { exit !(seen && passed ~ /^[0-9]+$/ && total ~ /^[0-9]+$/ && passed==total && total>0) }' "$output_file" || \
            { echo "BLOCKED: P05C-P04 DB tests did not complete with every discovered test passing"; exit 1; }
        working-directory: project/apps/api

  # Frontend (apps/web) vitest — jsdom unit/component testleri. DB/prisma GEREKMEZ.
  # RFA-018: web testleri artık CI'da; kırmızı web testi merge'i bloklar.
  web-tests:
    name: Web Tests (vitest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 8.15.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: project/pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
        working-directory: project
      - name: Web vitest (jsdom)
        run: pnpm --filter @hukuk/web test
        working-directory: project
      # SB-013: next build gate — vitest geçse de route/slug/tip build kırıklarını yakalar (#185 dersi:
      # /intake/[token] vs (dashboard)/intake/[id] slug conflict CI yeşilken web BOOT olmamıştı).
      # DB/prisma/API/env GEREKMEZ (next build = statik derleme + type-check + lint + route-collect;
      # lokal doğrulandı: BUILD_EXIT=0). package.json değişmedi; mevcut build script'i (next build) kullanılır.
      - name: Web build (next build)
        run: pnpm --filter @hukuk/web build
        working-directory: project
  client-workspace-live-smoke:
    name: Client Workspace Live Smoke
    runs-on: ubuntu-latest
    needs: [test-suite, web-tests]
    timeout-minutes: 20
    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/hukuk_test
      JWT_SECRET: client-workspace-live-smoke-ci-secret
      JWT_EXPIRES_IN: 7d
      NODE_ENV: test
      PORT: 8080
      CORS_ORIGIN: http://localhost:3002
      NEXT_PUBLIC_API_URL: http://localhost:8080
      CLIENT_WORKSPACE_LIVE_SMOKE: "1"
      CLIENT_WORKSPACE_WEB_BASE_URL: http://localhost:3002
      CLIENT_WORKSPACE_API_BASE_URL: http://localhost:8080
      CLIENT_WORKSPACE_SMOKE_CLIENT_ID: client-workspace-live-smoke-v1-client
      CLIENT_WORKSPACE_SMOKE_EMAIL: admin@hukuk.com
      CLIENT_WORKSPACE_SMOKE_PASSWORD: admin123
      CLIENT_WORKSPACE_SMOKE_TENANT_SLUG: demo-firma
      ALLOW_CLIENT_WORKSPACE_LIVE_SMOKE_SEED: "1"
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: hukuk_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6.0.9
        with:
          version: 8.15.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: project/pnpm-lock.yaml
      - name: Install
        run: pnpm install --frozen-lockfile
        working-directory: project
      - name: Disable optional pdf-poppler for Client Workspace smoke runtime
        run: |
          pnpm --filter @hukuk/api exec node -e "const fs = require('fs'); const p = require.resolve('pdf-poppler'); fs.writeFileSync(p, 'module.exports = { convert: async () => { throw new Error(\"pdf-poppler disabled in Client Workspace live smoke CI\"); } };\n'); console.log('Stubbed optional pdf-poppler at ' + p);"
        working-directory: project
      - name: Prisma generate
        run: pnpm --filter @hukuk/api exec prisma generate
        working-directory: project
      - name: Prisma migrate deploy
        run: pnpm --filter @hukuk/api exec prisma migrate deploy
        working-directory: project
      - name: Seed base data
        run: pnpm --filter @hukuk/api db:seed
        working-directory: project
      - name: Seed Client Workspace live smoke fixture
        id: seed-client-workspace-fixture
        run: pnpm --filter @hukuk/api seed:client-workspace-live-smoke -- --reset
        working-directory: project
      - name: Build API
        run: pnpm --filter @hukuk/api build
        working-directory: project
      - name: Build Web
        run: pnpm --filter @hukuk/web build
        working-directory: project
      - name: Install Playwright Chromium
        run: pnpm --filter @hukuk/web exec playwright install --with-deps chromium
        working-directory: project
      - name: Start API
        run: |
          pnpm --filter @hukuk/api start > api-live-smoke.log 2>&1 &
          echo $! > api-live-smoke.pid
        working-directory: project
      - name: Start Web
        run: |
          PORT=3002 pnpm --filter @hukuk/web start > web-live-smoke.log 2>&1 &
          echo $! > web-live-smoke.pid
        working-directory: project
      - name: Wait for API and Web
        run: |
          for i in {1..60}; do
            if curl --silent http://localhost:8080/api/auth/me >/dev/null 2>&1; then
              api_ready=1
            fi
            if curl --fail --silent http://localhost:3002 >/dev/null 2>&1; then
              web_ready=1
            fi
            if [ "${api_ready:-0}" = "1" ] && [ "${web_ready:-0}" = "1" ]; then
              exit 0
            fi
            sleep 2
          done
          echo "API/Web readiness timeout"
          echo "--- API log ---"
          cat api-live-smoke.log || true
          echo "--- Web log ---"
          cat web-live-smoke.log || true
          exit 1
        working-directory: project
      - name: Client Workspace Playwright live smoke
        run: pnpm --filter @hukuk/web smoke:client-workspace
        working-directory: project
      - name: Cleanup Client Workspace live smoke fixture
        if: always() && steps.seed-client-workspace-fixture.outcome == 'success'
        run: pnpm --filter @hukuk/api seed:client-workspace-live-smoke -- --cleanup
        working-directory: project
      - name: Stop API and Web
        if: always()
        run: |
          if [ -f api-live-smoke.pid ]; then kill "$(cat api-live-smoke.pid)" 2>/dev/null || true; fi
          if [ -f web-live-smoke.pid ]; then kill "$(cat web-live-smoke.pid)" 2>/dev/null || true; fi
        working-directory: project
      - name: Upload Client Workspace live smoke diagnostics
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: client-workspace-live-smoke-diagnostics
          path: |
            project/api-live-smoke.log
            project/web-live-smoke.log
            project/apps/web/playwright-report/client-workspace
            project/apps/web/test-results
