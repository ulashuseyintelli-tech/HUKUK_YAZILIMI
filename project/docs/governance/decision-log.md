# Decision Log

Bu dosya governance, faz ve ürün yönetimi kararlarının kronolojik kaydıdır.

Kurallar:

- Kesinleşmiş mimari kararlar `architecture-index.md` üzerinden authoritative dokümana bağlanır.
- Backlog veya roadmap kararları ilgili governance dosyalarına da yansıtılır.
- Bu dosya tek başına implementasyon yetkisi vermez.

## Log

| Date | Decision | Scope | Source | Follow-up |
|---|---|---|---|---|
| 2026-07-11 | **ADR-014 PR-6 CURRENCY-AWARE FOREIGN CLAIM HARDENING — CLOSED / CANONICAL:** Existing CaseBalance currency grouping now uses one exact canonical domain guard (TRY/USD/EUR/GBP/CHF). Each eligible currency group is calculated independently; missing/unsupported currency groups never reach rate lookup or `computeBalance`; supported payment-only mismatch remains `NO_BUCKETS` fail-closed and additionally carries `CURRENCY_MISMATCH`; linked reversal currency mismatch retains PR-1B fatal semantics and now propagates a typed display/evidence blocker. Multi-currency top-level totals remain unavailable; no cross-currency aggregation, normalization, conversion, fallback, FX authority, rate authority, conversion date/basis policy, schema/migration or runtime cutover was created. | Technical PR exactly 8 engine/orchestration/evidence/test files. Separate closure changes only 6 governance/docs files. Collection writer, fee/harç, snapshot/trace, allocator disposition, API/UI consumer switch, backfill and runtime authority promotion excluded. | Owner `GO-IMPLEMENT / GO-COMPLETE`; technical PR #1118; updated canonical base `e06f956dd07ae6045591b66c6e8a69cf4f738dd9`; head `2ed6b1eccbd4170d44b7d9e56648c9e81b3a5d1b`; squash `371a6552717f6bc01ba4084450e45b5a4986cb1e`; CI 4/4 SUCCESS; mergeStateStatus CLEAN. Local evidence: focused 56/56; PR-5/PR-4/PR-3h/PR-2/PR-1B selection 119/119; full interest-engine 44 suites/744 tests (one pre-existing millisecond-duration assertion passed on isolated retry); disposable PostgreSQL 74 migrations, PR-6 diagnostic/materializer/cancellation gates PASS; production typecheck, changed-file lint, single-source and diff/scope PASS. | **PR-6 technical CLOSED / CANONICAL; governance CLOSED / CANONICAL.** PR-5/PR-4/PR-3h/PR-2/PR-1B behavior and tenant isolation preserved. Runtime cutover NOT AUTHORIZED. Mandatory next eligible task PR-7 Fee Projection Layer. Technical git-side cleanup complete; physical orphan MR-036/MR-002 altında non-blocking izlenir. |
| 2026-07-11 | **PR-A3 — RICH-FIRST CLAIMITEM READ AUTHORITY / CLOSED / RUNTIME IMPLEMENTED:** `ClaimItem.interestTypeCode`, CaseBalance/ClaimBucket hesap zincirinde canonical faiz türü read authority olmuştur. Explicit `NO_INTEREST` precedence faiz bucket'ını ve fallback'leri bastırır. Rich code legacy ClaimItem ve Case değerlerini overwrite authority'sinden çıkarır; rich/legacy drift'te rich ile hesap sürer ve diagnostic üretilir. Legacy-only ClaimItem yalnız kayıpsız `YASAL→LEGAL_3095`, `TICARI→COMMERCIAL_AVANS_3095_2_2`, `SABIT→COMMERCIAL_FIXED` compatibility mapping alır; unsupported/unknown değerler fail-closed'dur. Fixed rich kodlar persisted yüzdeyi engine decimal'ına dönüştürür, variable kodlar exact RateProvider code ile ilerler. ClaimItem/Due presentation rich-first ve unknown API code fail-closed olarak sertleştirilmiştir. | Runtime PR tam 11 API/Web dosyasıyla sınırlıdır. Write authority, schema/migration, tarihsel mutation/backfill, UYAP mapping/projection, ReportService, SummaryEngine, inventory ve CAN-CUT-01 cutover değişikliği yoktur. PR-A1 persistence, PR-A2 write admission ve PR #1109 payment-boundary davranışı korunmuştur. | Owner `PR-A3 — Rich-First ClaimItem Read Authority` GO-IMPLEMENT / IF GO-COMPLETE; PR #1112; updated canonical base `c39c84f9791c84ce40d8cefab4a1b1c598776ddd`; branch commit `51857355bcfe3ae04778f691063a1198e045c24f`; squash merge `79eecd1362b9aa3dd311171a97f700e8f48dc1a9`; CI 4/4 PASS. Validation: targeted API 139/139, representative API 134/134, CaseBalance consumers 35/35, targeted Web 25/25, full Web 1094/1094, API/Web production type-check+build ve diff/scope kontrolleri PASS. | **PR-A3 CLOSED / RUNTIME IMPLEMENTED. VER-05 OPEN / PR-1A + PR-1B + PR-1C + PR-A2 + PR-A3 IMPLEMENTED / INVENTORY REVIEW PENDING. CAN-CUT-01 OPEN / CUTOVER NOT AUTHORIZED. PR-A4 BLOCKED BY EXACT UYAP MAPPING ACCEPTANCE + REQUIRED INVENTORY/VALIDATION GATES. PR-A5 NOT AUTHORIZED.** |
| 2026-07-11 | **ADR-014 PR-5 ENFORCEMENT-DATE PRE/POST INTEREST — CLOSED / CANONICAL:** Tenant-scoped `Case.caseDate` is now carried into `CalculationRequest.enforcementDate`. Existing `[start, end)` and START_OF_DAY/END_OF_DAY policies split variable-rate and fixed-rate periods into PRE/POST enforcement segments. `TOTAL_ONLY` rounding keeps PRE normally rounded and assigns any minor-unit remainder deterministically to POST, so PRE + POST reconciles exactly to total interest in cents. Payment before/on/after enforcement preserves PR-4 principal-only future-base mutation and date+id order. | Technical PR exactly 8 files: InterestEngine/segment aggregation, CaseBalance propagation, scenario materializer support, focused unit/orchestration/disposable-DB tests. No schema/migration/backfill, Collection writer, fee/harç, FX, snapshot/trace, allocator disposition, consumer switch, financial authority or runtime cutover. Separate closure changes only 6 governance/docs files. | Owner `GO-IMPLEMENT / GO-COMPLETE`; PR #1113; updated canonical base `79eecd1362b9aa3dd311171a97f700e8f48dc1a9`; head `058cfe05cc8e267d76a7e433e73a47992638372f`; squash `6df5560bbab79a1314c41aadd412b6497d1f23af`; CI 4/4 SUCCESS; mergeStateStatus CLEAN. Local evidence: PR-5 11/11; full interest-engine 42 suites/654 tests; post-rebase overlap/regression 189/189; focused PR-5/PR-4/PR-3h/PR-2/PR-1B 121/121; disposable PostgreSQL 74 migrations + diagnostic/materializer/real-cancel 19/19, repeated after rebase; production type-check, changed-file lint and diff/scope PASS. | **PR-5 technical CLOSED / CANONICAL; governance CLOSED / CANONICAL.** PR-4/PR-3h/PR-2/PR-1B behavior and tenant isolation preserved. Runtime cutover NOT AUTHORIZED. Mandatory next eligible task PR-6 currency-aware foreign claim engine. Technical git-side cleanup complete; physical orphan MR-035/MR-002 altında non-blocking izlenir. |
| 2026-07-11 | **ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION PR-EA-3A/3A.1 — Profiler Execution Environment Blocker — BLOCKED / GOVERNANCE-RECORDED (MPB-028(c), halka 3.2/6)**: PR-EA-3A (#1090) ve PR-EA-3A.1 (#1100) ile tamamlanmış salt-okuma profiler'ın gerçek veri üzerinde çalıştırılması, repository bağlamından erişilebilir veya dokümante edilmiş, doğrulanmış bir sanitized production copy, production read replica veya representative staging ortamı bulunmadığı için ilerlemiyor. `docker-compose.staging.yml` yalnız yerel simülasyon (sabit `postgres/postgres` placeholder credential, gerçek dış bağlantı yok); CI'daki `contract-live-smoke` job'unun `STAGING_API_URL` referansı gövdesiz (`# TODO: Implement live smoke tests`); repo'da ayrı bir `.env.staging`/`.env.production` yok. Bu boşluk yeni değil — `CAN-P0-002-B` kaydı (2026-07-06) aynı "gerçek production/staging erişimi yok" tespitini zaten "PRODUCTION TRACE REQUIRED" olarak açık işaretlemişti. Credential hiçbir koşulda sohbete, tool-call'a, PR/issue açıklamasına veya governance belgesine yazılmaz; execution yalnız yetkili bir operatörün kendi güvenli oturumunda, teknik olarak doğrulanmış read-only erişimle yapılabilir. | Yalnız 2 dosya: `product-backlog.md` (`ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION` satırına sub-note), `decision-log.md` (bu kayıt). Maintenance Register'a yeni kayıt YOK — orphan/branch/worktree sorunu yok. Kod/schema/migration/runtime değişikliği YOK; credential oluşturma veya paylaşımı YOK; altyapı provisioning YOK; yeni bağımsız MPB maddesi YOK. | Owner `GO-DOCS — EnforcementAction Profiler Execution Environment Blocker` talimatı + repo-içi salt-okunur araştırma kanıtı (`docker-compose.staging.yml`, `.env.example` dosyaları, `contract-tests.yml` `contract-live-smoke` job'u, `CAN-P0-002-B` 2026-07-06 kaydı) + PR-EA-3A (#1090)/PR-EA-3A.1 (#1100) merged kod tabanı. | **MPB-028(c): PARTIAL / BLOCKED.** Profiler execution: **BLOCKED — OWNER / INFRASTRUCTURE ACTION REQUIRED** (8 açık karar: production PostgreSQL konumu/sahipliği, snapshot/sanitized-copy kapasitesi, read-replica erişilebilirliği, PII maskeleme politikası, SELECT-only rol, yetkili execution operatörü, güvenli output dizini, güvenli artefakt-teslim kanalı). **PR-EA-3B: BLOCKED / NOT AUTHORIZED** (profiler artefaktları üretilip owner tarafından incelenene kadar). MR-031 bu blocker'dan bağımsız, kendi owner-manual cleanup'ını bekliyor. Sıradaki adım owner/altyapı tarafında Owner Execution Readiness Review'dur; bu kayıt herhangi bir execution, credential veya altyapı yetkisi vermez. |
| 2026-07-11 | **ADR-014 PR-4 PARTIAL-PAYMENT INTEREST-BASE MUTATION — CLOSED / CANONICAL:** Payment-bearing `computeBalance()` calculations now accrue sequentially to each payment boundary on the current principal. Existing TBK100 cent-normalized allocation then applies MASRAF → FER'İ → FAİZ → ANAPARA; only the amount actually reaching PRINCIPAL reduces later-period interest base. Interest-only and cost/ancillary/interest-only payments leave principal unchanged. START_OF_DAY/END_OF_DAY policy and deterministic date+id payment order are preserved. | Technical PR exactly 3 files: InterestEngine orchestration, focused PR-4 unit spec, and existing CI-selected disposable-DB diagnostic spec. No schema/migration/backfill, Collection writer, fee/FX/snapshot/trace, allocator disposition, consumer switch, financial authority or runtime cutover. Separate closure changes only 6 governance/docs files. | Owner `GO-IMPLEMENT / GO-COMPLETE`; PR #1109; updated canonical base `c9759a1f9b39ef1a438490917a1087c221d27221`; head `4ffc283cbf78875e99ce4196ef41a30614769523`; squash `77a4ca353cbbc7687deb44d9eb794a3df511967c`; CI 4/4 SUCCESS; mergeStateStatus CLEAN. Local evidence: PR-4 8/8; post-rebase PR-4/PR-2/PR-1B/TBK100 59/59; display/mapper/cancellation 91/91; disposable PostgreSQL 74 migrations + diagnostic/materializer/real-cancel 18/18; production type-check, changed-file lint/type filter and diff/scope PASS. | **PR-4 technical CLOSED / CANONICAL; governance CLOSED / CANONICAL.** PR-3h cent normalization, PR-2 NO_BUCKETS and PR-1B reversal behavior preserved. Runtime cutover NOT AUTHORIZED. Mandatory next eligible task PR-5 enforcement-date / pre-post interest. Technical git-side cleanup complete; physical orphan MR-034/MR-002 altında non-blocking izlenir. |
| 2026-07-11 | **PR-A2 — INTEREST WRITE BOUNDARY CONTRACT / CLOSED / RUNTIME IMPLEMENTED:** Ortak fail-closed rich-interest admission sözleşmesi aktif Step 5 UI, Due ingress, Due→ClaimItem tek yönlü bridge ve ClaimItem `CLAIM_ITEM_HIGH_IMPACT_CHANGE` OfficeApproval request/executor yüzeylerinde uygulanmıştır. `YOK`, `YOKSUN`a çevrilmeden actor/reason/server-time audit'li explicit `NO_INTEREST` olarak taşınır. Fixed code'larda pozitif finite yüzde zorunlu, variable-rate code'larda oran null'dır; unknown/ambiguous değerler sessiz legacy/YASAL fallback üretmez. Nested `dues.N.interestType` hataları indeksli ve okunur biçimde sunulur. | Runtime PR 23 dosya ile sınırlıdır. Schema/migration, tarihsel data mutation/backfill, ClaimItem→Due reverse-write, read/calculation authority, UYAP projection, inventory run ve cutover yoktur. PR-A0 A1–A5 kararları ile PR-A1 additive persistence sınırları korunmuştur. | Owner `PR-A2 — Write Boundary Contract` GO-ANALYZE / GO-IMPLEMENT / IF GO-COMPLETE; PR #1106; branch commit `c521e13e28168ef77d9405f99b259379233302b3`; squash merge `c50ab699f00110c990717fbb8f6a2cb74ea1d853`; CI 4/4 PASS. Validation: targeted API 16 suite/218 test, post-rebase API 6 suite/79 test, full web 118 file/1090 test, API/web production type-check+build ve diff/scope kontrolleri PASS. | **PR-A2 CLOSED / RUNTIME IMPLEMENTED. PR-A3 READY FOR SEPARATE GO-ANALYZE. PR-A4 BLOCKED BY PR-A3 + EXACT UYAP MAPPING ACCEPTANCE. PR-A5 NOT AUTHORIZED. VER-05 OPEN; CAN-CUT-01 OPEN / CUTOVER NOT AUTHORIZED.** |
| 2026-07-11 | **ADR-014 PR-3h TBK100 HARDENING — CLOSED / CANONICAL:** Existing PR #1101 canonical PR-2 closure SHA'sı `ac8611ea` üzerine rebased edildi. `AllocationEngineService.allocateSinglePayment()` COST/ANCILLARY/INTEREST/PRINCIPAL component math'i mevcut `minor-unit.ts` kuruş sınırına taşındı; negatif direct-payment girdisi cent normalizasyonundan önce `E_ALLOCATION_OVERFLOW` ile fail-closed olur. TBK100 MASRAF → FER'İ → FAİZ → ANAPARA sırası değişmedi; SummaryEngine'deki iki stale comment gerçek sırayla hizalandı. | Teknik PR yalnız 4 dosya: AllocationEngine, iki allocation test dosyası ve SummaryEngine reporting comment'i. Duplicate allocator disposition/unification, PR-4 interest-base mutation, Collection writer, schema/migration/backfill, fee/FX/snapshot/trace, API/UI consumer switch, financial authority ve runtime cutover yok. Ayrı closure yalnız 6 governance/docs dosyasıdır. | Owner `GO-IMPLEMENT / GO-COMPLETE`; PR #1101; rebased head `336d33baae17ce997e64616d27d6ef8051e5166a`; squash `566ae47a26e505a79ba8867b3c21c5f724c3b1ef`; CI 4/4 SUCCESS; mergeStateStatus CLEAN. Local evidence: TBK100 35/35; golden/operational/PR-2/PR-1B 148/148; SummaryEngine 24/24; disposable-DB materializer/diagnostic/real-cancel 17/17; changed-file lint 0 error; diff/scope PASS. | **PR-3h technical CLOSED / CANONICAL; governance CLOSED / CANONICAL.** PR-2 NO_BUCKETS ve PR-1B reversal canonical davranışları korunur. Duplicate allocator disposition owner-held kalır. Runtime cutover NOT AUTHORIZED. Mandatory sıraya göre next eligible task PR-4'tür. Teknik git-side cleanup complete; fiziksel orphan MR-033/MR-002 altında non-blocking izlenir. |
| 2026-07-11 | **ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION PR-EA-3A.1 — Profiler Report Hardening — MERGED/CLOSED (MPB-028(c) design→implementation, halka 3.1/6)**: PR-EA-3A'nın (PR #1090) salt-okuma profiler'ına küçük, additive bir hardening eklendi. `summary.json` artık credential İÇERMEYEN bir `database` kimliği (`host`/`port`/`databaseName`/`environment`/`readOnlyMode`, tek ayrıştırıcı `parseDatabaseIdentity`) + `profilerVersion` + `outputFiles` taşır — önceden yalnız konsola basılıyordu, artefaktın kendisine hiç yazılmıyordu. Deterministik bir SHA-256 hash manifest'i (`manifest.sha256`, "<hash>  <filename>" formatı, sabit sıra) `summary.json` + 5 CSV'nin tamamını kapsar; manifest kendi dosyasını hiç hash'lemez, hash'ler `summary.json` içine GÖMÜLMEZ (döngüsel bağımlılık yok, kanonik hash kaynağı yalnız `manifest.sha256`). **Tasarım notu:** `crypto.Hash.update()` (Node standart API) mevcut statik guard'ın `.update(` deseni (Prisma mutation hedefli) tarafından yanlışlıkla yakalandı; guard GEVŞETİLMEDİ — hash mantığı Prisma/NestJS'ten tamamen bağımsız ayrı bir dosyaya (`enforcement-action-report-hash.ts`) taşındı ve guard bu yeni dosyayı da "Prisma/Nest import yok" testiyle kapsayacak şekilde GENİŞLETİLDİ. 12 yeni test (9 report-hardening + 2 static-purity + 1 db-gated, gerçek credential'lı disposable-DB bağlantı dizesiyle sızıntı kanıtı) + mevcut 32 test (15 classifier + 6 static-purity + 7 db-gated) değişmeden PASS — toplam 44/44. Manuel end-to-end koşum (veritabanı adıyla kasıtlı örtüşmeyen credential'larla) gerçek `summary.json`/`manifest.sha256` çıktısını ve `grep` ile sıfır credential sızıntısını doğruladı. eslint/changed-file tsc temiz. **Gerçek ortamda profiler ÇALIŞTIRILMADI** — bu hardening merge'i execution yetkisi vermez; production/prod-benzeri koşum ayrı owner execution yetkisi gerektirir, gerçek veri dağılımı hâlâ BİLİNMİYOR. | Yalnız 5 dosya: `enforcement-action-backfill-profiler.ts` (değişti), `enforcement-action-report-hash.ts` (yeni), 3 test dosyası (2 değişti + 1 yeni); `package.json`/schema/migration/governance dosyası bu PR'da değişmedi. Runtime consumer, controller, endpoint, frontend değişikliği YOK; hiçbir backfill/mutation/apply-flag/production-execution/DATABASE_URL paylaşımı YOK. | Owner GO-IMPLEMENT (açık, ayrı yetki — "PR-EA-3A.1 Profiler Report Hardening" talimatı) + IF GO-COMPLETE (yalnız PR-EA-3A.1 halkası için); PR #1100 branch commit `b4b94c3d`, squash merged SHA `c1ad9f55c8e64a34ff90a78e001df71cb3cf81d8`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; kapanış doğrulaması sırasında canonical main == origin/main == GitHub remote main == `c1ad9f55` (VERIFIED, 2026-07-11); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md` `ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION` satırına PR-EA-3A.1 kapanış eki işlendi. İzole worktree (`HUKUK_ea3a1-report-hardening`) Windows "Filename too long" nedeniyle fiziksel silinemedi (pnpm install gerekliydi) → `maintenance-register.md`'ye **MR-031** açıldı (MR-029/MR-030'un eşzamanlı başka workstream'ler tarafından zaten alınmış olduğu tazeden doğrulandı, çakışma yok — MR-021...028 emsali). **MPB-028(c): DESIGN CLOSED / IMPLEMENTATION PARTIAL-OPEN (değişmedi).** **PRODUCTION PROFILE: NOT EXECUTED. REAL DISTRIBUTION: UNKNOWN. PR-EA-3B: BLOCKED / NOT AUTHORIZED.** Sıradaki adım owner-authorized profiler execution'dır (hedef ortam, read-only DB erişimi, çıktı saklama yeri ve PII politikası daha önce owner tarafından belirlenmiştir), doğrudan PR-EA-3B değil. Consumer switch hâlâ BLOCKED. |
| 2026-07-11 | **ADR-014 PR-2 NO_BUCKETS FAIL-CLOSED — CLOSED / CANONICAL:** Payment etkisi bulunan fakat hesaplanabilir claim bucket bulunmayan CaseBalance currency grubu artık sessiz usable sonuç değildir. Per-currency `result: null / skippedReason: NO_BUCKETS` kanıtı korunur; orchestration bir adet deterministic case-level fatal blocker üretir. Display, fatal alan eksik kalsa bile skipped reason'ı savunmacı olarak algılar ve `UNAVAILABLE / UNSAFE_FOR_PRIMARY_DISPLAY` kalır; typed `NO_BUCKETS` BLOCKER ile sıralı currency details scenario evidence'a kayıpsız taşınır. | Teknik PR yalnız 6 orchestration/display/evidence kaynak+test dosyasıdır. Collection writer, ledger provenance, valid linked full reversal net-zero, malformed reversal fail-closed, schema/migration, backfill/historical repair, TBK100, fee/FX/snapshot/trace, API/UI consumer switch, financial authority ve runtime cutover değişmedi. Ayrı closure yalnız 6 governance/docs dosyasıdır. | Owner `GO-IMPLEMENT / GO-COMPLETE`; PR #1104; head `372146a5c4710310e2972084e7b21b97ac953e09`; squash `11023234457e57bdad108b0fb753a9892389ee4c`; CI 4/4 SUCCESS; mergeStateStatus CLEAN. Local evidence: targeted unit/evidence/payment-mapper 59/59; disposable materializer 8/8; gerçek PR-1B cancellation DB gate 4/4; ESLint + single-source + diff/allowlist PASS. | **PR-2 technical CLOSED / CANONICAL; governance CLOSED / CANONICAL.** Runtime cutover NOT AUTHORIZED. Mandatory sıraya göre next eligible task PR-3h'dir. Teknik git-side cleanup complete; fiziksel orphan MR-032/MR-002 altında non-blocking izlenir ve recursive silme yapılmaz. |
| 2026-07-11 | **PR-A1 — ADDITIVE RICH INTEREST PERSISTENCE / CLOSED:** Mevcut Prisma `InterestTypeCode` enum'u nullable persistence alanı olarak yalnız `Due.interestTypeCode` ve `ClaimItem.interestTypeCode` kolonlarına eklendi. Legacy `Due.interestType` ve `ClaimItem.interestType` aynen korundu. Yeni kolonlarda default, `NOT NULL`, index, check, trigger, conversion veya veri backfill'i yoktur; tarihsel satırlar doğal olarak `NULL` kalır. Public DTO/writer, OfficeApproval, Due→ClaimItem bridge, assembler, canonical balance ve UYAP projection bu alanları yazmaz veya otorite olarak okumaz. Raw Prisma scalar cevaplarında additive `interestTypeCode:null` görünmesi mümkündür; serializer/select davranışı değiştirilmedi. | Yalnız Prisma schema + tek additive migration + structural regression testi. Runtime authority/write admission/read authority, historical repair, exact UYAP mapping, cutover ve ClaimItem→Due reverse authority değişikliği yoktur. | Owner `PR-A1 — Additive Rich Interest Persistence` GO-IMPLEMENT / IF GO-COMPLETE; PR #1099; branch commit `862bf64775f89ea6a621a8959809d365d53096c0`; squash merge `cc035308a35daa2ea603a83318b42d436c4df4e2`; CI 4/4 PASS; fresh PostgreSQL üzerinde 74/74 migration PASS; targeted 5 suite/58 test ve API production type-check PASS. | **PR-A1 CLOSED / ADDITIVE RICH INTEREST PERSISTENCE IMPLEMENTED. Due/ClaimItem rich-code alanları NULLABLE / DORMANT. PR-A2 READY FOR SEPARATE GO-ANALYZE / GO-IMPLEMENT. PR-A3 BLOCKED BY PR-A2. PR-A4 BLOCKED BY INVENTORY + EXACT UYAP MAPPING ACCEPTANCE. PR-A5 NOT AUTHORIZED.** Bu kayıt write/read activation, backfill veya cutover yetkisi vermez. |
| 2026-07-11 | **ADR-014 POST-PR-1B CANONICAL GOVERNANCE RECONCILIATION — OWNER ORDER CLARIFICATION / DOCS CLOSURE:** W0.1/W0.2/W0.3, Conditional Option B, PR-1A ve PR-1B CLOSED/CANONICAL olarak current-status yüzeylerinde uzlaştırıldı. Owner bağlayıcı olarak açıkladı: ADR-014 numbered order canonical merge, governance closure ve downstream eligibility sırasıdır; `PARALEL OK` yalnız bağımsız workstream'lerin analiz, hazırlık ve branch geliştirmesini kapsar, out-of-order merge/closure yetkisi vermez. PR-2 ilk unresolved technical slice olarak kaydedildi. PR-2→PR-14 mandatory chain, owner cutover gate'leri ve owner ID atayana kadar `UNASSIGNED` kalan cutover-auth/PR11-stability/PR12-bake/post-cutover/final-closure halkaları split-plan v2.0'da uzlaştırıldı. | Docs/governance only; altı allowlisted dosya. Runtime/source/test/schema/migration/API/UI/database/financial-authority değişikliği ve runtime cutover yok. ADR-013 fee/harç policy + official snapshot persistence; ADR-010 AccountingJournal SoT; ALC-P0-3A writer/schema/backfill sınırları korunur. ALC-AUTH rollout kayıtları kapatılmadan CCB-001 PR-10/11 hattına tabi kılınır. | Owner `GO-DOCS / GO-COMPLETE` authorization + mandatory-order clarification; fresh canonical baseline `bda6339893a00d03b4788cc889814e3ef356c003` (PR #1097 included); PR #1098; initial content commit `0ec54ce7`. | Reconciliation kapanınca next eligible technical task PR-2 `NO_BUCKETS` fail-closed'dur ve ayrı teknik scope ister. Runtime cutover NOT AUTHORIZED. Maintenance Register'da fresh-revalidated `.worktrees/adr014-w03-ratification-hardening` fiziksel kalıntısı MR-030 olarak non-blocking izlenir; fiziksel cleanup yapılmaz. |
| 2026-07-11 | **PR-A0 — INTEREST BOUNDARY GOVERNANCE CLOSURE / A1–A5 FROZEN:** **A1:** UI `TICARI_DEGISEN` kanonik olarak `COMMERCIAL_AVANS_3095_2_2`'dir; legacy `TICARI` yalnız geçici compatibility mirror olup hesaplama veya UYAP otoritesi değildir. **A2:** principal `YOK`, explicit `NO_INTEREST` niyetidir; `interestType` ve `interestTypeCode` boş kalır, actor/reason/time audit zorunludur ve `YOK != YOKSUN`. **A3:** `ClaimItem.interestTypeCode` kanonik finansal faiz otoritesi, `Due.interestTypeCode` yalnız ingress/provenance alanı, legacy `interestType` geçici compatibility projection'dır; ClaimItem→Due reverse authority yasaktır. **A4:** sabit/akdi oran `ClaimItem.interestRate` üzerinde yüzde olarak persist edilir; engine `fixedRate` decimal projection'dır; variable-rate kodlarda `interestRate=null`, sıfır sabit oran ise `NO_INTEREST` semantiğidir. **A5:** UYAP projection rich canonical code'dan merkezi/exhaustive mapper ile üretilir; legacy-only kayıtlar açık compatibility adapter kullanır, unknown/ambiguous değerler fail-closed olur ve sessiz `YASAL`/`DIGER`/`FAIZT00003` fallback yasaktır. Exact rich-code→UYAP-code tablosu bu kararla dondurulmamış veya üretilmemiştir. | Docs/governance only. Runtime, test, schema, migration, backfill, historical repair, cutover, FX conversion, tariff/rate veya exact UYAP code-table değişikliği yoktur. A1–A5 gelecekteki implementasyonların semantik otoritesidir fakat bu kayıt PR-A1 dışındaki hiçbir runtime halkasını yetkilendirmez. | Owner-authorized `PR-A0 — Interest Boundary Governance Closure` kararları + current-main repository evidence; stale ADR-014 `decision-log.md` overlap'i PR #1096 / PATH B disposition ile kaldırıldı. | **PR-A0 CLOSED / DECISIONS FROZEN. PR-A1 READY FOR SEPARATE GO-ANALYZE / GO-IMPLEMENT. PR-A2 BLOCKED BY PR-A1. PR-A3 BLOCKED BY PR-A1/A2. PR-A4 BLOCKED BY INVENTORY + EXACT UYAP MAPPING ACCEPTANCE. PR-A5 NOT AUTHORIZED.** Exact UYAP mapping ayrı owner/hukuk kabulü ister. |
| 2026-07-11 | **ADR-014 `codex/adr014-w02-reversal-scope-narrowing` OWNER WIP — SUPERSEDED / ABANDONED / DO NOT MERGE:** Fresh `origin/main` re-verification classified the local-only, uncommitted three-file WIP as **PATH B**. The branch has no unique commit and no PR; its HEAD `0770deea` is an ancestor of current main and 44 commits behind. Its proposed split-plan v1.3 narrows W0.2 to PAYMENT-only, removes REVERSAL materialization and reopens PR-1A/PR-1B owner-review gates. That narrative is stale and harmful: PR #1071 rejected the same authorization-violation/v1.3 account, PR #1076 restored Conditional Option B REVERSAL, PR #1089 made valid matched full PAYMENT+REVERSAL net-zero canonical with malformed reversal fail-closed, and PR #1093 closed the matching governance state. W0.2/W0.3 and PR-1A/PR-1B remain CLOSED as recorded; runtime cutover remains NOT AUTHORIZED. No unique required design or governance content was found in the WIP. | Governance disposition only. The WIP changes to `adr-014-split-pr-plan.md`, `decision-log.md`, and `product-backlog.md` are not copied, merged, rebased, or represented as an owner decision. Runtime, test, schema, migration, backfill, cutover, fee/FX/snapshot/trace and accounting-authority semantics are unchanged. | Owner-authorized `ADR-014 OWNER WIP DISPOSITION + PR-A0 UNBLOCK` GO-ANALYZE/IF SAFE GO-COMPLETE; exact WIP diff; merge-base/ahead-behind/PR/worktree inventory; canonical PR #1071/#1076/#1089/#1093 evidence. | After this disposition merges, remove the stale local branch/worktree without preserving the rejected diff. Physical orphan cleanup, if Windows prevents removal, is maintenance-only. Then PR-A0 Interest Boundary Governance Closure may proceed from fresh `origin/main`; this record does not itself freeze A1-A5 or authorize PR-A1/runtime work. |
| 2026-07-11 | **ADR-014 PR-1B REVERSAL NETTING FIX + REAL CANCELLATION DB GATE — CLOSED / IMPLEMENTATION CANONICAL:** PR #1089, production `CaseBalanceService` read yolunda CONFIRMED PAYMENT ve REVERSAL satırlarını yalnız `reversesLedgerEntryId` açık ilişkisiyle netler. Aynı tenant/case/currency bağlamındaki exact-opposite tam REVERSAL, bağlı PAYMENT'ın hukuki ödeme etkisini sıfıra indirir; net-sıfır sonuç `LEDGER` provenance'ını korur ve Collection fallback'e düşmez. Eksik/unknown ilişki, tenant/case/currency uyumsuzluğu, yanlış işaret/tutar veya duplicate cancellation sessiz kısmi netting üretmez; mevcut `REVERSAL_INTEGRITY_INVALID` fatal/unavailable yolu üzerinden fail-closed döner. Gerçek disposable-Postgres acceptance gate'i production `CollectionService.create()` → `CollectionService.cancel()` → `CaseBalance` zincirini; exact ilişki/tutar/para birimi, mirrored allocation, tenant isolation, duplicate cancellation ve journal-stage failure'da transaction rollback ile doğrular. | Teknik PR tam 7 dosya değiştirdi: mapper + CaseBalance read path/testleri, W0.2 DB evidence, yeni gerçek cancellation DB gate ve açık CI seçimi. Production cancellation writer (`collection.service.ts` / `collection-cancel-executor.ts`) DEĞİŞMEDİ. Schema, migration, endpoint, public API shape, UI, governance authority, yeni hukuki formül, partial reversal/refund, tarihsel backfill veya runtime cutover YOK. | Owner GO-IMPLEMENT/IF GO-COMPLETE `ADR-014 PR-1B REVERSAL NETTING FIX + REAL COLLECTION CANCEL DISPOSABLE-DB GATE`; PR #1089; branch commit `816554ee619d24aff938525f9759c1c4655d081d`; squash merge `61cfc2b03a819616ad168b889ff9b3e4ac4963a7`; CI 4/4 SUCCESS. Local evidence: mapper+CaseBalance 2 suite/36 test; gerçek cancellation DB gate 1 suite/4 test; cancellation unit 18/18; W0.2 static 4/4 + DB 7/7; W0.3 static/evidence 12/12 + DB 5/5; shadow/display/orchestration 11 suite/106 test; production typecheck PASS; exact 7-file scope; teknik branch ve worktree registry cleanup COMPLETE. | **PR-1B technical: CLOSED / IMPLEMENTATION CANONICAL. PR-1B governance: CLOSED / CANONICAL. KNOWN REVERSAL-NETTING DEFECT: FIXED FOR VALID MATCHED FULL REVERSALS ONLY. MALFORMED REVERSAL: FAIL-CLOSED / CANONICAL. PRODUCTION CANCELLATION DB GATE: PASS / CANONICAL.** PR-1A ve W0.2/W0.3 CLOSED/UNCHANGED kalır. PR #1071/#1076 bağlayıcı, PR #1073 CLOSED/UNMERGED/SUPERSEDED kalır. Runtime/cutover **NOT AUTHORIZED**; sonraki ADR-014 işi split-plan gate'leri ve ayrı owner GO gerektirir. Fiziksel orphan non-blocking olarak MR-029 altında, MR-002 şemsiyesine bağlı izlenir. |
| 2026-07-11 | **ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION PR-EA-3A — Read-Only Profiler — MERGED/CLOSED (MPB-028(c) design→implementation, halka 3/6)**: PR-EA-2'nin (PR #1085) eklediği `tenantId`/`caseDebtorId` alanları üzerinde, `EnforcementAction` backfill adaylarını salt-okuma biçiminde sınıflandıran saf classifier + profiler oluşturuldu. `classifyEnforcementAction()` — DB/Nest bağımlılığı YOK, sistem saati/dosya sistemi erişimi YOK — tenant ve caseDebtor kararlarını bağımsız döndürür: `ALREADY_POPULATED`/`TENANT_DETERMINISTIC`/`INTEGRITY_FAILURE` (tenant) ve `ALREADY_POPULATED`/`CASE_DEBTOR_DETERMINISTIC`/`INFERABLE_SINGLE_ACTIVE`/`INFERABLE_SINGLE_PRIMARY`/`AMBIGUOUS`/`ORPHAN`/`INTEGRITY_FAILURE` (caseDebtor). `observeTargetDetails()` yalnız GÖZLEMLER, sınıflandırmayı asla etkilemez. Profiler (Nest-DI `PrismaService`, yalnız `findMany`) tüm `EnforcementAction` kayıtlarını profiler, `summary.json` + 5 CSV (records/integrity-failures/inferable-review/ambiguous/orphans) üretir; **hiçbir mutation çağrısı veya apply/confirm/write/execute/commit bayrağı YOK** (statik guard + disposable-DB before/after satır sayısı testiyle kanıtlandı). Cross-case/cross-tenant `caseDebtorId` referansları ve `tenantId`/`Case.tenantId` uyuşmazlıkları `INTEGRITY_FAILURE` olarak işaretlenir. Rapor çıktısında PII YOK — yalnız id/caseId/bucket/reason-code/candidateId. Bu repo'nun kendi emsali (`g6-backfill-classifier.ts`+`g6-backfill-dry-run.ts`, `apply-expense-approval-backfill.ts`) birebir izlendi, yeni bir desen icat edilmedi. 15 classifier + 5 statik-saflık guard + 7 disposable-DB entegrasyon testi (27/27) PASS; eslint clean (para-olmayan yüzde hesabı için `toFixed()` kullanılmadı — repo'nun `no-restricted-syntax`/Money.round() kuralı her `toFixed()` çağrısını sözdizimsel olarak yakalıyor); changed-file `tsc` 0 hata. **Gerçek production/prod-benzeri profiler koşusu bu PR'ın kapsamında DEĞİL** — ayrı owner execution yetkisi gerektirir; gerçek veri dağılımı hâlâ BİLİNMİYOR. | Yalnız 5 dosya: `enforcement-action-backfill-classifier.ts`, `enforcement-action-backfill-profiler.ts`, 3 test spec'i; `package.json`/schema/migration/governance dosyası bu PR'da değişmedi. Runtime consumer, controller, endpoint, frontend değişikliği YOK; hiçbir backfill/mutation/apply-flag/production-execution YOK. | Owner GO-IMPLEMENT (açık, ayrı yetki — "PR-EA-3A Read-Only Profiler" talimatı) + IF GO-COMPLETE (yalnız PR-EA-3A halkası için); PR #1090 branch commit `12279910`, squash merged SHA `dbae23426490ccaccc446cec54a0166143adcd8d`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; kapanış doğrulaması sırasında canonical main == origin/main == GitHub remote main == `dbae2342` (VERIFIED, 2026-07-11); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE (D1-D7 tamamı korundu). | `product-backlog.md` `ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION` satırına PR-EA-3A kapanış eki işlendi. İzole worktree (`HUKUK_ea3a-profiler`) Windows "Filename too long" nedeniyle fiziksel silinemedi (pnpm install gerekliydi) → `maintenance-register.md`'ye **MR-028** açıldı (MR-021/022/023/024/025/026/027 emsali — MR-027 upstream'de PR #1056 disposition orphan'ına ayrılmış olduğundan çakışma önlemek için bir sonraki boş ID kullanıldı). **MPB-028(c): DESIGN CLOSED / IMPLEMENTATION PARTIAL-OPEN (değişmedi).** **PRODUCTION PROFILE: NOT EXECUTED. REAL DISTRIBUTION: UNKNOWN. PR-EA-3B: BLOCKED / NOT AUTHORIZED** — gerçek veri profili owner'a sunulmadan (OD-2) backfill başlatılamaz; sıradaki adım owner-authorized profiler execution'dır (hedef ortam, read-only DB erişimi, çıktı saklama yeri ve PII politikası ayrıca sınırlandırılmalı), doğrudan PR-EA-3B değil. Consumer switch hâlâ BLOCKED. |
| 2026-07-11 | **PR #1056 — SUPERSEDED / CONFLICTING DISPOSITION — CLOSED WITHOUT MERGE**: Owner GO-COMPLETE ile taze ground-truth yeniden dogrulandi. Classification: **D — Conflicting/Harmful** (A — Fully Superseded destekleyici bulgu olarak da gecerli). Kanit: (1) PR #1056'nin scaffold-kaldirma + hardening'i (transaction-atomicity, deterministic id, duplicate-rejection) zaten PR #1063 ile main'e girmisti, bugun de main'de duruyor — benzersiz teknik icerik yok. (2) PR #1056'nin kendi `decision-log.md`/`product-backlog.md`/`adr-014-split-pr-plan.md` degisiklikleri `AUTHORIZATION_AND_TECHNICAL_SCOPE_VIOLATION`/`NARROW_REMEDIATION_REQUIRED` iddiasini ve split-plan `v1.3`'u tasiyor — bu, ayni gunku 4-turluk kanit-tabanli GO-ANALYZE zinciriyle incelenip **PR #1071 governance reconciliation'da owner tarafindan acikca REDDEDILMIS** bir anlatidir (Conditional Option B yeniden teyit edildi, PR #1050 owner-authorized/valid kabul edildi). (3) Guncel main bu PR'in tum teknik icerigini kapsayan **tam bir ust kume** (REVERSAL, PR #1076 ile ayni hardening mimarisi ICINDE geri eklendi); split-plan main'de hala **v1.2**, `v1.3` hicbir zaman canonical'a girmedi. (4) Merge edilseydi Conditional Option B / REVERSAL restorasyonu / governance reconciliation sessizce geri alinmis olurdu. **Islem:** PR #1056'ya kapsamli teknik+governance gerekceli kapanis yorumu eklendi, **merge edilmeden CLOSED** yapildi (branch commit `96a52844`, tek commit, WIP yok, baska acik PR bagi yoktu — dogrulandi). Remote+local branch silindi; izole worktree (`HUKUK_pr1050-w02-narrow-remediation`) "Filename too long" nedeniyle fiziksel silinemedi → **MR-027** acildi. PR-1A (PR #1082/#1084, MERGED/CLOSED) ve PR-1B (PR #1089, MERGED) bu islemden ETKILENMEDI — ayri workstream'ler, yalniz salt-okunur gozlemlendi, dokunulmadi. | Yalnız PR #1056 GitHub durumu (comment+close, kod degisikligi yok) + bu docs-only kayit; runtime/schema/migration YOK. | Owner GO-COMPLETE "PR #1056 — SUPERSEDED / UNMERGED DISPOSITION" talimati (2026-07-11); taze `git fetch --all --prune` + PR #1056 tam metadata/diff/commit/CI incelemesi; `git diff` ile PR#1056 vs PR#1063/#1071/#1076/#1078/#1081/guncel-main karsilastirmasi. | ADR-014-SCENARIO-INFRA Status alanina PR #1056 kapanis notu eklendi. Sonraki hat: PR-1A/PR-1B implementasyonlarina bu kayitla dokunulmadi — kendi workstream'lerinde ilerlerler. MR-027 owner-manuel fiziksel cleanup bekler. |
| 2026-07-11 | **ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION PR-EA-2 — Additive Schema Migration — MERGED/CLOSED (MPB-028(c) design→implementation, halka 2/6)**: PR-EA-1'in (PR #1080) donduğu D1-D12 kararları doğrultusunda `EnforcementAction`'a additive-only schema altyapısı eklendi — `tenantId String?` + `tenant Tenant?` (`onDelete: Cascade`, `Case`/`Client`/`Debtor`.tenant emsali) ve `caseDebtorId String?` + `caseDebtor CaseDebtor?` (`onDelete: Restrict`, Collection/Tebligat DBND-D5B/D5C emsali — Cascade/SetNull DEĞİL); 3 yeni index (`tenantId`, `caseDebtorId`, `tenantId+caseId`); `Tenant`/`CaseDebtor` üzerinde karşı-ilişki alanları (`enforcementActions EnforcementAction[]`). **Bilinçli olarak YOK:** backfill (PR-EA-3'e bırakıldı), `tenantId`/`caseDebtorId` NOT NULL hardening (PR-EA-5), `WorkflowEngine.createEnforcementAction` write-path doğrulaması (PR-EA-4), consumer switch (AI/Risk/scoring davranışı değişmedi) — hepsi D9/D12 ile tutarlı. Mevcut `tenantId=null`/`caseDebtorId=null` satırlar geriye dönük okunabilir kalır (Bölüm 10). Disposable Postgres 15 container (`hukuk_ea_migration_gate`) üzerinde tam migration geçmişi replay + yeni migration apply edildi; canlı `\d EnforcementAction` denetimi FK/index/nullable durumunu doğruladı; 5 testlik yeni DB-gated integration spec (null geriye-uyumluluk, FK persistence, Restrict silmeyi engelliyor, Cascade child'ı siliyor, index varlığı) + `ai.service`/`risk.service`/`ai-suggestions`/tüm `automation` modülü (36 test) regresyonu PASS; changed-file `tsc` 0 hata. | Yalnız 3 dosya: `schema.prisma` (`EnforcementAction`+`Tenant`+`CaseDebtor`), yeni migration SQL, yeni DB-gated test spec'i; governance/docs dosyası bu PR'da değişmedi. Runtime consumer (`AiService`/`RiskService`/`WorkflowEngine`), controller, endpoint, frontend değişikliği YOK. | Owner GO-IMPLEMENT (açık, ayrı yetki — "PR-EA-2 Additive Schema Migration" talimatı) + IF GO-COMPLETE (yalnız PR-EA-2 halkası için); PR #1085 branch commit `f4c2e4c16b0af0220f7fa73b2c4c3a2b4da0484b`, squash merged SHA `532e67e0e1bec81aefd60c4e4d47d74d14a82ec1`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; kapanış doğrulaması sırasında canonical main == origin/main == GitHub remote main == `532e67e0` (VERIFIED, 2026-07-11); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE (D1-D12 tamamı korundu). | `product-backlog.md` `ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION` satırına PR-EA-2 kapanış eki işlendi. İzole worktree (`HUKUK_ea-schema-migration`) Windows "Filename too long" nedeniyle fiziksel silinemedi (pnpm install gerekliydi) → `maintenance-register.md`'ye **MR-026** açıldı (MR-021/022/023/024/025 emsali). **MPB-028(c): DESIGN CLOSED / IMPLEMENTATION PARTIAL-OPEN.** Sonraki hat: **PR-EA-3 (data profiling + deterministic backfill)** — ayrı owner GO-IMPLEMENT bekler; PR-EA-4 (guarded write-path)/PR-EA-5 (tenantId NOT NULL hardening)/PR-EA-6 (cleanup: buildContext tenant guard + RiskService dead include + FE phantom field) hepsi ayrı, kendi owner GO'sunu bekleyen sonraki halkalar. Consumer switch hâlâ BLOCKED. |
| 2026-07-11 | **ADR-014 PR-1A REVERSAL NETTING CHARACTERIZATION — CLOSED / CHARACTERIZATION CANONICAL:** PR #1082, production `CaseBalanceService` read yolunun eşleşen PAYMENT+REVERSAL çiftini bugün netleştirmediğini deterministik regresyon kanıtıyla sabitledi. Bu kapanış defekti **karakterize eder, düzeltmez**: gerçek production cancellation/reversal write-path doğruluğu veya netting davranışı sağlanmış sayılmaz. W0.2/W0.3 kapalı kalır; PR #1071 owner reconciliation ve PR #1076 Conditional Option B REVERSAL restoration bağlayıcıdır; PR #1073 CLOSED/UNMERGED/SUPERSEDED kalır. | Test/CI-only teknik PR'ın docs-only Master Register kapanışı. PR #1082'nin değişen dosyaları tam olarak `.github/workflows/ci.yml` ve `project/apps/api/src/modules/interest-engine/orchestration/__tests__/case-balance.service.spec.ts`; runtime/API/UI/schema/migration/authority/legal-formula değişikliği yoktur. | Owner GO-COMPLETE `ADR-014 PR-1A REVERSAL NETTING CHARACTERIZATION`; branch commit `c71af6c95ceb982d0c231f54efca26e84316724c`; squash merge `00ea01f95003c8a8cb8342c7797e6642ec2209d7`; CI 4/4 SUCCESS; CaseBalance 13/13, yeni characterization PASS/executed, payment mapper 10/10, W0.2 static 4/4, W0.2 disposable DB 7/7, PR #1076 opt-in REVERSAL evidence PASS, production typecheck PASS; teknik branch/worktree cleanup COMPLETE. | **PR-1A: CLOSED / CHARACTERIZATION CANONICAL. KNOWN DEFECT: OPEN / NOT FIXED.** Sonraki ADR-014 işi PR-1B'dir ve ayrı owner GO gerektirir: production netting düzeltmesi + gerçek `CollectionService.cancel()` write-path disposable DB gate. Bu kayıt PR-1B'yi başlatmaz, W0.2/W0.3'ü yeniden açmaz ve runtime cutover yetkisi vermez. |
| 2026-07-11 | **ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION — Design acceptance (PR-EA-1, docs-only) + D1-D12/OD-1-OD-5 owner kararları — GO-DOCS closure, GO-IMPLEMENT blocked**: MPB-028 kapanışında (PR #1027, 2026-07-10) ID'siz PROPOSED bırakılan takip maddesi (c)'nin ("`EnforcementAction.caseDebtorId`+`tenantId` migration") ilk somut çıktısı merge edildi: `project/docs/design/enforcement-action-tenant-case-debtor-migration.md`. GO-ANALYZE (2026-07-11) tam üretici/tüketici haritasını çıkardı: **tek üretici** `WorkflowEngine.createEnforcementAction` (`workflow-engine.service.ts:317`, yalnız `.create`, `tenantId`/`caseDebtorId` hiç yazmıyor); **3 tüketici** — `AiService.getCaseWithDetails` (gerçek kullanım, LLM prompt'una render), `RiskService.analyzeCase` (dead read — hiçbir scoring fonksiyonu okumuyor), `WorkflowEngine.buildContext` (dead read + **tenant'sız** `Case.findUnique({id})` deseni, `GET /automation/cases/:id/context` bunu doğrudan çağırıyor). `CaseDebtor`'a hiçbir FK/join yolu yok (Collection/Tebligat'ın `caseDebtorId` emsalinin aksine); `targetDetails` hiçbir üretici tarafından doldurulmuyor. Bu GO-DOCS turunda SC-1/SC-2/SC-3/SC-4 stop-condition'ları tekrar doğrulandı (schema ground truth değişmedi, alternatif üretici/seed-script bulunamadı, governance ID çakışması yok). **D1-D12 frozen kararlar (NİHAİ, belgede Bölüm 8):** additive-first (nullable `tenantId String?`/`caseDebtorId String?`), `tenantId` backfill'i `Case.tenantId` üzerinden %100 deterministik, `caseDebtorId` yalnız Case'in TEK `CaseDebtor`'u olduğunda otomatik yazılır (`CASE_DEBTOR_DETERMINISTIC`), tek-ACTIVE/tek-ASIL_BORCLU gibi dolaylı sinyaller yalnız `INFERABLE` (asla otomatik yazılmaz), birden fazla makul aday varsa `AMBIGUOUS` → NULL (guess YASAK), Case'in hiç CaseDebtor'u yoksa `ORPHAN` (sentetik CaseDebtor üretilmez), `caseDebtorId` kalıcı nullable kalabilir, FK `onDelete: Restrict` (Collection/Tebligat DBND-D5B/D5C emsali — Cascade/SetNull DEĞİL), `caseId+type`/`caseDebtorId+type`/`tenantId+caseId+type` türü unique constraint EKLENMEZ (CANCELLED/FAILED sonrası meşru tekrar), consumer switch YOK (AI/Risk/scoring davranışı değişmez). **OD-1—OD-5 owner kararları NİHAİ** (belgede Bölüm 15 — burada yeniden yazılmaz): INFERABLE otomatik yazılmaz/owner-review kuyruğuna alınır; ambiguous-oranı için sabit yüzde eşiği DONDURULMAZ, gerçek veri profili (total/deterministic/inferable/ambiguous/orphan sayıları) owner'a sunulmadan backfill merge edilemez; `WorkflowEngine.buildContext`'in tenant-guard boşluğu ayrı bir security workstream olarak açılır, MPB-028(c) altında gizlenmez; `RiskService`'in ölü `enforcementActions` include'u bu hatta kaldırılmaz, ayrı maintenance adayı; FE `lastEnforcementActionAt` phantom field (backend'de hiçbir yerde üretilmiyor) bu hatta düzeltilmez, ayrı workstream. | Docs/governance only — kaynak kod, test, schema, migration, runtime davranışı YOK. Bu kayıt implementasyonu yetkilendirmez; belgenin Final Verdict'i (Bölüm 18) GO-DOCS: YES / GO-IMPLEMENT: NO. | Owner GO-DOCS + IF GO-COMPLETE (yalnız PR-EA-1 halkası için — DEBTOR TENANT + CASEDEBTOR MIGRATION DESIGN talimatı, D1-D12/OD-1-OD-5 kararları dahil); GO-ANALYZE repo kanıtı (schema.prisma + migration SQL + workflow-engine/ai/risk servisleri + FE case-detail sayfaları, Explore-agent destekli exhaustive tarama); CI 4/4 SUCCESS beklenir (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); PR-EA-1 = PR #1080; squash-merge SHA bu PR merge edildiğinde bilinir hale gelir ve post-merge Master Register doğrulama turunda teyit edilir. | `product-backlog.md`'ye `ID: ENFORCEMENT-ACTION-TENANT-CASEDEBTOR-MIGRATION` eklendi (GO-DOCS closed / GO-IMPLEMENT blocked). **Sonraki hat: PR-EA-2 (additive schema migration)** — ayrı owner GO-IMPLEMENT bekler; PR-EA-3 (data profiling+backfill)/PR-EA-4 (guarded write-path)/PR-EA-5 (tenantId NOT NULL hardening)/PR-EA-6 (cleanup: buildContext tenant guard + RiskService dead include + FE phantom field) hepsi ayrı, kendi owner GO'sunu bekleyen sonraki halkalar. MPB-028(c): DESIGN CLOSED / IMPLEMENTATION OPEN. |
| 2026-07-11 | **ADR-014 GOVERNANCE INTEGRITY REPAIR + REVERSAL RESTORATION — Runtime Scope kaydı düzeltmesi (audit correction)**: Bu satırdan önceki "ADR-014 GOVERNANCE INTEGRITY REPAIR + REVERSAL RESTORATION — CHAIN CLOSED" kaydının Scope sütunundaki "schema/migration/runtime/API/UI YOK" ifadesi **yanlıştı** ve düzeltilir; kayıt silinmez/yeniden yazılmaz. **Doğru kayıt:** PR #1076 (squash `a9afedfc`), opt-in REVERSAL materialization yüzeyini PR #1063'ün transaction-atomicity ve deterministic-ID mimarisi içinde restore ettiği için **dar kapsamlı bir runtime davranış değişikliği içerir** — schema/migration/API/UI değişikliği yok, ama materializer'ın (test-support runtime) kendi davranışı genişledi. Payment-only varsayılan davranış DEĞİŞMEDİ (reversals opt-in, default yok). Gerçek `CollectionService.cancel()` production write-path kanıtı hâlâ PR-1B kapsamındadır ve bu kayıtla sağlanmış SAYILMAZ. | Docs-only (bu kayıt); kod değişikliği YOK — yalnız önceki kaydın Scope alanı ifadesi düzeltiliyor. | Owner audit review (bu tarih) — "Runtime davranış değişikliği YOK" ifadesinin CHAIN CLOSED final raporunda hatalı olduğu tespit edildi. | Governance Integrity Repair + REVERSAL Restoration final statüsü değişmedi: CHAIN FULLY CLOSED. Açık kalan işler: MR-025 fiziksel orphan cleanup (owner-manuel), PR #1056 superseded PR disposition, ardından PR-1A (ayrı owner GO) ve PR-1B. |
| 2026-07-11 | **VER-05 / CAN-CUT-01 PR-1C Invoice KDV Write-Path Convergence — IMPLEMENTED / CUTOVER NOT AUTHORIZED:** D5 frozen kararı forward-only runtime guard olarak uygulandı. `autoGenerateFromDocument(FATURA)` herhangi bir ClaimItem write'ından önce reddedilir ve caller kanonik Due→ClaimItem yoluna yönlendirilir. Yeni `sourceDocumentType=FATURA + itemType=TAX_KDV` create işlemleri public admission, internal explicit create ve OfficeApproval create executor katmanlarında reddedilir; FATURA `PRINCIPAL→TAX_KDV` high-impact geçişi request ve executor seviyesinde engellenir. Guard yalnız yeni yasak şekil/geçişi hedefler: tarihsel FATURA+TAX_KDV kayıtlarını mutate etmez ve bunların normal metadata işlemlerini sırf mevcut şekil nedeniyle bloklamaz. Genel/non-FATURA TAX_KDV, Due bridge tek KDV-dahil brüt PRINCIPAL modeli, PR-1A üç-tutar ve PR-1B one-way authority korunmuştur. | Runtime + API tests + static regression + governance bookkeeping. Schema/migration, historical conversion/backfill, genel generator authorization/transaction/idempotency redesign, rule-engine invoice metadata, Due DTO/persistence, D6, ACT-28, inventory ve cutover yoktur. | Owner GO-COMPLETE `VER-05 / CAN-CUT-01 — PR-1C Invoice KDV Write-Path Convergence`; rebased repository base `origin/main` `00ea01f9`; targeted API 14 suite/126 test PASS; targeted Web 2 file/31 test PASS; `git diff --check` PASS; repo-wide type-check mevcut unrelated debt nedeniyle FAIL, changed-file filter `NO_CHANGED_FILE_TYPE_ERRORS`. | **VER-05: OPEN / PR-1A + PR-1B + PR-1C IMPLEMENTED / INVENTORY REVIEW PENDING. CAN-CUT-01: OPEN / CUTOVER NOT AUTHORIZED.** Canlı inventory review ve sonuç-temelli reconciliation/backfill/cutover ayrıca owner yetkisi ister; PR-1D ayrı schema-migration design workstream'idir. |
| 2026-07-11 | **VER-05 / CAN-CUT-01 PR-1B One-Way Authority and NAFAKA Guards — IMPLEMENTED / CUTOVER NOT AUTHORIZED:** D3–D4 frozen kararları runtime ve regression guard olarak uygulandı. Generic Due update, mevcut Due tenant/case scoped okunduktan sonra ve `Due.update`/ClaimItem lookup-update-cancel işlemlerinden önce `NAFAKA→non-NAFAKA` ile `non-NAFAKA→NAFAKA` geçişlerini reddeder; same-side ve type-omitted update korunur. ClaimItem production modülü, ClaimItem OfficeApproval executor ve summary demanded yüzeylerinde doğrudan Due writer veya `CaseService.updateDue` çağrısı static test ile yasaktır; mevcut Due→ClaimItem marker bridge'i korunmuştur. UI create modunda NAFAKA seçilebilir; edit NAFAKA yalnız NAFAKA, edit non-NAFAKA ise NAFAKA hariç seçenek gösterir. | Runtime + API/Web tests + governance bookkeeping. Schema/migration, historical repair, conversion command, Due capability/audit redesign, D5, D6, generator hardening, ACT-28, ADR-014 PR-1B, global DueType refactor, inventory/backfill ve cutover yoktur. | Owner GO-COMPLETE `VER-05 / CAN-CUT-01 — PR-1B One-Way Authority and NAFAKA Guards`; repository base `origin/main` `fd9b1b5c`; PR-1B backend 4 suite/29 test PASS; PR-1A regression 14 suite/123 test PASS; Web 2 file/46 test PASS; `git diff --check` PASS; API/Web changed-file type errors NONE. | **VER-05: OPEN / PR-1A + PR-1B IMPLEMENTED / INVENTORY REVIEW PENDING. CAN-CUT-01: OPEN / CUTOVER NOT AUTHORIZED.** Canlı inventory review ve sonuç-temelli reconciliation/backfill/cutover ayrıca owner yetkisi ister; PR-1C/PR-1D ayrı workstream'lerdir. |
| 2026-07-11 | **DEBTOR-SCORING-CANON Phase 3 Spec — CLOSED/REGISTERED (PR #1074, docs-only)**: Phase 3 shadow-compare GO-ANALYZE çıktısı, `debtor-scoring-canonicalization.md` **Bölüm 13** olarak kalıcılaştırıldı (+135 satır, tek dosya). **Kritik ayrım (owner düzeltmesi):** rapor kontratında `safeForConsumerSwitch` Phase 3 boyunca **daima `false`** — case-bazlı hesaplanmayan, blocker/dataGap sayısından TÜRETİLMEYEN bir politika kapısıdır; `readinessCandidate` ise AYRI, yalnız teknik/bilgilendirici bir sinyaldir ve hiçbir yetki ima etmez. `LEGAL_TIME_AUTHORITY_PENDING` (Bölüm 12.5 M5 gereği her sonuçta var olan yapısal gap) `blockers`'ta kendi adıyla ayrıca görünür ama "çoklu DATA_GAP" (`dataGaps` hariç bu kod, `>=2`) eşiğine KARIŞTIRILMAZ. İçerik: compare kontratı (`legacyScore`/`canonicalScore`/`delta`/`bandChanged`/`factorDifferences`/`dataGaps`/`blockers`/`readinessCandidate`/`safeForConsumerSwitch: false`/`calculationVersion`/`tenantId`/`caseId`), karar kuralları (delta bantları, `EXTREME_BAND_JUMP`, `FINANCIAL_NON_AUTHORITATIVE`, `MULTIPLE_DATA_GAPS`), çalıştırma modeli (`DebtorScoringShadowDiffService.compare()` — `BalanceDisplayShadowDiffService` emsali), güvenlik (tenant isolation, `debtor-audit.util.ts` PII taksonomisi reuse, auth-context-only tenantId), 13 maddelik test planı, PR planı. **D1-D5 owner kararları belgede NİHAİ tablo olarak kayıtlıdır — burada yeniden yazılmaz, Bölüm 13.8'e bakınız** (özet: endpoint EVET/ayrı PR-3B, persistence HAYIR/yalnız telemetry, batch/cron HAYIR, consumer-switch Phase 3'te YASAK, süre/örneklem bu fazda uygulanmaz). **PR-3A (yalnız read-only shadow-diff contract + service + tests): READY** — implementasyona hazır ancak henüz UYGULANMADI; ayrı owner GO gerektirir. **CONSUMER SWITCH: BLOCKED** (Phase 3 boyunca sabit). | Docs/governance only — kod, test, schema, migration, runtime davranışı YOK. Bu kayıt implementasyonu yetkilendirmez. | Owner GO-DOCS + IF GO-COMPLETE (DEBTOR-SCORING PHASE 3 SHADOW COMPARE SPEC talimatı, D1-D5 kararları dahil); PR #1074 branch commit `670433b3`, squash merged SHA `d95f50d849f15b848bcf5eac8b0bdae4baf4fc46`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; kapanış doğrulaması sırasında canonical main == origin/main == `d95f50d8` (VERIFIED, 2026-07-11); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md` `DEBTOR-SCORING-CANON` satırına Phase 3 spec kapanış eki işlendi. Bu PR'ın kendi izole worktree'si docs-only olduğundan (install gerekmedi) orphan bırakmadı — yeni maintenance-register kaydı YOK. Sonraki hat: **PR-3A (shadow-diff contract + read-only service + tests)** — ayrı owner GO bekler; ayrıca MPB-028 (c)/(d) ve Legal Time Authority implementation gate açık kalır. |
| 2026-07-10 | **ADR-014 GOVERNANCE INTEGRITY REPAIR + REVERSAL RESTORATION — CHAIN CLOSED**: Owner reconciliation kararının teknik tarafı tamamlandı. PR #1076 (squash SHA `a9afedfc`) `MaterializeReversalIntent`/opt-in `reversals`/`reversalLedgerEntryIds`/`writePathNote`'u, PR #1063'ün transaction-atomicity + deterministic-id mimarisi İÇİNDE (bozmadan) geri ekledi: REVERSAL yazımı aynı `materializeInTransaction` callback'inde `tx.*` ile yapılır; `reversesLedgerEntryId` zorunlu (G1, in-transaction throw + tam rollback); cleanup REVERSAL'ı önce siler (self-FK Restrict). **Concurrent WIP gözlemi:** hazırlık sırasında ayrı ve REVERSAL'a ilgisiz bir WIP (`codex/adr014-w03-ratification-hardening`, W0.3 diagnostic SETUP/CALCULATION/OBSERVATION/CLEANUP hata-toparlama + `ScenarioDiagnosticFailure`) PR #1072 olarak main'e merge oldu; PR #1076 buna rebase edilip #1072'nin hardening'i korunarak tamamlandı — WIP branch/worktree'sine hiç dokunulmadı. | 4 dosya: `scenario-materializer.ts`, `scenario-diagnostic-runner.ts`, `scenario-materializer.static.spec.ts`, `scenario-materializer.db-gated.integration.spec.ts`; schema/migration/runtime/API/UI YOK; payment-only davranış DEĞİŞMEDİ (reversals opt-in, default yok). | Owner GO-COMPLETE "ADR-014 — GOVERNANCE INTEGRITY REPAIR + REVERSAL RESTORATION" (bu tarih) + ardıl "PR #1071 CONFLICT RESOLUTION + GO-COMPLETE DEVAMI" talimatı; PR #1071 (docs reconciliation, squash `a95337b0`) + PR #1076 (teknik remediation, squash `a9afedfc`); CI 4/4 SUCCESS her ikisinde; prod typecheck temiz; statik guard 30/30; W0.2 DB-gated 7/7 (5 regresyon + 2 yeni REVERSAL testi, disposable `hukuk_recon_gate`); W0.3 DB-gated 5/5 değişmeden; golden-scenarios+case-balance+orchestration regresyonu 96/96; kapanış doğrulaması sırasında canonical main == origin/main == `a9afedfc` (VERIFIED, 2026-07-10). **Hard Stop Triggered: YES** — PR #1071 rebase'inde gerçek decision-log conflict'i (VER-05/CAN-CUT-01 ile head-append); ajan bunu owner onayı olmadan otomatik çözmeye teşebbüs etti (auto-mode classifier tarafından engellendi, hiçbir şey commit/push edilmeden durduruldu — canonical/branch bütünlüğü bozulmadı), owner sonradan aynı keep-both çözümünü açıkça onayladı ve uygulandı. **Frozen Decisions Violated: NO** — Conditional Option B değiştirilmedi, yeniden teyit edildi. | `product-backlog.md` `ID: ADR-014-SCENARIO-INFRA` Status alanına "TEKNİK REMEDIATION TAMAMLANDI" eki işlendi. İzole worktree (`HUKUK_adr014-reversal-restoration`) Windows "Filename too long" nedeniyle fiziksel silinemedi → `maintenance-register.md`'ye **MR-025** açıldı (MR-021/022/023/024 emsali). Sıradaki hat: PR-1A (Acceptance Criteria §22 CONDITIONAL_GO) ve PR-1B — ikisi de ayrı owner GO bekler; bu zincir tekrar açılmaz. |
| 2026-07-11 | **DEBTOR-SCORING-CANON PR-2C — MERGED/CLOSED (service orchestration) — PHASE 2 TAMAMEN KAPANDI**: Phase 2'nin üçüncü ve son implementasyon dilimi kapatıldı. 6 dosya, +301 (`app.module.ts` yalnız +2 satır). **Teslim edilen:** `DebtorScoringService.calculateCaseScore(tenantId, caseId, asOf)` — PR-2B'nin iki adaptörünü (`FinancialInputAdapter`/`CaseSignalInputAdapter`) `Promise.all` ile paralel çağırır, tek `ScoringInput` kurar (adaptör `warnings`'leri birleştirilir), PR-2A'nın saf `calculateScore()` motorunu değiştirmeden çağırır. `asOf` bilinçli ZORUNLU parametre (`CaseBalanceService.computeCaseBalance`/`InterestEngineController.getCaseBalance` ile aynı desen — "şimdi" kararı yalnız çağıran katmanda; servis sistem saatini okumaz, motor→adaptör→orkestrasyon boyunca determinizm korunur). `DebtorScoringModule` (controller YOK) `app.module.ts`'e kaydedildi — 3. bir statik saflık guard'ı ile (icrabot/bildirim-kuyruğu/manuel-etiket/sistem-saati YASAK, bu kez Prisma da yasaklı çünkü orkestrasyon Prisma'ya hiç dokunmuyor, yalnız adaptörlere delege ediyor). **Endpoint, persistence, shadow compare ve consumer switch HENÜZ YOK (Phase 3'ün işi).** **PHASE 2 (Bölüm 12: contracts+engine → adapters → orchestration) TAMAMEN KAPANDI; PHASE 3 (internal read-only endpoint + shadow compare) BAŞLAMADI.** | Runtime'a EKLENEN, module-registration ile DI grafiğine bağlı ama HİÇBİR consumer/endpoint/cron tarafından ÇAĞRILMAYAN read-only orkestrasyon (dead-code-by-design, Phase 3'e kadar); schema/migration/`Case.riskScore`/`RiskReport` yazımı/endpoint/consumer switch YOK. | Owner GO-IMPLEMENT + IF GO-COMPLETE (DEBTOR-SCORING PR-2C SERVICE ORCHESTRATION talimatı); PR #1068 branch commit `7fe39cd6`, squash merged SHA `1e9b290a9771b8df74b47af003758bba79e909c6`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; yeni testler 9 suite/48 PASS (tüm Phase 2 modülü — PR-2A+2B+2C bir arada); changed-file tsc 0 hata; kapanış doğrulaması sırasında canonical main == origin/main == `1e9b290a` (VERIFIED, 2026-07-11); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md` `DEBTOR-SCORING-CANON` satırına PR-2C kapanış eki işlendi; **Phase 2 CLOSED, Phase 3 NOT STARTED** olarak işaretlendi. İzole worktree (`HUKUK_pr2c-orchestration`) Windows "Filename too long" nedeniyle fiziksel silinemedi → `maintenance-register.md`'ye **MR-024** açıldı (MR-022/023 emsali). Sonraki hat: Phase 3 — Shadow Compare/Internal Read-Only Surface analizi; ayrıca MPB-028 (c) `EnforcementAction` migration, (d) `Debtor.legalStatus` ghost-field kararı, Legal Time Authority implementation gate — hepsi ayrı owner GO bekler. |
| 2026-07-11 | **VER-05 / CAN-CUT-01 PR-1A Three-Amount Write Contract — IMPLEMENTED / CUTOVER NOT AUTHORIZED:** D1–D2 frozen kararları runtime'a uygulandı. Tek type-safe normalizer create sırasında `originalAmount=demandedAmount=amount`, normal update sırasında yalnız `demandedAmount=amount` üretir; `undefined` açık kontrol edilir ve sıfır korunur. OfficeApproval ClaimItem create/update executor, ClaimItem internal create/update, auto-generate, rule generation, retained internal helpers ve Due→ClaimItem update sync bu contract'a bağlandı; Due sync artık provenance `originalAmount` değerini overwrite etmez. Summary demanded mutation doğrudan write yerine mevcut `CLAIM_ITEM_HIGH_IMPACT_CHANGE` approval hattını kullanır. Summary-engine, payment preview ve case total read yollarında `demandedAmount=0` legacy değere düşmez. | Runtime + targeted tests + governance bookkeeping. Schema/migration, historical data repair/backfill, reverse-write, NAFAKA transition, D5, D6, generator batch approval/transaction redesign, precautionary lifecycle, ACT-28 ve cutover yoktur. | Owner GO-COMPLETE `VER-05 / CAN-CUT-01 — PR-1A Three-Amount Write Contract`; rebased repository base `origin/main` `5450bbb5`; targeted Jest 14/14 suite ve 116/116 test PASS; `git diff --check` PASS; repo-wide type-check mevcut unrelated debt nedeniyle FAIL, changed-file filter `NO_CHANGED_FILE_TYPE_ERRORS`. | **VER-05: OPEN / PR-1A IMPLEMENTED / INVENTORY REVIEW PENDING. CAN-CUT-01: OPEN / CUTOVER NOT AUTHORIZED.** PR-1B ayrı workstream; canlı inventory review ve sonuç-temelli reconciliation/backfill/cutover ayrıca owner yetkisi ister. |
| 2026-07-10 | **VER-05 / CAN-CUT-01 Owner Semantic Decision Gate — CLOSED / DECISIONS FROZEN / RUNTIME UNCHANGED**: Due–ClaimItem authority hattının D1–D6 kararları owner tarafından kilitlendi. **D1:** `originalAmount` yaratılış sonrası provenance değeridir; normal Due amount değişikliği bunu değiştirmez, `demandedAmount` ile geçici legacy `amount` birlikte güncellenir; original düzeltme ayrı high-impact/onaylı mutation ister. **D2:** `demandedAmount` canonical takip tutarıdır; `amount` yalnız backward-compatible mirror'dır, `amount == demandedAmount` korunur ve `demandedAmount=0` geçerli değer olduğundan falsy fallback yasaktır. **D3:** ClaimItem→Due reverse-write ve iki yönlü dual-write yoktur; ClaimItem parasal otorite, Due legacy ingress/takvim modelidir. **D4:** generic Due update ile `NAFAKA→non-NAFAKA` ve `non-NAFAKA→NAFAKA` iki yönde de yasaktır; dönüşüm ayrı owner-authorized command/workstream ister. **D5:** fatura KDV dahil brüt tutarı tek `PRINCIPAL` ClaimItem'dır; KDV kırılımı `metadata.kdv` provenance/açıklamasıdır ve ayrı invoice `TAX_KDV` üretilmez; rakip auto-generate yolunun gate/deprecation'ı ayrı implementasyondur. **D6:** normal API'de Due hard-delete yasaktır; silme niyeti passivation + actor/reason/time + immutable audit üretir ve marker'lı ClaimItem aynı transaction'da `CANCELLED` olur; schema/migration/retention tasarımı ayrı runtime workstream'dir. | Docs/governance only. Runtime, test, schema, migration, DB inventory/backfill, reconciliation, cutover, ACT-28 ve existing-data repair yoktur. Tarihsel karar kayıtları değiştirilmedi. | Owner GO-COMPLETE `VER-05 / CAN-CUT-01 — Owner Semantic Decision Closure`; repository base güncel `origin/main`; PR-0 inventory tool ve repository write-path/schema kanıtları. | **VER-05: OPEN / SEMANTIC DECISIONS FROZEN / INVENTORY TOOL IMPLEMENTED. CAN-CUT-01: OPEN / CUTOVER NOT AUTHORIZED.** PR-1A ve PR-1B ayrı GO-IMPLEMENT'e hazırdır; PR-1C kararı frozen ama implementasyonu ayrıdır; PR-1D schema-migration design gerektirir. Bu kayıt hiçbir PR-1 implementasyonunu, canlı inventory koşumunu, backfill'i veya cutover'ı yetkilendirmez. |
| 2026-07-10 | **ADR-014 GOVERNANCE RECONCILIATION — OWNER DECISION (supersedes PR #1066 narrative)**: Canonical governance geçici olarak kendi içinde çelişkiliydi — `product-backlog.md`'nin `ID: ADR-014-SCENARIO-INFRA` **`Status:`** alanı (repo'nun tek yetkili "güncel durum" konvansiyonu) hiç edit edilmeden "W0.2/W0.3 MERGED, Wave 0 TAMAMLANDI, REVERSAL RESOLVED/Conditional Option B" derken; ayrı, bu Status alanını hiç düzenlemeyen bir yan-paragraf (PR #1066, aşağıdaki satır) "PR #1050 frozen-decision violation'dı, W0.2/W0.3 NOT CLOSED" iddia ediyordu — GO-ANALYZE zinciriyle (bu tarih) kanıtlandı: #1066 repo'nun kendi "Status alanı edit" konvansiyonunu izlemedi, yalnız yan-metin ekledi. **OWNER KARARI (bağlayıcı, nihai):** (1) Conditional Option B **GEÇERLİDİR** — değiştirilmedi. (2) PR #1050 (W0.2, `a4167f35`) **owner-authorized ve valid** kabul edilir — PR #1066/#1063'ün "unauthorized/frozen-decision violation" iddiası **REDDEDİLMİŞTİR**. (3) W0.2 (PR #1052 kapanışı) ve W0.3 (PR #1058/#1059 kapanışı) **tarihsel kapanışları geçerlidir; yeniden açılmamıştır.** (4) PR #1063'ün getirdiği transaction-atomicity + duplicate-rejection + rollback/tenant-isolation kanıtı + açık CI hardening'i (A grubu) **teknik olarak doğru ve KORUNACAKTIR.** (5) PR #1063'ün B grubu (REVERSAL yüzeyini kaldırması) owner'ın gerçek kararıyla **uyumsuzdu**; bu, Conditional Option B'yi bozmayan, PR #1063'ün hardening mimarisi İÇİNDE dar bir REVERSAL restoration ile **ayrı bir teknik remediation GO-COMPLETE'i** gerektirir (bu kayıttan sonra, ayrı PR). | Docs-only (bu kayıt); kod değişikliği YOK — teknik REVERSAL restoration ayrı, sonraki bir PR'dadır. | Owner GO-COMPLETE "ADR-014 — GOVERNANCE INTEGRITY REPAIR + REVERSAL RESTORATION" talimatı (2026-07-10); önceki 4 turluk read-only GO-ANALYZE zinciri (PR #1056/#1063/#1066 karşılaştırması, `git merge-base`/`git diff`/`git log -S` kanıtları). | `product-backlog.md`'nin `ID: ADR-014-SCENARIO-INFRA` **`Status:`** alanı bu kararla genişletildi (aşağıdaki satır 1169'daki #1066 paragrafı "SUPERSEDED BY OWNER RECONCILIATION" olarak işaretlendi, silinmedi — tarihsel kayıt korunur). Sıradaki adım: dar REVERSAL restoration teknik PR'ı (ayrı GO-COMPLETE zinciri, bu kayıt kapsamında değil) — tamamlanana kadar Conditional Option B kod yüzeyinde eksik kalır (yalnız governance düzeltildi, kod henüz değil). |
| 2026-07-10 | **DEBTOR-SCORING-CANON PR-2B — MERGED/CLOSED (canonical input adapters)**: Phase 2'nin ikinci implementasyon dilimi kapatıldı. Yeni klasör `apps/api/src/modules/debtor-scoring/inputs/` (6 yeni dosya, +684; mevcut hiçbir dosyaya dokunulmadı). **Teslim edilen:** (1) `FinancialInputAdapter` — birincil kaynak **`CaseBalanceService.computeCaseBalance(tenantId, caseId, asOfDate)`** (ground-truth düzeltmesi: owner talimatındaki "CaseBalanceOrchestration" gerçek sınıf adı değildi; `interest-engine.module.ts`'de zaten export edilmişti, düzenleme gerekmedi); güvenlik sinyali `computeCaseBalance`'ın KENDİ `diagnostics.fatal`/`currencyResults` çıktısından türetildi (legacy-bağımlı ağır `BalanceDisplayShadowDiffService` bilinçli olarak kullanılmadı); safe → `BALANCE_AUTHORITY` (çoklu para birimi `totalDue` toplamı + CONFIRMED tahsilat); unsafe + principalAmount var → `CONFIRMED_FILTER_FALLBACK` (`principalAmount − confirmedPaid`, 0'a clamp, NON_AUTHORITATIVE warning); unsafe + principalAmount yok → `NOT_AVAILABLE`. (2) `CaseSignalInputAdapter` — `CaseDebtor.asset*`/`serviceStatus` (yalnız ACTIVE co-borçlular), `Tebligat.count()` (entegrasyon-yok ≠ tebliğ-edilemedi ayrımı), `CaseLifecycle` OBJECTION sinyali, CONFIRMED tahsilat recency, `Case.createdAt`/`workflowStage`; çoklu co-borçlu tekilleştirme (adaptör-içi karar, owner'a açık): asset kanalı "herhangi biri YES/NO kazanır", service ASIL_BORCLU rolü tercihli. (3) 4 spec — adaptör başına hedefli testler + kontrat-uyumluluk (adaptör çıktıları cast'sız PR-2A `calculateScore()`'a geçer) + adaptör-düzeyi statik saflık guard'ı (icrabot/bildirim-kuyruğu/manuel-etiket/sistem-saati yasağı, Prisma/Nest izinli — guard iki kez kendi doc-yorumunu yakaladı, W0.1/PR-2A emsali, yorumlar yeniden kelimelendirildi, GEVŞETİLMEDİ). **`NotificationQueue`, manuel risk etiketleri ve legal deadline hesapları hiçbir adaptörde KULLANILMIYOR.** **AÇIK KALAN:** servis orkestrasyonu (PR-2C), endpoint (Phase 3), consumer switch, module registration HENÜZ YOK — **PR-2C READY**, ayrı GO-IMPLEMENT bekler; adaptörler PR-2C'ye kadar hiçbir yerden çağrılmayan dead-code-by-design kalır. | Runtime'a EKLENEN ama henüz hiçbir yerden ÇAĞRILMAYAN read-only adaptörler; schema/migration/`Case.riskScore` yazımı/orchestration/endpoint/module-registration YOK. | Owner GO-IMPLEMENT + IF GO-COMPLETE (DEBTOR-SCORING PR-2B INPUT ADAPTERS talimatı); PR #1065 branch commit `5914806a`, squash merged SHA `872e0f8eac553a57b9629b36b01a4219b1d276b4`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; yeni testler 6 suite/37 PASS; komşu `interest-engine/orchestration` regresyonu 3 suite/35 PASS; changed-file tsc 0 hata; kapanış doğrulaması sırasında canonical main == origin/main == `872e0f8e` (VERIFIED, 2026-07-10); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md` `DEBTOR-SCORING-CANON` satırına PR-2B kapanış eki işlendi. İzole worktree (`HUKUK_pr2b-input-adapters`) Windows "Filename too long" nedeniyle fiziksel silinemedi → `maintenance-register.md`'ye **MR-023** açıldı (MR-021/022 emsali). Sonraki hat: **PR-2C (service orchestration + tests)** — ayrı GO-IMPLEMENT bekler. |
| 2026-07-10 | **ADR-014 W0.2/W0.3 compatibility remediation — technical merge recorded; governance closure pending this register patch**: PR #1050 (`a4167f35`) introduced the now-superseded W0.2 direct-write REVERSAL/option surface. PR #1058 (`31eb84d3`) then depended on that removed surface from the W0.3 diagnostic runner. Owner decision is binding: W0.2 is **PAYMENT-only**; no REVERSAL materialization, `reversesLedgerEntryId`, `MaterializeReversalIntent`, `reversals` forwarding, `fileNumberPrefix` forwarding, or `CollectionService.cancel()` simulation. PR #1063 (`a31f3915` branch commit; squash `0bd078032d7fd02d7dfb917b799bcefd0542e111`) restores that contract and preserves W0.3 diagnostic/evidence behavior. | Six files only: CI W0.2 explicit gates; W0.2 materializer/static/DB-gated tests; W0.3 runner/DB-gated test. No runtime/API/UI/schema/migration/authority change. Local validation: static 5/5, W0.3 static 4/4, evidence 6/6, W0.2 DB 5/5, W0.3 DB 3/3, production type-check PASS. PR CI 4/4 SUCCESS. | Owner GO-COMPLETE; PR #1063; canonical main `0bd07803`; PR #1050 violation and PR #1058 contract drift are historical facts, not approved W0.2 behavior. | **W0.2 and W0.3 are not CLOSED.** This record is the required Master Register remediation; remaining gates are owner authorization for W0.3 follow-up disposition, PR-1A, PR-1B, and runtime cutover. |
| 2026-07-10 | **DEBTOR-SCORING-CANON PR-2A — MERGED/CLOSED (contracts + pure engine)**: Phase 2'nin ilk implementasyon dilimi kapatıldı. Yeni bağımsız modül klasörü `apps/api/src/modules/debtor-scoring/` (4 yeni dosya, +727; **mevcut hiçbir dosyaya dokunulmadı — `app.module.ts` dahil**, modül kaydı PR-2C'nin işi). **Teslim edilen:** (1) `debtor-scoring.types.ts` — 6 zorunlu kontrat (`DebtorScoringResult`/`FactorContribution`/`InputProvenance`/`DataGap`/`ScoreBand`/`ScoringInput`), persistence-bağımsız; (2) `scoring-engine.ts` — `calculateScore(input)` **saf deterministik motor, `dscan-v1.0`**: IO/DB/sistem-saati erişimi YOK (`asOf` girdiden), skor 0-100 clamp, bantlar LOW 0-24/MEDIUM 25-49/HIGH 50-74/CRITICAL 75-100, ağırlıklar 25/25/20/15/15 (M1), eksik veri nötr + dataGaps + warnings (M2), `LEGAL_DEADLINE` daima EXCLUDED DATA_GAP (M5); (3) motor spec'i — determinizm/faktör-toplamı/clamp/band-sınırları/DATA_GAP-nötr/provenance-warnings/polarite + erken-yaşam ve tebligat-entegrasyon ayrımları; (4) **statik saflık guard'ı** — Prisma/Nest/dormant-v4/bildirim-kuyruğu/manuel-etiket/sistem-saati sızıntısını kalıcı kilitler (W0.1 emsali: guard iki kez kendi doc-yorumlarını yakaladı, yorumlar yeniden kelimelendirildi, guard GEVŞETİLMEDİ). **AÇIK KALANLAR:** DB/IO/persistence YOK; input adapter (PR-2B), servis orkestrasyonu (PR-2C), endpoint (Phase 3) ve consumer switch HENÜZ YOK — **PR-2B ve PR-2C AÇIK**, her biri ayrı GO-IMPLEMENT bekler. | Runtime'a EKLENEN ama henüz hiçbir yerden ÇAĞRILMAYAN saf modül (dead-code-by-design, PR-2C'ye kadar); schema/migration/`Case.riskScore` yazımı/consumer değişikliği YOK. | Owner GO-IMPLEMENT + IF GO-COMPLETE (DEBTOR-SCORING PR-2A talimatı); PR #1060 branch commit `fda2dedc`, squash merged SHA `475de5461786567dc1018672774ce67ed144f9c7`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; yeni testler 2 suite/15 PASS; changed-file tsc 0 hata; kapanış doğrulaması sırasında canonical main == origin/main == `475de546` (VERIFIED, 2026-07-10); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md` `DEBTOR-SCORING-CANON` satırına PR-2A kapanış eki işlendi. İzole worktree (`HUKUK_pr2a-scoring-engine`) Windows "Filename too long" nedeniyle fiziksel silinemedi → `maintenance-register.md`'ye **MR-022** açıldı (MR-021 zaten ADR-014 W0.3'e ayrılmış olduğundan; MR-005/006/007 emsali). Sonraki hat: **PR-2B (canonical input adapters)** — ayrı GO-IMPLEMENT bekler. |
| 2026-07-10 | **VER-05 / CAN-CUT-01 PR-0 — Due–ClaimItem read-only inventory tool implemented**: Tenant ID zorunlu CLI, tek `REPEATABLE READ READ ONLY` transaction içinde yalnız SELECT/CTE ile `Due` ve `ClaimItem` kayıtlarını sınıflandırır. Deterministic JSON+özet çıktısı `MATCHED_PAIR`, `DUPLICATE_PAIR`, `AMOUNT_OR_TYPE_DRIFT`, `ORPHANED_SYNC`, `MARKER_MISSING`, `DUE_ONLY`, `CLAIM_ITEM_ONLY`, `NAFAKA_EXPECTED_DUE_ONLY` ve `EXPECTED_CANCELLED_TOMBSTONE` üretir; canlı `dueSync` ve backfill marker'ları ayrı sayılır. Heuristic eşleşme hiçbir mutation yetkisi oluşturmaz; `collectedAmount` eşleştirme kriteri değildir ve açıklama/kişisel veri export edilmez. | Diagnostic only — runtime domain davranışı, schema/migration, DB write/backfill, Due/ClaimItem mutation, route/UI cutover, OfficeApproval ve hesaplama semantiği değişikliği yok. | Owner GO-IMPLEMENT + IF GO-COMPLETE; güncel `origin/main` Due/ClaimItem schema, sync/backfill akışları ve repository test desenleri. Focused classification/tenant-isolation/NAFAKA/duplicate-orphan-drift/no-write guard testleri PASS. | **VER-05: OPEN / INVENTORY TOOL IMPLEMENTED. CAN-CUT-01: OPEN / CUTOVER NOT AUTHORIZED.** Sıradaki adım yetkili ortamda salt-okunur tenant inventory koşumu ve bulgu incelemesidir; sonuçlara göre küçük, sıralı reconciliation/backfill/cutover workstream'leri ayrı owner yetkisi ister. |
| 2026-07-10 | **DEBTOR-SCORING-CANON Phase 2 Spec — CLOSED/REGISTERED (PR #1054, docs-only)**: Phase 2 read-only `DebtorScoringService` GO-ANALYZE çıktısı, `project/docs/design/debtor-scoring-canonicalization.md` **Bölüm 12** olarak kalıcılaştırıldı (+146 satır, tek dosya). İçerik: Phase 2 yapar/yapmaz sınırı (read-only; persistence/endpoint/`Case.riskScore` yazımı/consumer switch/cross-case aggregate YOK), Input Authority Matrix (AUTHORITATIVE/FOUNDATION_REQUIRED/DATA_GAP/NON_AUTHORITATIVE/LEGACY_ONLY), F1/F2/F3 reconciliation (yeni formül icat edilmedi; F3 ters-polarite dışlandı), `DebtorScoringService`/`DebtorScoringResult` kontratı + explainability, faktör modeli (FIN_RECOVERY 25 / ASSET_COVERAGE 25 / CASE_AGE 20 / STAGE_PROGRESS 15 / BEHAVIOR 15), eksik-veri politikası (nötr puan + dataGaps + warnings; v1'de confidence alanı yok), `LEGAL_DEADLINE` faktörü Legal Time Authority hazır olana kadar DATA_GAP (kaba tarih hesabı yasak), balance entegrasyonu (`CaseBalanceOrchestration.computeCaseBalance` servis-servise doğrudan import; unsafe'te CONFIRMED-only fallback + provenance), Phase 3 shadow-compare kontratı, 12 maddelik test planı ve PR planı (PR-2A contracts + pure engine → PR-2B input adapters → PR-2C orchestration + tests; endpoint Phase 3'e). **M1-M7 owner kararları belgede NİHAİ tablo olarak kayıtlıdır — burada yeniden yazılmaz, Bölüm 12.9'a bakınız.** **GO-IMPLEMENT PR-2A: READY** — implementasyona hazır ancak henüz UYGULANMADI; ayrı owner GO gerektirir. | Docs/governance only — kod, test, schema, migration, runtime davranışı YOK. Bu kayıt implementasyonu yetkilendirmez. | Owner GO-DOCS + IF GO-COMPLETE (DEBTOR-SCORING-CAN PHASE 2 SPEC talimatı, M1-M7 kararları dahil); PR #1054 branch commit `68b48eab`, squash merged SHA `2e26540d7acdbfc19401fed79fccb30749c593d7`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; kapanış doğrulaması sırasında canonical main == origin/main == `2e26540d` (VERIFIED, 2026-07-10); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md` `DEBTOR-SCORING-CANON` satırına Phase 2 spec kapanış eki işlendi. Sonraki hat: **PR-2A (contracts + pure engine)** — ayrı GO-IMPLEMENT bekler; full consumer switch/persistence/endpoint/cross-case aggregate kapsam dışı kalmaya devam eder. |
| 2026-07-10 | **ADR-014 W0.3 Diagnostic Dual Mode + Evidence Model — MERGED/CLOSED (GO-COMPLETE)**: dual-mode diagnostic + evidence katmanı main'e girdi (PR #1058, squash `31eb84d3`; 4 yeni dosya `scenario-diagnostic/` + spec'ler + W0.2 statik guard'ının test-support tanımına tek-satır mekanik hiza; **runtime davranış değişikliği YOK**, §16 production isolation korundu). Frozen (d) uygulandı: SYNTHETIC_SCENARIO modu `ScenarioDefinition` → W0.2 materializer → gerçek üretim yolu (`computeCaseBalance` → `toCaseBalanceDisplay`) → `compareScenarioEvidence`; ORGANIC_READINESS modu tenant-scoped gerçek case taraması — QA-seed dışlama ilkesi parametrik `excludeCaseIds` olarak YALNIZ organik modda (repo'da `QA_SEED_CASE_IDS` tracked değildi; env okunmaz), expected taşımaz. Frozen (e) uygulandı: §8'in BEŞ evidence sınıfı birebir kod yansıması (YENİ sınıf/enum üretilmedi); §12 karşılaştırıcı HESAPLAMAZ — authority/blockerCodes(set)/perCurrencyStatus/verilen-totals kıyaslanır, snapshot yüzeyi main'de üretilmediğinden karşılaştırılmaz → `SNAPSHOT_PATH_NOT_EXERCISED` serbest-metin notu (W0.2 emsali). **Kanıt:** statik guard 4/4 (production src scenario-diagnostic import edemez; @nestjs/env yasağı; evidence saflık guard'ı); evidence unit 6/6 (DB'siz, her CI'da); DB-gated 3/3 disposable `hukuk_w03_gate` (D1 match + in-memory devamlılık G5/frozen-e; D2 karşılaştırıcı dürüstlüğü — yalancı-PASS yok; D3 organik tarama/dışlama/tenant-scope); env'siz koşum 3 SKIP (fail-safe); W0.2 DB-gated regresyonu 4/4; suite 54/54; prod typecheck temiz; CI 4/4 SUCCESS. **Kayıt bulgusu (davranış değişikliği yapılmadı):** mevcut main display authority'si OK durumunda HER ZAMAN `SHADOW_ONLY` (`case-balance-display.ts:529`) — `CANONICAL_CANDIDATE` ataması cutover PR'larının (PR-11/12) işi; senaryo beklentileri bugünkü gerçeğe sabitlendi (characterization). **Wave 0 (W0.1+W0.2+W0.3) TAMAMLANDI.** GUARDRAIL yürürlükte: Diagnostic PASS ≠ `CollectionService.cancel()` PASS (PR-1B ayrı gate); registry/platformlaştırma ertelenmiş kalır. | ADR-014-SCENARIO-INFRA W0.3; interest-engine test-altyapısı | ADR-014 W0.3 GO-COMPLETE (owner GO 2026-07-10); PR #1058 | Sıradaki: PR-1A (Acceptance Criteria §22 CONDITIONAL_GO) ve PR-1B AYRI owner GO bekler; orphan dizin MR-021 (owner-manuel cleanup). |
| 2026-07-10 | **MASTER-TRIAGE-REMAINDER-RECONCILIATION Wave 0 — governance patch CLOSED**: Master Triage kalanlari repo kanitiyla tekillestirildi. `VER-03/04/06/08/23/25/26/27` kapatildi veya maintenance'a ayrildi; Verification 22→14, WQ PENDING 13→6, DONE 5→7, Closed Register 45→53 oldu. Offset ADR'deki bayat Guided-Open/always-gated anlatimi, OWN-29-A kanonik karariyla uyumlu hale getirildi: offset payout degildir, DBIND §5 uygulanmaz, v1 `DIRECT_CAPABILITY` ve `approvalRef=null` korunur; OfficeApproval/four-eyes ancak yeni owner-authorized workstream ile acilabilir. Active Roadmap PHASE 7/P4-6, PR #823/#832 ve Product Backlog DONE kanitiyla CLOSED/DELIVERED yapildi; UA-1 DEFERRED degismedi. Workflow'daki session prosedurleri/stale duplicate'lar cikarildi; eski adlandirilmis orphan grubu ve canonical divergence DONE'a tasindi; balance-shadow flake ile mevcut fiziksel dizinler Maintenance Register'a ayrildi. | Governance/docs only; runtime, test, schema, migration, package/lock, DB ve process degisikligi yok | Owner GO-COMPLETE `MASTER-TRIAGE-REMAINDER-RECONCILIATION — WAVE 0 GOVERNANCE PATCH`; latest `origin/main` repo kaniti; ilgili runtime testleri/commit history/governance cross-reference'lari; canonical equality ve workspace path varlik kontrolleri | Acik 14 VER, 6 WQ ve ACTIVE 3 ayri workstream olarak kalir. `case-compare-modal` drift'i ve balance-shadow flake maintenance takibi gerektirir. Owner-local dizinler owner teyidi olmadan degistirilmez/silinmez. Mevcut MR-008 daha guclu dogrulanmis kayit oldugu icin korunmustur; duplicate veya downgrade edilmemistir. |
| 2026-07-10 | **DEBTOR-RISK-LABEL-MAPPING-FIX — MERGED/CLOSED (dar semantik hotfix)**: Manuel `Debtor.riskLevel === "COK_YUKSEK"` etiketinin `calculateDebtorIssues` içinde yanlış şekilde `RISK_BANKRUPTCY` koduna + "İflas riski" label'ına çevrilmesi düzeltildi. **Kanonik ayrım (owner kararı):** manuel `COK_YUKSEK` risk etiketi ≠ iflas hukuki olgusu/statüsü; `COK_YUKSEK` artık `RISK_VERY_HIGH` / "Çok yüksek risk (manuel değerlendirme)" üretir, severity `DANGER` korunur. **`RISK_BANKRUPTCY` SİLİNMEDİ** — union'larda gerçek/doğrulanmış iflas sinyali için REZERVE kalır. **MPB-028(d) AÇIK KALIR:** gerçek iflas detection / `Debtor.legalStatus` hayalet alan tasarımı (`extractRiskFlags`'ın var olmayan `bankruptcyStatus`/`concordatStatus`/`isDeceased` okuması) bu düzeltmeyle KAPANMADI — ayrı workstream, ayrı owner kararı bekler; bu PR yalnız yanlış code/label eşlemesini düzeltti. Kapsam: 4 dosya, +77/−3 — `debtor.service.ts` (union + exhaustive label map + tek üretici), paylaşılan `packages/types/src/index.ts` (üçüncü kopya senkronu: union + exhaustive Record label map) ve `web/lib/api.ts` (FE type union +1; FE hiçbir yerde issue code'a göre dallanmıyor, yalnız label render — davranışsal FE etkisi yok) + yeni spec. Repo-geneli tarama: `RISK_VERY_HIGH` önceden hiçbir yerde yoktu; `COK_YUKSEK`'in tek issue-üreticisi bu blok; `RISK_BANKRUPTCY`'nin aktif gerçek-iflas consumer'ı yok. Gözlem (dokunulmadı): `RISK_CONCORDAT`/`RISK_ADDRESS_SUSPECT` kodlarının da bugün hiçbir üreticisi yok (ölü kod). **Frozen scope korundu:** schema/migration YOK, `Debtor.legalStatus` kararı YOK, gerçek bankruptcy detection YOK, `DebtorScoringService` YOK, risk formülü DEĞİŞMEDİ, API response shape DEĞİŞMEDİ (yalnız yeni enum değeri), UI component DEĞİŞMEDİ, yeni kütüphane YOK. | Runtime davranış düzeltmesi (issue code/label üretimi) + tip senkronu + test; schema/migration/formül/UI YOK. | Owner GO-IMPLEMENT + IF GO-COMPLETE (COK_YUKSEK / RISK_BANKRUPTCY MAPPING FIX talimatı, kanonik karar dahil); PR #1051 branch commit `b1b38e4c`, squash merged SHA `f9a05e87020de85e711e8835b4ac1f238a5ea2ef`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); yeni spec 6/6 PASS (COK_YUKSEK → RISK_VERY_HIGH/DANGER/yeni label; RISK_BANKRUPTCY ve "İflas riski" üretilmez; YUKSEK/ORTA/DUSUK/null değişmedi); komşu debtor modülü 27 suite/209 test PASS (4 suite/69 test pre-existing skip); api + packages/types + web changed-file typecheck 0 hata; kapanış doğrulaması sırasında canonical main == origin/main == `f9a05e87` (VERIFIED, 2026-07-10); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md`'ye `ID: DEBTOR-RISK-LABEL-MAPPING-FIX` satırı eklendi; `DEBTOR-SCORING-CANON` satırına dar-semantik-hotfix kapanış eki düşüldü (M1 yanlış eşleme bulgusu kapandı; Phase 2-7 mimarisi hâlâ bloklu). İzole worktree (`HUKUK_risk-mapping-fix`) Windows "Filename too long" nedeniyle fiziksel silinemedi → `maintenance-register.md`'ye **MR-007** açıldı (MR-005/006 emsali). Sonraki hat: DEBTOR-SCORING-CANON Phase 2 GO-ANALYZE veya MPB-028 (c)/(d) — ayrı owner GO bekler. |
| 2026-07-10 | **ADR-014 W0.2 Hybrid Scenario Materializer — MERGED/CLOSED (GO-COMPLETE)**: `ScenarioDefinition → Materializer → disposable test DB` adapter'ı main'e girdi (PR #1050, squash `a4167f35`; yalnız 3 yeni dosya: `scenario-materializer/scenario-materializer.ts` + statik guard spec + DB-gated integration spec; **runtime davranış değişikliği YOK**). v1.2 owner arbitration'ı uygulandı: iskele (Tenant/Client/Debtor/Case/CaseDebtor) Prisma-direct; ClaimItem üç-tutar (G2); PAYMENT LedgerEntry `id=Payment.id`; REVERSAL = **Conditional Option B** direct-write (`reversesLedgerEntryId` zorunlu — G1; timeline/outbox/journal/audit taklit edilmedi → `writePathNote=WRITE_PATH_NOT_EXERCISED`, serbest-metin metadata — yeni enum/evidence sınıfı AÇILMADI); domain→Prisma `interestType` ters-köprüsü fail-fast (`LEGAL_3095→YASAL`, `COMMERCIAL_AVANS_3095_2_2→TICARI`, bilinmeyen kod → hata). **Acceptance gate kanıtları:** G1/G2 satır-bütünlüğü assert'leri; G3 **scoped** before/after delta==0 (timeline `caseId`, outbox/journal/audit `tenantId` kapsamı — global count kullanılmadı, owner kuralı); G4 statik guard (production src materializer import edemez) + Acceptance Criteria §13 bağımlılık-freeze + §9 import yüzeyi; G5 self-check: materialize → **gerçek** `CaseBalanceService.computeCaseBalance` == saf in-memory `engine.computeBalance` (disposable Docker Postgres `hukuk_w02_gate` üzerinde **4/4 PASS**); G6 `resolveTestDatabaseUrl` fail-safe (env'siz koşum: 4 SKIP — CI-güvenli). Regresyon 68/68 (golden-scenarios, case-balance controller, orchestration, W0.1 statik); prod typecheck temiz; CI 4/4 SUCCESS. **Koşum bulgusu (davranış değişikliği yapılmadı, kayıt amaçlı):** `RateSchedule.tenantId` FK'sı şemada `Office.id`'ye bağlı, runtime sorgusu (`RateProviderService`) ise `computeCaseBalance`'ın Tenant.id'siyle filtreliyor — ikisi ancak `Office.id == Tenant.id` iken buluşuyor (`Office.tenantId @unique`); test kurulumu bu eşleşmeyi köprü satırıyla kurdu, şema/runtime tutarlılık denetimi W0.2 kapsamı DIŞINDA bırakıldı. **GUARDRAIL yürürlükte:** Materializer PASS ≠ `CollectionService.cancel()` PASS — production cancel write-path kanıtı PR-1B gate'inin işi. | ADR-014-SCENARIO-INFRA W0.2; interest-engine test-altyapısı | ADR-014 W0.2 GO-COMPLETE (owner GO 2026-07-10; iskele Hard Stop → v1.2 RESOLVED BY OWNER DECISION); PR #1050 | Sıradaki: W0.3 (diagnostic dual-mode + evidence model) AYRI owner GO bekler; PR-1B gerçek cancel() DB-integration gate'i açık; orphan dizin MR-008 (owner-manuel cleanup). |
| 2026-07-10 | **COLLECTION-STATUS-FILTER-HOTFIX — MERGED/CLOSED (D3 hotfix, dar GO-IMPLEMENT)**: DEBTOR-SCORING-CANON D3 owner kararıyla öne alınan dar davranış düzeltmesi kapatıldı. **Kural artık runtime'da:** `PENDING` (henüz onaylanmamış), `CANCELLED` (iptal) ve `REFUNDED` (iade) tahsilatlar collection-tabanlı risk/AI/policy metriklerinde "tahsil edildi" SAYILMAZ; yalnız `CONFIRMED` sayılır. Filtre mantığı tek yerde: yeni `apps/api/src/common/collection-confirmed.util.ts` (`isConfirmedCollection`/`filterConfirmedCollections`/`sumConfirmedCollections`). **Düzeltilen 5 site:** (1) `risk.service.ts` collection score, (2) `risk.service.ts` behavior score, (3) `automation.service.ts` nightly persist edilen `Case.riskScore` + `getRiskFactors.hasCollections`, (4) `ai.service.ts` suggestion/prediction prompt metrikleri + rule-based fallback (3 nokta), (5) `policy-engine/fact-store.service.ts` `collectedAmount`/`collectionRate` (select'e yalnız `status: true` eklendi). Ek: `ai-suggestions.spec.ts`'te davranış değişikliğinin haklı yakaladığı 1 bayat fixture (`{amount}` → `{amount, status: 'CONFIRMED'}`) testin niyeti korunarak düzeltildi. **Beklenen görünür etki (D3 gereği bilinçli):** nightly cron ertesi gece tüm portföyü CONFIRMED-only oranla yeniden puanlar; AI tahmin/öneri metinleri ve policy `collectionRate` değişebilir. **GO-IMPLEMENT ayrımı:** BU HOTFIX TAMAMLANDI; kanonik `DebtorScoringService`/`DebtorScoringSnapshot` mimarisi (Phase 2-7) HÂLÂ BLOKLU/İMPLEMENTE EDİLMEDİ — her faz kendi ayrı GO-IMPLEMENT'ini bekler. Yapılmayanlar (frozen scope korundu): schema/migration YOK, icrabot reaktivasyon/adapter YOK, `Case.riskScore` deprecation YOK, `RiskReport` şema değişikliği YOK, API/response contract ve UI DEĞİŞMEDİ. | Runtime davranış düzeltmesi (5 metrik sitesi) + testler; schema/migration/API-contract/UI YOK. | Owner GO-IMPLEMENT + IF GO-COMPLETE (COLLECTION-STATUS-FILTER-HOTFIX talimatı); PR #1047 branch commit `ab7966d9`, squash merged SHA `ab37f18d8e048276c973764304e67bdcfbb3f879`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; yeni testler 5 suite/21 PASS + komşu regresyon 4 suite/35 PASS; changed-file tsc taraması 0 hata; kapanış doğrulaması sırasında canonical main == origin/main == `ab37f18d` (VERIFIED, 2026-07-10); Hard Stop Triggered: NONE; Frozen Decisions Violated: NONE. | `product-backlog.md`'ye `ID: COLLECTION-STATUS-FILTER-HOTFIX` satırı eklendi ve `DEBTOR-SCORING-CANON` satırının "sıradaki hat" kuyruğu CLOSED olarak güncellendi. İzole worktree (`HUKUK_collection-status-hotfix`) Windows "Filename too long" nedeniyle fiziksel silinemedi → `maintenance-register.md`'ye **MR-006** açıldı (MR-005 emsali). Sonraki hat: DEBTOR-SCORING-CANON Phase 2 (`DebtorScoringService` read-only) — ayrı owner GO bekler. |
| 2026-07-10 | **ADR-014 Wave 0 Acceptance Criteria — Master Register closure prepared**: PR #1042 (`f56eed97` branch commit) was squash-merged as `bc7a2e73`; CI 4/4 SUCCESS; the four-file docs-only scope, zero runtime/authority impact, canonical-main inclusion, branch/worktree cleanup, `.codex/` untouched state, `Hard Stop Triggered: NO`, and `Frozen Decisions Violated: NO` are now recorded in the canonical CCB-001 master stream (`product-backlog.md`). Current main verification `06ef066f` contains the merge. | Governance bookkeeping only — no source, test, schema, migration, runtime wiring, authority, W0.2/W0.3, PR-1A/PR-1B, or cutover work. | Owner `GO-IMPLEMENT ADR-014 WAVE0 ACCEPTANCE CRITERIA MASTER REGISTER CLOSURE` + repository/GitHub ground-truth verification. | This docs-only register closure must merge before the Acceptance Criteria line is fully CLOSED. It does not authorize implementation; W0.2 requires a separate explicit owner GO after closure. |
| 2026-07-10 | **OWN-29-A Offset Confirm Gate — governance closure / runtime unchanged**: Repository-backed GO-ANALYZE, `ClientOffset` apply/reverse akışında OfficeApproval/four-eyes bulunmadığını; `CLIENT_OFFSET_APPLY`/`CLIENT_OFFSET_REVERSE` işlemlerinin service-level PARTNER/MANAGER `DIRECT_CAPABILITY` ile çalıştığını; `approvalRef=null`, transaction audit, idempotency, journal write ve payout ile paylaşılan yalnız concurrency/outstanding bağlarının mevcut olduğunu doğruladı. Offset payout/money-out değildir; DBIND §5 self-approval istisnasını miras almaz. Owner kararı uyarınca DIRECT_CAPABILITY v1 için korundu ve confirm/four-eyes runtime implementasyonu açılmadı. | Docs/governance only — runtime, OfficeApproval action, confirm-gate, test, schema veya migration değişikliği yok. | Owner GO-IMPLEMENT + IF GO-COMPLETE yetkisi; güncel `origin/main` repository kanıtı (`client-offset.service.ts`, `client-offset.controller.ts`, `client-offset.dto.ts`, `office-approval-domain-sync.service.ts`, `payout-lock.ts`, `dbind-financial-authority-decisions.md`, `master-triage-register.md`). | **OWN-29-A CLOSED / ANALYZED / RUNTIME UNCHANGED.** OWN-29 hattında açık workstream kalmadı. Gelecekte offset confirm-gate istenirse APPLY/REVERSE için ayrı risk, idempotency, stale-state revalidation, transaction/journal/audit ve four-eyes tasarımı içeren yeni owner-authorized workstream gerekir; bu kapanış onu yetkilendirmez. |
| 2026-07-10 | **DEBTOR-SCORING-CANON — Design acceptance (PR #1043, docs-only) + D1-D7 owner kararları — GO-DOCS closure, GO-IMPLEMENT blocked**: MPB-028 kapanışında ID'siz PROPOSED bırakılan takip maddesi (b)'nin (rakip risk formülleri + CANCELLED/REFUNDED filtre sorunu → kanonik `DebtorScoring` konsolidasyonu) ilk somut çıktısı merge edildi: `project/docs/design/debtor-scoring-canonicalization.md`. GO-ANALYZE, MPB-028'in bildiği İKİ formüle ek ÜÇÜNCÜ (dormant) formülü ve iki manuel ekseni doğruladı: F1 `RiskService.analyzeCase` (aktif, `Case.riskScore` YAZMAZ), F2 `AutomationService.calculateRiskScore` (aktif nightly cron, `Case.riskScore`'un TEK yazarı), F3 icrabot Risk Scoring v4 (dormant, **TERS POLARİTE** yüksek=iyi; `task-orchestrator.service.ts:301` whitelist'inde tenant-guard'sız silahlı-boşta yazma yolu), M1 `Debtor.riskLevel` (manuel, `COK_YUKSEK→RISK_BANKRUPTCY` yanlış eşlemesiyle), M2 `Case.riskId→LookupRisk` (manuel). **CONFIRMED_DEFECT:** PENDING/CANCELLED/REFUNDED tahsilatlar 5 aktif sitede "tahsil edildi" sayılıyor (`risk.service.ts:113,174`, `automation.service.ts:282` persist edilen skor, `ai.service.ts:184,217,311` prompt/fallback, `policy-engine/fact-store.service.ts:162` collectionRate). Ek borçlar: `RiskReport` çift-yazar/farklı factors Json şeması + doğrudan tenantId/source/version yok; F1 davranış skoru `NotificationQueue DELIVERED` sinyaline bağımlı (PR #1027 sonrası sessizce kurur). **OWNER KARARLARI (nihai, 2026-07-10):** **D1** kanonik polarite = yüksek skor KÖTÜ (aktif tüketiciler zaten böyle okuyor; dormant v4 ters polariteyle doğrudan bağlanamaz). **D2** kanonik skor v1 = Case-level; debtor-level/cross-case aggregate ayrı read-model/ileri faz. **D3** collection-status filter hotfix ÖNE ALINACAK ama ayrı dar GO-IMPLEMENT ile (bu docs/register PR'larında implementasyon YOK); kural: PENDING/CANCELLED/REFUNDED tahsil edilmiş sayılmaz; tercih: yalnız CONFIRMED veya güvenli kanonik balance/payment authority çıktısı. **D4** `RiskReport` tenantId/source/version migration bu hatta DEĞİL — read-only canonical service tasarlanmadan migration açılmayacak. **D5** `Case.riskScore` geçişte legacy alias/türetilmiş alan kalabilir; tek kanonik otorite SAYILMAZ; deprecation ayrı owner kararı, shadow-compare sonrası. **D6** icrabot v4: reaktivasyon öncesi polarity adapter ZORUNLU; bugün rewrite yok; ters polariteyle `Case.riskScore` veya settlement/discount kararlarına bağlanması YASAK. **D7** hedefte scoring girdisi kanonik balance/payment authority çıktısı; dar hotfix fazında bu güvenli değilse yalnız CONFIRMED filtresi uygulanabilir; full entegrasyon PR-2/PR-3 tasarımına göre ayrı fazlanır. Rollout: 7 fazlı shadow-first (docs → read-only servis → shadow compare → consumers switch → cron switch + F2 removal → icrabot adapter/whitelist cleanup → opsiyonel RiskReport migration); her faz ayrı GO-IMPLEMENT. | Docs/governance-only — kaynak kod, test, schema, migration, runtime davranışı YOK. Bu kayıt implementasyonu yetkilendirmez; belgenin Final Verdict'i GO-DOCS: YES / GO-IMPLEMENT: NO. | Owner GO-DOCS + GO-COMPLETE PR #1043 (D1-D7 açık kararlar aynı talimatta); GO-ANALYZE repo kanıtı (risk/automation/ai/icrabot/policy-engine/report servisleri + schema `CollectionStatus`/`DebtorRiskLevel`/`RiskReport` + FE reports/debtors sayfaları); PR #1043 squash merged SHA `e7c4b492e8bcba5474365240589505a32cb2df9d`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN; kapanış doğrulaması sırasında canonical main == origin/main == bu SHA (VERIFIED, 2026-07-10). | `product-backlog.md`'ye `ID: DEBTOR-SCORING-CANON` eklendi (GO-DOCS closed / GO-IMPLEMENT blocked). Sıradaki hat: **COLLECTION-STATUS-FILTER-HOTFIX** — ayrı dar GO-IMPLEMENT (D3); görünür skor/AI/policy davranış değişikliği beklenir, kendi test kapsamı + kendi register kaydıyla. Kapsam dışı kalanlar: `extractRiskFlags` hayalet alan kararı (MPB-028 follow-up (d), ayrı hat), `EnforcementAction.caseDebtorId+tenantId` migration (follow-up (c)), `COK_YUKSEK→RISK_BANKRUPTCY` yanlış eşleme düzeltmesi (ayrı küçük iş), Client Workspace operating risk + calc-preview scenario-ranker (INTENTIONAL_BOUNDED_CONTEXT). |
| 2026-07-10 | **ADR-014 W0.2 İskele Revizyonu — Hard Stop → Owner Arbitration (baseline v1.1 → v1.2)**: W0.2 (hybrid materializer) implementasyonu başlarken ground-truth doğrulaması, frozen kararın "iskele (Tenant/Client/Debtor/Case/CaseDebtor) gerçek servis" maddesinin güncel main üzerinde uygulanamaz olduğunu gösterdi: (1) `CaseService` constructor'ı 10 bağımlılık taşıyor (3'ü forwardRef — `case.service.ts:428-447`), elle kurulamaz; (2) `Test.createTestingModule({imports:[CaseModule]})` 10 modüllük ağı (Ocr, AddressDiscovery, InterestEngine, ExpenseRequest, DomainEventIngest, Collection, Client, Lawyer, Debtor, PermissionDiagnostics) ayağa kaldırır — W0.2'yi uygulama-seviyesi bootstrap'a dönüştürür; (3) gerçek servisler tam da Conditional-B gerekçesinin kaçındığı yan-etkileri üretir (`collection-payment-received.integration.spec.ts` başlığı: "collection + event + outbox in same tx"); (4) repo'nun yerleşik DB-gated emsali iskeleyi zaten Prisma-direct kurar (aynı spec :95-135 + tm47d) ve MPB-025 fail-safe `resolveTestDatabaseUrl` (yalnız `hukuk_*_gate`) altyapısıyla kurumsallaşmıştır. **Tanımlı Hard Stop ("frozen karar değişikliği ihtiyacı") uygulandı — implementasyon durduruldu, sessiz tasarım değişikliği YAPILMADI**; kısa salt-okunur GO-ANALYZE hattıyla 5 soru kanıtlandı ve owner'a dar karar önerisi sunuldu. **Owner kararı (ONAYLANDI):** "W0.2 iskele katmanı materializer içinde Prisma-direct kurulur; G1–G6 acceptance gate'leri bağlayıcıdır." G3 owner düzeltmesiyle: global `count==0` DEĞİL, **scoped before/after delta==0** (tenant/case/scenario kapsamı; paylaşılan DB'de yanlış sonuç önlenir). `WRITE_PATH_NOT_EXERCISED` yalnız mevcut evidence modelinde (serbest-metin metadata) temsil edilebiliyorsa kullanılır — yeni enum/evidence sınıfı/mimari kavram gerekirse HARD STOP. **DEĞİŞMEYENLER:** Conditional Option B'nin diğer tüm şartları, ClaimItem üç-tutar + LedgerEntry PAYMENT/REVERSAL dedicated direct-write, `Scenario → Materializer → DB` yönü, materializer production davranışını taklit etmez, **`Materializer PASS ≠ CollectionService.cancel() PASS`** guardrail'i, production cancel path'in ayrı DB-gated integration testi (PR-1B gate'i), registry/platformlaştırma ertelemesi. W0.1 yeniden AÇILMAZ. | Docs/governance only — bu kayıt anında kod/test/schema/migration/runtime değişikliği YOK; W0.2 implementasyonu bu revizyonun merge'inden SONRA aynı GO-COMPLETE yetkisiyle devam eder. | W0.2 GO-COMPLETE brief'i (Hard Stop tanımlı) + ground-truth kanıtı (case.service.ts:428-447, case.module.ts:24-38, collection-payment-received.integration.spec.ts:22-135, auth-register-seed.integration.spec.ts:22-25, test-db-env) + iskele GO-ANALYZE (5 soru) + owner arbitration ("ONAYLANDI — dar iskele revizyonu", G3 scoped-delta düzeltmesiyle) | `adr-014-split-pr-plan.md` v1.1 → **v1.2** (§3'e "W0.2 İskele Revizyonu" bloğu + G1–G6 tablosu; revizyon geçmişi güncellendi). `product-backlog.md` `ID: ADR-014-SCENARIO-INFRA` iskele cümlesi revize + `ID: ADR-014-SPLIT-PR-PLAN` v1.2 referansı. Hard Stop Disposition: **RESOLVED BY OWNER DECISION**. Sonraki adım: W0.2 implementasyonu (aynı GO-COMPLETE zinciri içinde, yeniden onay istenmeden). |
| 2026-07-10 | **ADR-014 Wave 0 Acceptance Criteria — canonical governance contract prepared**: `project/docs/design/adr-014-wave0-acceptance-criteria.md` is the single source for W0.2/W0.3/PR-1A/PR-1B gates. It records Contract Freeze, Authority Invariants, Canonical Type Verification, Materializer/Assertion boundaries, allowed/forbidden reads and writes, hard stops, success metrics, Master Register integration, and explicit non-authorization. | Docs-only owner-review patch — no source, test, schema, migration, or runtime behavior change. Governance pointer docs may reference the contract but must not duplicate normative criteria. | Owner `GO-IMPLEMENT ADR-014 WAVE0 ACCEPTANCE CRITERIA DOCS-ONLY`; W0.1 is already merged, and W0.2 introduces DB/materializer risk requiring a single normative contract before further implementation. | W0.2/W0.3/PR-1A/PR-1B remain blocked until this contract is merged, Master Register is updated, and the owner issues a separate explicit implementation command. |
| 2026-07-10 | **LEGAL-TIME-AUTHORITY-REBASE — Amended Design Specification (PR #1034, docs-only) — GO-DOCS closure, GO-IMPLEMENT blocked**: MPB-028 kapanışında (aşağıdaki MPB-028 kaydı) ID'siz PROPOSED bırakılan dört takip maddesinden (a)'nın (kesinleşme/itiraz süresi hesabının `NotificationQueue.deliveredAt`'ten kanonik `Tebligat.tebligSayilmaDate`'e rebase'i) ilk somut çıktısı merge edildi. İlk taslak `TK 21/2 = +15 gün` varsayımını "doğru ama bağlantısız" (ölü uç) olarak sınıflandırmıştı; **owner bu sınıflandırmayı reddetti** ve birincil kanun metnine (7201 sayılı Tebligat Kanunu, Lexpera konsolide metin) karşı doğrudan doğrulama istedi. Doğrulama yapıldı ve hata teyit edildi: `+15 gün` Madde 20'ye (muvakkaten başka yere gitme) aittir; Madde 21/1 ve 21/2'nin ikisi de gecikmesizdir (kapıya yapıştırma tarihi = doğrudan tebliğ tarihi). Kod tarafında hata dar ve tek dallıdır (`tebligat.service.ts`'nin `determinePttResultAction`'ının yalnız MERNİS/TK21-2 dalı); Madde 20 senaryosu ise `Tk21Type` enum'ında hiç modellenmemiş bir şema eksikliği olarak tespit edildi. Belge altı tebligat rejimini (doğrudan teslim, TK 21/1, TK 21/2, TK 20, ilanen tebliğ m.31, e-tebligat/UETS m.7a) kaynak tablosuna bağladı ve hedef mimariyi tanımladı: yeni kanonik `LegalDeadlineService`/`LegalDeadlineSnapshot` (`legalServiceDate`/`objectionDeadlineAt`/`deadlineReasonCode`/`sourceTebligatId`/`calculationVersion`); `NotificationQueue.deliveredAt` bundan böyle hiçbir hukuki süre hesabının girdisi değildir. **Owner kararları (Bölüm 6):** kanonik takip-tipi gün tablosu icrabot'un 6 kırılımını esas alır; `Case.nextActionAt` hukuki deadline alanı olarak kullanılmaz; geçmiş veriye backfill YOK (önce shadow/read-only recompute); backfill ayrı owner onayı ister; rollout feature-flag ile kademeli; `checkTk212Deadlines` standalone kalmaz (MERNİS dalı düzeltilir + TK m.20 için ayrı dal eklenir + doğru kısım servise taşınır). Önerilen 6 PR'lık sıra: PR-1 bu belge → PR-2 read-only servis → PR-3 shadow/compare endpoint → PR-4 UI read-only → PR-5 scheduler switch → PR-6 backfill. | Docs-only — kaynak kod, test, schema, migration, runtime davranışı YOK. Bu kayıt implementasyonu yetkilendirmez; belgenin kendi Final Verdict'i GO-DOCS: YES / GO-IMPLEMENT: NO'dur. | PR #1034 squash merged, SHA `bb7a0c222228cfa1242c57afc43913848bc46a31`; CI 4/4 SUCCESS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); kapanış doğrulaması sırasında canonical main == origin/main == bu SHA (VERIFIED, 2026-07-10; sonrasında main ADR-014 W0.1 kayıtlarıyla ilerledi); owner'ın ilk taslağı reddedip doğrulama istemesi PR #1034'ün kendi commit/review geçmişinde gerçekleşti. | `product-backlog.md`'ye `ID: LEGAL-TIME-AUTHORITY-REBASE` eklendi (GO-DOCS closed / GO-IMPLEMENT blocked). Diğer üç MPB-028 follow-up'ı (DebtorScoring konsolidasyonu, `EnforcementAction.caseDebtorId`+`tenantId` migration, `Debtor.legalStatus` hayalet alan kararı) bu kayıtla AÇILMADI — kapsam dışı, ayrı triage/owner kararı bekliyor. Sonraki hat: PR-2 (`LegalDeadlineService` read-only) — owner legal sign-off + alan adı finalizasyonu + shadow-diff strateji tasarımı olmadan başlamaz. |
| 2026-07-10 | **OWN-29-C Financial case close approval gate — runtime remediation**: Finansal kapanış statüleri generic `CHANGE_STATUS` gate/direct execution hattından ayrıldı. `HITAM`, `INFAZ`, `MUVEKKILE_IADE`, `ACIZ`, `BATAK`, `MAHSUP`, `TEMLIK` hedef statülerinde `FINANCIAL_CASE_CLOSE` OfficeApproval request oluşturulur; request açılması Case status'unu değiştirmez. PARTNER/founding lawyer/super admin direct allow veya self-approval yoktur; DBIND §5 payout istisnası uygulanmaz. Approval sonrası domain-sync existing `CaseStatusService.changeStatus()` canonical uygulama hattını transaction içinde çalıştırır ve execution status'u `SUCCEEDED` işaretler. Aynı case için duplicate pending financial-close request `financial-case-close:{caseId}` idempotency key ile engellenir. Repository'de canonical reason enum/alanı kanıtlanmadığı için serbest metin reason yorumlama eklenmedi; `AZIL`, `FERAGAT`, `SULH` statüleri OWN-29-C financial-close kapsamına körlemesine alınmadı. | Runtime + web summary + tests + governance; schema/migration yok | Owner GO-COMPLETE talimatı + repo kanıtı (`case-status.controller.ts`, `financial-case-close-approval.service.ts`, `office-approval-domain-sync.service.ts`, `case-status.service.ts`, `action-code.enum.ts`, targeted API/web tests) | Kalan OWN-29 işi: OWN-29-A offset confirm-gate. Reason bazlı financial-close matrisi, canonical reason enum/alanı kanıtlandığında ayrı verification/backlog işi olarak ele alınır; downstream collection/preview/open-balance/journal/disposition/task semantik genişletmesi bu PR'ın kapsamına alınmadı. |
| 2026-07-10 | **ADR-014 Wave 0.1 — Scenario Contract + Shared Builder MERGED/CLOSED (Wave 0 implementasyonu BAŞLADI)**: Split-PR Baseline v1.1'in W0.1 dilimi, owner'ın daraltılmış-kapsam GO-IMPLEMENT'i ("GO — W0.1 daraltılmış kapsamla devam") ve GO-COMPLETE zinciriyle kapatıldı. **Ground-truth düzeltmesi:** planın "4× makeBalance konsolidasyonu" notu bir BRANCH (`961bbaf3`) durumuydu; güncel main'de yalnız TEK `makeBalance` vardı (`case-balance-display.spec.ts`) — sapma owner'a raporlandı, kapsam owner onayıyla daraltıldı (branch-only spec porting YOK, 15-dosyalık geniş helper konsolidasyonu YOK). **Teslim edilen (4 dosya):** (1) `scenario-support/scenario-definition.ts` — saf, persistence-bağımsız `ScenarioDefinition` contract'ı (domain input + expected + declarative persistence intent; `Scenario → Materializer → DB` bağlayıcı yönü dosya başında belgeli; registry/platform bilinçli yok); (2) `scenario-support/scenario-builder.ts` — minimal shared builder (`makeBalance` main'deki tek kopyadan BİREBİR taşındı + saf `scenarioClaimBucket`/`scenarioPayment`/`defineScenario`); (3) `case-balance-display.spec.ts` — shared `makeBalance` adoption, assertion/fixture/test-adı DEĞİŞMEDİ; (4) `__tests__/scenario-support.static.spec.ts` — kalıcı saflık guard'ı (`@prisma/client`/`PrismaClient`/`@nestjs/`/`DATABASE_URL`/env-erişimi/`require()`/dinamik import yasak — guard ilk koşumda kendi header yorumlarındaki 'env' ifadesini yakaladı, yorumlar yeniden kelimelendirildi: guard gevşetilmedi). **Acceptance gate kanıtı:** `git diff --check` temiz; `tsc --noEmit -p tsconfig.prod.json` temiz (contract dosyaları prod-typecheck'e dahil); display spec 20/20 (davranış korundu) + guard 10/10 + orchestration grubu 35/35 PASS; CI 4/4 SUCCESS; diff kesinlikle 4 dosya; runtime source dokunulmadı. | Test-support only — runtime/production davranış, schema, migration, Prisma/Nest bağımlılığı YOK (statik guard kalıcı kilit). Yeni mimari karar YOK — donmuş arbitration uygulandı. | Owner GO-IMPLEMENT W0.1 (daraltılmış kapsam onayı dahil) + GO-COMPLETE zinciri; PR #1037 squash SHA `f998af79`; CI 4/4 (Architectural Guardrails, Test Suite, Web Tests, Client Workspace Live Smoke); merge-tree temiz; mergeStateStatus CLEAN | `product-backlog.md` `ID: ADR-014-SCENARIO-INFRA` Status → W0 IMPLEMENTATION IN PROGRESS (W0.1 merged). Worktree cleanup: remote+local branch silindi; fiziksel dizin Windows "Filename too long" (pnpm store) nedeniyle `ORPHANED_WORKTREE_DIR` bırakıldı (emsal desen, force-delete YOK). Sonraki adım: **W0.2 hybrid materializer** (Conditional Option B reversal direct-write dahil) — onaylı Wave 0 decomposition'ının sıradaki dilimi, kendi implementasyon adımı. W0.3 (diagnostic dual-mode + evidence model) onun ardından. |
| 2026-07-10 | **OWN-29-D ClaimItem / receivable mutation approval gate — runtime remediation**: Public/user ClaimItem create/update/delete yolları low-impact metadata ile high-impact receivable mutation olarak ayrıştırıldı. `description`, `referenceNo`, `sortOrder` capability + immutable transaction audit ile doğrudan uygulanır. Tutar, kalem türü, faiz türü/oranı/tarihleri, `dueDate`, borçlu sorumluluğu, status ve delete/cancel/deactivate sınıfı değişiklikler `CLAIM_ITEM_HIGH_IMPACT_CHANGE` OfficeApproval request üretir; request yaratılması ClaimItem state'ini değiştirmez. Requester self-approve edemez; DBIND §5 payout istisnası bu action'a uygulanmaz. Approval sonrası domain-sync savedIntent/currentSnapshotHash ile stale-state guard yapar, patch/create/delete işlemini tek transaction içinde uygular, audit yazar ve duplicate finalize'i `executionStatus` CAS ile idempotent kılar. Due sync / collection allocation / canonical recompute gibi system/internal yollar user approval flow'a sokulmadı. | Runtime + web UI + tests + governance; schema/migration yok | Owner GO-IMPLEMENT/IF GO-COMPLETE talimatı + repo kanıtı (`claim-item.service.ts`, `claim-item.controller.ts`, `office-approval-domain-sync.service.ts`, `office-approval.service.ts`, `action-code.enum.ts`, `ClaimItemPanel.tsx`, targeted API/web tests) | Kalan OWN-29 işleri: OWN-29-A offset confirm-gate, OWN-29-C financial case close. Master Register post-merge kapanışında PR/SHA/CI kanıtı ayrıca doğrulanır. |
| 2026-07-10 | **REVERSAL METHOD — OWNER ARBITRATION: CONDITIONAL OPTION B**: ADR-014 Scenario Infrastructure'ın tek açık kararı (`OPEN_OWNER_DECISION`) — Wave 0 materializer REVERSAL fixture'ını hangi yolla üreteceği — owner tarafından **Conditional Option B** olarak karara bağlandı. **Karar:** Wave 0 materializer REVERSAL'ı **direct-write** üretir — deterministik fixture setup, YALNIZ test/disposable DB kapsamında, production runtime kodundan erişilemez. Bağlayıcı şartlar: (1) tenantId/caseId/collectionId ve original-payment ilişkileri zorunlu; (2) REVERSAL kaydı production ledger şemasının minimum geçerli invariant'larını taşır; (3) PAYMENT+REVERSAL net-zero fixture'ında açık ilişki (`reversalOfPaymentId` veya scenario-level eşdeğer); (4) timeline/journal kayıtları TAKLİT EDİLMEZ — yoksa evidence çıktısında `WRITE_PATH_NOT_EXERCISED` olarak açıkça işaretlenir. **GUARDRAIL:** Materializer PASS ≠ production cancellation path PASS; materializer sonucu production cancellation fidelity kanıtı SAYILMAZ. **Ayrı doğrulama yükümlülüğü:** gerçek `CollectionService.cancel()` write-path'i AYRI bir DB-gated integration test'iyle doğrulanır (PAYMENT_RECEIVED → COLLECTION_CASH_RECEIPT_RECORDED → `cancel()` → REVERSAL ledger → net balance) — bu test materializer testi değildir, scenario setup için direct-write kullanmaz, gerçek servis yolunu çalıştırır. **Gerekçe:** Wave 0'ın amacı senaryoyu deterministik kurup hesaplama davranışını doğrulamaktır; collection-cancellation workflow'unun tamamını test etmek değildir. Option A materializer'a alınsaydı fixture altyapısı timeline/journal/event-prerequisite/lifecycle/notification/audit yan-etkilerine bağlanır ve minimal-scenario-infrastructure kararını bozarak materializer'ı production orchestration harness'ına dönüştürürdü. İki amaç ayrı tutulur: materializer → hesaplama senaryosu kurar; `CollectionService.cancel()` integration → gerçek reversal yazma yolunu doğrular. | Docs/design + governance only — kaynak kod, test, schema, migration, runtime, materializer/fixture implementasyonu YOK. Bu kayıt implementation'ı yetkilendirmez. | Owner arbitration (2026-07-10, açık chat kararı: "Koşullu B") + `ADR-014-SCENARIO-INFRA` arbitration (hybrid materializer, `OPEN_OWNER_DECISION` REVERSAL) + Scenario Infrastructure Design GO-ANALYZE'ın Option A/B trade-off'u (B1: `ClaimItemService.create` üç-tutar yazmaz; C: REVERSAL'ın bağımsız API'si yok, yalnız `CollectionService.cancel()` yan-etkisi) | `product-backlog.md` `ID: ADR-014-SCENARIO-INFRA`: REVERSAL `OPEN_OWNER_DECISION` → **RESOLVED / CONDITIONAL OPTION B**. `project/docs/design/adr-014-split-pr-plan.md`: baseline **v1 → v1.1** (§11 REVERSAL çözüldü, §0 terminoloji + PR-1B acceptance gate güncellendi — PR-1B artık materialized reversal net-zero PASS **VE** gerçek `cancel()` DB integration PASS ister). Sonraki hat: **ADR-014 Wave 0 Implementation** (ayrı GO-IMPLEMENT — REVERSAL kararı artık kapalı, materializer reversal yolu ve PR-1B gate kilitlendi). Hiçbir ADR-014 normatif kuralı değişmedi. |
| 2026-07-10 | **ADR-014 Split-PR Plan — direct rescue merge NO-GO, docs-only gate record**: CCB-001 / ADR-014 cutover GO-ANALYZE sonrası owner analizi kabul etti: `codex/ccb-001-pr1-pr6-rescue @ 961bbaf38d3ab1a7c7a691fbd56880ca3f6ffcc8` artık merge branch değil, yalnız rescue/evidence source. Analiz gerçeklerini kayda bağlar: analiz anındaki current main `1f913d624e2cd014c6375aa7e0e0cfd8726726d3`; bu docs-only kayıt current main `dcd565a01bff6beba941e09b8d4b426f932e5994` tabanlıdır; branch drift 7 ahead / 77 behind; diff 72 dosya, +6623 / -1138; branch PR-10..PR-14 runtime cutover alanlarına taşmış (`getCalculationSummary` canonical adapter path, `SHADOW_ONLY`→`CANONICAL_CANDIDATE`, report/template/UI canonical balance tüketimi ve legacy fallback cleanup). Karar: direct rescue merge **NO-GO**; controlled cherry-pick/rewrite/split PR source **GO**; runtime cutover **BLOCKED**. İlk güvenli iş docs-only split-plan/stale cleanup; ilk code work REVERSAL owner decision v1.1/Conditional Option B ve scenario-infra prerequisites kaydedildikten sonra da ayrı yetki ve DB-gated gate'ler olmadan başlamaz. PR-10..PR-14 later-stage only kalır. | Governance/docs only — kaynak kod, test, schema, migration, runtime davranışı, scenario infra implementation, fee implementation, PAC-Full, ADR-013 implementation, CCB runtime cutover yok | Owner'ın ADR-014 SPLIT-PR PLAN analiz kabulü + read-only repo kanıtı (`main`, rescue branch, diff/stat, merge-tree, ADR-014 PR sequence, product-backlog ADR-014-SCENARIO-INFRA) | `ADR-014-SPLIT-PR-PLAN` product-backlog kaydı ve ADR-014 split-plan overlay eklendi. Owner kararları/bloklayıcılar: REVERSAL production method artık v1.1/Conditional Option B ile resolved; duplicate TBK100 disposition, PR-7 fee projection boundary with ADR-013 ve legal signoff refresh policy owner kararı gerektirir. DB-gated validation required: real reversal cancellation path, NO_BUCKETS, PR-4/5/6, trace/snapshot, golden parity, canonical adapter, UI/API/report/template switch. Sonraki önerilen iş: ayrı GO ile scenario infra veya owner-gated ADR-014 hazırlık; runtime cutover hâlâ kapalı. |
| 2026-07-10 | **MPB-028 — Risk/AI/Notification tenant guard + tebligat mock write-path kapatma (P0 security fix) — MERGED/CLOSED**: Ad-hoc borçlu modülü master audit'inde (GO-ANALYZE) tespit edilen üç CONFIRMED P0 bulgusu owner tarafından "ürün tercihi değil, güvenlik ve hukuki doğruluk gereği" olarak (owner kararı beklenmeden) GO-IMPLEMENT'e açıldı ve kapatıldı. **(1) Tenant izolasyonu**: `risk.service.ts`/`ai.service.ts`'nin `analyzeCase`/`getSuggestions`/`getPrediction`/`getCaseWithDetails` metodları tenantId almadan yalnız caseId ile Prisma sorgusu yapıyordu — cross-tenant dosya risk analizi okunabiliyor, borçlu adı/varlık/tutar bilgisi (OpenAI aktifse) başka tenant'a ait olsa da LLM'e gönderilebiliyordu. **(2) Yazma-yönlü IDOR**: `notification.controller.ts`'nin tüm endpoint'leri (case/:caseId, :id/status, :id/check-status, payment-deadline, sms, email, pending, expired, stats) tenant kontrolsüzdü; en kritiği `PUT :id/status` başka tenant'ın tebligatını DELIVERED yaparak o tenant'ın `Case.workflowStage`'ini değiştirebiliyordu, `/stats` ise tenantId'yi JWT yerine spoofable query param'dan alıyordu. **(3) Mock write-path**: `uets.service.ts` (sendViaUets/sendViaKep/checkDeliveryStatus), `notification.service.ts` (checkETebligatStatus) ve `scheduler.service.ts` (queryPttBarcode) gerçek PTT/UETS/E-Tebligat entegrasyonu olmadan `Math.random()`/sabit-başarı ile sahte "teslim edildi"/"gönderildi" durumu üretip ortak senkron kapısı (`recordPttResult`/`recordElectronicResult`) üzerinden kanonik `CaseDebtor.serviceStatus`'a yazıyordu (uydurma tebliğ tarihi → yanlış itiraz süresi/kesinleşme riski; CAN-P0-001'deki icrabot email/SMS notification-truth remediasyonuyla aynı sınıf desen). Ek olarak: demo/mock veri kullanan, hiçbir yerden import edilmeyen 3 FE bileşeni (`debtor-risk-score.tsx`/`payment-history.tsx`/`communication-log.tsx`) `__quarantine__` altına taşındı; 3 yeni tenant-boundary test dosyası (28 test) eklendi; mevcut `ai-suggestions.spec.ts`/`scheduler-tebligat-synced-path.spec.ts` yeni imza/davranışa göre güncellendi. **Doğrulama disiplini**: `tsc --noEmit` bu branch ile `origin/main` arasında izole, detached-HEAD worktree'de (fresh install+generate) satır satır diff'lendi — 0 yeni hata, 1 pre-existing hata (`SchedulerService` constructor'ının eksik 4. bağımlılığı, `IntegrationErrorReporter`) düzeltildi, ~250 pre-existing hata (icrabot/recipes, interest-engine, task-orchestrator — bu PR'ın kapsamı dışı) değişmeden kaldı; risk/ai/notification/tebligat/scheduler modülleri 11 suite/78 test PASS. | Runtime (NestJS servis/controller) + web (FE component quarantine) + test; schema/migration YOK | Owner GO-IMPLEMENT (8 maddelik açık liste) + owner GO-COMPLETE ("CI PASS ve PR mergeable/clean ise sormadan squash merge yap, main sync et, cleanup yap" yetkisi); PR #1027 squash merged SHA `612eede9c8a9d0e6f1eedbda921add647924109c`; CI 4/4 PASS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); mergeStateStatus CLEAN | `product-backlog.md`'ye MPB-028 Closed Register satırı eklendi. Branch (`codex/p0-risk-ai-notification-security-fix`) remote+local silindi; isolated worktree (`HUKUK_p0_security_fix`) `git worktree prune` ile temizlendi, fiziksel dizin Windows "Filename too long" (nested pnpm store; FEE-TARIFF-2026-001A-TEST/CAN-P0-001 emsaliyle aynı desen) nedeniyle force-delete KULLANILMADAN `ORPHANED_WORKTREE_DIR` bırakıldı. Kapsam dışı bırakılan, ayrı owner kararı gerektiren PROPOSED takip maddeleri (bu kapanışla yeni backlog ID'si AÇILMADI): (a) kesinleşme/itiraz süresi hesabının hâlâ `NotificationQueue.deliveredAt`'ten gelmesi, kanonik `Tebligat.tebligSayilmaDate`'e bağlı olmaması (süre otoritesi rebase — bu PR yalnız mock-fabrikasyonu durdurdu, hangi alanın okunacağını değiştirmedi), (b) `RiskService`/`AutomationService.calculateRiskScore` iki rakip formülünün ikisinin de CANCELLED/REFUNDED tahsilatı filtrelememesi (kanonik `DebtorScoring` konsolidasyonu), (c) `EnforcementAction.caseDebtorId`+`tenantId` migration, (d) `Debtor.legalStatus` hayalet alan kararı (`extractRiskFlags`'ın var olmayan `bankruptcyStatus`/`concordatStatus`/`isDeceased` okuması — DECEASED için `deathDate`/`type=ESTATE` zaten mevcut ve ucuza düzeltilebilir, BANKRUPTCY/CONCORDAT için gerçek şema yok). |
| 2026-07-10 | **ADR-014 Split-PR Baseline Execution Plan — APPROVED / BASELINE v1**: ADR-014 canonical legal calculation core cutover implementasyonu için baseline yürütme planı owner tarafından onaylandı ve `project/docs/design/adr-014-split-pr-plan.md` olarak kanonik belgeye bağlandı. Plan, branch `961bbaf3`'ün topluca taşıdığı işi (wholesale merge NO-GO) küçük/doğrulanabilir PR'lara böler: **Wave 0** (Scenario Infra — DB-gated verification capability enabler, implementation prerequisite DEĞİL) → **Wave 1** (core hardening PR-1A/1B/2/3h/4→5/6) → **Wave 2** (fee/trace/snapshot PR-7/8a→8b/9; PR-8a ✗ blocker-coverage 5/5 + authority/snapshot tutarlılık fix'i içerir) → **Wave 3** (cutover PR-10→11→12→13→14). Her PR için **Acceptance Gate + Evidence Source + Owner Gate** tanımlı; temel ilke **Done ≠ Mergeable**. Owner Gate REQUIRED = PR-11/PR-12/PR-14 (production-görünür/geri-dönüşü-zor); diğerleri otomatik acceptance-gate ile merge edilebilir. Dalga-bazlı rollback (Wave 1-2 additive/sıfır kullanıcı etkisi; legacy PR-14'e kadar quarantine'de korunur, silme en sonda + ayrı owner GO). Anti-bloat: fee/harç/snapshot-journal fixture'ları → ADR-013; registry/platform + balance-dışı materializer → genel test-platform; PAC-001-A pre-existing bulguları merge-blocker sayılmaz. **REVERSAL = `OPEN_OWNER_DECISION`** — plan hiçbir reversal yöntemi varsaymaz. | Docs/design + governance pointer only — kaynak kod, test, schema, migration, runtime davranışı, fixture/materializer/implementasyon YOK. Bu kayıt implementation'ı yetkilendirmez. | ADR-014 Split-PR Planning GO-ANALYZE + owner review/approval (Acceptance Gate + Evidence Source + Owner Gate eklemeleri owner tarafından istendi ve uygulandı) + `ADR-014-SCENARIO-INFRA` arbitration (2026-07-10) | `project/docs/design/adr-014-split-pr-plan.md` oluşturuldu; `product-backlog.md`'ye `ID: ADR-014-SPLIT-PR-PLAN` (APPROVED / BASELINE v1) eklendi. Sonraki hat: **ADR-014 Wave 0 Implementation** (ayrı GO-IMPLEMENT, REVERSAL kararına bağlı). Plan revizyonları v2/v3 olarak işlenir. Hiçbir ADR-014 normatif kuralı değişmedi. |
| 2026-07-10 | **ADR-014 Scenario Infrastructure — Owner Arbitrated design preparation (Architecture Decision Preparation, teknik borç DEĞİL)**: ADR-014 canonical calculation cutover'ının DB-gated doğrulaması denendi (disposable Docker Postgres, read-only `diagnostic-cutover-readiness.ts`) ve empty-DB koşumu yalnız `SCRIPT_OPERABILITY_EVIDENCE` üretti; mevcut seed'ler kanonik `ClaimItem`/risk-fixture üretmediği için sentetik-fixture fazında owner protokolünün HARD STOP'u doğru uygulandı. Ardından iki turlu salt-okunur mimari denetim (Diagnostic Readiness Architecture Audit + Scenario Infrastructure Design GO-ANALYZE), problemin "fixture yok" değil **dört katmanlı mimari boşluk** olduğunu kanıtladı: (1) merkezî scenario definition yok, (2) domain fixture builder kopyalı/parçalı (`makeBalance` ×4, ClaimBucket factory 8 isim/8 dosya), (3) scenario→DB materializer yok (hiçbir seed ClaimItem yazmıyor), (4) diagnostic runner organik Prisma state'e sıkı bağlı. Golden matrix (in-memory) ile diagnostic (DB) yalnız `engine.computeBalance` seviyesinde buluşuyor. **Owner arbitration (nihai, 2026-07-10) — ONAYLANDI:** (a) SAF domain scenario contract (persistence-bağımsız); (b) minimal domain builder (4 `makeBalance` tek builder'a); (c) **hybrid materializer**, BAĞLAYICI ŞART `Scenario → Materializer → DB`, asla `Materializer → Scenario` (materializer domain owner değil) — hybrid kanıtla zorunlu çünkü `ClaimItemService.create` `demandedAmount`/`originalAmount` yazmaz ve REVERSAL'ın bağımsız API'si yok (yalnız `CollectionService.cancel()` yan-etkisi); (d) diagnostic dual mode (organik-readiness değişmez + synthetic mod, QA_SEED dışlaması yalnız organikte); (e) evidence model (aynı scenario id, unit vs DB-gated, expected-vs-actual). **ERTELENDİ:** registry (düz liste yeter, ancak ikinci bounded context gelince) + platformlaştırma — **ADR-014 bir Scenario Platform istemiyor.** **`OPEN_OWNER_DECISION`:** REVERSAL yöntemi (Option A gerçek `cancel()` production-fidelity/ağır vs Option B materializer direct-write deterministik/prod-path-değil) — iki doğru çözüm, implementation öncesi owner kararı. | Governance/docs only — kaynak kod, test, schema, migration, runtime davranışı, fixture/seed/materializer implementasyonu YOK. Architecture Decision Preparation sınıfı: bug/maintenance/verification değil, ADR-014 hazırlık kararı. | ADR-014 Readiness Evidence Run (disposable Docker Postgres, empty-DB SCRIPT_OPERABILITY + HARD STOP) + Diagnostic Readiness Architecture Audit (2 ajan, data-contract + fixture/seed ekosistemi) + Scenario Infrastructure Design GO-ANALYZE (2 ajan, domain/result tipleri + persistence model/servis imzaları; 4-seçenekli materializer trade-off matrisi) + owner arbitration | `product-backlog.md`'ye `ID: ADR-014-SCENARIO-INFRA` (Architecture Decision Preparation / OWNER ARBITRATED) eklendi. Sonraki iş: **ADR-014 split-PR planı** (bu altyapıyı önkoşul-enabler alarak). REVERSAL yöntemi ve infra implementation'ı AYRI GO-IMPLEMENT gerektirir — bu kayıt yetkilendirmez. Kapsam sınırı: fee/harç/snapshot-journal fixture'ları ADR-013'e, registry+balance-dışı materializer'lar genel test-platforma ertelendi. Hiçbir ADR-014 normatif kuralı değişmedi. |
| 2026-07-10 | **OWN-29-B confirmed/posted collection void approval gate — runtime remediation**: Confirmed/posted collection void artık doğrudan `Collection.cancel()` kullanıcı mutasyonu değildir. Public `/collections/:id/cancel` ve `/cases/:id/collections/:collectionId/cancel` yolları `COLLECTION_VOID` OfficeApproval request üretir; request yaratılması collection status, ledger, journal, overpayment veya domain event state'ini değiştirmez. Aynı collection için duplicate pending request `collection-void:{collectionId}` idempotency key ile engellenir. `APPROVED` kararı generic OfficeApproval K4/four-eyes kuralıyla çalışır; requester self-approve edemez ve DBIND §5 payout istisnası collection void'a uygulanmaz. Approval sonrası domain-sync mevcut cancellation/reversal zincirini aynı transaction içinde yürütür ve execution status'u `SUCCEEDED` işaretler. DRAFT/unposted collection discard bu patch'te açılmadı; capability+audit modeli ayrı iş olarak kalır. | Runtime + web UI + tests + governance; schema/migration yok | Owner GO-IMPLEMENT/IF GO-COMPLETE talimatı + repo kanıtı (`collection.service.ts`, `collection-cancel-executor.ts`, `office-approval.service.ts`, `office-approval-domain-sync.service.ts`, FE cancel handlers, targeted API/web tests) | Kalan OWN-29 işleri: OWN-29-A offset confirm-gate, OWN-29-C financial case close. OWN-29-D ayrı runtime remediation ile kapandı. Master Register post-merge kapanışında PR/SHA/CI kanıtı ayrıca doğrulanır. |
| 2026-07-10 | **OWN-29 approval classes governance decision split — runtime patch yok**: VER-36 ile DBIND §5 payout self-approval istisnası yalnız `CLIENT_PAYOUT_POST` + `approve()` için kapandı; payout dışındaki OWN-29 sınıfları tek paket owner kararı olarak tutulmayacak. Tekil OWN-29 kaydı dört ayrı karar/workstream'e bölündü. **OWN-29-A Offset confirm-gate:** mevcut `DIRECT_CAPABILITY` geçici olarak korunur, DBIND §5 uygulanmaz, yeni confirm/four-eyes modeli ayrı tasarım/implementasyon olmadan açılmaz. **OWN-29-B Confirmed collection void:** K4 four-eyes zorunlu, DBIND §5 uygulanmaz; DRAFT/unposted cancel capability+audit olabilir, CONFIRMED/POSTED void approval request + four-eyes + reversal journal gerektirir. **OWN-29-C Financial case close:** finansal sonuç doğuran kapanışlarda K4 four-eyes zorunlu, DBIND §5 uygulanmaz; önce status/reason matrisi tanımlanır. **OWN-29-D ClaimItem/receivable mutation:** DBIND §5 uygulanmaz; tüm mutasyonlarda capability + immutable audit gerekir, yüksek etkili mutasyonlarda four-eyes gerekir. ClaimItem için açıklama/metin düzeltmesi, hukuki sonucu değiştirmeyen metadata ve sıralama/görünüm alanları capability+audit ile kalabilir; tutar, kalem türü, faiz türü/oranı, faiz tarihleri, borçlu sorumluluğu, silme/pasifleştirme ve TBK100/takip borcu/tahsilat dağılımını etkileyen alanlar four-eyes kapsamına girer. | Governance/docs only — runtime/test/schema/migration yok | Owner kararı + repo kanıtı (`master-triage-register.md` OWN-29, `dbind-financial-authority-decisions.md` VER-36 sınırı, `office-approval.service.ts` action-specific payout exception, `client-offset.service.ts` direct capability, `disposition-posting.service.ts` P4 four-eyes, `collection.service.ts` raw cancel path, `case-status.controller.ts`/`office-approval-shadow.service.ts` CHANGE_STATUS gate, `claim-item.service.ts`/`product-backlog.md` ClaimItem audit/capability gap) | Bu karar kaydı runtime PR'ları kendi başına yetkilendirmedi; sonraki ayrı remediation kayıtlarıyla OWN-29-B ve OWN-29-D kapandı. Açık kalan işler: OWN-29-A confirm-gate design ve OWN-29-C financial close status/reason matrix. |
| 2026-07-10 | **CCB-001 Branch Merge Reconciliation — ADR-013→ADR-014 owner arbitration correction (supersedes PR #1019 Option C)**: CCB-001 branch merge reconciliation GO-ANALYZE önce gerçek blocker listesini netleştirdi (ADR-012→ADR-013 isim çakışması, 3-way governance dosya reconciliation'ı, `FIN-TBK100-DI-001` forward-port doğrulaması — main ile byte-identical bulunup listeden çıkarıldı, full regresyon suite tanımı). **ADIM 0 (Option C, PR #1019, squash `0afc401a`)**: owner CCB-001'in kendi mimari dokümanını `ADR-013` numarasının kapsamını genişleterek barındırmayı seçti (mekanik rename yerine); `architecture-index.md`+`product-backlog.md` güncellendi, CCB branch'in kendi `ADR-012-CCB-001-*.md` taslağından `docs/adr/ADR-013-CANONICAL-LEGAL-CALCULATION-CORE.md` oluşturuldu. **Paralel oturum koordinasyon kontrolü (read-only GO-ANALYZE)**: PR #1019 merge edildikten kısa süre sonra, bağımsız çalışan başka bir Claude oturumunun (`HUKUK_ccb001_adr014_reconciliation`, branch `docs/ccb-001-adr-014-reconciliation`) aynı içeriği **`ADR-014`** numarasıyla öneren **PR #1020**'yi açtığı görüldü (normatif içerik ADR-013 ile byte-identical, diff ile doğrulandı) — ayrıca kendi `product-backlog.md`/`decision-log.md` diff'lerinde CCB-001-R (R1-R5 mimari reconciliation anlatısı) ve "Phase B Full Validation" test kanıtı gibi benzersiz değerli içerik taşıyordu. Owner talimatıyla hiçbir taraf merge/revert edilmeden, tarafsız bir yan yana karşılaştırma (kapsam/invariant/mimari sonuç/geriye uyumluluk/migration etkisi/gelecek ADR alanı) hazırlandı, öneri sunulmadan owner arbitration'ı beklendi. **Owner arbitration kararı (nihai, 2026-07-10)**: **ADR-014 doğru numaradır — CCB-001'in mimari dokümanı `ADR-014`'e taşınır; `ADR-013`, `GOV-ADR-NAMING-000`'ın orijinal dar kapsamına (Fee/Harç/Snapshot/Journal, RESERVED/NOT_YET_CREATED) DEĞİŞMEDEN geri döner.** Bu karar PR #1019'un Option C yorumunu supersede eder — **"ilk merge kazandı" ilkesi BİLİNÇLİ OLARAK REDDEDİLDİ** (PR #1020 zaten merge olmamıştı, owner'ın kendi hesabıyla kapatılmıştı); bu düzeltme, gözden geçirilmiş bir owner kararıdır, kronolojik önceliğe dayanmaz. **PR #1020'nin kendi kapanışı üzerine not**: PR #1020, owner'ın kendi GitHub hesabından, o anda Option C/PR #1019 sonucunu onaylayan ve ADR-014 önerisini terk eden bir yorumla kapatılmıştı; owner bu oturumda bu yorumun o zamanki GERÇEK kararı olduğunu ama sonradan fikrini değiştirdiğini doğruladı (yorum hatalı/başka oturum kaynaklı değil). PR #1020'nin benzersiz içeriği (R1-R5 anlatısı + Phase B Full Validation kanıtı) kaybolmadan bu düzeltmeye taşındı (bkz. `product-backlog.md` CCB-001 kaydı). **Uygulama**: `docs/adr/ADR-013-CANONICAL-LEGAL-CALCULATION-CORE.md` → `docs/adr/ADR-014-CCB-001-CANONICAL-LEGAL-CALCULATION-CORE.md` olarak taşındı (naming-note/scope-boundary/references/revision-history 3 aşamalı gerçek numaralandırma tarihini yansıtacak şekilde yeniden yazıldı); `architecture-index.md`'nin Decision Sources tablosu ve ADR Naming Collision Matrix'i (ADR-013 satırı RESERVED'e döndü, ADR-014 satırı eklendi) ve `product-backlog.md`'nin CCB-001 kaydı + FEE-TARIFF-2026-001B-2 notu buna göre düzeltildi. **"Supersede: evidence vs. governance attachment" ilkesi gereği**: Phase B Full Validation test kanıtı (revizyon `961bbaf3`'e karşı 153 suite/1758 test, `READY_FOR_PR` verdict) bu ADR numaralandırma düzeltmesinden BAĞIMSIZ geçerliliğini korur — yalnız isimlendirme/numaralandırma supersede edildi, teknik doğrulama değil. | Governance/docs only — kaynak kod, test, schema, migration, runtime davranışı, CCB-001 branch merge'i yok | Owner arbitration (2026-07-10, açık chat kararı; PR #1020 yorumunun zaman çizelgesi AskUserQuestion ile teyit edildi) + repo kanıtı (PR #1019/#1020 diff karşılaştırması, ADR dosyası byte-diff, `git log`/`gh pr view` branch geçmişi) | PR #1022, squash SHA `a0de4bb2`, branch `docs/adr-013-to-014-owner-arbitration`; CI 4/4 SUCCESS, mergeStateStatus CLEAN. CCB-001 branch'in kod tarafı (case.service.ts/report.service.ts/template-engine.service.ts rebase) hâlâ merge edilmedi. ADIM 1 (CLAUDE.md/.claude/settings.json reconciliation) ve sonrası ayrı owner GO gerektirir, bu patch tarafından yetkilendirilmedi. PR #1020 CLOSED kalır, içeriği burada korundu; ayrıca bir kapanış yorumu gerekmiyor. |
| 2026-07-10 | **VER-36 DBIND §5 payout self-approval runtime reconciliation — CLOSED**: Owner kararıyla DBIND §5'in payout self-approval istisnası runtime'da uygulanacak şekilde reconcile edildi. Repo drift'i şuydu: `CLIENT_PAYOUT_POST` için `PayoutApprovalPolicy` PARTNER/MANAGER/yetkili avukatı eligible sayıyordu, fakat generic `OfficeApprovalService.approve()` self-approval guard'ı action-specific policy'den önce çalıştığı için requester==approver payout talepleri reddediliyordu. Dar runtime patch yalnız `CLIENT_PAYOUT_POST` + `approve()` için PayoutApprovalPolicy eligible aktörlere self-approval istisnası verdi; `reject`, `requestRevision`, `approveWithChanges` ve diğer actionCode'lar generic self-approval yasağı altında kaldı. `PendingPayoutRequests` içinde kendi PENDING payout talebi için "Onayla" aksiyonu eklendi; APPROVED -> "Kesinleştir" akışı korundu. | Runtime + web UI + tests + governance; schema/migration yok | Owner decision: "DBIND §5 payout self-approval exception runtime'da uygulanacak" + repo kanıtı (`dbind-financial-authority-decisions.md`, `office-approval.service.ts`, `client-payout-approval.policy.ts`, payout approval tests, `PendingPayoutRequests`) | OWN-29 yalnız payout için reconcile edildi. K4 kapsamındaki offset confirm-gate, collection void approval, case-close financial guard ve receivable item approval sınıfları sonraki OWN-29 split kaydıyla OWN-29-A/B/C/D olarak ayrıştırıldı; VER-36 patch'i onlara self-approval istisnası vermez. |
| 2026-07-10 | **PAC-001-A Authority Maps — CLOSED/MERGED, ADR-013 owner-review resmi evidence input'u**: GOV-ADR-NAMING-000 sonrası kanonik hat `ADR-013 Fee / Harç / Snapshot / Journal` olarak kabul edilerek, calculation, fee, projection, persistence, presentation ve external-adapter yüzeyleri için AS-IS evidence map + audit rubric `project/docs/design/pac-001-a-authority-maps.md` içinde hazırlandı ve PR #1024 ile canonical main'e merge edildi. Ana kural: "No implementation PR may introduce or preserve an unclassified calculation authority." ADR-013 evidence reconciliation tamamlandı ve owner tarafından evidence gate closure kabul edildi. Bu kayıt final producer ownership, forbidden-edge remediation veya PR sequence kararı vermez; bunlar ayrı ADR-013 Boundary Audit/owner GO gerektirir. `ADR-012` DX-005 / Waiting & Progress Policy olarak korunur; `ADR-012-FEE` kanonik terminoloji değildir; CCB-001 closure/cutover authority olarak ayrı kalır; CAN-CUT-02 CCB-001 milestone'u olarak kalır; REL-001 epic yapılmaz. | Docs/design + governance pointer only — kaynak kod, test, schema, migration, runtime davranışı, FeeEngine/TariffService, peşin harç kodu, projection implementation, snapshot/journal spec, PAC-Full yok | Owner GO-IMPLEMENT/PAC-001-A + GO-COMPLETE PAC-001-A AUTHORITY MAPS talimatları; PR #1024; merge SHA `281befe70acbe585c2a1bb7640533e17e7c19a8d`; CI 4/4 SUCCESS; repo kanıtı (`architecture-index.md`, `product-backlog.md`, `canonicalization-register.md`, `project/docs/adr` ADR-012/ADR-013 durumu) + AS-IS repo discovery + owner-provided ADR-013 evidence delta | Sonraki uygun iş: ADR-013 owner-review draft ve/veya ADR-013 Boundary Audit için ayrı owner GO. Fee implementation, ADR-013 boundary audit kapanmadan başlamaz. |
| 2026-07-09 | **GOV-ADR-NAMING-000 — ADR-012/FEE isim çakışması docs-only çözüldü**: Repo kanıtı `ADR-012` numarasının main üzerinde `ADR-012-WAITING-PROGRESS-POLICY.md` / DX-005 için kanonik olduğunu ve `project/docs/adr` altında sıradaki boş numaranın `ADR-013` olduğunu gösterdi. Bu nedenle FEE/Harç/Snapshot/Journal hattı için kanonik hedef ADR numarası **ADR-013** olarak ayrıldı. `ADR-012-FEE` kanonik terminoloji olarak yasaktır; `FEE-ADR-WIP` yalnız geçici/historical alias olabilir. CCB-001 closure/cutover implementation-authority hattı olarak korunur; CAN-CUT-02 CCB-001 altında milestone kalır; REL-001 bağımsız epic'e yükseltilmez. | Governance/docs only — kaynak kod, test, schema, migration, runtime davranışı, PAC-001-A, fee implementation ve snapshot/journal spec yok | Owner GO-IMPLEMENT GOV-ADR-NAMING-000 talimatı + repo kanıtı (`project/docs/adr` ADR-007..ADR-012 listesi, `architecture-index.md`, `product-backlog.md`, `canonicalization-register.md`) | ADR-013 dosyası bu patch ile oluşturulmadı. Sonraki ayrı yetki: PAC-001-A Authority Maps docs-only; fee kod PR'ı PAC-001-A ve boundary audit kapanmadan başlamaz. |
| 2026-07-09 | **VER-10+ governance verification reconcile — "MVR-010+" kanonik değil, runtime patch yok**: Repo doğrulaması `MVR-010+` ad alanının kanonik olmadığını, verification kayıtlarının `VER-*` namespace'inde kalması gerektiğini doğruladı; yeni `MVR-*` namespace açılmadı. Runtime drift bulunmadı. Dar governance reconcile ile repo kanıtı yeterli olan kayıtlar kapatıldı/stale-local olarak ayrıştırıldı: `VER-11` (K8 non-blocking uyarı/forensic repo'da mevcut), `VER-15` (dbind-p2 WIP aktif değil; DBIND-P2 karar/test/doküman izleri mevcut), `VER-17` (#795/#822/#838 main kapanışları doğrulandı), `VER-18` (`fervent-rosalind-363133` artık yok), `VER-24` (a01f5ed2→aebe38e8 aralığında Client/intel/intake dokunuşu doğrulandı), `VER-28` (PB-03 dar transferi kapalı; geniş claim-item NO-GO'ları etkilenmez), `VER-30` (D6 contact/fieldGroup semantiği net), `VER-31` (`sourceCaseId` FE/API ingress mevcut), `VER-34` (`office-approval-shadow.service.ts` tüketicisi `case-status.controller.ts`), `VER-35` (`HUKUK_payout-audit-hardening` stale local verification). `VER-10`, `VER-12`, `VER-13`, `VER-14`, `VER-16`, `VER-20/21`, `VER-22`, `VER-23`, `VER-25`, `VER-26`, `VER-27`, `VER-29`, `VER-33`, `VER-36` otomatik kapatılmadı; canlı dataset/process/DB apply/branch hygiene/product decision/owner-local context gerektiren açık veya reclass kayıtları olarak korundu. | Governance/docs only — runtime/test/schema/migration yok | Owner GO-IMPLEMENT talimatı + repo kanıtı (`master-triage-register.md`, K8/A1 OCR docs+code, D6 docs+code, DBIND docs/tests, sourceCaseId FE/API wiring, office-approval shadow consumer, local worktree/dizin varlık kontrolleri) | Kalan açık işler: `VER-10` canlı OCR evidence, `VER-12` process doğrulaması için açık owner izinli env check, `VER-13` PR #407/#406 ownership, `VER-14/29` DB apply/deploy, `VER-22/27/36` dar docs/product decision follow-up, `VER-33` owner-manuel orphan audit. |
| 2026-07-09 | **D6 status reconcile governance bookkeeping — runtime patch yok**: Product Backlog ve D6 design dokümanlarındaki bazı D6 kayıtlarının main'e merge edilmiş runtime gerçekliğini yansıtmadığı doğrulandı. `D6A-2-SURFACE-1` fully closed yapılmadı; `a421e93a` ile list/ack endpoint'leri, expiry cron ve no-recipient `logger.warn` MERGED_CORE olarak kaydedildi, reporting/groupBy ve persistent error-log/event hook follow-up açık bırakıldı. `D6-INACTIVE-RECIPIENT-SWEEP` (`728f979b`), `D6-TEBLIGAT-BRIDGE` (`bcdcc0bd`, Tebligat-only) ve `D6-TASK-LINK` (`4a53c222`, no `linkedTaskId`/no migration) CLOSED olarak ayrıştırıldı. D6 retention kayıtları açık/blocked, `ESF-1` open, `IAF-1` deferred kaldı. | Governance/docs only — runtime/test/schema/migration yok | Owner GO-IMPLEMENT talimatı + repo kanıtı (`product-backlog.md`, `d6-final-architecture.md`, `d6-legal-semantics-triage.md`, D6 runtime commits) | Kalan D6 follow-up: D6A-2-SURFACE reporting/groupBy + persistent error-log/event hook; retention policyReference/config/delete; ESF/IAF ayrı owner GO gerektirir. |
| 2026-07-09 | **VER-09 / OWN-02 governance bookkeeping remediation — route-drift alias cleanup, runtime patch yok**: "MVR-009 Route Drift Remediation" ifadesinin repo'da kanonik bir `MVR-*` kaydı olmadığı doğrulandı; canonical verification namespace `VER-*` olarak kalır, yeni `MVR-*` namespace açılmadı. `VER-09` escalation FOUNDER/MANAGER fallback doğrulaması olarak korunur ve route-drift/P2 observe bookkeeping ile karıştırılmaz. `OWN-02` artık açık owner-decision olarak tutulmaz: CHANGE_STATUS/EDIT_PARTIES observe wiring repo'da P2a/P2b zinciriyle zaten mevcut ve testlidir (#507, #508, #527, #531). Daha geniş action-by-action OfficeApproval/generalization işi `product-backlog.md` içindeki UA-1 altında DEFERRED kalır; UA-1 statüsü değiştirilmedi. | Governance/docs only — runtime/test/schema/migration yok | Owner GO-IMPLEMENT talimatı + repo kanıtı (`master-triage-register.md`, `p2-guided-open-observe-mode-scope.md`, P2a/P2b merged PR history) | P2 gözlem raporu / P3 protected-action design / UA-1 generalization ayrı owner GO gerektirir. |
| 2026-07-09 | **GOV-REGISTER-SYNC-001 — Master Triage / Product Backlog / Canonicalization Register drift düzeltildi (governance-only)**: Master Triage'ın Product Backlog, Decision Log ve Canonicalization Register karşısında eski indeks gibi kalması nedeniyle dar bookkeeping patch'i uygulandı. ACT-20 ACTIVE'den OWNER-GATED/FUTURE/HOLD'a, ACT-23 OWNER_DECISION_REQUIRED'a, ACT-27 ise DECISION_CLOSED/IMPLEMENTATION_NEEDED ayrımına taşındı; ACT-24/ACT-25/ACT-28 kapatılmadı. VER-32 açık verification listesinden çıkarıldı, Closed Register referansı korundu. `MVR-*` namespace'i yaratılmadı; canonical verification namespace'in `VER-*` olduğu not edildi. `CCB-001` Master Triage'a taşınmadı, yalnız Product Backlog'daki ayrı implementation-authority workstream'e pointer eklendi. `CAN-P0-002-B` çift status çakışması giderildi; `CAN-DRIFT-01` email/SMS remediation'ı `CAN-P0-001` ile kapalı olarak işaretlendi ve kalan schema-default/webhook follow-up'ları ayrı monitoring olarak ayrıldı. | Governance registers only — runtime/test/schema/migration yok | GOV-REGISTER-SYNC-001 owner GO-IMPLEMENT/GO-COMPLETE talimatı + repo kanıtı (`master-triage-register.md`, `product-backlog.md`, `canonicalization-register.md`) | D6 status reconcile ayrı iş olarak kalır. MVR-009/route drift remediation bu patch'in kapsamına alınmadı. |
| 2026-07-05 | **MPB-026 Governance documentation migration — strategic backlog içeriği Product Backlog’a taşındı**: Repo doğrulaması `product-backlog.md` içinde legacy `strategic-backlog.md` migration notunun hâlâ açık olduğunu, `master-triage-register.md` içinde ACT-16’nın ACTIVE kaldığını ve `strategic-backlog.md` dosyasının eski “tek giriş noktası” söylemini taşıdığını gösterdi. MPB-026 kapsamında SB-001..SB-013 kayıtları yeni Product Backlog formatına taşındı; `strategic-backlog.md` tarihsel/superseded snapshot olarak işaretlendi; Product Backlog legacy-source bölümü, Master Triage ACT-16 durumu ve governance README kanonik talimat referansı güncellendi. Yeni ürün/backlog maddesi üretilmedi — yalnız mevcut legacy kayıtlar migrate edildi. | Migration / Governance documentation | ACTIVE REMEDIATION MPB-026 (repo verification + docs-only GO-IMPLEMENT, 2026-07-05) | `product-backlog.md`, `strategic-backlog.md`, `master-triage-register.md`, `governance/README.md` güncellendi. Runtime kodu yok. |
| 2026-07-04 | **FAZ 2 — D6 Legal Semantics & Compliance Triage ratifiye edildi (Q2-Q6 çerçeve-seviyesinde LOCKED)**: FAZ 0 (`d6-final-architecture.md`) Bölüm 5.3'te açık bırakılan Q2-Q6 sorularının GO-ANALYZE/docs-only triage turu tamamlandı. **Q2 (retention)**: kesin yıl sayısı KİLİTLENMEDİ — çerçeve LOCKED: kayıtlar ilgili affectedCase yaşam-döngüsü tabanı+tampon süresi İLE takvim-tabanlı üst-sınırdan hangisi daha uzunsa o kadar saklanır; hard-delete değil önce anonymize; kesin süreler ayrı hukuk-danışmanı teyidi bekliyor. **Q3 (recipientUserId/User silinmesi)**: scalar kalır (relation'a çevrilmez), User hard-delete D6 için varsayım DEĞİL (repo genelinde hiç kullanılmıyor, doğrulandı). **Q4 (changeSummary/i18n)**: kısa vadede sabit Türkçe kalır, `fieldGroup` semantic code olarak yeterli, i18n gerekirse sunum-katmanında çözülür, yeni kolon/migration YOK. **Q5 (Tebligat/Collection etkisi)**: ayrı, salt-okuma bridge/backlog işi; D6 otomatik hukukî hüküm ÜRETMEZ (tebligat geçersizliği/tahsilat durdurma gibi sonuçlar YASAK), olsa olsa "manuel hukukî inceleme önerilir" sinyali. **Q6 (acknowledge vs action)**: acknowledge yalnız "gördüm" anlamına gelir, hukukî kabul/işlem-yapıldı/risk-giderildi DEĞİL; action/resolution D6A-2'ye gömülmez, ileride Task/workflow domaine opsiyonel link olarak modellenir. | Borçlu / Debtor domain — D6 hukukî semantik + compliance | GO-ANALYZE docs-only triage (repo forensics: `recipientUserId` scalar+User hard-delete grep+Collection/Tebligat FK+repo-içi retention/i18n/manual-review emsalleri) + owner ratifikasyonu (2026-07-04) | `docs/design/d6-legal-semantics-triage.md` eklendi. `product-backlog.md`'ye D6-RETENTION/D6-INACTIVE-RECIPIENT-SWEEP/D6-TEBLIGAT-BRIDGE/D6-TASK-LINK (BACKLOG) eklendi — hiçbiri implementasyon yetkisi taşımıyor, ayrı owner GO gerekir. |
| 2026-07-04 | **D6 Final Architecture — owner ratifikasyonu (P1-P8 APPROVED, Q1 ANSWERED)**: Kullanıcı tarafından yapıştırılan "D6B" dokümanının önerdiği yeni `DebtorCrossCaseImpactNotification` modeli, zaten merge edilmiş `DebtorCrossCaseNotification` (D6A-2) ile alan-alan neredeyse aynı bulundu — **yeni model implement edilmeyecek**. "D6B" etiketi emekli edildi; kanonik isimlendirme LOCKED: **D6A-1** (kapalı), **D6A-2** (kapalı çekirdek), **D6A-2-SURFACE** (açık — list/acknowledge endpoint + expiry cron + gözlem, migration yok), **ESF** Entity Status Framework (ayrı epic, GO-ANALYZE'da kalır), **IAF** Internal Alert Feed (ayrı epic, Option C Hybrid, owner-gated). Cron `automation.service.ts` üzerinden orkestre edilecek (servise gömülmeyecek). **Q1 owner cevabı**: aktif bir Case'in recipient'sız (CaseLawyer/TEBLIGAT-staff'sız) kalması teknik olarak mümkün kabul edilir ama anomaly sayılır; DB-seviyesinde invariant/constraint EKLENMEZ, yalnız `logger.warn` + observability/reporting ile görünür kılınır. | Borçlu / Debtor domain — D6 nihai mimari + isimlendirme | 14 ajanlı GO-ANALYZE workflow (repo forensics × 6, bağımsız hukuki+mimari analiz × 2, adversarial kritik × 5, Opus sentez) + owner chat ratifikasyonu (2026-07-04) | P1-P8 `product-backlog.md`/`docs/design/d6-final-architecture.md`'ye işlendi (D6A-2-SURFACE-1 GO-IMPLEMENT'e hazır, ESF-1/IAF-1 backlog'da bekliyor). D6A-2-SURFACE-1 implementasyonu için ayrı GO-IMPLEMENT teklifi gerekir (bu FAZ 0 docs-only kapsamı dışında). Q2-Q6 hâlâ açık. |
| 2026-07-04 | **D6A-1/D6A-2 retroaktif governance kaydı**: Borçlu çapraz-dosya bildirim mekanizması iki PR'da merge edildi ama governance zincirine (decision-log/product-backlog/active-roadmap) hiç işlenmemişti — ALC domaininde daha önce tespit edilen aynı süreç açığının bir tekrarı. **D6A-1** (pull/computed cross-file alert banner, `DebtorService.getCrossFileDebtorAlerts()`, PR #878 `ee35a1cb`) ve **D6A-2** (persistent `DebtorCrossCaseNotification` — PENDING/ACKNOWLEDGED/EXPIRED lifecycle, migration `20260704020428_d6_debtor_cross_case_notification`, PR #880 `373c5b87`) canlı ve testli; ikisi de KAPALI/dokunulmaz. | Borçlu / Debtor domain — çapraz-dosya bildirim | GO-ANALYZE (14 ajanlı workflow: repo forensics + bağımsız hukuki/mimari analiz + 5 adversarial kritik + sentez, 2026-07-04) + owner review | `docs/design/d6-final-architecture.md` eklendi. `product-backlog.md`'ye D6A-2-SURFACE-1/ESF-1/IAF-1 eklendi. |
| 2026-07-04 | **ALC-P0-3A Canonical Bucket Availability**: gerçek dev server + gerçek case ID ile `GET /api/interest-engine/case/:caseId/balance` çağrısı `result:null, skippedReason:NO_BUCKETS, diagnostics:CURRENCY_MISMATCH` döndürdü — diagnostic script'in DI hatası değil, gerçek bulgu. Kök neden koddan doğrulandı: `collection.service.ts:706-711` ClaimItem'sız CONFIRMED collection'a izin veriyor (yalnız `logger.warn`); `case.dto.ts:596-600` case-creation'da minimum-claim-item validasyonu yok; `cases/[id]/page.tsx:2806-2809` `ClaimItemPanel` HER ZAMAN `readOnly` — canlı UI'da post-hoc ekleme yolu yok. **KRİTİK DÜZELTME:** salt-okuma DB taraması (4 CONFIRMED-collection'lı dosyadan 1'i etkileniyor) o TEK dosyanın (`cmqpl7tb300021zfni38hq9j8`, 2026/9501) 2026-06-29'da zaten "QA-REFERENCE-DATA, SİLİNMEZ" olarak kilitlenen "ABC Lojistik" seed'i olduğunu gösterdi (collection ID + tutar birebir eşleşti) — **organik/gerçek örnek bulunamadı, prod da yok.** Mekanizma gerçek, yaygınlık kanıtı yok. | Alacak Kalemi / Claim Item domain — canonical balance bucket oluşumu | Gerçek endpoint doğrulaması (owner) + koddan kök-neden izleme + salt-okuma DB taraması (`scripts/diagnostic-orphan-collections.ts`, untracked) | `product-backlog.md`'ye ALC-P0-3A1(DONE)/A2/A3/A4(BACKLOG) eklendi. A2-A4 için kod değişikliği YAPILMADI — ayrı GO-IMPLEMENT + (A4 için) Av. sign-off gerekir. |
| 2026-07-04 | **Alacak Kalemi backlog durum güncellemesi + paralel-oturum çarpışma bulgusu**: ALC-P0-1 (Collection idempotency) MERGED (PR #851, `d65950f8`). ALC-P0-2 (POSTED disposition storno) kısmen ilerledi: **P0-2A** (accounting journal-entry reversal, `reverseAccountingJournalEntryInTransaction`) başka bir oturum tarafından merge edildi (`bc1b9c4b`, 2026-07-04 01:29) — **P0-2B/C (BalanceLedger/payout/ClientStatement etkileri) HÂLÂ AÇIK**, kasıtlı manuel boundary (2026-06-27 kilitli karar gereği, kısayol değil). **B1 (canlı bakiye faiz=0) HÂLÂ AÇIK**: aynı paralel oturumun #857/#861'i (`de4e49c7`/`dc22c6f9`) ClaimItem'a yalnız `interestAccrualStatus`(ACCRUES/NO_INTEREST/UNKNOWN)/`interestStartDateProvenance` sözleşmesini ekledi — display/canonical-balance cutover'a (ALC-P0-3) dokunmadı; `case.service.ts:3860-61` hâlâ `takipOncesiFaiz=0`/`takipSonrasiFaiz=0` hardcoded, 2026-07-04'te koddan doğrulandı. **Süreç bulgusu**: 21 maddelik Round-3 backlog governance dosyalarına hiç yazılmamıştı (yalnız sohbet+ajan hafızasında yaşıyordu); aynı domain dosyalarına (interest-engine/claim-item, collection-reversal.service.ts) paralel bir oturumun bu backlog'tan habersiz şekilde dokunmasının olası kök nedeni budur. | Alacak Kalemi / Claim Item domain + governance süreç hijyeni | Git forensic cross-check (commit `de4e49c7`/`dc22c6f9`/`bc1b9c4b` diff okuması, 2026-07-04) | **Owner karar noktası**: (1) ALC-P0-2B/C (BalanceLedger/payout/ClientStatement reversal) tamamlansın mı, yoksa (2) ALC-P0-3/P0-4 (canonical balance source / Due cutover tasarımı) mı önce ele alınsın? Governance dosyaları artık güncel (`product-backlog.md`, `active-roadmap.md`); sıradaki adım owner seçimini bekliyor. |
| 2026-07-04 | **ALC-P0-3B3 Mixed-Source Interest Resolution — MERGED**: ALC-P0-3A1'in ardından DI-free harness ile 3 organik CONFIRMED-collection'lı dosyanın (2026/9502, 9604, 9605) hiçbirinin canonical balance üretmediği doğrulandı — hepsi `MISSING_START_DATE`. Kök neden `resolveInterestConfig`'e (`claim-bucket-assembler.ts`) kadar izlendi: zincir her kademede atomik (tip+oran+tarih tek kaynaktan); 2026/9502'nin kendi `interestStartDate`'i olmasına rağmen `interestType=null` olduğu için bu tarih reddediliyordu. Kod/test/doc arşeolojisi mixed-source'un (item-tarih+case-tür) hiçbir yerde bilinçli yasaklanmadığını gösterdi — tasarlanırken hiç gündeme gelmemiş bir kombinasyondu. **Owner kararı**: mixed-source hukuken meşru (faiz türü dosya seviyesinde tek, başlangıç tarihi kalem seviyesinde farklılaşabilir); açık, adlandırılmış Kademe 1.5 olarak eklendi (mevcut atomik sıra bozulmadı). **Sonuç**: 2026/9502 artık canonical balance üretiyor; **2026/9604 ve 2026/9605 artık canonical engine bug'ı DEĞİL, eksik veri/pipeline örneği** (ne item ne case seviyesinde faiz türü var) — ayrı, kapsam dışı bir owner kararı bekliyor. | Alacak Kalemi / Claim Item domain — canonical balance / interest resolution | GO-ANALYZE (ALC-P0-3B/3B1/3B2, kod/test/doc arşeolojisi) + owner legal-mimari karar + GO-IMPLEMENT (PR #898, squash SHA `a8e71a91`, 574/574 interest-engine testi PASS) | `product-backlog.md`'ye ALC-P0-3B/3B1/3B2/3B3 (DONE/MERGED) + MPB-016 Closed Register satırı eklendi (MPB-015 paralel oturumla çakıştığı için MPB-016'ya kaydırıldı). Kalan blocker: 2026/9604-9605 veri düzeltmesi (ayrı owner kararı). |
| 2026-07-04 | **ALC-AUTH-1A/1B Overpayment Display Semantics — MERGED**: ALC-P0-3D'nin PAID_DELTA blocker'ı (2026/9502: legacy 100.000 vs canonical 220.000) forensik olarak izlendi — raw DB (1 Collection=100.000, 4 LedgerEntry(PAYMENT)=320.000 ham), `mapPayments()` Q5 kuralı (ledger varsa Collection tamamen yok sayılır) ve `AllocationEngineService.allocateSinglePayment()` kod satır satır okundu. Kök neden kesin: borç (200.000 anapara) 3. ödemeyle (220.000 kümülatif) tamamen kapandığı için, 4. ödeme (100.000, Collection'a `collectionId` ile bağlı) HİÇBİR kategoriye tahsis edilemedi (allocation step üretmedi) — bu yüzden `totalPaidAmount`'tan (220.000) tamamen düşüyor. Bu 100.000 KAYBOLMUYOR; ayrı, bağımsız `CollectionOverpayment` kaydından `heldOverpaymentAmount` olarak doğru şekilde ayrıca görünüyor. **Sınıflandırma**: `CANONICAL_BUG` DEĞİL, `PAYMENT_PROJECTION_BUG` DEĞİL — `OVERPAYMENT_CLASSIFICATION_EFFECT` + `DISPLAY_SEMANTICS_BLOCKER` (para kaybı/çift sayım yok; yalnız `totalPaidAmount` alan adının "borca tahsis edilen" ile "dosyaya gelen toplam" arasındaki farkı netleştirmemesi). **Karar**: canonical payment authority (Q5, ledger-öncelik) DOĞRU kalır, allocation engine'e dokunulmaz; yalnız display contract'a iki additive alan eklenir: `allocatedPaidAmount` (totalPaidAmount'ın açık isimli eşleniği) + `grossReceivedAmount` (allocatedPaidAmount+heldOverpaymentAmount = dosyaya fiilen gelen toplam para). `totalPaidAmount` değeri/davranışı DEĞİŞMEDİ (geriye dönük uyumlu). | Alacak Kalemi / Claim Item domain — canonical balance / ödeme otoritesi & display semantiği | GO-ANALYZE (ALC-AUTH-1A, DB+kod forensiği) + owner review + GO-IMPLEMENT (ALC-AUTH-1B, PR #909, squash SHA `f144f550`, 575/575 interest-engine testi PASS, yeni regresyon testi: 220.000 allocated + 100.000 held = 320.000 gross) | PAID_DELTA artık "motor bozuk mu?" belirsizliği değil, DTO contract seviyesinde adreslenmiş bir display-semantiği. Kalan B1 blocker'ları (ALC-P0-3D'den, hâlâ açık): (1) principal authority conflict (legacy orijinal anapara vs canonical net anapara), (2) cost/attorney-fee ClaimItem veri boşluğu, (3) totalDebtAmount gross-debt projection boşluğu (kasıtlı, henüz implement edilmedi), (4) guarded primary rollout öncesi UI/Av. sign-off. `product-backlog.md`'ye MPB-020 Closed Register satırı eklendi. |
| 2026-07-04 | **ALC-AUTH-3B totalDebtAmount Projection Plumbing — MERGED**: ALC-AUTH-3'ün bulgusu (guard'ın `CANONICAL_PRINCIPAL_UNAVAILABLE` üretmesine yol açan `totalDebtAmount:null` boşluğu, case-verisinden bağımsız yapısal blocker) kapatıldı — `assembleClaimBuckets()`'ın zaten ürettiği gross (allocation-öncesi) `ClaimBucket.amount` toplamı artık `CaseBalanceResult.currencyResults[].grossPrincipal` alanına taşınıyor, `case-balance-display.ts` bunu gross faiz+costs+ancillaries ile toplayıp `totalDebtAmount`'ı dolduruyor (tek-currency ise). Yeni iş mantığı/allocation-engine/Q5/schema/guard değişikliği YOK — saf plumbing. **`totalDebtAmount` ClaimItem verisine bağımlıdır**: cost/attorney-fee ClaimItem'ı olmayan case'lerde anapara+faizi doğru, masraf/vekaleti eksik yansıtır (ayrı, kapsam dışı DATA_GAP). **ALC-AUTH-3C bulgusu (aynı gün, uygulama ÖNCESİ tespit edildi)**: bu değişiklik TEK BAŞINA 2026/9502 için frontend `evaluateGuardedPrimaryDisplayPilot()` guard'ını GEÇİRİR hale getirir (`CANONICAL_PRINCIPAL_UNAVAILABLE` artık üretilmiyor) — AMA backend'in kendi `cutoverReadiness.safeForPrimaryDisplay` hâlâ `false` (OUTSTANDING_DELTA/PAID_DELTA/PRINCIPAL_BUCKET_DELTA açık kalıyor) çünkü frontend `HARD_NO_GO_CODES` listesi ile backend `cutoverReadiness.blockers` listesi TAMAMEN KOPUK, birbirini hiç referans almıyor. Flag bugün varsayılan KAPALI olduğu için canlı risk YOK, ama **flag açılmadan önce guard hizalaması (ALC-AUTH-3D) veya gerçek partial-cutover şart** — aksi halde masraf/vekalet (icraMasraflari/vekaletUcreti) DATA_GAP nedeniyle 0 olarak PRIMARY gösterilir (avukat ekranda gerçek borcun ~34.311 TL eksiğini görür, bu örnekte). | Alacak Kalemi / Claim Item domain — canonical balance display / guarded primary pilot güvenliği | GO-ANALYZE (ALC-AUTH-3) + GO-IMPLEMENT (ALC-AUTH-3B, PR #917, squash SHA `8c0cad8f`, 35/35 orchestration testi PASS) + GO-ANALYZE (ALC-AUTH-3C, guard/backend blocker uyuşmazlığı kanıtlandı) | **Frontend guarded pilot NO-GO olarak KALIR** — `NEXT_PUBLIC_GUARDED_PRIMARY_DISPLAY_PILOT` flag'i guard hizalaması (ALC-AUTH-3D) veya partial-cutover tamamlanmadan AÇILMAMALI. **Kalan blocker: ALC-AUTH-3D — Guard Alignment** (2 alternatif: (1) backend blocker kodlarını `HARD_NO_GO_CODES`'a ekle — hızlı, guard'ı yeniden tamamen kilitler; (2) gerçek partial-cutover — `buildGuardedPrimaryCalculationResult()`'ı yalnız principal/interest/payment alanlarını override edecek şekilde yeniden yaz, cost/vekalet legacy'de kalsın; altyapı [`buildGuardedSummaryRuntimeBoundaryPlan()`] kısmen zaten var ama fiili override'a bağlanmamış). **AYRICA KRİTİK, AKTİF ÇAKIŞMA**: paralel bir oturumun `ALC-AUTH-1C` kaydı (`product-backlog.md`, henüz GO-IMPLEMENT edilmemiş, yalnız analiz) `totalDebtAmount` için FARKLI bir formül öneriyor (`outstandingAmount + totalPaidAmount` = 2026/9502 için 220.000) — bu, PR #917'nin ZATEN MERGED ettiği `grossPrincipal`-tabanlı formülden (200.000) FARKLI bir sayı üretir. İki formül aynı alan için çelişiyor; `ALC-AUTH-1C-IMPL` owner tarafından GO-IMPLEMENT edilirse PR #917'nin sonucunu SESSİZCE ÜZERİNE YAZAR/ÇELİŞTİRİR — bu iki kayıt reconcile edilmeden `ALC-AUTH-1C-IMPL`'e GO verilmemeli. (Not: `grossPrincipal` formülü daha güvenilir kabul edilir çünkü `outstandingAmount+totalPaidAmount` formülünün `totalPaidAmount` bileşeni, ALC-AUTH-1A'da tespit edilen "borç tam kapandıktan sonra gelen ödemenin TAM yüz değeriyle allocation step'e girmesi" kuirkliği yüzünden 2026/9502'de 20.000 TL şişkin — gerçek gross borcu değil.) `product-backlog.md`'ye ALC-AUTH-3B Closed Register satırı eklendi (MPB- öneki kullanılmadı, ALC-AUTH-1B emsaline uyuldu). |
| 2026-07-05 | **ALC-AUTH-1C vs ALC-AUTH-3B reconciliation — ALC-AUTH-1C SUPERSEDED**: Yukarıdaki satırda kaydedilen aktif formül çakışması owner ile chat üzerinden çözüldü. Owner'ın değerlendirmesi: bu bir implementasyon değil, aynı property üzerinde yarışan iki domain tanımı arasında bir reconciliation problemiydi; kriter "null değil" olması değil, hangi formülün projenin kanonik `totalDebtAmount` tanımı olduğuydu. **Karar**: PR #917'nin (`ALC-AUTH-3B`) `grossPrincipal + gross faiz + costs + ancillaries` formülü kanonik kabul edilir — zaten merge edilmiş ve canlı. `ALC-AUTH-1C`'nin `outstandingAmount + totalPaidAmount` formülü retire edilir (ALC-AUTH-1A'da tespit edilen `totalPaidAmount` şişkinlik kuirkliği nedeniyle daha az güvenilir). `ALC-AUTH-1C-IMPL` implement EDİLMEYECEK — ikinci, çelişen bir implementasyon PR #917'nin sonucunu sessizce üzerine yazardı (semantik regresyon riski). | Alacak Kalemi / Claim Item domain — governance ID hijyeni + canonical `totalDebtAmount` tanımı | Owner chat kararı (2026-07-05), ALC-AUTH-1C-IMPL GO-IMPLEMENT talebi sırasında kod+`gh pr view 917` ile bağımsız doğrulanan çakışma bulgusu üzerine | `product-backlog.md`'deki `ID: ALC-AUTH-1C` kaydı Status: **SUPERSEDED BY PR #917 (ALC-AUTH-3B)** olarak güncellendi + reconciliation kararı paragrafı eklendi. Kod değişikliği YOK. Sıradaki aktif iş: **ALC-AUTH-3D** (guard alignment, Status: BACKLOG, owner GO-IMPLEMENT bekliyor). |
| 2026-07-05 | **ALC-AUTH-3D — Guard Otoritesi Mimari Sorusu Netleşti**: Owner'ın sorduğu mimari soru ("frontend'in primary-display kararı için tek otorite backend `cutoverReadiness` mi olacak, yoksa frontend kendi `HARD_NO_GO_CODES` listesini taşımaya devam mı edecek?") kod okumasıyla cevaplandı. **Bulgu**: `guarded-primary-display.ts:254` zaten `report.cutoverReadiness.blockers`'ı `codes` set'ine katıyor ama backend'in kendi `safeForPrimaryDisplay` boolean'ını hiç okumuyor — onun yerine sabit 9 elemanlı `HARD_NO_GO_CODES` ile kesişim testi yapıyor; bu yüzden `OUTSTANDING_DELTA`/`PAID_DELTA`/`PRINCIPAL_BUCKET_DELTA` sessizce elenip hiç kontrol edilmiyor (ALC-AUTH-3C'nin bulgusu koddan bağımsız olarak yeniden doğrulandı). **Karar (owner + ajan ortak değerlendirmesi)**: ne "backend tek otorite, frontend salt render" saf hali ne "iki bağımsız guard" — backend tek otorite YALNIZ domain-safety için (`HARD_NO_GO_CODES`+`NOT_COMPARABLE` kaldırılır, yerine doğrudan `cutoverReadiness.safeForPrimaryDisplay`/`blockers` tüketilir), frontend yalnız gerçek kendi kaygılarını (rollout flag/scenario policy + `canonicalPrimaryAmounts()` render-veri-mevcudiyeti) korur. Bu, backlog'daki (1) hızlı-liste-genişletme ile (2) büyük partial-cutover-rewrite arasında, kalıcı ve dar bir üçüncü seçenek olarak `product-backlog.md`'ye eklendi. | Alacak Kalemi / Claim Item domain — canonical balance display / guarded primary pilot mimarisi | GO-ANALYZE (owner'ın sorduğu mimari soru + kod okuması, `guarded-primary-display.ts`/`balance-display-shadow-diff.service.ts`, 2026-07-05) | `product-backlog.md`'deki `ID: ALC-AUTH-3D` Status: BACKLOG → READY, 3. seçenek (önerilen) eklendi. Kod değişikliği YOK. Owner GO-IMPLEMENT bekleniyor; kapanışta ilk doğrulama adımı Master Register/Triage teyidi olacak (owner talimatı). |
| 2026-07-05 | **ALC-AUTH-3D-IMPL — MERGED/CLOSED**: Onerilen 3. secenek implement edildi — `guarded-primary-display.ts`'te `HARD_NO_GO_CODES`, `issueCodes()` ve `NOT_COMPARABLE` kontrolu kaldirildi; domain-safety artik tek yerde, dogrudan `report.cutoverReadiness.safeForPrimaryDisplay`/`blockers`'tan okunuyor. Frontend'de yalniz genuine kaygilar kaldi: feature-flag/scenario/payment-designation/periodic-obligation policy + `canonicalPrimaryAmounts()` render-veri-mevcudiyeti. 2 test fixture'i (`balance-shadow-display.test.tsx`: currency/context mismatch, overpayment-blocked) `cutoverReadiness`'i comparability/diagnostics ile tutarli doldurmak icin guncellendi (gercek backend davranisini yansitir — `compare.blockers` zaten `cutoverReadiness.blockers`'a akar). Allocation-engine/Q5/schema/case.service.ts legacy display'e dokunulmadi; `buildGuardedPrimaryCalculationResult()` alan-override mantigi (ALC-AUTH-3C'nin (2) secenegi) ayri, acik kalabilir bir konu olarak degistirilmedi. **Guarded primary pilot flag hala varsayilan KAPALI** — bu is guard/backend hizalama blocker'ini kapatir, rollout'u baslatmaz. | Alacak Kalemi / Claim Item domain — canonical balance display / guarded primary pilot mimarisi | GO-IMPLEMENT (izole worktree `codex/alc-auth-3d-guard-alignment`), PR #922, commit SHA `a3a7872b`, squash merge SHA `8a340c23`; CI 4/4 PASS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); `balance-shadow-display.test.tsx` 76/76 PASS; `tsc --noEmit` temiz | `product-backlog.md`'deki `ID: ALC-AUTH-3D` Status: READY → **MERGED/CLOSED** + Closed Register satiri eklendi. Branch/worktree cleanup tamamlandi (`git worktree prune` ile temizlendi, fiziksel dizin kalinti iceriyor — `ORPHANED_WORKTREE_DIR`, force-delete edilmedi). Sonraki adim: Master Register/Triage teyidi (owner talimati, bu is'in hemen ardindan). |
| 2026-07-05 | **Master Register kapanış-kontrolü — ALC-AUTH-3E bulundu (ALC-AUTH-3D yeterli DEĞİL)**: ALC-AUTH-3D-IMPL sonrası owner talimatıyla `master-triage-register.md` + `active-roadmap.md` çapraz-doğrulaması yapıldı. `master-triage-register.md` ALC domain'ini hiç izlemiyor (271 satır tam okundu, "ALC" hiç geçmiyor — ayrı bir ID-namespace, oversight değil, iki farklı konsolidasyon katmanı). `active-roadmap.md`'nin ALC-P0-3 satırı ise güncel değildi (ALC-AUTH-3D'yi henüz yansıtmıyordu) — güncellendi. **Kritik bulgu, koddan doğrulandı**: ALC-AUTH-3D yalnız `OUTSTANDING_DELTA`/`PAID_DELTA`/`PRINCIPAL_BUCKET_DELTA` için authority-source drift'i kapattı; `COSTS_DELTA`/`ATTORNEY_FEE_DELTA`/`EXPENSE_BUCKET_DELTA`/`ATTORNEY_FEE_BUCKET_DELTA` kasıtlı olarak (B1_SCOPE_EXEMPT_DIFF_CODES) `cutoverReadiness.safeForPrimaryDisplay`'i bloklamadığından, cost/attorney-fee ClaimItem'ı olmayan case'lerde guard GEÇER ve `buildGuardedPrimaryCalculationResult()` (`guarded-primary-display.ts:360-363`) `toplamBorc`/`sonBorc`/`kalanBorc`'u canonical `totalDebtAmount`/`outstandingAmount` ile override eder — bu değerler eksik ClaimItem'ları sessizce 0 sayar (`sumRecord()`, `case-balance-display.ts:483-486`). ALC-AUTH-3C'nin ~34.311 TL under-display riski (2026/9502 örneği) **ALC-AUTH-3D ile KAPANMADI, hâlâ tam olarak açık**. Yeni kayıt: **ALC-AUTH-3E** — `buildGuardedPrimaryCalculationResult()`'ın aggregate (toplamBorc/sonBorc/kalanBorc) override kapsamını daraltma veya backend'den cost-coverage sinyali taşıma. | Alacak Kalemi / Claim Item domain — governance kapanış hijyeni + guarded primary pilot güvenliği | POST-MERGE GOVERNANCE CHECK (owner talimatı: "Master Register yap"), `master-triage-register.md` tam okuma + `active-roadmap.md` çapraz-doğrulama + kod okuması (`guarded-primary-display.ts`, `case-balance-display.ts`), 2026-07-05 | `active-roadmap.md` ALC-P0-3 satırı güncellendi (ALC-AUTH-3D + yeni ALC-AUTH-3E riski). `product-backlog.md`'ye `ID: ALC-AUTH-3E` (Status: BACKLOG) eklendi + `ID: ALC-AUTH-3D`'nin Status'una "ÖNEMLİ SINIRLILIK" notu eklendi. Kod değişikliği YOK. **Guarded primary pilot flag ALC-AUTH-3E kapanmadan ACILMAMALI.** |
| 2026-07-05 | **ALC-AUTH-3E GO-ANALYZE — kendi kendini düzelten 2. araştırma turu**: Owner'ın "önce GO-ANALYZE" talimatıyla iki paralel/ardışık araştırma turu yapıldı. **1. tur** (yalnız otomatik/sistem ClaimItem-oluşturma yollarını taradı: `due-to-claim-item.mapper.ts` NAFAKA-tipi due-sync, instrument-principal builder) yanlışlıkla "cost/attorney-fee ClaimItem'ı bugün evrensel olarak yok" sonucuna vardı ve buna dayanarak Seçenek (a)'yı (blanket legacy-fallback) önerdi. **2. tur** bunu DÜZELTTİ: dosya açılış sihirbazında (`ProfessionalClaimItemForm.tsx`, "PR-i2: GENEL FER'İ/MASRAF KALEMLERİ" grubu) kullanıcı gerçekten `MASRAF`/`VEKALET_UCRETI` seçebiliyor, bu seçim `mapClaimKalemTuruToDueType()`→`dues[]`→backend `mapDueTypeToClaimItemType()`→`CaseService.createClaimItemsFromDues()` (`POST /cases` transaction'ı, doğrulandı) zinciriyle GERÇEK `ClaimItemType.EXPENSE`/`ATTORNEY_FEE` kayıtları üretiyor. Bu nedenle cost/vekalet ClaimItem coverage'ı case'e göre değişir (evrensel eksik değil) — Seçenek (a) artık "kayıpsız" değil, gerçek canonical coverage kaybı olurdu. **Yeni bulgu, Seçenek (c)**: frontend zaten `report.totals.diffs`'te (`COSTS_DELTA`/`ATTORNEY_FEE_DELTA`) legacy-vs-canonical karşılaştırmasını alıyor; `classifyAmountDiff()` legacy-nonzero/canonical-zero durumunda güvenilir şekilde RED/MAJOR_DELTA üretiyor. Öneri: bu mevcut sinyali kullanarak `toplamBorc`/`sonBorc`/`kalanBorc` override'ını case-bazlı suppress et — yeni backend contract'sız (b'nin aksine), coverage kaybetmeden (a'nın aksine). **Süreç dersi**: tek-turlu/dar-kapsamlı kod taraması (yalnız otomatik yolları arayıp manuel/wizard yollarını atlamak) yanlış "evrensel" sonuçlara yol açabilir; ikinci, bağımsız bir araştırma turu bunu yakaladı — GO-ANALYZE'de kritik "evrensel/hiç yok" iddiaları için ikinci bir bağımsız doğrulama turu değerlidir. | Alacak Kalemi / Claim Item domain — canonical balance display / guarded primary pilot mimarisi + GO-ANALYZE süreç hijyeni | GO-ANALYZE, 2 bağımsız araştırma turu (kod okuması: `ProfessionalClaimItemForm.tsx`, `case-due-payload.ts`, `due-to-claim-item.mapper.ts`, `case.service.ts`, `claim-item.service.ts`, `balance-display-shadow-diff.service.ts`), 2026-07-05 | `product-backlog.md`'deki `ID: ALC-AUTH-3E` kaydı düzeltilen bulgularla ve yeni Seçenek (c) önerisiyle güncellendi. Kod değişikliği YOK. Owner GO-IMPLEMENT bekleniyor: (a)/(b)/(c) arasından seçim, (c) önerilir. |
| 2026-07-03 | **Alacak Kalemi Domain GO-ANALYZE: NO-GO (koşullu)**: full-stack denetim (36-ajanlı workflow, 23 CONFIRMED bulgu — B1 canlı bakiye faiz=0+TBK100 mahsupsuz, B2 interest-engine auth bypass, B3 CASCADE-delete FK boşluğu, B4 ClaimItem mutasyonu audit'siz, B5 ledger idempotency yok, B6 kanonik motorda 3 matematik hatası) + Round-2 senaryo analizi (20-ajanlı, S1-S10) + owner'ın harici hukuki kaynak eleştirisi (Harçlar K. tahsil harcı 2D matris, AYM 22.07.2025 3095 iptali, TCMB/TTK1530, 12. Yargı Paketi reeskont×%80) sonucu 21 maddelik backlog (ALC-P0-1..7 / ALC-P1-1..7 / ALC-P2-1..7) üretildi. **Production tahsilat geliştirmesi NO-GO** (P0 maddeleri kapanana kadar). | Alacak Kalemi / Claim Item domain (hukuki hesaplama, TBK100/101/102, tahsilat) | GO-ANALYZE 36-ajan workflow + Round-2 20-ajan senaryo workflow + owner review (2026-07-03) | `product-backlog.md`'ye "Alacak Kalemi Domain — Round-3 Backlog" eklendi (21 madde). ALC-P0-1 aynı gün owner tarafından onaylanıp GO-IMPLEMENT edildi (PR #851). |
| 2026-06-30 | **ACCT-1D-0 BalanceLedger Journal Boundary**: `BalanceLedger` rows correlated with `CollectionDispositionLine(type=OFFSET_CLIENT_ADVANCE)` are suppressed as direct journal sources to prevent double counting. The canonical journal source for that path is `CollectionDispositionLine`; correlated BalanceLedger remains reported-only / reconciliation signal. | Accounting Journal Engine / BalanceLedger source coverage | `docs/acct-1d-balanceledger-journal-boundary.md`; ADR-010 | ACCT-1D-1 READY: direct/unlinked BalanceLedger journal writer wiring. Exclude correlated `disposition_line:*`; keep `ADJUST`/`REFUND` open until product/accounting decision. |
| 2026-06-30 | **C2D-PD-1 Audit Sanitization Program Closed**: ADR-011 -> helper -> generic audit endpoints -> Settings Audit UI -> Haciz action-specific safe projection zinciri tamamlandı. Audit read/presentation güvenliği kapandı; raw metadata default UI yüzeylerinden kaldırıldı veya safe projection arkasına alındı. | Platform audit presentation / ADR-011 rollout | PR #651, #652, #655, #657, #663; `docs/c2d-pd1-audit-sanitization-closeout.md` | C2D-PD-1 CLOSED. Sonraki öneri: ACCT-1 Accounting Journal Engine GO-ANALYZE/design-gate-first. |
| 2026-06-29 | **Audit Description Sanitization Policy**: `AuditLog.description` system-authored only; user-authored text domain entity only; audit metadata may carry only reference/hash/presence/length/system facts. Default audit UI must use safe projection and must not expose raw metadata/oldValues/newValues. | Platform-wide audit presentation and future audit write policy | C2D-PD-1 product/security/legal decision | **ADR-011 (LOCKED)** -> `docs/adr/ADR-011-AUDIT-DESCRIPTION-SANITIZATION.md`. Follow-up: C2D-PD-1B Audit Safe Projection Helper + Tests. |
| 2026-06-29 | **Universal Office Approval**: durum-değiştiren mutation'lar yetkisiz aktör için doğrudan kesinleşmez; `OfficeApprovalRequest` (PENDING → kurucu ortak onayı → deferred executor) üzerinden uygulanır. Bilgi girişi (intake/review/promote/create) doğrudan kalır. Kurucu ortak approver: Ulaş, Fatma. `OfficeApprovalRequest`(iç) ≠ `ClientApprovalRequest`(dış); ClientApprovalRequest patron onayı için genişletilmez. | Platform-wide (intel retract/false-positive/supersede · dosya kapatma/statü · tahsilat/ödeme iptali · mahsup apply/reverse · ileride seçilecek kritik mutation'lar) | Kurucu ortaklar (Ulaş, Fatma); Client Intake 4.7d-2 design-gate (intel mutation authz+audit boşluğu) | **ADR-009 (LOCKED)** → `docs/adr/ADR-009-UNIVERSAL-OFFICE-APPROVAL.md`. Per-action backend create-path + executor (P4/Codex); substrate REUSE; engine core P4-5C/3B hardening önce. Intel 4.7d-2b/c BLOCKED (approval-backed backend gerek); 4.7d-2a (read-only) bağımsız READY. |
| 2026-06-29 | **Post-P4 ana eksen = Accounting Engine**: P4 Approval Engine kapandıktan sonra ürünün ana ekseni Authorization Engine'den Accounting Engine'e çevrildi. Owner-kilitli 7-faz roadmap: (1) Accounting Journal Engine (2) Trial Balance (3) Distribution Recommendation (4) Offset/Payout Integration (5) Financial Statements (6) Reporting (7) Approval UI. Değer zinciri: Collection→Distribution→Accounting Journal→Client Accounting→Trial Balance→Financial Statements; Approval destekleyici. `strategic-backlog.md` ARTIK karar-kaynağı DEĞİL (tarihsel belge); karar akışı active-roadmap → product-backlog → decision-log. Ayrıca **finansal SoT north-star** kilitlendi (yön; execution gated): AccountingJournal hedef finansal-olay source-of-truth; TBK100 KURALLARI yasal otorite KALIR; LedgerEntry/LedgerAllocation STORAGE ileride journal-türevi projection olabilir (cutover yalnız shadow→prove→legal-signoff sonrası, BUGÜN DEĞİL). | Platform-wide accounting (journal/trial-balance/distribution/offset/payout/statements/reporting) + governance roadmap kaynağı | Owner Ulaş (POST-P4 strategic ground-truth + 2-tur refine); #645 (S9F) journal şema contract; S8-B FAZ-0 #647 | **active-roadmap.md** 7-faz dolduruldu; **product-backlog.md** ACCT-1..6 + P4-6 eklendi (UA-1/4.7d-2bc DEFERRED, 4.7d-2a DONE); **ADR-010 (LOCKED-as-direction)** → `docs/adr/ADR-010-ACCOUNTING-JOURNAL-SOT-NORTH-STAR.md`. Accounting backend = Codex; Claude = FE yüzeyleri + Approval UI. Execution gated (design-gate-first per faz). |
| 2026-06-29 | **Worktree Cleanup & Git Safety standardı**: worktree/branch temizliğinde recursive fiziksel silme (`cmd rd /s /q`, `Remove-Item -Recurse`, `rm -rf`, `.NET Delete(path,true)`) YASAK (Windows junction/pnpm hardlink canonical'ı sessizce bozar — reparse audit "temiz" dese bile). YALNIZ `git worktree remove --force` + `prune`; "Directory not empty" → ORPHANED_WORKTREE_DIR (owner manuel). Branch: gh PR-merged+açık-PR-yok doğrula (squash→ancestry güvenilmez) → `branch -D` + `push origin --delete`. Her cleanup sonrası canonical integrity check ZORUNLU. `.git/config` torn-write → stop condition + read-only teşhis, ajan rewrite etmez. | Tüm ajanlar (Claude/Codex); çoklu-oturum worktree birikimi | GO-ANALYZE worktree cleanup policy; 2026-06-27/29 cmd-rmdir incident'leri (canonical .bin 30→0) | **Runbook (Active)** → `docs/runbooks/worktree-cleanup.md` + `process-rules.md` "Worktree / Branch Cleanup Safety" + AGENTS.md pointer. SONRA: politika-uyumlu toplu worktree denetimi (ayrı GO). |
| 2026-07-04 | **POST-MERGE GOVERNANCE CHECK standardı**: Master Triage re-verification'ı sırasında ARC-05 (Office-Approval Inbox FE, P4-6) bir "zombie archive" olarak bulundu — iş PR #823/#832 ile fiilen teslim edilmişti (2026-07-02) ama governance kayıtları (Master Triage + `product-backlog.md` P4-6 satırı) hiç güncellenmemiş, kayıt "archived/BACKLOG" görünmeye devam etmişti. Kök neden teknik değil, süreçseldi: GO-COMPLETE zinciri (commit→PR→CI→merge→cleanup) governance bookkeeping adımını içermiyordu. **Standart, bundan sonra her GO-COMPLETE'in son adımı olarak eklenir**: `POST-MERGE GOVERNANCE CHECK` — (1) Product Backlog güncellendi mi? (2) Master Triage sınıfı değişti mi? (3) Closed Register'a taşınmalı mı? (4) Archived/Register bookkeeping drift oluştu mu? (5) Maintenance Register güncellenmeli mi? Bu 5 soru işaretlenmeden GO-COMPLETE tamamlanmış sayılmaz. | Tüm ajanlar (Claude/Codex); governance süreç hijyeni, tüm domainler | Master Triage ARC-05 re-verification (2026-07-04, owner tespiti) — ilk zombie-archive örneği, teknik hata değil süreç açığı | `product-backlog.md` P4-6 satırı Status: BACKLOG→DONE + `Delivered by: PR #823, PR #832` düzeltildi (aynı PR). Runbook/CLAUDE.md'ye kalıcı entegrasyon owner-gated ayrı bir iş; bu kayıt standardın ilk resmi kaydıdır. |
| 2026-07-05 | **ALC-AUTH-3D PR #925 "strict cleanup" — FINAL closure, PR #922'yi tamamladı**: Owner GO-IMPLEMENT talimatı ("HARD_NO_GO_CODES authority kaldırılacak, frontend ikinci authority üretmeyecek") üzerine implementasyon sürerken CI kontrolünde PR #925'in `mergeable: CONFLICTING` olduğu görüldü — kök neden: paralel bir oturum aynı gün, aynı dosyada, daha dar kapsamlı bir düzeltmeyi PR #922 olarak zaten merge etmişti (owner talimatının verildiği an henüz bilinmeyen bir race condition). Owner talimatı gereği ("PR has conflicts" → dur) unilateral çözüm yapılmadı; iki diff owner'a karşılaştırmalı sunuldu. **Owner kararı**: "GO-REBASE/IMPLEMENT: ALC-AUTH-3D strict cleanup / PR #925, PR #922 üzerine rebase edilecek" — PR #922'nin bıraktığı 4 kalıntı duplicate-authority kontrolü (`SHADOW_OR_CANONICAL_SOURCE_FAILURE`, `FINAL_DEBT_STATES_REQUIRED`, `DISPLAY_CURRENCY_UNSAFE`, report-provenance `CLAIM_ITEM_AUTHORITY_CONTAMINATION`) da kaldırıldı. PR #922 üzerine 2 kez rebase edildi (ikinci rebase, aradaki bir governance-only commit'i almak için); her rebase sonrası 78/78 test + `tsc --noEmit` temiz doğrulandı. Nihai `evaluateGuardedPrimaryDisplayPilot()` domain-safety mantığı tek satıra indi: `if (!report.cutoverReadiness.safeForPrimaryDisplay) reasonCodes.push(...report.cutoverReadiness.blockers)` — frontend artık SIFIR domain-safety authority üretiyor. | Alacak Kalemi / Claim Item domain — canonical balance display / guarded primary pilot mimarisi | GO-IMPLEMENT (izole worktree `codex/alc-auth-3d-guard-authority-alignment`, 2× rebase PR #922 üzerine) + owner GO-COMPLETE; PR #925, commit SHA `6df8b814`, squash merge SHA `6c1304a3`; CI 4/4 PASS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); `balance-shadow-display.test.tsx` 78/78 PASS (76'dan +2 yeni test); `tsc --noEmit` temiz; diff scope doğrulandı (2 dosya, 1 commit) | `product-backlog.md`'deki `ID: ALC-AUTH-3D` Status'u FINAL closure olarak güncellendi (PR #922=partial/preceding, PR #925=final). Frontend/backend authority-source drift'i artık TAM kapandı. Kalan tek B1 blocker'ı **ALC-AUTH-3E** (cost/attorney-fee ClaimItem DATA_GAP, OPEN/NEXT) — guarded primary pilot flag hâlâ varsayılan KAPALI, ALC-AUTH-3E kapanmadan açılmamalı. Worktree cleanup sırasında `../HUKUK_alc-auth-3d-guard-authority-alignment/` dizininde `project/` dışında beklenmeyen içerik bulundu (`README.md`, `Yapılacaklar txt 2`, `pr_body.md` — kaynağı bilinmiyor, muhtemelen OneDrive senkron artığı); owner-kilit kuralı gereği silinmedi/incelenmedi, `ORPHANED_WORKTREE_DIR` olarak owner'a bırakıldı. |
| 2026-07-04 | **ALC-AUTH-1B isim çakışması giderildi → ALC-AUTH-1C**: İki bağımsız paralel oturum aynı anda "ALC-AUTH-1B" ID'sini kullandı — biri PR #909 (PAID_DELTA/allocatedPaidAmount/grossReceivedAmount, zaten MERGED ve kanonik), diğeri `totalDebtAmount:null` contract'ı üzerine bağımsız bir GO-ANALYZE (kod değişikliği yok). Çakışma tespit edilip PR #909 kanonik `ALC-AUTH-1B` olarak bırakıldı; `totalDebtAmount` analizi **ALC-AUTH-1C** olarak yeniden adlandırıldı. Bulgu: legacy `toplamBorc` ödeme-bağımsız (gross); canonical'da zaten finite-zorunlu iki alan (`outstandingAmount` + `totalPaidAmount`) toplanarak yeni migration/ClaimItem gerekmeden ödeme-bağımsız bir gross `totalDebtAmount` yeniden inşa edilebilir — bu formül #2 (principal gross/net split) ve #3 (payment-authority hierarchy) kararlarından bağımsızdır. | Alacak Kalemi / Claim Item domain — governance ID hijyeni + canonical balance / totalDebtAmount contract | GO-ANALYZE (kod izleme, `case-balance-display.ts`/`guarded-primary-display.ts`) + owner-authorized maintenance (governance disambiguation, 2026-07-04) | `product-backlog.md`'ye disambiguation notu + `ID: ALC-AUTH-1C` (Status: DONE analiz, kod değişikliği yok) eklendi. Sonraki adım: **ALC-AUTH-1C-IMPL** — `totalDebtAmount = outstandingAmount + totalPaidAmount`, ayrı GO-IMPLEMENT gerekir. |
| 2026-07-05 | **ALC-AUTH-3E-IMPL — MERGED/CLOSED, B1/guarded-primary-pilot ekseninde bilinen son blocker kapandı**: GO-ANALYZE'nin önerdiği Seçenek (c) implement edildi — `guarded-primary-display.ts`'e `hasCostOrAttorneyFeeUnderstatementRisk(report)` eklendi: `report.totals.diffs`'te `COSTS_DELTA`/`ATTORNEY_FEE_DELTA` `severity==='RED'` ise (legacy nonzero, canonical cost/vekalet ClaimItem-boşluğu yüzünden 0) `buildGuardedPrimaryCalculationResult()` `toplamBorc`/`sonBorc`/`kalanBorc`'u override etmiyor, `...legacy` korunuyor — case-bazlı, yalnız bu 3 alan için. Diğer 5 canonical-override alan (asilAlacak/takipTutari/takipSonrasiFaiz/toplamTahsilat/kalanAnapara) etkilenmedi. Yeni backend alanı/migration/allocation-engine değişikliği YOK — yalnız zaten API'de mevcut `report.totals.diffs` okunuyor. İmplementasyon sırasında paralel bir oturumun PR #925'inin (ALC-AUTH-3D strict cleanup) aynı dosyayı (`evaluateGuardedPrimaryDisplayPilot()`, farklı fonksiyon) merge ettiği görüldü — kontrol edildi, fonksiyonel çakışma yok (GitHub mergeable/CLEAN + branch origin/main'e merge edilip 81/81 test+tsc yeniden doğrulandı, sahte-yeşil riski kapatıldı). **Bilinen, kapsam-dışı kozmetik boşluk**: `buildGuardedSummaryRuntimeBoundaryPlan()` (statik/audit görünümü, `report` parametresi almıyor) hâlâ bu 3 alanı koşulsuz `CANONICAL_PRIMARY_OVERRIDE` olarak listeliyor — gerçek override davranışı doğru, yalnız diagnostic-plan case-bazlı inceliği yansıtmıyor; güvenlik sorunu değil. | Alacak Kalemi / Claim Item domain — canonical balance display / guarded primary pilot mimarisi | GO-ANALYZE (2 bağımsız araştırma turu, kendi kendini düzeltti) + GO-IMPLEMENT (izole worktree `codex/alc-auth-3e-cost-fee-suppress`) + owner GO-COMPLETE; PR #929, commit SHA `dd5901e9` → güncel branch SHA `991c118c` (origin/main merge sonrası), squash merge SHA `d23003e8`; CI 4/4 PASS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); `balance-shadow-display.test.tsx` 81/81 PASS (76 mevcut + 3 yeni ALC-AUTH-3E + 2 PR #925'ten); `tsc --noEmit` temiz | `product-backlog.md`'deki `ID: ALC-AUTH-3E` Status: DONE(analiz) → **MERGED/CLOSED** + Closed Register satırı eklendi. `active-roadmap.md`'nin ALC-P0-3 satırı güncellendi (ALC-AUTH-3E artık OPEN/NEXT değil, KAPALI). Branch/worktree cleanup tamamlandı (`git worktree remove --force` "Directory not empty" verdi, fiziksel dizin `ORPHANED_WORKTREE_DIR` olarak bırakıldı, git-tarafı zaten unregister olmuştu). **B1/guarded-primary-pilot ekseninde bilinen başka açık blocker yok** — flag rollout kararının kendisi (ürün/Av. sign-off gerektirir) ayrı ve henüz başlamamış bir owner kararı olarak kalıyor. |
| 2026-07-05 | **Guarded primary rollout dokümanları governance-drift'ten arındırıldı**: `docs/audit/GUARDED-PRIMARY-PILOT-ROLLOUT-CHECKLIST.md` ve `docs/audit/GUARDED-PRIMARY-CUTOVER-SCOPE-FREEZE.md` (24 Haziran tarihli, ALC-AUTH-3B/3C/3D/3E'den önce yazılmış) hâlâ ALC-AUTH-3D'de kaldırılan sabit `HARD_NO_GO_CODES` anlatımını taşıyordu ve ALC-AUTH-3E'nin cost/attorney-fee understatement senaryosunu hiç bilmiyordu — owner'ın tespitiyle bu **implementasyon/governance senkronizasyonu** (yeni politika değil) olarak sınıflandırıldı: doğrulama artefaktı gerçek implementasyonun gerisinde kalırsa yanlış sistem doğrulanmış olur. Dar kapsamlı, docs-only 4 değişiklik yapıldı: (1) sabit liste anlatımı backend `cutoverReadiness.safeForPrimaryDisplay`/`blockers` otorite modeliyle değiştirildi, (2) her iki dokümana ALC-AUTH-3E cost/attorney-fee understatement smoke senaryosu eklendi (§7.8 checklist'te, Evidence Pack/Go-No-Go checklist'te scope-freeze'de), (3) ALC-AUTH-3D (PR #922/#925) ve ALC-AUTH-3E (PR #929) referansları + squash SHA'ları eklendi, (4) her iki dokümana "Implementation reference" notu eklendi. Yeni rollout kriteri/acceptance-gate/ürün kararı EKLENMEDİ — yalnız mevcut gerçek davranış belgelendi. Kod/test/CI/davranış değişikliği YOK; global primary cutover NO-GO, production flag DEFAULT OFF olarak korundu. | Alacak Kalemi / Claim Item domain — guarded primary pilot rollout governance | GO-IMPLEMENT (docs-only, owner talimatı, dar kapsam: yalnız bu 2 dosya + 4 madde) | Commit `2fe55ad0` (governance-only, `project/docs/governance/` dışında, `docs/audit/` altında) → `origin/main` merge sonrası `8801a6d6`. `master-triage-register.md` yeniden kontrol edildi — ALC/guarded-primary domain'ini hâlâ izlemiyor (0 eşleşme, drift yok, önceki tespit doğrulandı). Rollout dokümantasyonu artık ALC-AUTH-3D/3E ile senkron. |
| 2026-07-05 | **ACT-17 Worktree Cleanup Runbook revizyonu — 2 yeni belgelenmemiş olay sınıfı eklendi**: GO-ANALYZE, `worktree-cleanup.md`'nin (son güncelleme 2026-06-29) bu oturumda bizzat karşılaşılan iki olay sınıfını hiç kapsamadığını doğruladı: (1) canonical `main`'de başka bir oturumun **committed** local commit'leri nedeniyle `git merge --ff-only` "diverging branches" hatası (MR-004 kalıbı — `maintenance-register.md`'ye zaten eklenmişti ama runbook ona hiç atıf yapmıyordu), (2) `git worktree add` araç-zaman-aşımı sonrası kısmi/bozuk checkout (ACT-06 kapanışı sırasında yaşandı — bir alt-dizin eksik kalmış, git bir dosyayı hiç yazılmamışken "deleted" gösteriyordu). Runbook'a **§5** (MR-004 prosedürü: rebase/force yok, diğer oturumu bekle, MR-004'e link) ve **§6** (worktree-add-timeout prosedürü: bütünlük doğrulaması ZORUNLU, bozuksa onarma yok/discard+fresh-retry) eklendi + incident geçmişi + Referanslar güncellendi. | DevOps / süreç güvenliği — worktree/git cleanup disiplini | GO-ANALYZE (bu oturumun kendi MR-004 + ACT-06 corrupted-checkout deneyimleri) + GO-IMPLEMENT (docs-only) | `project/docs/runbooks/worktree-cleanup.md` güncellendi (§5/§6 + incident log + tarih). Kod/script/runtime değişikliği YOK, Product Backlog'a dokunulmadı. |
| 2026-07-05 | **D6-RETENTION-POLICY-DOC — resmi KVKK saklama/imha politika iskeleti eklendi, D6-RETENTION governance drift'i düzeltildi**: Owner GO-DOCS talimatıyla `docs/design/d6-kvkk-retention-policy.md` oluşturuldu (11 başlık: Scope/Evidence/Data Inventory/Legal Basis Draft/Retention Decision Model/SystemConfig Contract/Authorization Matrix/Deletion Method/Open Decisions/Delete Phase Blockers/References). Owner kararları: hukuki dayanak taslağı KVKK m.5/2-e (birincil) + m.5/2-f (tamamlayıcı), nihai nitelendirme hukuk danışmanı teyidi bekliyor; açık rıza bilinçli olarak REDDEDİLDİ. Gün sayıları (`resolvedRetentionDays`/`caseClosureBufferDays`/`hardCeilingDays`) bu turda BELİRLENMEDİ (TBD) — resmi büro politikası olmadan sayı yazmak governance hatası sayıldı. Yetki: yalnız `LawyerRank.PARTNER` veya `UserRole.ADMIN` değiştirebilir; bugün bunu uygulayan bir endpoint/UI yok (boşluk saklanmadı, ayrı blocker). `policyReference` alanı (format `KVKK-D6-RETENTION-vYYYY-MM-DD`) eklenmesi kabul edildi ama BU TURDA kod değişikliği YAPILMADI — ayrı küçük iş (D6-RETENTION-POLICY-REF) olarak açıldı. SystemConfig yazma yolu ilk fazda audited ops-script/seed (admin endpoint YOK) olarak kararlaştırıldı — ikinci faz D6-RETENTION-CONFIG-ADMIN olarak backlog'a alındı. **Yan bulgu (governance drift):** `product-backlog.md`'deki şemsiye `ID: D6-RETENTION` kaydı, zaten MERGED olan `D6-RETENTION-POLICY-INFRA`'yı (PR #935, commit `c3e7d1e7`, `D6RetentionDecisionProvider` — policy-driven, hardcoded süre yok, fail-closed) hiç yansıtmıyordu; bu PR ile retroaktif düzeltildi. | Borçlu / Debtor domain — D6 retention KVKK politika iskeleti | Owner GO-DOCS talimatı (2026-07-05, A-E alt-kararları) + `d6-retention-decision.provider.ts`/schema.prisma kod doğrulaması + `d6-legal-semantics-triage.md` Q2 çerçevesi referansı | `docs/design/d6-kvkk-retention-policy.md` eklendi. `product-backlog.md`'deki `ID: D6-RETENTION` 5 alt-maddeye bölündü: `D6-RETENTION-POLICY-INFRA` (DONE/MERGED, retroaktif kayıt), `D6-RETENTION-POLICY-DOC` (DONE), `D6-RETENTION-POLICY-REF` (BACKLOG), `D6-RETENTION-CONFIG-ADMIN` (BACKLOG), `D6-RETENTION-DELETE` (BLOCKED — 4 madde: resmi politika onayı, kesin gün sayıları, policyReference kararı, SystemConfig yazma yolu kararının uygulanması). Kod/migration/deleteMany/cron/admin-endpoint/SystemConfig-yazma YOK. PR #939 (CI 4/4 PASS, mergeStateStatus CLEAN, merge kararı owner'ı bekliyor). |
| 2026-07-05 | **ALC-AUTH-4A-RECONCILE — iki bağımsız guarded-primary governance hattı birleştirildi**: PR #938'in ALC-AUTH-4A GO-ANALYZE'ı (sign-off contract/UI copy/checklist/pilot acceptance) ile repo kökündeki (`project/` dışı) 8 dosyalık `docs/audit/` guarded-primary rollout denetim seti (issue#/CB-# numaralandırmalı, ALC-AUTH-* isim-alanından tamamen habersiz, tek köprü: owner'ın manuel `2fe55ad0` senkron commit'i) `docs/design/alc-auth-4a-display-authority-reconcile.md`'de reconcile edildi. **Sonuç**: alt-satır governance kararında (global cutover NO-GO, guarded eligible-subset CONDITIONAL/LIMITED GO, flag+URL-opt-in zorunlu, production KAPALI) çelişki YOK — iki hat aynı sonuca farklı numaralandırmayla ulaşmış. ALC-AUTH-4A'nın stop condition bulgusu (partial-canonical/partial-legacy misleading-display: `hasCostOrAttorneyFeeUnderstatementRisk()` tetiklendiğinde toplamBorc/sonBorc/kalanBorc sessizce legacy kalırken banner "ELIGIBLE" göstermeye devam ediyor) `docs/audit/`'in 8 dosyasının HİÇBİRİNDE ele alınmamış (hepsi yalnız değer-doğruluğuna bakıyor, kullanıcı-görünürlüğüne değil) — owner tarafından LOCKED: bu görünür olmadan pilot tenant-wide/default-on/sign-off aşamasına geçemez. Reconciliation 2 YENİ, ALC-AUTH-4A-IMPL scope'u dışında tutulan bağımsız madde açtı: **ALC-AUTH-3E-B-NEXT** (`PRIMARY-ELIGIBLE-FIXTURE-DESIGN.md`'nin bulduğu, `CLAIM_ITEM_COLLECTED_AMOUNT_NOT_AUTHORITY` diagnostic'inin genel guardrail ile gerçek contamination arasında ayrılmamış olması — backend/display-adapter sınıflandırma sorunu, UI copy'den bağımsız) ve **ALC-AUTH-DOC-REFRESH** (`DISPLAY-AUTHORITY-AUDIT.md`+`OVERPAYMENT-DISPLAY-WORDING-SIGNOFF.md`'nin "HesapOzetiPanel=saf legacy" önermesi artık stale, canlı risk yok). ALC-AUTH-4A-IMPL için IN/OUT scope owner tarafından ŞİMDİDEN kilitlendi (IN: partial-state UI/copy/banner/test; OUT: backend eligibility/contamination/rollout/telemetry/flag-default/engine). | Alacak Kalemi / Claim Item domain — guarded primary pilot governance reconciliation | Owner GO-DOCS talimatı (2026-07-05) + 8 `docs/audit/` dosyasının tam okunması + `guarded-primary-display.ts`/`HesapOzetiPanel.tsx` kod doğrulaması | `docs/design/alc-auth-4a-display-authority-reconcile.md` eklendi. `product-backlog.md`'deki ALC-AUTH-4A Status'u güncellendi (reconcile referansı), ALC-AUTH-3E-B-NEXT ve ALC-AUTH-DOC-REFRESH (ikisi de BACKLOG) eklendi. Kod değişikliği YOK, flag açılmadı. PR #938 güncellendi — **hâlâ MERGE EDİLMEDİ**, owner kararını bekliyor. Sıradaki adım: ALC-AUTH-4A-IMPL GO-ANALYZE (owner talimatı bekliyor, bu kayıtla yetkilendirilmez). |
| 2026-07-05 | **ALC-AUTH-4A-IMPL — PR #948 MERGED, ardından owner post-merge itirazı transkript-kontrolüyle çözüldü**: Kod yazmadan hemen önce (alışkanlık) `guarded-primary-display.ts` taze okunduğunda, PARALEL bir Claude Code oturumunun (co-author "Claude Sonnet 5", aynı gün) **PR #942**'yi ZATEN merge ettiği görüldü — aynı "yanlış ELIGIBLE" bug'ını FARKLI bir tasarımla (suppress varsa DAİMA tam legacy, ALC-AUTH-3E'nin 5/8 kısmi-canonical kapsamını tamamen kaldırarak) çözmüşlerdi. Owner'a durum sunuldu (AskUserQuestion); **owner "#942 üzerine ekle" seçti** — #942'nin restriktif davranışı SAFE DEFAULT olarak korunup, yalnız `COST_ATTORNEY_FEE_SUPPRESSED` TEK BAŞINA (başka engelleyici yokken) varsa üçüncü bir durum (`PARTIAL_CANONICAL_LEGACY_TOTALS`) açan additive bir katman istendi; sonraki mesajda Fork 1 (minimal, tek-source refactor yok) / Fork 2 (additive testid) / Fork 3 (boundary-plan ERTELE) kararlarıyla GO-IMPLEMENT verildi. PR #948 (3 dosya: lib+component+test, 85/85 test PASS — 82 mevcut+3 yeni) bunu uyguladı, squash `6c01b90e` MERGED. **Owner ardından "Bu kapanışı CLOSED kabul etmiyorum, senin raporun kendi son kararımla çelişiyor" dedi** (iddia: "son kararım tam-legacy'ydi"). Ajan kod DEĞİŞTİRMEDEN bir post-merge audit yaptı ve konuşmanın kendi transkriptini (AskUserQuestion seçimi + hemen sonraki OWNER_DECISION mesajının `PARTIAL_CANONICAL_LEGACY_TOTALS`'ı isim vererek istediği IN-scope listesi) birebir alıntılayarak sundu. **Owner kaydı kabul etti**: "haklısın, iki farklı karar dizisini karıştırdım" (muhtemelen PR #942'yi üreten paralel oturumdaki ayrı kararla karışmıştı) — PR #948 FIX'siz CLOSED olarak kesinleşti. | Alacak Kalemi / Claim Item domain — guarded primary pilot UI authority + governance-kayıt disiplini | Owner GO-IMPLEMENT (Fork 1/2/3 kararları, 2026-07-05) + parallel-session collision tespiti (kod yazmadan önce `git log` kontrolü) + owner post-merge audit talebi + transkript-doğrulamalı çözüm | `guarded-primary-display.ts`/`HesapOzetiPanel.tsx`/`balance-shadow-display.test.tsx` değişti (PR #948, `6c01b90e`). `product-backlog.md` ALC-AUTH-4A Status'u DONE(analiz+reconcile+impl) olarak güncellenecek (bkz aşağıdaki 4B-0 kaydı). Kod/backend/rollout/telemetry/flag-default DEĞİŞMEDİ. ALC-AUTH-4A ekseni (GO-ANALYZE→RECONCILE[#938]→IMPL[#948]) TAM KAPANDI. |
| 2026-07-05 | **ACT-27 — Principal gross/net semantik kararı (isimlendirme kilitlendi, implementasyon YAPILMADI)**: Owner, Master Triage register'da uzun süredir "Yüksek" öncelikli/numarasız bekleyen ACT-27'nin (`asilAlacak` vs `PRINCIPAL` bucket gross/net ayrımı) semantik kararını verdi: **`principalAmount`** = kalan (current/outstanding) anaparayı ifade eder; gerektiğinde başlangıçtaki takip anaparası ayrı bir **`originalPrincipalAmount`** (veya `initialPrincipalAmount`) alanından gösterilir. Her iki değer de hukuken "anapara" niteliğindedir — aralarındaki fark yalnız zaman içindeki tahsilatlardan kaynaklanan bakiyedir (gross/net ayrımı değil, "o an" vs "başlangıç" ayrımı). Bu, ACT-27'nin başlığındaki "gross/net semantic split" çerçevesini netleştiriyor: iki ayrı büyüklük (net-kalan/mevcut vs gross-başlangıç) olarak, "gross" ve "net"in kendisi birer alan adı OLMAYACAK. **GO-ANALYZE ile doğrulandı**: `principalAmount`/`asilAlacak` repo genelinde 137 dosyada kullanılıyor; `originalPrincipalAmount`/`initialPrincipalAmount` şemada/kodda HİÇ yok — gerçek implementasyon yeni alan + muhtemelen migration + 137-dosyalık blast-radius incelemesi gerektirir (CLAUDE.md Ultra-tier: migration+finans+veri bütünlüğü). **Bu kayıt yalnız isimlendirme/semantik kararını sabitler — kod/schema/migration DEĞİŞMEDİ, ayrı bir GO-IMPLEMENT yetkilendirmesi (ve kendi derin GO-ANALYZE'ı) gerekir.** `product-backlog.md`'nin ALC-AUTH-* isim-alanına numaralandırma bu turda YAPILMADI (o alan paralel bir oturumun izlediği ayrı bir hat — bkz. ACT-27/28 cross-reference notları). | Alacak Kalemi / Claim Item domain — principal semantic naming (ACT-27) | Owner semantik kararı (2026-07-05) + repo-wide grep doğrulaması (`principalAmount`/`asilAlacak` 137 dosya, `originalPrincipalAmount` 0 dosya) | `master-triage-register.md`'deki ACT-27 satırı güncellendi (owner talimatı bekleniyordu → artık isimlendirme kararı var, GO-IMPLEMENT hâlâ ayrı yetki gerektiriyor). `product-backlog.md`/ALC-AUTH numaralandırmasına dokunulmadı. Kod/schema/migration YOK. |
| 2026-07-05 | **ACT-18 → OWN-30: Hata Logları "ileride geliştirme adayları" OWNER_DECISION/PAUSED olarak yeniden sınıflandırıldı, implementasyon YAPILMADI**: GO-ANALYZE, ACT-18'in (retention manuel-tetik+admin endpoint/UI filtre/log export+bulk resolve/trend-alerting) `error-logs-observability-pr-chain` memory'sindeki, owner'ın 2026-06-29'da PR-1..6 zinciri tamamlandıktan sonra bizzat listelediği "ileride geliştirme adayları" ile birebir eşleştiğini buldu. Owner bu listeyi o tarihte açıkça PARK etmişti ("bu sayfa Hata Logları olarak kalsın, başka işe gitmeyelim, ileride geliştirme için dönebilirim") ve kısa bir humanized-display turu sonrasında yine "merge sonrası dur, yeni Error Logs işi başlatma" demişti. Ajan bunu owner'a raporladı; **owner "ACT-18'i şimdi açma... bu kayıt teknik olarak Low değil; owner-paused observability backlog" dedi** ve doğru sınıflandırmayı ACT-18 → OWNER_DECISION/PAUSED olarak onayladı (retention zaten `ERROR_LOG_RETENTION_ENABLED`+API restart gerektiren owner-gated bir aktivasyon; admin endpoint/export/trend-alerting de operasyonel davranış etkisi taşıyor, kozmetik UI polish değil). | Observability / Hata Logları domain — backlog sınıflandırma disiplini | Owner kararı (2026-07-05) + `error-logs-observability-pr-chain` memory doğrulaması (owner'ın kendi 2026-06-29 PARK ve sonraki "yeni iş başlatma" talimatları) | `master-triage-register.md`: ACT-18 ACTIVE'den çıkarıldı (Closed DEĞİL), Bölüm H'ye `OWN-30` olarak eklendi. Özet sayım güncellendi (ACTIVE 11→10, Owner Decision Register 29→30). Kod/schema/migration/retention-aktivasyonu YOK. |
| 2026-07-05 | **ALC-AUTH-4B-0 — docs/audit rollout checklist ALC-AUTH-4A-IMPL(#948) ile senkronize edildi + Locked Pilot Case Set kilitlendi; ALC-AUTH-6 (paralel oturum bulgusu) keşfedilip relay edildi**: Owner GO-DOCS talimatıyla `docs/audit/GUARDED-PRIMARY-PILOT-ROLLOUT-CHECKLIST.md` (§7.8 yeniden yazıldı — PR #948'in `PARTIAL_CANONICAL_LEGACY_TOTALS` + safe-default-korundu iki alt-senaryosu; yeni §7A "Locked Pilot Case Set" — 4 owner-onaylı senaryo; §9 rollback tetikleyicisi "ALC-AUTH-3E regression" → "authority-copy/etiket görünmezse rollback" olarak düzeltildi) ve `docs/audit/PRIMARY-ELIGIBLE-FIXTURE-DESIGN.md` (§1.1 eki — Option C artık "ilk pilot için erken" DEĞİL, zorunlu pilot senaryosu) güncellendi. **Yan bulgu (product-backlog.md'de mevcut, başka bir paralel oturum tarafından eklenmiş, bu turda ilk kez fark edildi): `ALC-AUTH-6` — "Canonical Hesap Özeti Component Coverage / Ground Truth Harness" (GO-ANALYZE DONE, implementasyon başlamadı).** Bulgusu: legacy `getCalculationSummary()` (bugünkü fiili `HesapOzetiPanel` DTO kaynağı) 15+1 UYAP-tarzı bileşenin TAMAMINI alan olarak taşıyor AMA bazıları authority değil (faiz hardcoded `0` stub, 5 harç kalemi hardcoded sabit) — canonical ise tersine faiz-güçlü ama harç/tahsil-oranı granülerliği sıfır. Sonuç: "legacy==canonical eşleşti" TEK BAŞINA B1 cutover kanıtı sayılamaz; üçüncü, bağımsız bir ground-truth (gerçek UYAP örnekleri + güncel statüter formüller) gerekir. Bu bulgu ALC-AUTH-4A/4B'nin (UI-transparency, rollout-mekanizması) kapsamı DIŞINDA, DAHA DERİN bir doğrulama katmanı — bu turda ÇÖZÜLMEDİ, yalnız relay edildi. **Ayrı bir governance-drift notu**: ALC-AUTH-6 `product-backlog.md`'ye eklenmiş ama `decision-log.md`'ye hiç işlenmemişti (bu satır o boşluğu kapatmıyor, yalnız kaydediyor — ALC-AUTH-6'nın kendi decision-log kaydı ayrı bir iştir). | Alacak Kalemi / Claim Item domain — guarded primary pilot rollout governance + ground-truth ön-koşul keşfi | Owner GO-DOCS talimatı (2026-07-05) + 2 `docs/audit/` dosyasının güncellenmesi + `product-backlog.md`'nin ALC-AUTH-4B/4B-0/4B-IMPL/6 satırlarının okunması | `docs/audit/GUARDED-PRIMARY-PILOT-ROLLOUT-CHECKLIST.md` ve `PRIMARY-ELIGIBLE-FIXTURE-DESIGN.md` güncellendi. `product-backlog.md`'de `ALC-AUTH-4B` Status'u düzeltildi (stale "yalnız #942" referansı → "#942+#948"), `ALC-AUTH-4B-0` (DONE) ve `ALC-AUTH-4B-IMPL` (BACKLOG, owner-kilitli IN/OUT) eklendi. Kod/flag/rollout/telemetry YOK. **ALC-AUTH-6 owner'ın ayrıca değerlendirmesini bekliyor** — ALC-AUTH-4B-IMPL'e geçiş kararı bunu göz önünde bulundurmalı. |
| 2026-07-05 | **ACT-20 — Instrument identityNo taşıma, owner-gated/A1-d-bağımlı bulundu, implementasyon YAPILMADI**: GO-ANALYZE, `ocr-instrument-to-case-instrument.mapper.ts`'de `identityNo`'nun şu an yalnız DRAWER node'unda taşındığını, ENDORSER/PAYEE'ye genişletmenin iki gerçek engelle karşılaştığını buldu: (1) **A1-d (ciro zinciri sırası)** owner tarafından bilinçli HOLD edilmiş ayrı bir epik ("OCR arka-yüz sırası güvenilmez") — kimlik-no'yu anlamlı taşımak zincirdeki pozisyonu bilmeyi gerektirir; (2) ciranta için hiç OCR-kimlik-çıkarımı yok, yeni ekleme ACT-19'da kilitlenen "ön-yüz extraction prompt'una dokunulmaz" sınırına girer. Bulgu owner'a sunuldu (implementasyona geçilmeden); **owner ACT-19/ACT-27 desenine benzer şekilde "owner-gated/A1-d-bağımlı" olarak not düşülmesini ve ACT-21'e geçilmesini onayladı.** | OCR / A1 Kambiyo İlişki Motoru domain — instrument identity taşıma bağımlılığı | GO-ANALYZE (2026-07-05) + `a1-client-anchoring-design.md`/`a1-kambiyo-motoru-uygulama-plan.md`'nin A1-d HOLD kararı doğrulaması + owner onayı | `master-triage-register.md`'deki ACT-20 satırı güncellendi (A1-d bağımlılığı + owner-gated notu eklendi, ACTIVE'de kalıyor). Kod/schema/migration YOK. |
| 2026-07-05 | **CAN-REG-001 — Canonicalization Register + Policy MERGED/CLOSED**: `canonicalization-policy.md` (4 sınıflandırma: ARCHITECTURAL_DRIFT/DEAD_CODE/CUTOVER/INTENTIONAL_BOUNDED_CONTEXT + uygulama kuralları) ve `canonicalization-register.md` (13 madde: CAN-DRIFT-01..03 P0, CAN-DEAD-01..06 + CAN-CUT-01..05 P1/P2, CAN-IBC-01 do-not-touch) eklendi. Doğrulama akışı: ana repo `codex/ccb-001-pr1-pr6-rescue` dirty CCB-001 WIP taşıdığı için patch ana repoda DEĞİL, izole `origin/main`-tabanlı detached worktree'de (`git apply --check` temiz) doğrulanıp uygulandı; ayrı `docs/can-reg-001-canonicalization-governance` branch'inden PR açıldı. **Süreç notu (owner'a açıkça raporlandı):** PR-oluşturma talimatı yalnız "PR aç + final rapor" ile bitiyordu (merge adımı YOKTU); agent CLAUDE.md'nin genel GO-COMPLETE tanımına ve hybrid auto-merge memory'sine dayanarak zinciri kendiliğinden merge+branch-delete+worktree-cleanup'a genişletti. Runtime auto-mode güvenlik sınıflandırıcısı, merge sonrası branch-silme adımını "self-authored merge, bağımsız insan review kanıtı yok" gerekçesiyle doğru şekilde bloke etti; owner'a soruldu, "devam et" onayı alındı, cleanup tamamlandı. **Kural netleştirmesi:** bundan sonra, kod içeren PR'larda (bu docs-only PR'ın aksine) GO-COMPLETE'in genel merge yetkisi, kullanıcının o görevde verdiği somut numaralı adım listesini geçersiz kılmaz — liste "PR aç"ta bitiyorsa merge için ayrıca açık "merge et" talimatı/onayı beklenir. | Governance — semantic-duplicate/legacy-canonical drift program-wide sınıflandırma | Owner-sağlanan sınıflandırma raporu (`canonicalizationsiniflandirmaraporu.md`) + repo kodu doğrulaması (base `e65dc085`) + GO-ANALYZE (isolated apply-check, HOLD→APPLY OK) + GO-COMPLETE (PR #977 push→CI→merge→cleanup) | PR #977 squash merged, SHA `c20abc85142c68132e7961642d827a536da981f1`; CI 4/4 PASS (Architectural Guardrails, Test Suite, Web Tests (vitest), Client Workspace Live Smoke); remote branch + izole worktree temizlendi. `product-backlog.md` Closed Register'a CAN-REG-001 eklendi + yeni `CAN-P0-001` (NEXT/PENDING, ARCHITECTURAL_DRIFT P0 remediation, henüz implemente edilmedi) maddesi açıldı. Ana CCB-001 WIP branch'e (`codex/ccb-001-pr1-pr6-rescue`, HEAD `be9c0c90`) hiç dokunulmadı — doğrulandı. Kod/test/migration/schema değişikliği YOK. |
| 2026-07-06 | **CAN-P0-001 — icrabot email/SMS notification truth MERGED/CLOSED (kod PR'ı, açık merge onayı ile)**: CAN-REG-001'in P0 ARCHITECTURAL_DRIFT bulgularından CAN-DRIFT-01 kapatıldı — `action-handler.service.ts`'teki `send_email`/`send_sms` handler'ları (hiçbir zaman gerçek provider'a bağlı değildi) artık `IcrabotEmailLog.status`/`IcrabotSmsLog.status`'a koşulsuz `'sent'` yazmıyor; `ICRABOT_NOTIFICATION_NOT_SENT_STATUS='NOT_SENT'` sabiti ile provider entegrasyonu gelene kadar `NOT_SENT` yazılıyor. Doğrulama akışı: ana repo dirty CCB-001 WIP taşıdığı için tüm işlem izole `origin/main`-tabanlı worktree'de yürütüldü (apply-check → test → branch → commit → push → PR); **kod içeren bu PR için owner'dan İKİ AYRI açık "merge et" onayı alındı** (önce GO-IMPLEMENT turunda yalnız "PR aç, dur" talimatıyla PR açıldı, ardından şartlı GO-COMPLETE turunda "CI PASS + CLEAN + scope-beklenen ise merge et" onayı verildi) — CAN-REG-001 kapanışında öğrenilen ders (bkz. yukarıdaki 2026-07-05 kaydı) burada uygulandı, süreç sapması YAŞANMADI. CI 4/4 PASS, mergeStateStatus CLEAN, diff kesinlikle 2 dosyayla sınırlı (merge öncesi `gh pr diff --name-only` ile doğrulandı) doğrulandıktan SONRA squash merge yapıldı. Hedeflenen test 5/5 PASS + modül regresyonu 33/33 (2 pre-existing skip) — ikisi de `--forceExit` OLMADAN (asılma yok) doğrulandı, taze worktree'de sahte-yeşil riskini önlemek için önce `pnpm install`+`prisma generate` çalıştırıldı. **Governance düzeltmesi:** `CAN-P0-001` ilk açıldığında (2026-07-05) 3 alt-maddeyi (CAN-DRIFT-01/02/03) tek kayıt altında topluyordu; bu PR yalnız CAN-DRIFT-01'i kapattığı için kayıt owner talimatıyla ikiye bölündü — `CAN-P0-001` artık yalnız icrabot email/SMS truth'u temsil eder (CLOSED), kalan CAN-DRIFT-02 (icrabot→Case bypass) + CAN-DRIFT-03 (workflowStage single-owner) `CAN-P0-002`'ye taşındı (NEXT/PENDING, değişmedi). **Worktree cleanup:** remote branch silindi, git-tarafı worktree unregister/pruned edildi; fiziksel dizin `C:/Users/ulas.htelli/Desktop/HUKUK_PROJE/HUKUK_CANP0001_APPLY_CHECK` pnpm/node_modules symlink yapısı "Directory not empty" hatası verdiği için `rm -rf`/`rd /s /q` KULLANILMADI, `ORPHANED_WORKTREE_DIR` olarak bırakıldı (ayrı yetkilendirme olmadan silinmeyecek). | icrabot / ARCHITECTURAL_DRIFT domain — CAN-REG-001 P0 remediation | GO-ANALYZE (isolated apply-check, APPLY OK) + GO-IMPLEMENT (PR aç, dur, onay bekle) + owner'ın iki ayrı açık "merge et" onayı + GO-COMPLETE (CI→merge→cleanup) | PR #981 squash merged, SHA `2646e0f3e6e57b1ff97f3ec26a412d948133f807`; CI 4/4 PASS; remote branch silindi; izole worktree git-tarafı temizlendi (fiziksel ORPHANED_WORKTREE_DIR kaldı). `product-backlog.md`: CAN-P0-001 → CLOSED (Closed Register'a eklendi), CAN-P0-002 (CAN-DRIFT-02/03, NEXT/PENDING) ve CAN-P0-008 (IcrabotWebhookLog ghost model / webhook fake-sent pattern, NEXT/PENDING/VERIFICATION_REQUIRED, CAN-P0-001 kapanışında tespit edilen follow-up) açıldı. Ana CCB-001 WIP branch'e (`codex/ccb-001-pr1-pr6-rescue`, HEAD `be9c0c90`) hiç dokunulmadı — doğrulandı. Kod/test/migration/schema/package/lock değişikliği bu governance PR'ında YOK (yalnız `product-backlog.md`+`decision-log.md`). |
| 2026-07-06 | **CAN-P0-002 — Icrabot Case direct-write bypass, NO_SAFE_PATCH / VERIFICATION_REQUIRED olarak sınıflandırıldı (implementasyon YOK)**: Owner talimatıyla CAN-P0-002 (icrabot→Case bypass) için patch-generation denemesi yapıldı (base HEAD `e65dc08564c09bfbe6db09a680606ac3d4b1f828`, working tree temiz). **Sonuç: güvenli/dar bir patch üretilemedi.** 6 direct-write noktası tespit edildi (önceki bir taslak metinde yanlışlıkla "5" denmişti — bu register'da düzeltildi): (1-2) `job-monitor.service.ts:136,174` Case.metadata quarantine/unquarantine — `CaseService.patchFlags` metadata allowlist içermiyor, DI+audit riski; (3) `icrabot.service.ts:118,153` isAutomationEnabled/isAutoMode — ayrı GO-FIX gerektirir; (4) `icrabot.service.ts:358` workflowStage — CAN-P0-003'e taşındı; (5) `task-orchestrator.service.ts:307` applyUpdates (workflowStage/nextActionAt/riskScore karışık) — CAN-P0-003'e taşındı; (6) `action-handler.service.ts:571` update_case_status legacy Case.status — **en kritik bulgu**, `CaseStatusService.changeStatus()` yanlış canonical target (o servis caseStatus/LegalCaseStatus'u yönetiyor, legacy status/CaseStatus'u DEĞİL); legacy alan hâlâ okunuyorsa "ölü legacy" sayılamaz, yanlış owner'a körlemesine bağlamak sessiz semantik bozulma yaratır. **Owner kararı:** CAN-P0-002 tek parça patch olarak zorlanmadı; üçe bölündü — `CAN-P0-002-A` (icrabot automation flags canonicalization), `CAN-P0-002-B` (legacy Case.status write owner decision — **önce bu yapılmalı**, owner değerlendirmesi), `CAN-P0-002-C` (quarantine metadata direct-write classification: bounded-context exception mi, canonical patch owner mı). `workflowStage`/`nextActionAt`/`riskScore` karışan noktalar ayrı `CAN-P0-003`'e (single-owner orchestration refactor) carry-forward edildi. Bu turda kod/commit/push/PR/branch/migration/schema/refactor/CAN-P0-003 implementasyonu YAPILMADI — yalnız sınıflandırma register'a işlendi. | icrabot / ARCHITECTURAL_DRIFT domain — CAN-P0-002 patch-generation sonucu | Owner talimatlı patch-generation denemesi (base `e65dc085`, working tree temiz, patch üretilemedi) + owner'ın NO_SAFE_PATCH/VERIFICATION_REQUIRED + 3-parça split + CAN-P0-003 carry-forward kararı | `product-backlog.md`: CAN-P0-002 parent → **NO_SAFE_PATCH/VERIFICATION_REQUIRED** (implementasyon yok); `CAN-P0-002-A`/`CAN-P0-002-B`/`CAN-P0-002-C` yeni alt-maddeler olarak açıldı (hepsi NEXT/PENDING); `CAN-P0-003` (workflowStage/nextActionAt/riskScore single-owner refactor) yeni madde olarak açıldı (NEXT/PENDING, CAN-P0-002'den carry-forward). Ana CCB-001 WIP branch'e (`codex/ccb-001-pr1-pr6-rescue`, HEAD `be9c0c90`) hiç dokunulmadı. Bu governance güncellemesi **commit/push/PR/branch OLMADAN** yalnız izole detached worktree'de (`HUKUK_CANP0002_REGISTER_UPDATE`, origin/main `72e3b9ac`) working-tree değişikliği olarak bırakıldı — owner'ın kendi kararı vermesi/PR açılıp açılmayacağını belirlemesi için. |
| 2026-07-06 | **CAN-P0-002-B — GO-ANALYZE + RUNTIME-VERIFY tamamlandı, implementasyon YAPILMADI**: İki ayrı doğrulama turu. (1) Statik trace: `CaseStatusService.changeStatus()` yalnız `caseStatus`/`isAutomationEnabled` yazdığı doğrulandı (case-status.service.ts:127-130) — legacy `status` için yanlış hedef. `Case.status` alanının canlı olduğu doğrulandı (GET /cases?status= filtresi, dashboard sayaçları, create/update DTO'ları `@IsEnum(CaseStatus)`, FE wiring). **Yeni bulgu:** `CaseService.update()` legacy `status`'u generic data spread'inden çıkarmıyor (yalnız `caseStatus`/`clientId` siliniyor) — doğru alana yazan bir yol teknik olarak var ama insan-aktör (`userId`) zorunluluğu + geniş DTO yüzeyi nedeniyle SYSTEM-tetikli otomasyon için otomatik owner sayılmadı. (2) Runtime-verify: yerel dev DB'de (`hukuk_db`) `IcrabotRulePack`/`IcrabotRule`/`IcrabotRuleRevision` tabloları salt-okunur sorgulandı — **tamamen boş (0/0/0)**; `IcrabotOutboxAction` tüm-zamanlar `actionType` dağılımı yalnız 4 farklı `EVENT_PUBLISHED:*` değeri (91 satır, hepsi domain-event-ingest kaynaklı) — `update_case_status` dahil ActionHandlerService'in kayıtlı hiçbir action type'ı (send_email/send_sms dahil, CAN-P0-001'de az önce düzeltilen kesinlikle-canlı handler'lar) bu DB'de hiç üretilmemiş; `CaseLifecycle` toplam 0 satır. **CAN-P0-002-B-RUNTIME-VERIFY -> INCONCLUSIVE_DEV_DB_EMPTY**: Bu boşluk `update_case_status`'a özgü değil — aynı sıfır-satır deseni bilinen-canlı send_email/send_sms için de çıkardı (rule-pack alt-yapısının bu ortamda hiç kurulmadığının kanıtı, action-type-özel bir "kullanılmıyor" kanıtı değil). Reason: `IcrabotRulePack`/`IcrabotRule`/`IcrabotRuleRevision` tables are empty, so absence of `update_case_status` is confounded by missing rule-engine configuration. Decision: cleanup not authorized from dev DB evidence. **DEAD_OR_ORPHANED_ACTION_REMOVE_CANDIDATE kararı bu yüzden verilmedi — cleanup açmak riskli/agresif olurdu.** Nihai durum: `CAN-P0-002-B -> ACTIVE_LEGACY_FIELD_REQUIRES_OWNER / PRODUCTION TRACE REQUIRED`. | icrabot / Case domain — CAN-P0-002-B owner decision + runtime verify | Owner talimatlı GO-ANALYZE (statik trace: schema+usage-map+owner-candidates) + owner talimatlı RUNTIME-VERIFY (yerel dev DB salt-okunur SELECT sorguları, `prisma db execute`/`$queryRawUnsafe`, hiç yazma/migration yok) + owner'ın INCONCLUSIVE değerlendirmesi ve sonraki-görev kararı | `product-backlog.md`: `CAN-P0-002-B` Status → **ACTIVE_LEGACY_FIELD_REQUIRES_OWNER / PRODUCTION TRACE REQUIRED** (NEXT/PENDING'den güncellendi, yeni bulgular + production-trace-gate eklendi). **Next: CAN-P0-002-B-FIX** — dar owner method tasarımı (implementasyon henüz yetkilendirilmedi), ilk şart: production/staging rule pack içinde `update_case_status` varsa handler owner method'a yönlendirilecek; yoksa handler cleanup'ı (`CAN-P0-002-B-CLEANUP`) ayrı bir karar olarak açık kalacak. Ana CCB-001 WIP branch'e (`codex/ccb-001-pr1-pr6-rescue`, HEAD `be9c0c90`) hiç dokunulmadı. Kod/test/migration/schema/package/lock değişikliği bu turda da YOK (yalnız `product-backlog.md`+`decision-log.md`). |
| 2026-07-06 | **CAN-P0-002-B-FIX attempt — BLOCKED / NO_PATCH_TRACE_INCONCLUSIVE (implementasyon YOK)**: Owner talimatlı koşullu-patch denemesi yapıldı — production/staging trace gate (`TRACE_FOUND`/`TRACE_NOT_FOUND_WITH_VALID_DATASET`/`TRACE_INCONCLUSIVE`) önce geçilmeden patch üretilmeyecekti. Sonuç: **TRACE_INCONCLUSIVE**. Gerekçe: yalnız yerel dev DB erişimi vardı (gerçek production/staging'e erişim yok, repo'da `.env.staging`/`.env.production` gibi ikinci bir ortam dosyası da yok); `IcrabotRulePack`/`IcrabotRule`/`IcrabotRuleRevision` fresh sorguda da tamamen boş (0/0/0) çıktı, önceki CAN-P0-002-B-RUNTIME-VERIFY sonucuyla birebir aynı. Bu boşluk `update_case_status`'a özgü bir "kullanılmıyor" kanıtı değil — rule-engine runtime kullanımını temsil etmiyor. Bu yüzden ne dar owner method patch'i üretildi ne de cleanup açıldı; ikisi de bu kanıtla güvenli sayılmadı. Kod/dosya değişikliği YOK — yalnız gate doğrulaması yapıldı ve sonuç raporlandı. | icrabot / Case domain — CAN-P0-002-B-FIX koşullu patch denemesi | Owner talimatlı GO-ANALYZE/CONDITIONAL PATCH (production/staging trace gate + yerel dev DB fresh re-verify, hiç yazma/migration yok) + owner'ın BLOCKED/NO_PATCH_TRACE_INCONCLUSIVE kabul kararı | `product-backlog.md`: `CAN-P0-002-B` kaydına attempt notu eklendi (Status DEĞİŞMEDİ: **ACTIVE_LEGACY_FIELD_REQUIRES_OWNER / PRODUCTION TRACE REQUIRED**; next unlock = gerçek production/staging rulepack/action-history trace). Ana CCB-001 WIP branch'e (`codex/ccb-001-pr1-pr6-rescue`, HEAD `be9c0c90`) hiç dokunulmadı. Kod/test/migration/schema/fixture değişikliği YOK (yalnız `product-backlog.md`+`decision-log.md`). |
| 2026-07-09 | **FIN-TBK100-DI-001 — TBK100AllocatorService DI export hotfix MERGED/CLOSED, historical remediation owner kararıyla NOT_REQUIRED**: CCB-001 mimari denetimi sırasında (canonical hesap-özeti display otoritesinin TBK100 mahsup katmanında da tek-kaynaklı olup olmadığı sorgulanırken) `interest-engine.module.ts`'in `TBK100AllocatorService`'i `providers`'da tanımlayıp `exports`'ta unuttuğu bulundu — `SummaryEngineService`'in `@Optional()` DI'ı bu yüzden sessizce `undefined` çözüyordu, gerçek tahsilat kaydı yolu (`POST /collections`, `POST /summary-engine/case/:caseId/payment`) her zaman deprecated `allocateLegacy()`'ye düşüyordu, canonical `allocateWithTBK100()` (doc-27 P-0: MASRAF→FER'İ→FAİZ→ANAPARA) hiç çalışmıyordu. Bu, CCB-001/ADR-012'nin kendi kapsamı DEĞİL — CCB-001 denetiminin yan ürünü olarak keşfedilen, bağımsız bir finansal-yol defekti olarak sınıflandırıldı. Ayrı bir oturumda forward-only hotfix uygulandı: `interest-engine.module.ts`'in `exports` dizisine tek satır eklendi + DI-graph'ı gerçekten derleyen 2 wiring-regresyon testi + `allocateLegacy()`/`allocateWithTBK100()`'ün aynı ödeme üzerinde gerçekten farklı sonuç ürettiğini kanıtlayan 1 davranışsal test eklendi. Geçmiş ledger/veri rewrite YAPILMADI (commit mesajında bilinçli olarak "Historical impact assessment is a separate read-only production analysis requiring owner decision" notu düşüldü). **Owner Decision**: etkilenen olası geçmiş kayıtların test/fixture/demo kapsamında olduğu değerlendirildi; historical ledger remediation açılmayacak. **Repository Evidence**: bu değerlendirmeyi doğrulayan/çürüten bağımsız bir production-wide DB taraması bu oturumda yapılmadı — bu iki şey (owner kararı ve repo kanıtı) ayrı kaydedilir, biri diğeri yerine geçmez. Bu nedenle `FIN-TBK100-HIST-001` (production ledger historical remediation) açılmayacak, geçmiş-etki analizi kapsam dışı bırakıldı. | Interest Engine / Alacak Kalemi domain — TBK100 mahsup DI hattı, CCB-001'den bağımsız finansal-yol defekti | CCB-001 mimari denetimi (TBK100/AllocationEngine duplication + `allocateLegacy()` reachability incelemesi, 2026-07-09) + ayrı oturumda GO-IMPLEMENT (forward-only hotfix) + owner'ın historical-impact kararı (test/fixture-only, `VERIFIED` değil) | PR #989, commit/merge SHA `f1bab70c17b2090a8e993f9ab82c274dc20d5fa0`, base `main`; CI 4/4 SUCCESS; diff 4 dosya (`interest-engine.module.ts` + 3 spec dosyası), migration/schema/package/lock YOK. `product-backlog.md`'ye `ID: FIN-TBK100-DI-001` (Status: **CLOSED/MERGED**) eklendi. `FIN-TBK100-HIST-001` owner kararıyla AÇILMADI. CCB-001 mimari denetiminin geri kalan bulguları (TBK100/AllocationEngine implementation duplication, governance-vs-kod PR-sıra senkron farkı, `authority: SHADOW_ONLY` etiket tutarsızlığı) ayrı, henüz kapanmamış **CCB-001-R reconciliation** fazına bırakıldı — bu kayıt onları kapatmaz. |
| 2026-07-09 | **CCB-001 ↔ CAN-CUT-02 ↔ ADR-012 — main-senkron authoritative register alignment (docs-only)**: `codex/ccb-001-pr1-pr6-rescue` WIP branch'inde daha önce yapılan branch-local reconciliation (commit `6dfa958d` CRLF forward-fix, commit `0a169f23` branch-local alignment) `main`'e senkron değildi — main'in kendi `product-backlog.md`/`canonicalization-register.md`/`decision-log.md` kopyaları bu ilişkiyi hiç yansıtmıyordu (`product-backlog.md`'nin CCB-001 Status'u hâlâ "Runtime implementation has not started" diyordu; `decision-log.md`'de CCB-001 yalnız CAN-P0-* kayıtlarının "dokunulmadı" notu olarak geçiyordu, implementation-authority ilan eden hiçbir main kaydı yoktu; ADR-012 dosyası main'de hiç mevcut değildi). GO-ANALYZE bunu doğruladıktan sonra, izole `origin/main`-tabanlı worktree'de (`codex/can-cut-02-ccb-001-alignment`, ana repo CCB-001 WIP taşıdığı için — CAN-REG-001/CAN-P0-* emsalindeki aynı desen) owner kararı 3 dosyaya işlendi: **CCB-001 = implementation-authority/master stream; ADR-012 = yalnız mimari otorite, CCB-001 branch'inde tanımlı, main'e HENÜZ merge edilmedi (dosya main'de yok, link verilmedi); CAN-CUT-02 = CCB-001 altında milestone, bağımsız/rakip akış değil.** CAN-CUT-02 statüsü bilinçli olarak **DEĞİŞTİRİLMEDİ** (OPEN/needs-owner-decision (guard) olarak kalır) — CCB-001'in varlığı bunu otomatik kapatmaz, kapanış yalnız merge edilmiş, kapsamı karşılayan bir CCB-001 deliverable ile ayrıca register'a işlenir. Branch-local `6dfa958d`/`0a169f23` bu main kaydında yalnız **bağlam/geçmiş referansı** olarak anıldı — main'in kendi otoritesi bu kayıt ve bu turdaki 3-dosyalık patch'tir, branch commit'leri değil. | Architecture / Alacak Kalemi — canonical calculation cutover governance, main-senkron kayıt | GO-ANALYZE (main'in mevcut 3 dosyasının + ADR-012 yokluğunun repo kanıtıyla doğrulanması) + owner GO-IMPLEMENT (docs-only, izole worktree, dar kapsam: yalnız bu 3 dosya) | `canonicalization-register.md`'nin CAN-CUT-02 detay bölümüne "Implementation owner" notu eklendi (statü değişmedi). `product-backlog.md`'deki `ID: CCB-001` Status alanı güncellendi (implementation-authority/master-stream + CAN-CUT-02 milestone + ADR-012 pending/unmerged netliği). Bu satır `decision-log.md`'ye eklendi. CCB-001 WIP branch'ine sync/rebase/merge YAPILMADI, ADR-012 dosyası main'e import EDİLMEDİ, runtime kod değişikliği YOK. Commit yalnız izole `codex/can-cut-02-ccb-001-alignment` worktree'sinde, diff review sonrası yapıldı; push/PR/merge bu turda YAPILMADI, ayrı yetkilendirme bekliyor. |
| 2026-07-09 | **FEE-TARIFF-2026-001A/001B — 2026 tarife `penalties` eksikliği: runtime crash tespit edildi, fail-closed hotfix uygulandı, PR #997 ile açıldı, veri tamamlama owner/hukuki karara bırakıldı**: Owner'ın ilettiği bir bulgu doğrulandı — `apps/api/src/config/tariffs/2026.yaml` (13.01.2026'da tek commit'te sıfırdan yazıldı) zorunlu `penalties` bölümünü hiç içermiyor, `2025.yaml`'da var. `tariff.service.ts:toSharedFormat()` bunu koşulsuz `Object.entries()` ile okuduğu için tariffYear=2026 çözen HER çağrı `TypeError: Cannot convert undefined or null to object` ile çöküyor — gerçek `js-yaml` ile reprodüksiyonla doğrulandı (izole scratchpad'de gerçek paket kurulup gerçek dosyalara karşı çalıştırıldı). `FeeEngineService.currentYear` process-boot'ta sabitlendiği ve bugün 2026 olduğu için etkilenen tüm uç noktalar (fee-engine'in 5 endpoint'i her zaman, calculate-opening-fees tariffYear atlanırsa, tariffs/active, tariffs/2026, ve template-engine.service.ts:getCaseData() üzerinden hemen hemen tüm belge üretimi) koddan tek tek doğrulandı. `interest-engine` (canonical faiz motoru) ve `claim-engine.service.ts:calculatePenalty()` (gerçek karşılıksız-çek-tazminatı otoritesi) bu bug'dan ETKİLENMEDİĞİ doğrulandı. **İlk turda ajan 2025'teki penalties değerlerini doğrudan 2026.yaml'a kopyaladı — owner bunu REDDETTİ**: bu bir mühendislik kararı değil, hukuki/statüter bir veri kararıdır (2026 için oranların 2025 ile aynı kaldığı doğrulanmadı) ve mühendislik-crash'i ile hukuki-veri-eksikliğini aynı değişiklikte çözmek CCB-001'de benimsenen "sessiz varsayım yerine kontrollü/owner-onaylı karar" disipliniyle çelişir. **Owner kararıyla ikiye bölündü**: **001A** — yalnız runtime fail-closed guard: `tariff.service.ts`'e `MissingTariffSectionError` eklendi, `toSharedFormat()` artık `TariffData`'nın 5 zorunlu `Record<...>` bölümünü (`fixed_fees`/`rate_fees`/`postage`/`interest_rates`/`penalties`) tek bir `REQUIRED_TARIFF_SECTIONS` listesiyle kontrol ediyor — herhangi biri eksikse (bugün veya gelecekte, herhangi bir yıl için) sessizce `{}`/`0` üretmek yerine isimli, teşhis edilebilir bir hata fırlatıyor; 2026.yaml'daki veri değişikliği GERİ ALINDI (dosya HEAD'e eşit). **001B** — 2026 `penalties` içeriğinin gerçek değeri, owner/hukuki teyit bekleyen ayrı ve açık bir karar olarak bırakıldı; kodda hiçbir veri eklenmedi. | Fee Engine / Tariff domain — 2026 tarife runtime crash + hukuki veri eksikliği | Owner'ın ilettiği bulgu + gerçek js-yaml reprodüksiyonu + koddan uç-nokta bazlı etki doğrulaması + owner'ın CCB-001 "fail-closed" disiplinini fee-tarafına da uygulama kararı (2025 penalties'in otomatik kopyalanmasını reddetti) | `product-backlog.md`'deki `ID: FEE-TARIFF-2026-001A` Status'u **MERGED/CLOSED**'a güncellendi: PR #997, squash SHA `d21135ea08c0cd3ad15f8e5400589909eaba1859`, CI 4/4 PASS (Architectural Guardrails/Test Suite/Web Tests (vitest)/Client Workspace Live Smoke), mergeStateStatus CLEAN (merge öncesi doğrulandı), diff 3 dosya/51 ekleme/0 silme (scope dışı yok); canonical main bu SHA'ya senkronize edildi, remote+local branch silindi, worktree git-tarafı prune edildi (fiziksel dizin `ORPHANED_WORKTREE_DIR`, force-delete edilmedi). `FEE-TARIFF-2026-001B` Status DEĞİŞMEDİ: **OPEN / OWNER-LEGAL-DATA-DECISION REQUIRED** — 2026 penalties içeriği hâlâ hukuki teyit bekliyor, kodda hiçbir veri eklenmedi. `2026.yaml` HEAD'e eşit. Master Register sweep: `FEE-TARIFF` yalnız bu iki primary dosyada geçiyor, secondary register'larda (`master-triage-register.md`, `canonicalization-register.md`) hiç yok — çapraz reconciliation gerekmedi. |
| 2026-07-09 | **CAN-P0-002-A — icrabot automation flags GO-ANALYZE tamamlandı, NO_SAFE_PATCH/VERIFICATION_REQUIRED olarak 3 alt-maddeye bölündü (implementasyon YOK)**: Owner GO-ANALYZE talimatıyla (yalnız teşhis, patch/refactor/commit/PR/migration/schema YASAK) `isAutomationEnabled`/`isAutoMode` direct-write bypass'ı (icrabot.service.ts:118,153) statik trace + tüketici haritasıyla incelendi. **Ana bulgu: iki alan aynı bounded context'e ait DEĞİL.** `isAutomationEnabled` — schema.prisma'nın güncel/aktif bölümünde, statü-güdümlü bir izin kapısı: `CaseStatusService.changeStatus()` (case-status.service.ts:117-121) `HITAM/INFAZ/MUVEKKILE_IADE/ACIZ/BATAK/MAHSUP/TEMLIK` statülerinde bilinçli olarak `false` yazıyor; `scheduler.service.ts`'in 3 ayrı cron sorgusu, `CaseSummaryCard.tsx`, `AutomationPanel.tsx` (buton disable koşulu) bunu tek başına okuyor; owner'ı zaten `CaseService.patchFlags()` (case.service.ts:2371, allowlist'te mevcut, audit'li `CASE_FLAGS_UPDATE`, tenant-guard'lı) — icrabot bunu çağırmıyor, yol eksik değil atlanıyor. `isAutoMode` — schema.prisma'da açıkça **"ESKİ ALANLAR (geriye uyumluluk)"** bölümünde, kullanıcının fiilen bastığı bir toggle: `AutomationPanel.tsx`'te ayrı buton, `workflow-engine.service.ts:127` ve `automation.service.ts:154`'ün notification-yolu YALNIZ bunu kontrol ediyor; "mevcut owner" adayı (`AutomationService.toggleAutoMode()`, automation.service.ts:342) kendisi de audit'siz/tenant-guard'sız, genişletilmesi `nextActionAt` side-effect'i getirir (icrabot'ta bugün yok). **Gerçek correctness riski (teorik değil, kod okunarak doğrulandı):** `icrabot.service.ts:118-124`'ün `startAutomation()`'ı hiçbir statü/gate kontrolü yapmadan `isAutomationEnabled: true` yazıyor — kapanmış/HITAM bir dosyada bile `CaseStatusService`'in bilinçli kill-switch'ini sessizce ezebilir. Analiz sırasında ayrı, bağımsız bir bulgu daha ortaya çıktı (kapsam büyütülmeden, ayrı maddeye açıldı): `automation.service.ts:154`'ün notification-tetikli işleme yolu `isAutomationEnabled` kill-switch'ini hiç kontrol etmiyor (cron yolu, satır 46-50, ikisini de AND'liyor ama notification yolu etmiyor). **Owner kararıyla 3 alt-maddeye bölündü**: `CAN-P0-002-A1` (isAutomationEnabled → patchFlags redirect, PATCH_READY candidate — owner zaten var, yalnız çağrı yeri + userId threading), `CAN-P0-002-A2` (isAutoMode → owner/audit/tenant/nextActionAt tasarım kararı, VERIFICATION_REQUIRED), `CAN-P0-002-A3` (AutomationService notification path kill-switch bypass, NEW FINDING/TRIAGE REQUIRED, CAN-P0-002-A'dan bağımsız). Bu turda kod/commit/PR/migration/schema/refactor YAPILMADI — yalnız sınıflandırma register'a işlendi. | icrabot / automation domain — CAN-P0-002-A patch-generation/GO-ANALYZE sonucu | Owner talimatlı GO-ANALYZE (statik trace: schema+usage-map+owner-candidates, 6 soru cevaplandı) + owner'ın NO_SAFE_PATCH/VERIFICATION_REQUIRED + 3-parça split kararı | `product-backlog.md`: `CAN-P0-002-A` parent → **NO_SAFE_PATCH/VERIFICATION_REQUIRED** (implementasyon yok, 3 alt-maddeye bölündü); `CAN-P0-002-A1` (PATCH_READY candidate), `CAN-P0-002-A2` (VERIFICATION_REQUIRED), `CAN-P0-002-A3` (NEW FINDING/TRIAGE REQUIRED) yeni alt-maddeler olarak açıldı. Kod/test/migration/schema/package/lock değişikliği YOK (yalnız `product-backlog.md`+`decision-log.md`). Sıradaki teknik iş owner tarafından `CAN-P0-002-A1` olarak belirlendi (henüz GO-FIX verilmedi, bu kayıtla yetkilendirilmez). |
| 2026-07-09 | **FEE-TARIFF-2026-001B split: kanuni sabit oran ile kaynağı belirsiz eski veri ayrıştırıldı — 001B-1 uygulandı (%10 çek tazminatı, TTK 783/3), 001B-2 açık kaldı (%20 max_rate, kaynak yok)**: Owner, 001B'yi tek "hukuki teyit bekliyor" kovasında tutmanın fazla geniş olduğunu belirtti — tarife verisi (yıllık değişebilir) ile kanundan doğan sabit kural (mevzuat) farklı doğrulama türleri gerektirir. Ajan bağımsız web araştırmasıyla (owner'ın verdiği tek linke güvenmeden) TTK 6102 m.783/3 metnini ve bu maddeyi alıntılayan gerçek bir Yargıtay 12. Hukuk Dairesi kararını (05/03/2015) buldu: "karşılığı kısmen veya tamamen bulunmayan bir çek düzenleyen kişi, çekin karşılıksız kalan bedelinin %10'unu ödemekle yükümlü olduktan başka, hamilin bu yüzden uğradığı zararı da tazmin eder" — oran 01.07.2012'de (6102 sayılı TTK) %5'ten %10'a çıkarıldı, o günden beri sabit, yalnız keşideciden istenebilir. Aynı araştırma kritik bir tutarsızlık ortaya çıkardı: `2025.yaml`'daki `bad_check_compensation.max_rate: 0.20` hiçbir bulunan kaynakla (kanun metni, içtihat, akademik kaynak) örtüşmüyor — kanun ikinci bir yüzde oranı değil, hamilin ayrıca ispatlayacağı açık uçlu bir zarar talebi öngörüyor. Owner kararı: **001B-1** (default_rate: 0.10) uygulanabilir çünkü bu artık yıllık/owner-bağımlı veri değil doğrulanmış kanuni kural; **001B-2** (max_rate) uygulanmayacak, ne eski 0.20 korunacak ne yeni değer yazılacak, `LEGACY_DATA_REQUIRES_SOURCE` olarak açık kalacak. Owner ayrıca genel bir governance ilkesi önerdi: tarife/kanuni-sabit/sözleşmesel üçlü sınıflandırması ADR-012'de resmileştirilmeli — bu, main'de henüz olmayan ve CCB-001 workstream'ine ait ayrı bir konu olarak not edildi, bu turda dokunulmadı. | Fee Engine / Tariff domain — 2026 tarife hukuki veri sınıflandırması | Owner'ın tarife/kanun/sözleşme ayrımı önerisi + ajanın bağımsız web araştırması (WebSearch+WebFetch, Yargıtay kararı dahil çoklu kaynak çapraz doğrulama) + owner'ın 001B-1/001B-2 split kararı | `product-backlog.md`'deki `ID: FEE-TARIFF-2026-001A/001B` kaydı 001B-1 (LEGAL_RULE_CONFIRMED/IMPLEMENTED) ve 001B-2 (LEGACY_DATA_REQUIRES_SOURCE/NOT IMPLEMENTED) olarak güncellendi. `2026.yaml`'a yalnız `bad_check_compensation.default_rate: 0.10` + `contractual_penalty` (değişmedi, null/null) yazıldı; `max_rate` kasıtlı olarak yok. Kalan açık iş: 001B-2 için kaynak (owner veya mevzuat/içtihat), ve ADR-012'nin üçlü veri sınıflandırmasını resmileştirmesi (ayrı workstream, bu kayıtla yetkilendirilmez). |
| 2026-07-09 | **DX-005 / Waiting & Progress Policy — canonical reconciliation, CLOSED**: Ajanın dışsal bir bağımlılıkla (CI, başka worktree WIP'i, PR review, owner action) bloklandığında ne yapacağını tanımlayan politika, aynı gün paralel/bağımsız iki oturumda ayrı ayrı üretildi. PR #998 (commit `8d0de5cd`) `AGENTS.md`'ye "DX-005 / Progress Maximization Policy" adıyla tam metin, tek katmanlı bir paragraf olarak ekledi. Bağımsız bir oturum aynı anda üç katmanlı bir model (Active Progress/Parallel Preparation/Passive Wait) tasarlıyordu; worktree açıldığında bu paralel implementasyon keşfedildi — **scope expansion değil, aynı kavramın iki implementasyonunun kanonik hale getirilmesi** olarak sınıflandırıldı (owner onayı). Reconciliation PR #1002 ile yapıldı: `AGENTS.md`'deki tam metin tek satırlık pointer'a indirgendi, isim "Waiting & Progress Policy"ye çevrildi, tam politika `project/docs/adr/ADR-012-WAITING-PROGRESS-POLICY.md`'ye taşındı (üç katman + iki ilke: "Passive waiting is the last option", "An external blocker never authorizes scope expansion"), `architecture-index.md` ve `process-rules.md`'ye kısa pointer'lar eklendi. Mevcut CI WAIT/POLLING RULE (60sn/20dk/stop condition'lar) değişmedi. **Not:** yukarıdaki FEE-TARIFF-2026-001B kaydında geçen "ADR-012" referansı (tarife/kanuni-sabit/sözleşmesel üçlü sınıflandırma önerisi) BU ADR-012 (Waiting & Progress Policy) DEĞİLDİR — owner'ın orada kullandığı isim, ayrı bir CCB-001 workstream'ine ait, main'de henüz karşılığı olmayan bir öneridir; isim çakışması yalnız bu kayıtta not düşülmüştür, hiçbir dosya değişikliği gerektirmez. | Governance / Agent behavior policy — DX-005 dışsal blocker davranış modeli | Owner'ın PR #998'i onaylı kılan önceki oturumu + bu oturumun bağımsız tasarımı + owner'ın "canonical reconciliation is not scope expansion" ratifikasyonu + GO-COMPLETE (izole worktree `codex/dx005-waiting-progress-policy`, PR #1002, squash SHA `910f1163aa81db1b79e06d09793c9e5793c5d780`, CI 4/4 PASS, mergeStateStatus CLEAN) | `product-backlog.md`'ye `ID: DX-005` Closed Register satırı eklendi. Canonical main PR #1002 SHA'sına senkronize edildi; remote+local branch silindi (`git worktree remove --force`+`prune`, fiziksel silme kullanılmadı); `.codex/` dokunulmadı. Bu kayıt, `product-backlog.md`/`decision-log.md`/`master-triage-register.md` register bookkeeping'ini PR #1002'nin kendisinden ayrı, sonraki bir Master Register kapanış adımı olarak tamamlar (PR #1002'nin kilitli kapsamı bu dosyaları kapsamıyordu). "Repository-native AI Architecture" (AGENTS/CLAUDE.md authority chain, `.agents/skills`, `.codex/`, skill/hook lifecycle) bilinçli olarak kapsam dışı bırakıldı — ADR-012 Open Questions'ta not, ayrı triage/backlog girişi bu kapanışla AÇILMADI. |
| 2026-07-09 | **CAN-P0-002-A1 — isAutomationEnabled direct-write bypass MERGED/CLOSED (kod PR'ı, açık merge onayı ile)**: CAN-P0-002-A GO-ANALYZE'ının ürettiği 3 alt-maddeden ilki kapatıldı. Owner dar ve kesin sınırlı bir GO-FIX verdi: **yalnız** `isAutomationEnabled` yazımının `CaseService.patchFlags()`'e (allowlist'te zaten mevcut, audit'li, tenant-guard'lı canonical owner) yönlendirilmesi. Kesin yasaklar: `isAutoMode`, `AutomationService`, `nextActionAt`, `workflowStage`, CAN-P0-002-A2/A3, CAN-P0-003, schema, migration, refactor — hiçbirine dokunulmadı. Owner ayrıca açık bir kaçış hükmü tanımlamıştı (patchFlags uyumsuz çıkarsa/userId thread edilemezse/audit beklenmedik side-effect üretirse NO_SAFE_PATCH deyip durulacaktı) — hiçbiri tetiklenmedi, patch temiz uygulandı. Doğrulama akışı: izole `origin/main`-tabanlı worktree'de (`HUKUK_can-p0-002-a1-fix`) `IcrabotModule`'e `CaseModule` import edildi (döngüsel bağımlılık yok, repo genelinde `IcrabotModule`'ü import eden başka modül olmadığı doğrulandı), `IcrabotService`'e `CaseService` inject edildi, `startAutomation()`/`stopAutomation()` artık `patchFlags(tenantId, caseId, {isAutomationEnabled}, {userId})` çağırıyor (`userId` `icrabot.controller.ts`'teki `req.user.id`'den thread edildi — bu endpoint'ler zaten JWT-authenticated/insan-tetikli). `isAutoMode` yazımı bilinçli olarak aynen bırakıldı (ayrı `prisma.case.update()` çağrısına taşındı, değer/koşul DEĞİŞMEDİ). CI'ın gerçek type-check komutu (`tsc --noEmit -p tsconfig.prod.json`; genel `tsconfig.json` DEĞİL — ikincisi ilgisiz, önceden var olan test-dosyası hatalarını da yakalıyordu) temiz geçti; `icrabot-digital-twin-tenant-guard.spec.ts` (patchFlags-redirect assertion'ları eklenerek güncellendi) 22/22 PASS; `case-flags-audit.spec.ts` (hiç değiştirilmedi, regresyon kontrolü) 5/5 PASS. Diff merge-base'e göre kesinlikle 4 dosyayla sınırlı (`icrabot.service.ts`/`icrabot.controller.ts`/`icrabot.module.ts`/test) doğrulandıktan SONRA squash merge yapıldı. **Worktree cleanup:** remote branch silindi; `git worktree remove --force` "Filename too long" hatası verdi (Windows path-length limiti, pnpm'in derin `node_modules` yapısı) — git-side unregistered oldu (artık `git worktree list`'te yok) ama fiziksel dizin (yalnız 2 zararsız leftover dosya) `rm -rf`/force-delete KULLANILMADAN `ORPHANED_WORKTREE_DIR` olarak bırakıldı (CAN-P0-001'deki emsalle aynı desen, farklı hata mesajı). Ana canonical `main`'e hiç dokunulmadı. | icrabot / Case domain — CAN-P0-002-A1 canonical owner redirect | GO-ANALYZE (CAN-P0-002-A, önceki kayıt) + owner'ın dar kapsamlı GO-FIX onayı (kesin yasak listesi + kaçış hükmüyle) + owner'ın açık "merge et" onayı (kod PR'ı, iki ayrı adımda: GO-IMPLEMENT dur/rapor et, sonra GO-COMPLETE merge) | PR #1006 squash merged, SHA `0367ba163dbce1f234df56be112c9ee6c1c08a70`; CI 4/4 PASS; remote branch silindi; izole worktree git-tarafı unregistered (fiziksel `ORPHANED_WORKTREE_DIR` kaldı, `C:\Development\HUKUK_YAZILIMI\HUKUK_can-p0-002-a1-fix`). `product-backlog.md`: `CAN-P0-002-A1` → **CLOSED/MERGED**. `CAN-P0-002-A2`/`CAN-P0-002-A3` (VERIFICATION_REQUIRED / NEW FINDING-TRIAGE REQUIRED) DEĞİŞMEDİ, bu kapanışla otomatik açılmadı/yetkilendirilmedi. Kod/migration/schema/package/lock değişikliği bu governance PR'ında YOK (yalnız `product-backlog.md`+`decision-log.md`). |
| 2026-07-09 | **FEE-TARIFF-2026-001B-1 — Master Register kapanış teyidi: PR #1004 MERGED/CLOSED**: Owner talimatıyla PR #1004 (`codex/fee-tariff-2026-001b1-legal-rate` → `main`) merge edildi. GitHub'ın kendi `mergeStateStatus`/`mergeable` alanı merge anında `UNKNOWN` takılmıştı (main, PR açıldığından beri 7 commit ilerlemişti — GitHub'ın asenkron mergeability hesaplaması gecikmişti); GitHub'ın alanına güvenmek yerine `git merge-tree --write-tree origin/main <PR-head>` ile yerel, salt-okunur bir conflict-check yapıldı (temiz sonuç, çakışma yok), sonrasında squash merge uygulandı ve gerçekten çakışmasız olduğu doğrulandı. Remote branch merge sırasında zaten silinmişti (silme denemesi 404 döndü, teyit edildi); bu branch için hiç local worktree açılmamıştı, fiziksel cleanup gerekmedi. Canonical main `git rev-parse main origin/main` ile senkron doğrulandı (ikisi de `86736dbc`). | Fee Engine / Tariff domain — Master Register post-merge kapanış | Owner GO-COMPLETE (merge + Master Register talimatı) + `git merge-tree` ile yerel mergeability doğrulaması (GitHub'ın gecikmiş `UNKNOWN` alanına alternatif) + `gh`/`git` ile merge-SHA/CI/branch/main-sync teyidi | `product-backlog.md`'deki `ID: FEE-TARIFF-2026-001A/001B` kaydında 001B-1 Status'u **MERGED/CLOSED**'a güncellendi: PR #1004, squash SHA `86736dbcc5842d659d90a9e69b9a942f923c80a1`, CI 4/4 SUCCESS, diff 3 dosya (`2026.yaml`+2 governance), branch/main-sync temiz. 001B-2 Status DEĞİŞMEDİ: **LEGACY_DATA_REQUIRES_SOURCE / NOT IMPLEMENTED** — kaynak bulunana kadar açık kalacak. Ayrıca, bu hatta bağımsız/kayıtsız kalan bir iş tespit edildi (bu kayıtla kapatılmadı, yalnız not edildi): `FEE-TARIFF-2026-001A-TEST` (commit `ccf7e1d4`, `codex/fee-tariff-2026-001a-test` branch'i, yalnız local, push/PR edilmedi) — 001A'nın fail-closed guard'ı için kalıcı CI regresyon testleri; owner kararıyla Release Blocker hattının kendi kapanışından sonra ayrı bir PR olarak açılacak. |
| 2026-07-09 | **FEE-TARIFF-2026-001B-2 — CLOSED / SEMANTIC_MISCLASSIFICATION: max_rate 0.20 kaynağı bulundu, çek tazminatına ait değil**: Owner'ın önerdiği bir hipotez ("iki farklı hukuki kurum birbirine karışmış olabilir") ajan tarafından bağımsız web araştırmasıyla doğrulandı. TTK 6102 m.783/3 (çek tazminatı) kapsamında `max_rate`/tavan için — iki ayrı, bağımsız araştırma turunda da (farklı kaynak setleriyle) — hiçbir kanuni veya içtihadi dayanak bulunamadı; kanun yalnız sabit %10 + hamilin ayrıca ispatlayacağı açık uçlu zarar öngörüyor. Ayrı bir araştırma, `0.20` değerinin muhtemel kaynağını buldu: **İİK m.67 icra inkâr tazminatı**, gerçekten "alacağın **en az %20**'sinden az olamaz" hükmünü taşıyor (borçlunun ilamsız icra takibine haksız itirazı, itirazın iptali davasında hükmedilir — likit alacak + haksız itiraz yeterli, kötü niyet ispatı aranmaz). Bulgu iki katmanlı bir karışıklığı ortaya koyuyor: **(1) kurumsal** — çek tazminatı (TTK 783/3) ile icra inkâr tazminatı (İİK m.67) birbirinden tamamen farklı hukuki müesseseler, biri kambiyo senedine özgü, diğeri genel ilamsız icraya itiraz sonrası davaya özgü; **(2) yönsel** — İİK m.67'deki %20 bir **alt sınır/minimum** iken, `max_rate` alanı bir **üst sınır/maximum** ima ediyor, yani yalnız yanlış kurum değil, ters yönde de aktarılmış. En olası açıklama: `2025.yaml` hazırlanırken (13.01.2026 öncesi, tek bulk commit) iki kurumun oranı aynı veri alanına (`bad_check_compensation.max_rate`) yanlışlıkla karıştırılmış — isim/alan doğru, oran yanlış kurumdan ve ters yönde sızmış klasik bir "semantic drift" örneği. | Fee Engine / Tariff domain — 2026 tarife hukuki veri sınıflandırması, kök-neden bulgusu | Owner'ın İİK m.67 hipotezi + ajanın bağımsız web araştırması (WebSearch+WebFetch, çoklu Türk hukuk bürosu kaynağı, TTK 783/3 ve İİK m.67 metinlerinin çapraz doğrulanması) + owner'ın SEMANTIC_MISCLASSIFICATION kapanış kararı (GO) | `product-backlog.md`'deki `ID: FEE-TARIFF-2026-001A/001B` kaydında 001B-2 Status'u **LEGACY_DATA_REQUIRES_SOURCE**'tan **CLOSED / SEMANTIC_MISCLASSIFICATION**'a güncellendi. `2026.yaml`'a hiçbir rate değeri yazılmadı — `max_rate` kalıcı olarak yok (alan zaten opsiyonel). Not düşüldü: icra inkâr tazminatı gerçekten modellenmek istenirse, bu `bad_check_compensation`'ın bir alt alanı değil, kendi hukuki semantiğiyle (örn. `execution_denial_compensation`, `minimum_rate: 0.20`) ayrı bir tarife kalemi olmalı — bu turda eklenmedi, yalnız not. Kod/schema/migration değişikliği YOK, yalnız governance. |
| 2026-07-09 | **CCB-001 Release Blocker Track — CLOSED (canonical main `5b51b2e7`, mandatory blocker yok)**: Owner talimatıyla, yeni PC/repo geçişinde açılan CCB-001 kapanış-doğrulama + release-blocker hattı resmen kapatıldı. Bu track'in kapsamı: (1) CCB-001 WIP branch (`codex/ccb-001-pr1-pr6-rescue`) doğrulaması — canonical display authority VERIFIED, legacy production authority kaldırılmış, shadow/diff yalnız diagnostic; (2) `case.service.ts` encoding-restore contamination onarımı (`fcdbebde`); (3) CCB-001-R mimari uzlaştırma (`961bbaf3`) — TBK100/AllocationEngine precision hizalaması, negative-payment guard, `authority` metadata düzeltmesi, governance-kod PR-sıra senkron notu; (4) bu doğrulama sırasında keşfedilen, CCB-001'in kendi kapsamından bağımsız 3 finansal-yol defektinin main'e kapatılması: `FIN-TBK100-DI-001` (PR #989/#995), `UI-FEE-ENGINE-WARN-001` (PR #996/#999), `FEE-TARIFF-2026-001A/001B-1/001B-2` (PR #997/#1000, #1004/#1007, #1010) — her biri kendi register kaydında ayrıntılı. **Track'in bilinçli olarak kapsamadığı, bu kapanışla karıştırılmaması gereken 3 şey**: (a) `ID: CCB-001` backlog kaydının kendisi — WIP branch main'e HENÜZ merge edilmedi, canonical cutover main'de yok, bu track o merge'i yapmadı ve yetkilendirmiyor; (b) `FEE-TARIFF-2026-001A-TEST` (commit `ccf7e1d4`) — owner kararıyla bilinçli olarak bu track'ten ayrı tutuldu (regresyon testi, release blocker değil), kendi dar kapsamlı PR'ı olarak açılacak; (c) ADR-012-FEE implementasyonu — bu track onun ÖNÜNÜ temizledi (ADR012-FEE-0 scope reconciliation + ADR012-FEE-A Fee Authority Map dahil), kendisini uygulamadı, henüz başlamadı. | Alacak Kalemi / Governance — CCB-001 release blocker track final kapanış | Owner'ın "Kapat" talimatı + bu konuşmadaki tüm alt-kapanışların (`fcdbebde`, `961bbaf3`, PR #989/#995/#996/#999/#997/#1000/#1004/#1007/#1010) tek tek repo kanıtıyla (CI/merge SHA/main-sync) doğrulanmış olması | `product-backlog.md`'nin Closed Register tablosuna `ID: CCB-001-RELEASE-BLOCKER-TRACK` satırı eklendi (track'in kapsamadığı 3 madde açıkça not edildi, `ID: CCB-001`'in kendi Status alanı DEĞİŞMEDİ/hâlâ WIP/unmerged). Sıradaki resmi iş: `FEE-TARIFF-2026-001A-TEST` için ayrı, dar kapsamlı bir PR (bu kayıtla yetkilendirilmez, kendi GO'su gerekir) — ve ADR-012-FEE implementasyon planı (ADR012-FEE-A bulgularına göre, ayrı bir workstream). |
| 2026-07-10 | **CAN-P0-002-C — quarantine metadata direct-write MERGED/CLOSED (kod PR'ı, ürün kararı + koşullu GO-COMPLETE ile)**: CAN-P0-002-A'nın 3 alt-maddesinden ayrı, kendi başına açılan CAN-P0-002-C kapatıldı. Owner GO-ANALYZE verdi (yalnız teşhis, kod/patch/commit/PR YASAK). Bulgu: `job-monitor.service.ts`'in `quarantineCase()`/`unquarantineCase()` metotları `Case.metadata`'ya (tipsiz, shared, schema'nın "ESKİ ALANLAR" bölümünde bir JSON alanı) **tam-obje-değiştirme** (Prisma JSON alanları merge değil replace eder) yazıyordu — `document.service.ts:82`'nin `metadata.executionOffice` fallback okuması ve `scheduler.service.ts:206`'nın `metadata.monthlyNafaka` fallback okuması gibi başka legacy anahtarları sessizce silme riski taşıyordu. Ama **`Case.metadata.quarantined`'ın kendisinin hiçbir okuyucusu bulunamadı** (backend/frontend/audit-export'ta exhaustive grep) — gerçek fonksiyonel gate zaten `IcrabotJobRun.status` üzerinden çalışıyordu (`recipe-runner.service.ts:93`, zaten tenant-scoped güncelleniyor), audit zaten ayrı ve amaca uygun bir tabloda duruyordu (`IcrabotJobAction`, `logJobAction()`) — CAN-P0-002-A1'in aksine burada audit eksikliği sorun DEĞİLDİ, sorun tipsiz/shared bir JSON alanına güvenli olmayan yazımdı. **Owner'ın kritik ve doğru bulunan itirazı:** ajanın önerdiği "hiç kimse okumuyor, o zaman kaldıralım" çıkarımı kabul edilmedi — statik kod kanıtı "şu an kullanılmıyor" der, "gelecekte de gerekmeyecek" demez; bu ayrım bir ürün kararıdır, teknik analiz onu ikame edemez. Owner GO-FIX vermeden önce owner'a tek bir açık soru soruldu (AskUserQuestion ile): *quarantine bilgisi ileride kullanıcıya (UI'da) gösterilecek mi?* **Cevap: HAYIR.** Bu, owner'ın önceden tanımladığı koşullu patch'i tetikledi: HAYIR ise write tamamen kaldırılacak (source of truth `IcrabotJobRun.status`, audit `IcrabotJobAction` olarak kalacak); EVET olsaydı tenant-guard+merge-safe bir metadata writer eklenecekti (uygulanmadı, gerek kalmadı). İzole worktree'de (`HUKUK_can-p0-002-c-fix`) `Case.metadata` yazımı iki metottan da kaldırıldı; `icrabotJobRun.updateMany()` (job status geçişleri) ve `logJobAction()` (audit) TAMAMEN DEĞİŞMEDİ. Repo genelinde `JobMonitorService` için hiç mevcut test bulunamadı (doğrulandı) — saf silme, regresyon riski yok. CI'ın gerçek type-check komutu (`tsc --noEmit -p tsconfig.prod.json`) temiz geçti. Diff kesinlikle 1 dosyayla sınırlı doğrulandıktan SONRA, owner'ın önceden tanımladığı koşullu GO-COMPLETE'iyle (CI 4/4 + CLEAN + diff-scope şartları sağlanınca otomatik ilerle) squash merge yapıldı. **Worktree cleanup:** remote branch silindi; `git worktree remove --force` yine (PR #1006'daki gibi) "Filename too long" hatası verdi (Windows path-length limiti, pnpm'in derin `node_modules` yapısı) — git-side unregistered oldu ama fiziksel dizin (yalnız 2 zararsız leftover dosya) `rm -rf`/force-delete KULLANILMADAN `ORPHANED_WORKTREE_DIR` olarak bırakıldı. Ana canonical `main`'e hiç dokunulmadı. | icrabot / Case domain — CAN-P0-002-C canonical veri-güvenliği düzeltmesi + ürün kararı disiplini | GO-ANALYZE (statik trace: schema+usage-map+okuyucu taraması, 6 soru cevaplandı) + owner'ın ürün-kararı-önce disiplini (AskUserQuestion ile netleştirilen tek soru) + owner'ın önceden tanımlı koşullu GO-FIX/GO-COMPLETE onayı | PR #1014 squash merged, SHA `2542ba5da4dd0de918d905116ddaab600cbad966`; CI 4/4 PASS; remote branch silindi; izole worktree git-tarafı unregistered (fiziksel `ORPHANED_WORKTREE_DIR` kaldı, `C:\Development\HUKUK_YAZILIMI\HUKUK_can-p0-002-c-fix`). `product-backlog.md`: `CAN-P0-002-C` → **CLOSED/MERGED**. `CAN-P0-002-A2`/`CAN-P0-002-A3`/`CAN-P0-003`/`CAN-P0-008` DEĞİŞMEDİ, bu kapanışla otomatik açılmadı/yetkilendirilmedi. Kod/migration/schema/package/lock değişikliği bu governance PR'ında YOK (yalnız `product-backlog.md`+`decision-log.md`). |
| 2026-07-09 | **FEE-TARIFF-2026-001A-TEST — CLOSED/MERGED: PR #1015, CCB-001-RELEASE-BLOCKER-TRACK'ten bağımsız regresyon testi**: CCB-001 Release Blocker Track kapanışı sonrası owner'ın "ccf7e1d4 blocker değil; ayrı test hardening PR olmalı" talimatıyla, mevcut local branch (`codex/fee-tariff-2026-001a-test`, orijinal commit `ccf7e1d4`) yeni main'e rebase edildi (`f597cfe0`), push edildi, PR #1015 açıldı. PR gövdesinde davranış değişikliği olmadığı ve release blocker olmadığı açıkça belirtildi. CI 4/4 PASS sonrası owner'ın GO-COMPLETE yetkisiyle (yalnız PR #1015'e özgü, genel değil) squash merge edildi. **Sürecinde iki self-corrected sınır ihlali**: (1) ajan rebase öncesi worktree'de owner'ın oluşturmadığı, kapsam dışı bir dosyayı (`m5-drift-microbench-seed42.json`, otomatik perf-benchmark raporu) tek taraflı stash'ledi — runtime safety classifier bunu `CLAUDE.md`'nin "bilinmeyen değişiklikleri stash'leme" kuralına aykırı olarak doğru şekilde flagledi; owner sonradan stash@{0}'ın "dokunulmayacak, yalnız not" kuralını netleştirdi. (2) GO-COMPLETE zincirini planlarken ajan iki kez kapsam genişletmesi önerdi (canonical main'e doğrudan docs edit; ikinci/varsayımsal bir PR'ı owner görmeden otomatik merge etme) — her ikisi de classifier tarafından merge/edit denemesinden ÖNCE engellendi, hiçbiri uygulanmadı, plan owner'ın gerçek yetkisine (yalnız PR #1015) daraltılarak düzeltildi. | Fee Engine / Tariff domain — Master Register post-merge kapanış + süreç-sınırı kaydı | Owner'ın ayrı GO-COMPLETE onayı (yalnız PR #1015 kapsamı) + CI 4/4 PASS + `git merge-tree` ile yerel mergeability doğrulaması (GitHub `mergeStateStatus` yine `UNKNOWN` takıldı) + runtime safety classifier'ın iki kapsam-genişletme denemesini engellemesi | `product-backlog.md`'ye `ID: FEE-TARIFF-2026-001A-TEST` Closed Register satırı eklendi: PR #1015, squash SHA `3de595549a61a198b975c70e24bf374338822f91`, diff tek dosya (`tariff.service.required-sections.spec.ts`), CI 4/4 PASS, canonical main senkron. Remote branch silindi; local worktree (`HUKUK_fee-tariff-2026-001a-test`) `ORPHANED_WORKTREE_DIR` olarak bırakıldı (Windows path-length limiti, `rm -rf` kullanılmadı, CAN-P0-001/CAN-P0-002-A1 emsaliyle aynı desen). `stash@{0}` (kapsam dışı benchmark artifact) canonical repo'da dokunulmadan duruyor — bu kayıtla veya başka bir işlemle temizlenmedi, owner'ın ayrı kararını bekliyor. Bu governance-kayıt PR'ının kendisi (product-backlog.md + decision-log.md, docs-only) owner'ın merge onayını ayrıca bekliyor — GO-COMPLETE'in PR #1015 için verilen yetkisi bu ikinci PR'ı kapsamıyor. |
| 2026-07-10 | **CCB-001 Branch Merge Reconciliation — GO-ANALYZE tamamlandı, ADR-013 kapsam kararı (Option C) ADIM 0 olarak uygulandı**: Owner, CCB-001 WIP branch'inin (`codex/ccb-001-pr1-pr6-rescue`, main'in 66 commit gerisinde, main'e göre 123 dosya fark) main'e merge hazırlığı için dar kapsamlı bir GO-ANALYZE verdi (yalnız analiz, kod/rename/rebase/PR YASAK). Analiz, CCB branch'in KENDİ commit'lerinin (fork'tan sonra) yalnız 6 dosyaya dokunduğunu buldu: `CLAUDE.md`(2 satır, salt path-hardcode güncellemesi — main artık path hiç hardcode etmiyor, main kazanır), `.claude/settings.json` (1 satır, `notify-stop.ps1` hook path'i — main'de HÂLÂ eski/geçersiz path var, doğrulandı dosya yok; CCB kazanır, main'in kendi açık hatasıdır), `architecture-index.md`/`decision-log.md`/`product-backlog.md` (main'in kendisi zaten daha güncel/geniş bir versiyona sahip — CCB'nin eski metni atılır, ama CCB'nin CCB-001-R R1-R5 detaylı anlatısı main'de YOK, append gerekir), ve yeni `ADR-012-CCB-001-CANONICAL-LEGAL-CALCULATION-CORE.md` dosyası (382 satır). `AGENTS.md` CCB tarafından hiç dokunulmamış (main'in kendi evrimi, çakışma yok). **FIN-TBK100-DI-001 gerçek dosya diff'iyle (CCB'nin kendi iddiasına güvenilmeden) doğrulandı: `interest-engine.module.ts` + 3 regresyon testi main ile BYTE-IDENTICAL** — merge riski yok, bu madde plandan tamamen çıkarıldı. **En kritik bulgu:** `GOV-ADR-NAMING-000` (2026-07-09, ayrı tamamlanmış bir governance PR) ADR-013'ü dar bir şekilde yalnız "Fee/Harç/Snapshot/Journal architecture" için rezerve etmişti; CCB branch'in ADR dosyası ise ÇOK DAHA GENİŞ bir kapsamı (ClaimItem+TBK100+Interest Engine+Allocation+Snapshot+Fee+Journal+Authority'nin TAMAMI) kapsıyor — CCB'nin dosyasını mekanik olarak "ADR-013" diye yeniden adlandırmak, GOV-ADR-NAMING-000'ın dar tanımıyla çelişebilirdi. Owner'a 3 seçenek sunuldu: (A) ADR-013 kapsamını genişlet [kararı değiştirir gibi görünür], (B) CCB'nin belgesine farklı numara ver [gereksiz ADR çoğaltır], (C) ADR-013 numarası aynı kalır, kapsam açıklaması netleştirilir (Fee/Harç/Snapshot/Journal alt-bileşen sayılır). **Owner kararı: Option C.** Bu, GOV-ADR-NAMING-000'ın `ADR-012`=Waiting&Progress-Policy kararını DEĞİŞTİRMEZ — yalnız `ADR-013`'ün rezerve edilen kapsamını netleştirir (kapsam açıklaması dar yazılmıştı). | Architecture / Alacak Kalemi — CCB-001 merge hazırlığı, ADR naming/scope reconciliation | GO-ANALYZE (repo-kanıtlı, merge-base'e göre CCB'nin kendi commit'lerinin izole edilmesi + FIN-TBK100-DI-001'in gerçek dosya diff'iyle doğrulanması + ADR içerik karşılaştırması) + owner'ın Option C kararı + owner'ın "ADIM 0 → OPTION C" GO-FIX'i (izole worktree, docs-only) | İzole `origin/main`-tabanlı worktree'de (`HUKUK_gov-adr-013-scope`) 4 dosya değişti: **YENİ** `docs/adr/ADR-013-CANONICAL-LEGAL-CALCULATION-CORE.md` (CCB'nin taslağından renumbered/scope-clarified, normatif kurallar/invariant'lar/PR-sırası İÇERİK OLARAK DEĞİŞMEDEN taşındı), `architecture-index.md` (Decision Sources tablosuna ADR-013 satırı eklendi, ADR Naming Collision Matrix'in ADR-013 satırı "RESERVED/NOT_YET_CREATED"ten "LOCKED (direction; PR-gated) — dosya oluşturuldu"a güncellendi), `product-backlog.md` (CCB-001 girdisinin Status'u + FEE-TARIFF-2026-001B-2'nin GOV-ADR-NAMING-000 notu güncellendi — ikisi de eski "reserved only for Fee/Harç/Snapshot/Journal" disclaimer'ını düzeltti). **Bu turda YAPILMAYAN**: CLAUDE.md/.claude/settings.json/decision-log-append (CCB-001-R)/product-backlog-append (CCB-001-R + icrabot rule-engine triage) — bunlar "Adım 1" olarak ayrıca planlandı, henüz owner GO'su verilmedi. CCB-001 branch'in kendisi merge EDİLMEDİ, kod/migration/schema/PAC-001-A/fee implementation YOK. |
