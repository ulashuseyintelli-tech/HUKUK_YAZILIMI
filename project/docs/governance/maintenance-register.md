# Maintenance Register

Bu register, urun backlog'u olmayan ve runtime/domain logic degisikligi gerektirmeyen bakim, temizlik ve senkronizasyon islerini tek yerde izler.

Repository son otoritedir. Owner WIP dosyalari otomatik overwrite, stash, revert veya cleanup konusu yapilmaz.

## Kayitlar

| ID | Baslik | Status | Reason | Exit condition | Next action |
| --- | --- | --- | --- | --- | --- |
| MR-001 | Canonical main sync deferred | Deferred | Canonical `main`, owner WIP nedeniyle `origin/main` gerisinde kalabilir; WIP dosyalara dokunmadan otomatik sync yapmak guvenli degil. | Owner WIP tamamlanir veya owner sync icin net izin verir; ardindan `main` temiz sekilde `origin/main` ile hizalanir. | Owner WIP durumunu kapatinca canonical `main` icin `git fetch origin` ve kontrollu fast-forward/sync uygula; WIP varken sync denemesi yapma. |
| MR-002 | ORPHANED_WORKTREE_DIR kayitlari | Owner cleanup required | Git worktree kaydi temiz olsa bile fiziksel dizinler kalabilir; recursive fiziksel silme yasak oldugu icin owner-manuel cleanup bekler. | Owner kalan fiziksel dizinleri manuel olarak temizler veya dizinlerin kalici olarak tutulacagini bildirir; sonraki `git worktree list` temiz/uyumlu gorunur. | Owner, orphaned worktree dizinlerini manuel inceleyip temizler; ajan yalniz `git worktree remove --force`, `git worktree prune` ve `git fetch --prune` guvenli sirasini kullanir. |
| MR-003 | Deferred governance sync | Deferred | Governance dosyalarinda owner WIP bulunabilir; otomatik overwrite/stash/revert repo otoritesini ve owner calismasini riske atar. | Owner WIP governance degisiklikleri merge edilir, kapatilir veya owner belirli dosyalar icin sync izni verir. | Owner WIP kapanana kadar governance sync'i ertele; yalniz ayrik maintenance register guncellemeleri yap ve Product Backlog/Master Triage'a yeni urun isi ekleme. |
| MR-004 | Canonical main divergence (committed parallel-session work) | CLOSED / RESOLVED | Tarihsel committed-divergence olayi guvenli sekilde dis oturum tarafindan reconcile edildi; 2026-07-10 Wave 0 baslangicinda `HEAD == main == origin/main == remote main` dogrulandi. Rebase/force/merge --no-ff uygulanmadi. | Tamamlandi; yeniden divergence olursa yeni kanitla ayri maintenance kaydi acilir. | Islem yok. MR-001 owner-WIP semsiye kaydi ayri nedenle gecerli kalir. |
| MR-005 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-w01-scenario-contract` (ADR-014 W0.1) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-w01-scenario-contract`. ADR-014 W0.1 (PR #1037, squash `f998af79`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store, 71k+ dosya — W0.1 gate'leri icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Git-side temiz unregistered (`git worktree list --porcelain` kaydi yok). **WIP YOK** — tum W0.1 isi commit+merge edildi, dizin yalniz checkout + `node_modules` icerir (salt-okunur dogrulandi). Top-level `node_modules` reparse-point DEGIL (duz Directory); derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-w01-scenario-contract"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari ~12/~30/~24, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-006 | ORPHANED_WORKTREE_DIR — `HUKUK_collection-status-hotfix` (COLLECTION-STATUS-FILTER-HOTFIX) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_collection-status-hotfix`. COLLECTION-STATUS-FILTER-HOTFIX (PR #1047, squash `ab37f18d`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hotfix'in targeted jest/tsc dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Git-side temiz unregistered (`git worktree list --porcelain` kaydi yok, prune yapildi). **WIP YOK** — tum hotfix isi commit+merge edildi (`ab7966d9` → `ab37f18d`), dizin yalniz checkout + `node_modules` icerir. Top-level `node_modules` reparse-point DEGIL (duz Directory, salt-okunur dogrulandi); derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_collection-status-hotfix"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-007 | ORPHANED_WORKTREE_DIR — `HUKUK_risk-mapping-fix` (DEBTOR-RISK-LABEL-MAPPING-FIX) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_risk-mapping-fix`. DEBTOR-RISK-LABEL-MAPPING-FIX (PR #1051, squash `f9a05e87`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hedefli jest + changed-file tsc dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list --porcelain` kaydi yok, prune yapildi, aktif branch baglantisi yok — local+remote branch silindi). **WIP YOK** — tum is commit+merge edildi (`b1b38e4c` → `f9a05e87`), dizin yalniz checkout + `node_modules` icerir. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ; owner-manual physical cleanup gerekir. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_risk-mapping-fix"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-008 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-w02-materializer` (ADR-014 W0.2) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-w02-materializer`. ADR-014 W0.2 Hybrid Materializer (PR #1050, squash `a4167f35`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — W0.2 typecheck/jest/DB-gated dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; remote/local branch silindi). **WIP YOK** — tum W0.2 isi commit+merge edildi (`bb8650e2` → `a4167f35`), dizin yalniz checkout + `node_modules` icerir. Derin pnpm junction/symlink'ler audit'te gozlendi (`node_modules/eslint*` vb.); recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-w02-materializer"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-009 | `case-compare-modal.tsx` stale DebtorType comparison | OPEN / LOW-RISK MAINTENANCE | Bilesendeki iki `d.type === 'REAL'` kontrolu kanonik `INDIVIDUAL/COMPANY/PUBLIC_INSTITUTION/ESTATE` enum'u ile uyumsuz; repo-ici canli caller bulunmadi. VER-08 urun verification'i kapatildi, runtime degisikligi yetkilendirilmedi. | Caller/urun niyeti dogrulanir ve dar UI patch'i ayri GO ile uygulanir veya bilesenin kullanilmadigi kanitlanip kaldirilmasi owner tarafindan kararlastirilir. | Yeni owner-authorized maintenance workstream olmadan kod degisikligi yapma. |
| MR-010 | `balance-shadow-display.test.tsx` flake monitoring | MONITORING / OPEN | `balance-shadow-diff-panel` testi PR #408 ve #902'de iki bagimsiz kez flake oldu; rerun ile CI 4/4 PASS'e dondu. Queue icindeki tek-seferlik is degil, tekrar-gozlem maintenance konusudur. | Flake kok nedeni dar bir reproducer ile belirlenir ve ayri owner GO ile duzeltilir veya yeterli ardışık kosumda tekrar etmedigi belgelenir. | Sonraki ilgili PR'larda sonucu izle; failure olursa log/reproducer kanitini bu kayda bagla. |
| MR-011 | ORPHANED_WORKTREE_DIR — `HUKUK_can-p0-002-a1-fix` | Owner cleanup required / OPEN | Fiziksel dizin 2026-07-10 workspace reconciliation sirasinda mevcut; urun backlog'u degildir. Icerige dokunulmadi ve recursive silme yetkisi yoktur. | Owner icerigi dogrular ve dizini manuel temizler veya tutulacagini bildirir. | Owner-manuel inceleme/cleanup; ajan fiziksel silme yapmaz. |
| MR-012 | ORPHANED_WORKTREE_DIR — `HUKUK_can-p0-002-c-fix` | Owner cleanup required / OPEN | Fiziksel dizin 2026-07-10 workspace reconciliation sirasinda mevcut; urun backlog'u degildir. Icerige dokunulmadi ve recursive silme yetkisi yoktur. | Owner icerigi dogrular ve dizini manuel temizler veya tutulacagini bildirir. | Owner-manuel inceleme/cleanup; ajan fiziksel silme yapmaz. |
| MR-013 | ORPHANED_WORKTREE_DIR — `HUKUK_fee-tariff-2026-001a-test` | Owner cleanup required / OPEN | Fiziksel dizin 2026-07-10 workspace reconciliation sirasinda mevcut; urun backlog'u degildir. Icerige dokunulmadi ve recursive silme yetkisi yoktur. | Owner icerigi dogrular ve dizini manuel temizler veya tutulacagini bildirir. | Owner-manuel inceleme/cleanup; ajan fiziksel silme yapmaz. |
| MR-014 | ORPHANED_WORKTREE_DIR — `HUKUK_p0_security_fix` | Owner cleanup required / OPEN | Fiziksel dizin 2026-07-10 workspace reconciliation sirasinda mevcut; urun backlog'u degildir. Icerige dokunulmadi ve recursive silme yetkisi yoktur. | Owner icerigi dogrular ve dizini manuel temizler veya tutulacagini bildirir. | Owner-manuel inceleme/cleanup; ajan fiziksel silme yapmaz. |
| MR-015 | Owner-local content verification — `HUKUK_ccb001_phaseB_validation` | OWNER-LOCAL CONTENT VERIFICATION REQUIRED / OPEN | Fiziksel dizin mevcut; icerik/WIP sahipligi repo governance kaydindan guvenle cikarilamaz. | Owner dizinin WIP, archive veya orphan oldugunu belirler. | Owner teyidi olmadan modify, stash, revert, move veya cleanup yapma. |
| MR-016 | Owner-local content verification — `HUKUK_ccb001_readonly_verify` | OWNER-LOCAL CONTENT VERIFICATION REQUIRED / OPEN | Fiziksel dizin mevcut; icerik/WIP sahipligi repo governance kaydindan guvenle cikarilamaz. | Owner dizinin WIP, archive veya orphan oldugunu belirler. | Owner teyidi olmadan modify, stash, revert, move veya cleanup yapma. |
| MR-017 | Owner-local content verification — `HUKUK_own29b_collection_void_approval_gate` | OWNER-LOCAL CONTENT VERIFICATION REQUIRED / OPEN | Fiziksel dizin mevcut; OWN-29-B runtime isi merge edilmis olsa da local dizinin icerigi salt isimden orphan sayilamaz. | Owner dizinin WIP, archive veya orphan oldugunu belirler. | Owner teyidi olmadan modify, stash, revert, move veya cleanup yapma. |
| MR-018 | Owner-local content verification — `HUKUK_own29c_financial_case_close` | OWNER-LOCAL CONTENT VERIFICATION REQUIRED / OPEN | Fiziksel dizin mevcut; OWN-29-C runtime isi merge edilmis olsa da local dizinin icerigi salt isimden orphan sayilamaz. | Owner dizinin WIP, archive veya orphan oldugunu belirler. | Owner teyidi olmadan modify, stash, revert, move veya cleanup yapma. |
| MR-019 | Owner-local content verification — `HUKUK_tbk100-export-hotfix` | OWNER-LOCAL CONTENT VERIFICATION REQUIRED / OPEN | Fiziksel dizin mevcut; TBK100 governance hatti ayri CCB-001/ADR-014/CAN-CUT-02 kapsaminda izlenir ve local icerik otomatik siniflandirilamaz. | Owner dizinin WIP, archive veya orphan oldugunu belirler. | Owner teyidi olmadan modify, stash, revert, move veya cleanup yapma. |
| MR-020 | Owner-local content verification — `adr-014-split-plan-docs-only` | OWNER-LOCAL CONTENT VERIFICATION REQUIRED / OPEN | Fiziksel dizin mevcut; ADR-014 split-plan iceriginin merge/WIP durumu dizin adindan guvenle cikarilamaz. | Owner dizinin WIP, archive veya orphan oldugunu belirler. | Owner teyidi olmadan modify, stash, revert, move veya cleanup yapma. |
| MR-021 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-w03-diagnostic` (ADR-014 W0.3) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-w03-diagnostic`. ADR-014 W0.3 Diagnostic Dual Mode (PR #1058, squash `31eb84d3`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — W0.3 typecheck/jest/DB-gated dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; remote/local branch silindi). **WIP YOK** — tum W0.3 isi commit+merge edildi (`1eef5983` → `31eb84d3`), dizin yalniz checkout + `node_modules` icerir. Derin pnpm junction/symlink'ler audit'te gozlendi (`node_modules/eslint*` vb.); recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-w03-diagnostic"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-022 | ORPHANED_WORKTREE_DIR — `HUKUK_pr2a-scoring-engine` (DEBTOR-SCORING PR-2A) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_pr2a-scoring-engine`. DEBTOR-SCORING PR-2A (PR #1060, squash `475de546`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hedefli jest + tsc dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list --porcelain` kaydi yok, prune yapildi, local+remote branch silindi). **WIP YOK** — tum PR-2A isi commit+merge edildi (`fda2dedc` → `475de546`), dizin yalniz checkout + `node_modules` icerir. Top-level `node_modules` reparse-point DEGIL (duz Directory, salt-okunur dogrulandi); derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. **Not:** bu kayit ilk denemede yanlislikla MR-021 olarak numaralandirilmisti; eszamanli PR #1059 ayni ID'yi ADR-014 W0.3 icin aldigindan (yukaridaki satir) MR-022'ye yeniden numaralandirildi, upstream MR-021 kaydina dokunulmadi. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_pr2a-scoring-engine"` + ANINDA canonical integrity check (runbook §3). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-023 | ORPHANED_WORKTREE_DIR — `HUKUK_pr2b-input-adapters` (DEBTOR-SCORING PR-2B) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_pr2b-input-adapters`. DEBTOR-SCORING PR-2B (PR #1065, squash `872e0f8e`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hedefli jest + tsc dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list --porcelain` kaydi yok, prune yapildi, local+remote branch silindi). **WIP YOK** — tum PR-2B isi commit+merge edildi (`5914806a` → `872e0f8e`), dizin yalniz checkout + `node_modules` icerir. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_pr2b-input-adapters"` + ANINDA canonical integrity check (runbook §3). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-024 | ORPHANED_WORKTREE_DIR — `HUKUK_pr2c-orchestration` (DEBTOR-SCORING PR-2C) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_pr2c-orchestration`. DEBTOR-SCORING PR-2C (PR #1068, squash `1e9b290a`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hedefli jest + tsc dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list --porcelain` kaydi yok, prune yapildi, local+remote branch silindi). **WIP YOK** — tum PR-2C isi commit+merge edildi (`7fe39cd6` → `1e9b290a`), dizin yalniz checkout + `node_modules` icerir. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_pr2c-orchestration"` + ANINDA canonical integrity check (runbook §3). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-025 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-reversal-restoration` (ADR-014 REVERSAL Restoration) | **CLOSED / OWNER-MANUAL DELETE VERIFIED (2026-07-11)** | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-reversal-restoration`. ADR-014 Governance Integrity Repair + REVERSAL Restoration (PR #1076, squash `a9afedfc`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi; force-delete/recursive fiziksel silme AJAN tarafindan YAPILMADI (runbook §2.3). **Karakterizasyon (owner GO-ANALYZE, read-only):** Classification **E — Safe Orphan** — reparse point/junction/symlink YOK (pozitif dogrulandi: `fsutil reparsepoint query` + `dir /al` ust dizinde sonuc yok); Git administrative worktree kaydi YOK (`git worktree list --porcelain` + `.git/worktrees/` bos); ilgili local/remote branch ve PR baglantisi kapali (PR #1076 MERGED, branch silinmis); dizindeki ek icerik (uyap_bot_*/v28_*/docx/zip) hedefe ozgu degil — bounded arama ile canonical calisma dizini dahil onlarca sibling worktree'de birebir ayni bulundu, benzersiz kullanici verisi degil. **Fiziksel silme denemesi ve guvenlik bulgusu:** ajan robocopy mirror-empty teknigiyle (path-length'e dayanikli, yalniz hedefe scope) silme denedi; auto-mode safety classifier'i islemi ENGELLEDI (geri-donusumsuz veri kaybi riski gerekcesiyle). **Ajan bu blokajin etrafindan dolanmadi, alternatif silme yontemi denemedi.** Fiziksel silme bunun yerine **owner tarafindan manuel olarak** (Windows Explorer/tercih edilen yontem) gerceklestirildi; owner bu islemi acikca bildirdi. **Dogrulama:** `Test-Path -LiteralPath` sonucu **False**; `git worktree list --porcelain` hedefi gormuyor; `.git/worktrees/` altinda kayit yok; local/remote branch yok; canonical repository ve sibling worktree'ler etkilenmedi; yeni orphan olusmadi. | Kapanis tamamlandi — owner-manuel fiziksel silme + ajan salt-okunur dogrulama + kapanis PR #1088. | MR-002 genel semsiye kaydi ayrica gecerli kalir. Bu satir bir daha acilmaz. |
| MR-026 | ORPHANED_WORKTREE_DIR — `HUKUK_ea-schema-migration` (PR-EA-2 Additive Schema Migration) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_ea-schema-migration`. PR-EA-2 (`EnforcementAction.tenantId`+`caseDebtorId` additive schema migration, PR #1085, squash `532e67e0e1bec81aefd60c4e4d47d74d14a82ec1`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — dogru pinlenmis `prisma@5.22.0` binary'si ve disposable-DB migration/integration test dogrulamasi icin `pnpm install` gerekliydi, runbook §2.4 istisnasi emsali: MR-021/022/023/024/025). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; local+remote branch silindi). **WIP YOK** — tum is commit+merge edildi (`f4c2e4c1` → `532e67e0`), dizin yalniz checkout + `node_modules` icerir. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_ea-schema-migration"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-027 | ORPHANED_WORKTREE_DIR — `HUKUK_pr1050-w02-narrow-remediation` (PR #1056 disposition) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_pr1050-w02-narrow-remediation`. PR #1056 ("narrow W0.2 materializer to payments"), owner GO-COMPLETE ile taze ground-truth sonrasi Classification D (Conflicting/Harmful — reddedilmis v1.3/AUTHORIZATION_AND_TECHNICAL_SCOPE_VIOLATION anlatisini tasiyordu) ile **merge edilmeden CLOSED** yapildi (bkz. decision-log 2026-07-11 "PR #1056 — SUPERSEDED / CONFLICTING DISPOSITION" kaydi). Kapanis sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store, runbook §2.4 istisnasi emsali: MR-021/022/023/024/025/026). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; local+remote branch silindi). **WIP YOK** — PR #1056 tek commit'ti (`96a52844`), merge edilmeden CLOSED yapildi, baska acik PR bagi yoktu (dogrulandi). Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_pr1050-w02-narrow-remediation"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-028 | ORPHANED_WORKTREE_DIR — `HUKUK_ea3a-profiler` (PR-EA-3A Read-Only Profiler) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_ea3a-profiler`. PR-EA-3A (`EnforcementAction` backfill saf classifier + salt-okuma profiler, PR #1090, branch commit `12279910`, squash `dbae23426490ccaccc446cec54a0166143adcd8d`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — dogru pinlenmis `prisma@5.22.0`/jest binary'leri ve disposable-DB entegrasyon testleri icin `pnpm install` gerekliydi, runbook §2.4 istisnasi emsali: MR-021/022/023/024/025/026). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; local+remote branch silindi). **WIP YOK** — tum is commit+merge edildi (`12279910` → `dbae2342`), dizin yalniz checkout + `node_modules` icerir. Bu dizinde ayrica, filtre argumani hatali yorumlandigi icin yanlislikla tum repo test suite'ini tarayan iki yetim arka-plan jest process'i tespit edildi ve durduruldu (dizini kilitleyen aktif process kalmadi). **Numaralandirma notu:** bu kayit ilk taslakta MR-027 olarak hazirlanmisti; PR #1091 ("PR #1056 disposition") ile eszamanli sekilde MR-027'yi yukaridaki farkli orphan icin ONCE canonical main'e merge ettigi tespit edilince (GO-RESOLVE-CONFLICT, bu tarih) MR-028'e yeniden numaralandirildi — upstream MR-027 kaydi degistirilmedi. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_ea3a-profiler"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-029 | ORPHANED_WORKTREE_DIR — `adr014-pr1b-reversal-netting` (ADR-014 PR-1B) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\project\.worktrees\adr014-pr1b-reversal-netting`. PR #1089 (branch commit `816554ee619d24aff938525f9759c1c4655d081d`; squash `61cfc2b03a819616ad168b889ff9b3e4ac4963a7`) merge ve CI 4/4 SUCCESS sonrasında normal `git worktree remove` Windows'ta `Result too large` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Git-side cleanup tamdır: worktree registry kaydı, local/remote branch ve remote-tracking ref yok; dizin canonical repository/worktree değildir ve PR-1B teknik veya governance kapanışını bloklamaz. Fiziksel dizin yerinde ve dokunulmadan kalır. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir; bu satır yalnız MR-002 genel şemsiyesi altında bireysel traceability sağlar. | Owner cleanup sonrası path yokluğu ve canonical/worktree bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance kapanışıyla CLOSED yapılır. MR-002 ayrıca geçerli kalır. |
| MR-030 | PHYSICAL_ORPHAN_CANDIDATE — `adr014-w03-ratification-hardening` (ADR-014 W0.3) | Owner review / cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\project\.worktrees\adr014-w03-ratification-hardening`. Fresh post-PR-1B revalidation (2026-07-11) dizinin fiziksel olarak mevcut ve top-level normal `Directory` olduğunu; reparse point/link olmadığını; `.git` marker, local/remote/remote-tracking ref ve `git worktree list --porcelain` registry kaydı bulunmadığını doğruladı. Dizin canonical worktree değildir. İçerik fiziksel olarak korunmuştur; bu kayıt WIP yokluğu veya safe-delete kararı vermez. ADR-014 technical/governance closure ve runtime readiness için blocker değildir. Fiziksel cleanup yapılmadı. | Owner içeriği doğrular ve manuel cleanup veya retention kararı verir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner kararı sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; cleanup tamamlanırsa ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-031 | ORPHANED_WORKTREE_DIR — `HUKUK_ea3a1-report-hardening` (PR-EA-3A.1 Profiler Report Hardening) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_ea3a1-report-hardening`. PR-EA-3A.1 (`enforcement-action-backfill-profiler.ts` credential-free DB identity + SHA-256 hash manifest, PR #1100, branch commit `b4b94c3d`, squash `c1ad9f55c8e64a34ff90a78e001df71cb3cf81d8`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hedefli jest/tsc/eslint dogrulamasi ve disposable-DB entegrasyon testleri icin `pnpm install` gerekliydi, runbook §2.4 istisnasi emsali: MR-021/022/023/024/025/026/027/028). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; local+remote branch silindi). **WIP YOK** — tum is commit+merge edildi (`b4b94c3d` → `c1ad9f55`), dizin yalniz checkout + `node_modules` icerir. **Numaralandirma notu:** MR-029 (ADR-014 PR-1B) ve MR-030 (ADR-014 W0.3) eszamanli baska workstream'ler tarafindan zaten alinmis oldugu tazeden dogrulanip sirdaki gercek bos ID (MR-031) kullanildi — cakisma yok, renumber gerekmedi. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_ea3a1-report-hardening"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-032 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr2-no-buckets-fail-closed` (ADR-014 PR-2) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr2-no-buckets-fail-closed`. PR #1104 (head `372146a5c4710310e2972084e7b21b97ac953e09`; squash `11023234457e57bdad108b0fb753a9892389ee4c`) merge ve CI 4/4 SUCCESS sonrasında normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin normal `Directory`, `.git` marker'sız ve `project/node_modules` içeren fiziksel kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. **WIP YOK** — tüm iş commit+merge edildi; PR-2 technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-031 concurrent PR #1103 tarafından ayrı orphan için kullanıldığından sonraki boş ID MR-032 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-033 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr3h-tbk100-hardening` (ADR-014 PR-3h) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr3h-tbk100-hardening`. PR #1101 (rebased head `336d33baae17ce997e64616d27d6ef8051e5166a`; squash `566ae47a26e505a79ba8867b3c21c5f724c3b1ef`) merge ve CI 4/4 SUCCESS sonrasında normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin normal `Directory`, `.git` marker'sız ve `project/node_modules` içeren fiziksel kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. **WIP YOK** — tüm iş commit+merge edildi; PR-3h technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-031 concurrent PR #1103 tarafından ayrı orphan için kullanılır; MR-032 PR-2 orphan'ıdır; sonraki boş ID MR-033 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-034 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr4-interest-base-mutation` (ADR-014 PR-4) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr4-interest-base-mutation`. PR #1109 (head `4ffc283cbf78875e99ce4196ef41a30614769523`; squash `77a4ca353cbbc7687deb44d9eb794a3df511967c`) merge ve CI 4/4 SUCCESS sonrasında normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin normal `Directory`, `.git` marker'sız ve `project/node_modules` içeren fiziksel kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. **WIP YOK** — tüm iş commit+merge edildi; PR-4 technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-033 PR-3h orphan'ıdır; sonraki boş ID MR-034 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-035 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr5-enforcement-interest` (ADR-014 PR-5) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr5-enforcement-interest`. PR #1113 (rebased head `058cfe05cc8e267d76a7e433e73a47992638372f`; squash `6df5560bbab79a1314c41aadd412b6497d1f23af`) merge ve CI 4/4 SUCCESS sonrasında `git worktree remove --force` Windows'ta `Filename too long` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin normal `Directory`, `.git` marker'sız ve `project/node_modules` içeren fiziksel kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. **WIP YOK** — tüm iş commit+merge edildi; PR-5 technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-034 PR-4 orphan'ıdır; sonraki boş ID MR-035 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-036 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr6-currency-hardening` (ADR-014 PR-6) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr6-currency-hardening`. PR #1118 (rebased head `2ed6b1eccbd4170d44b7d9e56648c9e81b3a5d1b`; squash `371a6552717f6bc01ba4084450e45b5a4986cb1e`) merge ve CI 4/4 SUCCESS sonrasında `git worktree remove --force` Windows'ta `Filename too long` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin fiziksel olarak mevcut, `.git` marker'sız ve `project/node_modules` içeren kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. **WIP YOK** — tüm iş commit+merge edildi; PR-6 technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-035 PR-5 orphan'ıdır; concurrent PR #1116/#1117 Maintenance Register'a dokunmadığından sonraki boş ID MR-036 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-037 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr7-fee-projection` (ADR-014 PR-7) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr7-fee-projection`. PR #1120 (head `842425b74a96424bea7c8cfecc5a18ccdde771ee`; squash `a3bfb26b719fe9dbf7cd9f197305ed7709867b5e`) merge ve CI 4/4 SUCCESS sonrasında `git worktree remove --force` Windows'ta `Filename too long` hatası verdi. Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin fiziksel olarak mevcut, `.git` marker'sız ve `project/node_modules` içeren kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. Local ve remote teknik branch silindi. **WIP YOK** — tüm iş commit+merge edildi; PR-7 technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-036 PR-6 orphan'ıdır; concurrent PR #1116 Maintenance Register'a dokunmadığından sonraki boş ID MR-037 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-038 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pr8a-snapshot-consistency` (ADR-014 PR-8a) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pr8a-snapshot-consistency`. PR #1125 (rebased head `1ce46b3bedc5c0a5ca7da777a6f6c1e9e4ab0615`; squash `ce40d98a47fcf77431468275a993e4f2a0255276`) merge ve CI 4/4 SUCCESS sonrasında normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin normal `Directory`, `.git` marker'sız ve `project/node_modules` içeren fiziksel kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking branch bulunmadığını doğruladı. Local ve remote teknik branch silindi. **WIP YOK** — tüm iş commit+merge edildi; PR-8a technical/governance closure ve runtime readiness için blocker değildir. **Numaralandırma:** MR-037 PR-7 orphan'ıdır; concurrent PR #1124 Maintenance Register'a dokunmadığından sonraki boş ID MR-038 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-039 | ORPHANED_WORKTREE_DIR — `HUKUK_ea4-guarded-write-path` (PR-EA-4 Guarded Write Path) | Owner cleanup required / OPEN | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_ea4-guarded-write-path`. PR-EA-4 (`WorkflowEngine.createEnforcementAction` guarded write-path, PR #1134, branch commit `37ef5d96`, squash `bb34c17eaad36f5e19a81d4904cdfc60093cde0e`) merge sonrasi `git worktree remove --force` "Filename too long" hatasi verdi (pnpm derin store — hedefli jest/tsc/eslint dogrulamasi ve disposable-DB entegrasyon testleri icin `pnpm install` + `prisma generate` gerekliydi, runbook §2.4 istisnasi emsali: MR-021...038). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydi yok, prune + `git fetch --prune` yapildi; local+remote branch silindi). **WIP YOK** — tum is commit+merge edildi (`37ef5d96` → `bb34c17e`), dizin yalniz checkout + `node_modules` icerir. **Numaralandirma notu:** MR-038 taze dogrulanip sirdaki gercek bos ID (MR-039) kullanildi — cakisma yok. Derin pnpm junction olasiligi nedeniyle recursive silme runbook `worktree-cleanup.md` §2.3 geregi AJAN tarafindan YAPILMAZ. | Owner manuel temizler VEYA dizinin kalici tutulacagini bildirir. Onerilen owner komutu: `cmd /c rd /s /q "C:\Development\HUKUK_YAZILIMI\HUKUK_ea4-guarded-write-path"` + ANINDA canonical integrity check (runbook §3: `git status`, `.bin` sayilari, nest/prisma/jest/tsc/next shim'leri). | Owner cleanup + integrity check sonrasi bu satir CLOSED yapilir; MR-002 genel semsiye kaydi ayrica gecerli kalir. |
| MR-040 | ORPHANED_WORKTREE_DIR — `HUKUK_adr014-pe01a-zero-cent-readiness` (ADR014-PE-01A) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pe01a-zero-cent-readiness`. Technical PR #1154 (head `54102a50e2399ee5df393b180959bacb52d670ff`; squash `52668ff97007a72496f351a701b1dbeaf8fe60d8`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Recursive/force fiziksel silme YAPILMADI. Fresh revalidation dizinin fiziksel olarak mevcut, top-level `.git` marker'sız ve `project/node_modules` içeren kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking technical branch bulunmadığını doğruladı. **WIP YOK** — tüm teknik iş commit+merge edildi; PE-01A technical/governance closure, representative-evidence preparation veya runtime readiness için blocker değildir. **Numaralandırma:** MR-039 başka canonical maintenance kaydıdır; sonraki boş ID MR-040 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-041 | ORPHANED_WORKTREE_DIR — `HUKUK_workflow-buildcontext-tenant-guard` (WorkflowEngine.buildContext Tenant Guard / OD-3) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_workflow-buildcontext-tenant-guard`. PR #1161 (squash `901f33f40358d05edbd04cbf4d124d707415c3e6`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi (unit/statik-guard/disposable-DB entegrasyon testleri ve changed-file tsc/eslint doğrulaması için `pnpm install` + `prisma generate` gerekliydi, runbook §2.4 istisnası emsali: MR-021...040). Force-delete/recursive fiziksel silme YAPILMADI. Fresh revalidation dizinin fiziksel olarak mevcut, top-level `.git` marker'sız ve `project/node_modules` içeren kalıntı olduğunu; `git worktree list --porcelain` registry kaydı, local/remote/remote-tracking technical branch bulunmadığını doğruladı. **WIP YOK** — tüm teknik iş commit+merge edildi (`901f33f4`); OD-3 technical/governance closure veya runtime readiness için blocker değildir. **Numaralandırma:** MR-040 başka canonical maintenance kaydıdır; sonraki boş ID MR-041 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-042 | ORPHANED_WORKTREE_DIR — `HUKUK_workflow-tenant-boundaries` (WorkflowEngine calculateNextActionTime/updateCaseStage Tenant Boundary) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_workflow-tenant-boundaries`. PR #1166 (squash `93ab9345222101bbf1beb032fd656f62e737b045`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi (unit/statik-guard/disposable-DB entegrasyon testleri ve changed-file tsc/eslint doğrulaması için `pnpm install` + `prisma generate` gerekliydi, runbook §2.4 istisnası emsali: MR-021...041). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydı yok, prune + `git fetch --prune` yapıldı; local+remote branch silindi). **WIP YOK** — tüm teknik iş commit+merge edildi (`93ab9345`); calculateNextActionTime/updateCaseStage tenant boundary technical/governance closure veya runtime readiness için blocker değildir. **Numaralandırma:** MR-041 başka canonical maintenance kaydıdır; sonraki boş ID MR-042 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-043 | ADR014-PE-05A1a physical worktree residue | OPEN / NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pe05a1a`. Technical PR #1168 (head `2e404312494a22d18492e8ba837b391dc7aa874e`; squash `49d7986507e62bb39a3d6de97a1b8fefccd39891`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Git worktree registration REMOVED; local ve remote task branch DELETED. Kalan dizin yalnız fiziksel filesystem residue'dur; registered Git worktree veya canonical branch değildir, canonical authority içermez ve sonraki ADR-014 işini bloklamaz. Canonical/tracked repository ve runtime etkisi YOKTUR. Recursive/force cleanup YAPILMADI; `MR-040` untouched kalır. | Owner ayrı cleanup yetkisi verir ve taze filesystem doğrulaması yapılır veya dizinin kalıcı tutulacağını bildirir. Bu kayıt recursive delete, force delete, long-path workaround, robocopy mirror-delete, nested manual deletion ya da Windows policy değişikliği yetkisi vermez. | Owner-authorized future cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından MR-043 ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-044 | ADR014-PE-05A2 physical worktree residue | OPEN / NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pe05a2`. Technical PR #1171 (head `1492a91cf98a6968887f583788cab7bd7bb27a98`; squash `3a8224fab6310800bfc09138d78936c776701118`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Git worktree registration REMOVED; local ve remote task branch DELETED. Kalan dizin yalnız fiziksel filesystem residue'dur; registered Git worktree veya canonical branch değildir, canonical authority içermez ve ADR014-PE-05A3'ü bloklamaz. Canonical/tracked repository ve runtime etkisi YOKTUR. Recursive/force fiziksel cleanup YAPILMADI; `MR-040` ve `MR-043` untouched kalır. | Owner ayrı cleanup yetkisi verir ve taze filesystem doğrulaması yapılır veya dizinin kalıcı tutulacağını bildirir. Bu kayıt recursive delete, force delete, long-path workaround, robocopy mirror-delete, nested manual deletion ya da Windows policy değişikliği yetkisi vermez. | Owner-authorized future cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından MR-044 ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-045 | ORPHANED_WORKTREE_DIR — `HUKUK_wave0-gate1-ci-coverage` (Wave 0 GATE-1 CI Tenant-Isolation Regression Coverage) | Owner cleanup required / OPEN — NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_wave0-gate1-ci-coverage`. PR #1174 (squash `e27f5f3cb9b57b66444f5214530b2fe1b2fc13aa`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi (unit/static test doğrulaması + disposable-DB entegrasyon testleri için `pnpm install` + `prisma generate` gerekliydi, runbook §2.4 istisnası emsali: MR-021...044). Force-delete/recursive fiziksel silme YAPILMADI. Git-side temiz unregistered (`git worktree list` kaydı yok, prune + `git fetch --prune` yapıldı; local+remote branch silindi). **WIP YOK** — tüm teknik iş commit+merge edildi (`e27f5f3c`); GATE-1 CI tenant-isolation coverage technical/governance closure veya Wave 0 exit değerlendirmesi için blocker değildir. **Numaralandırma:** MR-044 başka canonical maintenance kaydıdır; sonraki boş ID MR-045 seçildi. | Owner dizini manuel temizler VEYA kalıcı tutulacağını bildirir. Ajan recursive delete, move, stash, revert veya içerik değişikliği yapmaz. | Owner cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından bu satır ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-046 | ADR014-PE-05A3 physical worktree residue | OPEN / NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pe05a3`. Technical PR #1175 (head `edbbb0c5c1e5f7aaa50327ec716de7b50a653ce6`; squash `64e71c8f8feece7f711e07e85543248ab20009a5`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove` Windows'ta `Filename too long` hatası verdi. Git worktree registration REMOVED; local ve remote task branch DELETED. Kalan dizin yalnız fiziksel filesystem residue'dur; registered Git worktree veya canonical branch değildir, canonical authority içermez ve ADR014-PE-05A4'ü bloklamaz. Canonical/tracked repository ve runtime etkisi YOKTUR. Recursive/force fiziksel cleanup YAPILMADI; `MR-040`, `MR-043` ve `MR-044` untouched kalır. **Numaralandırma:** MR-045 concurrent Wave 0 GATE-1 governance closure PR #1178 tarafından rezerve edilmiştir; collision önlemek için sonraki ID MR-046 seçildi. | Owner ayrı cleanup yetkisi verir ve taze filesystem doğrulaması yapılır veya dizinin kalıcı tutulacağını bildirir. Bu kayıt recursive delete, force delete, long-path workaround, robocopy mirror-delete, nested manual deletion ya da Windows policy değişikliği yetkisi vermez. | Owner-authorized future cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından MR-046 ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |
| MR-047 | ADR014-PE-05A4 physical worktree residue | OPEN / NON-BLOCKING | Exact path: `C:\Development\HUKUK_YAZILIMI\HUKUK_adr014-pe05a4`. Technical PR #1180 (final rebased head `f80cbf70be7c59fd1781fcd0be470c31a055202c`; squash `347e7b3dde8391ffbb1ac7d1a905f067118078c6`; CI `4/4 SUCCESS`) merge sonrası normal `git worktree remove --force` Windows'ta `Filename too long` hatası verdi. Git worktree registration REMOVED; `.git` marker yok; local ve remote task branch DELETED. Kalan dizin yalnız fiziksel filesystem residue'dur; registered Git worktree veya canonical branch değildir, canonical authority içermez ve sonraki owner-assigned işi bloklamaz. Canonical/tracked repository ve runtime etkisi YOKTUR. Recursive/force fiziksel cleanup YAPILMADI; existing `MR-040`, `MR-043`, `MR-044` ve `MR-046` untouched kalır. **Numaralandırma:** canonical main'de MR-046 taze doğrulandı ve sonraki boş ID MR-047 seçildi. | Owner ayrı cleanup yetkisi verir ve taze filesystem doğrulaması yapılır veya dizinin kalıcı tutulacağını bildirir. Bu kayıt recursive delete, force delete, long-path workaround, robocopy mirror-delete, nested manual deletion ya da Windows policy değişikliği yetkisi vermez. | Owner-authorized future cleanup sonrası path/ref/worktree/canonical bütünlüğü salt-okunur doğrulanır; ardından MR-047 ayrı maintenance closure ile CLOSED yapılır. MR-002 genel şemsiyesi ayrıca geçerli kalır. |

## Kapsam Disi

- Product Backlog kaydi eklemek.
- Master Triage'a yeni urun isi eklemek.
- Runtime davranis, domain logic, schema veya migration degistirmek.
- Owner WIP dosyalarini overwrite, stash, revert veya fiziksel cleanup yapmak.
