# C4 — EKSİK TEK ÖLÇÜT: `403 SCHEDULER_MANUAL_RUN_DENIED_NOT_ELEVATED` (R01)

> **DURUM: HAZIR — CANLIDA KOŞULMADI.** Canlı koşum ayrı owner GO'su (`OWNER-GO-CLIENT-C4-YYYYMMDD-RNN`) ister.
> C4 bu ölçüt PASS olmadan, sentetik erişim kapanışı doğrulanmadan, CLIENT kaydı ve OFFICE listesi güncellenmeden kapanmaz.
> Teknik 18/18, hizmet kabulü 0/8 değişmez.

## 1. Eşleme — neden yalnız bu ölçüt

OFFICE listesi C4'ün yetkili rol ölçütleri üç tane. İ12 canlı koşumu (runId `92d04ef3`, 2026-09-18) iki tanesini
karşıladı; bunlar **yeniden koşulmaz**:

| Ölçüt | Kanıt | Sonuç |
|---|---|---|
| Hedef-scoped teslim | İ12 G7: hedef delivery +1, ledger SENT +1, yabancı tenant +0 | KARŞILANDI |
| Aynı dönem dedupe | İ12 G7: ikinci tetik +0 | KARŞILANDI |
| `403 …_NOT_ELEVATED` | İ12 G7-AUTH-REACH yalnız elevated `elev2` ile "401 değil" ölçtü | **EKSİK** → bu paket |

Anonim 401 bu ölçütü **karşılamaz**; betikte yalnız bilgi olarak kaydedilir.

## 2. Tasarım

- **Aktör:** setupI3 `user` hesabı. Rolü `USER` (VIEWER değil); `StaffMember` kaydı var; PARTNER/`canApproveOfficeActions`
  yok. Bu yüzden `isApproverEligible` false (`scheduler-manual-run-policy.ts` → `ELEVATED_DENIED`). Koşumdan önce DB'den
  salt okuma ile doğrulanır (C4-PRE); tutmazsa ölçüm yapılmaz.
- **Beklenmedik başarıya karşı düzen:**
  - Uç kapsamı her zaman aktörün tenant'ıdır (`client-statement.controller.ts` 48–54), yani yalnız sentetik tenant.
  - Sentetik tenant'ta ekstre 0 ve Office SMTP host yok (ön kapı). Alıcılar `*.invalid`.
- **Sıfır yan etki kapısı (C4-ZERO):** sentetik tenant ledger, bildirim ve ekstre +0; global teslim ledger +0.
- **Kapanış:** `finally` bloğunda personel erişimi kapatılır ve Case CLOSED yapılır. Ardından giriş/token 401 yoklaması
  ve izolasyon parmak izi alınır.

## 3. Prova (disposable, aday API R26, aylık teslim bayrağı AÇIK — canlıyla aynı)

| Prova | Sonuç | Kanıt sha256 (`Documents\CLIENT-EVIDENCE-20260911\c4-prova-20260922\`) |
|---|---|---|
| Canlı koşumla aynı (runId `bbe89be0`) | **5/5 PASS**: HTTP 403 + `SCHEDULER_MANUAL_RUN_DENIED_NOT_ELEVATED` (anonim 401); yan etki 0; kapanış 401; izolasyon eşit | `A4F9AE25…2310` |
| Beklenmedik başarı provası (runId `06f0b263`, aynı uç elevated `elev1` ile de çağrıldı) | Elevated çağrı 201 döndü ve **yalnız sentetik tenant'ta 1 ekstre satırı** oluştu. **Teslim ledger 0, bildirim 0, global ledger 0**: gönderim yok, gerçek tenant etkisi yok. Sayım kapısı FAIL verdi, yani böyle bir başarı ölçütü de düşürür | `E7AAEF05…91F0` |

## 4. Canlı koşum (owner GO'su ile)

İnsan eli gereken tek adım GO ref'in yerel olarak girilmesi. Blok SelfTest'i canlıda (salt okuma) PASS verdi:
canlı API R26, `:8080` tek dinleyici, yabancı süreç yok, DB `hukuk_db`.

| Dosya | sha256 |
|---|---|
| `scripts/c4-live-403.js` | `B159B6B08ABB05D0B387C653625658832A73B565FA906F6FD6ABE31F3334C8BE` |
| `scripts/c4-live-block.ps1` | `9A979192C48A30BF2C9306E3F62B52D1D8DC3D0ACD6DBAE86CB689FFB8F32573` |
| Paket digest (blok içinde pinli) | `09AE123298EDEA11C1CC4C4164EE4224B67D2DE8F95B6B5124BDDADFA21EAEAC` |

```powershell
& { $ErrorActionPreference='Stop'; $f='D:\Development\HUKUK_YAZILIMI\project\project\docs\governance\client-c4-live-403-r01\scripts\c4-live-block.ps1'; if((Get-FileHash -Algorithm SHA256 -LiteralPath $f).Hash -cne '9A979192C48A30BF2C9306E3F62B52D1D8DC3D0ACD6DBAE86CB689FFB8F32573'){ throw 'C4 BLOK SHA UYUSMUYOR - DUR' }; $global:LASTEXITCODE=-999; & powershell.exe -NoProfile -ExecutionPolicy Bypass -File $f; $rc=$global:LASTEXITCODE; 'C4 cikis=' + $rc; if($rc -ne 0){ throw 'C4 PASS DEGIL - kanit dizinindeki goref-consumed.json exitCode okunur (5 = kapanis dogrulanmadi -> i16-live-recover.js)' } }
```

Kurtarma (yalnız çıkış 5 ise): `R26-RELEASE-PACKAGE-R02.md` §4 B2-R bloğu aynı slug şemasıyla (`ah-<runId>`) çalışır.
Koşum sonrasında CLIENT, `r26-closure-verify.js` kalıbıyla READ ONLY kapanış doğrulaması yapar ve kaydı ayrı PR ile ekler.
